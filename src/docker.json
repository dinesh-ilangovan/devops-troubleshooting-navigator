[
  {
    "id": "docker-001",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Daemon startup — Engine & Daemon: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective daemon startup state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "daemon startup",
      "Engine & Daemon",
      "Daemon startup — Engine & Daemon: fails completely",
      "daemon",
      "startup",
      "engine",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon startup — Engine & Daemon: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon startup; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon startup — Engine & Daemon: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon startup — Engine & Daemon: fails completely”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon startup",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon startup configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon startup — Engine & Daemon: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the daemon startup inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon startup — Engine & Daemon: fails completely”",
      "Effective daemon startup configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon startup — Engine & Daemon: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Daemon startup — Engine & Daemon: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Daemon startup — Engine & Daemon: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon startup — Engine & Daemon: fails completely”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon startup — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon startup",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon startup\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon startup\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon startup",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon startup — Engine & Daemon: fails completely”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon startup — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon startup configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon startup configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon startup configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The daemon startup configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon startup — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon startup",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon startup\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon startup — Engine & Daemon: fails completely”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon startup configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon startup configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon startup — Engine & Daemon: fails completely”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Daemon startup — Engine & Daemon: fails completely: evidence found\nFor “Daemon startup — Engine & Daemon: fails completely”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon startup — Engine & Daemon: fails completely”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "check": "docker info",
        "output": "Daemon startup — Engine & Daemon: fails completely: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Daemon startup — Engine & Daemon: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-002",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Daemon startup — Engine & Daemon: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective daemon startup state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "daemon startup",
      "Engine & Daemon",
      "Daemon startup — Engine & Daemon: behaves differently than configured",
      "daemon",
      "startup",
      "engine",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon startup — Engine & Daemon: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon startup; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon startup — Engine & Daemon: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon startup — Engine & Daemon: behaves differently than configured”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon startup",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon startup configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon startup — Engine & Daemon: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the daemon startup inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon startup — Engine & Daemon: behaves differently than configured”",
      "Effective daemon startup configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon startup — Engine & Daemon: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Daemon startup — Engine & Daemon: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Daemon startup — Engine & Daemon: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon startup — Engine & Daemon: behaves differently than configured”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon startup — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon startup",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon startup\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon startup\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon startup",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon startup — Engine & Daemon: behaves differently than configured”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon startup — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon startup configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon startup configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon startup configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The daemon startup configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon startup — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon startup",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon startup\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon startup — Engine & Daemon: behaves differently than configured”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon startup configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon startup configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon startup — Engine & Daemon: behaves differently than configured”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Daemon startup — Engine & Daemon: behaves differently than configured: evidence found\nFor “Daemon startup — Engine & Daemon: behaves differently than configured”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon startup — Engine & Daemon: behaves differently than configured”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "check": "docker info",
        "output": "Daemon startup — Engine & Daemon: behaves differently than configured: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Daemon startup — Engine & Daemon: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-003",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Daemon startup — Engine & Daemon: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective daemon startup state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "daemon startup",
      "Engine & Daemon",
      "Daemon startup — Engine & Daemon: fails intermittently under load or restart",
      "daemon",
      "startup",
      "engine",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon startup — Engine & Daemon: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon startup; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon startup — Engine & Daemon: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon startup — Engine & Daemon: fails intermittently under load or restart”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon startup",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon startup configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon startup — Engine & Daemon: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the daemon startup inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon startup — Engine & Daemon: fails intermittently under load or restart”",
      "Effective daemon startup configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon startup — Engine & Daemon: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Daemon startup — Engine & Daemon: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Daemon startup — Engine & Daemon: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon startup — Engine & Daemon: fails intermittently under load or restart”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon startup — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon startup",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon startup\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon startup\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon startup",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon startup — Engine & Daemon: fails intermittently under load or restart”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon startup — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon startup configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon startup configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon startup configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The daemon startup configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon startup — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon startup",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon startup\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon startup — Engine & Daemon: fails intermittently under load or restart”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon startup configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon startup configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon startup — Engine & Daemon: fails intermittently under load or restart”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Daemon startup — Engine & Daemon: fails intermittently under load or restart: evidence found\nFor “Daemon startup — Engine & Daemon: fails intermittently under load or restart”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon startup — Engine & Daemon: fails intermittently under load or restart”, the effective daemon startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "check": "docker info",
        "output": "Daemon startup — Engine & Daemon: fails intermittently under load or restart: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Daemon startup — Engine & Daemon: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-004",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Docker socket access — Engine & Daemon: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Docker socket access state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "Docker socket access",
      "Engine & Daemon",
      "Docker socket access — Engine & Daemon: fails completely",
      "docker",
      "socket",
      "access",
      "engine",
      "daemon",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker socket access — Engine & Daemon: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker socket access; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker socket access — Engine & Daemon: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker socket access — Engine & Daemon: fails completely”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker socket access",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker socket access configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker socket access — Engine & Daemon: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Docker socket access inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker socket access — Engine & Daemon: fails completely”",
      "Effective Docker socket access configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker socket access — Engine & Daemon: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Docker socket access — Engine & Daemon: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker socket access — Engine & Daemon: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker socket access — Engine & Daemon: fails completely”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker socket access — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker socket access",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket access\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket access\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker socket access",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker socket access — Engine & Daemon: fails completely”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker socket access — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker socket access configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket access configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket access configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The Docker socket access configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker socket access — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker socket access",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket access\nThe effective runtime value differs from expectation.",
        "root": "For “Docker socket access — Engine & Daemon: fails completely”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker socket access configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket access configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker socket access — Engine & Daemon: fails completely”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Docker socket access — Engine & Daemon: fails completely: evidence found\nFor “Docker socket access — Engine & Daemon: fails completely”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker socket access — Engine & Daemon: fails completely”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "check": "docker info",
        "output": "Docker socket access — Engine & Daemon: fails completely: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Docker socket access — Engine & Daemon: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-005",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Docker socket access — Engine & Daemon: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Docker socket access state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "Docker socket access",
      "Engine & Daemon",
      "Docker socket access — Engine & Daemon: behaves differently than configured",
      "docker",
      "socket",
      "access",
      "engine",
      "daemon",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker socket access — Engine & Daemon: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker socket access; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker socket access — Engine & Daemon: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker socket access — Engine & Daemon: behaves differently than configured”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker socket access",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker socket access configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker socket access — Engine & Daemon: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Docker socket access inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker socket access — Engine & Daemon: behaves differently than configured”",
      "Effective Docker socket access configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker socket access — Engine & Daemon: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Docker socket access — Engine & Daemon: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker socket access — Engine & Daemon: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker socket access — Engine & Daemon: behaves differently than configured”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker socket access — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker socket access",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket access\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket access\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker socket access",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker socket access — Engine & Daemon: behaves differently than configured”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker socket access — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker socket access configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket access configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket access configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The Docker socket access configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker socket access — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker socket access",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket access\nThe effective runtime value differs from expectation.",
        "root": "For “Docker socket access — Engine & Daemon: behaves differently than configured”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker socket access configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket access configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker socket access — Engine & Daemon: behaves differently than configured”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Docker socket access — Engine & Daemon: behaves differently than configured: evidence found\nFor “Docker socket access — Engine & Daemon: behaves differently than configured”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker socket access — Engine & Daemon: behaves differently than configured”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "check": "docker info",
        "output": "Docker socket access — Engine & Daemon: behaves differently than configured: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Docker socket access — Engine & Daemon: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-006",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Docker socket access — Engine & Daemon: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Docker socket access state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "Docker socket access",
      "Engine & Daemon",
      "Docker socket access — Engine & Daemon: fails intermittently under load or restart",
      "docker",
      "socket",
      "access",
      "engine",
      "daemon",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker socket access — Engine & Daemon: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker socket access; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker socket access — Engine & Daemon: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker socket access — Engine & Daemon: fails intermittently under load or restart”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker socket access",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker socket access configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker socket access — Engine & Daemon: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Docker socket access inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker socket access — Engine & Daemon: fails intermittently under load or restart”",
      "Effective Docker socket access configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker socket access — Engine & Daemon: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Docker socket access — Engine & Daemon: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker socket access — Engine & Daemon: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker socket access — Engine & Daemon: fails intermittently under load or restart”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker socket access — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker socket access",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket access\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket access\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker socket access",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker socket access — Engine & Daemon: fails intermittently under load or restart”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker socket access — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker socket access configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket access configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket access configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The Docker socket access configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker socket access — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker socket access",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket access\nThe effective runtime value differs from expectation.",
        "root": "For “Docker socket access — Engine & Daemon: fails intermittently under load or restart”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker socket access configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket access configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker socket access — Engine & Daemon: fails intermittently under load or restart”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Docker socket access — Engine & Daemon: fails intermittently under load or restart: evidence found\nFor “Docker socket access — Engine & Daemon: fails intermittently under load or restart”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker socket access — Engine & Daemon: fails intermittently under load or restart”, the effective Docker socket access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "check": "docker info",
        "output": "Docker socket access — Engine & Daemon: fails intermittently under load or restart: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Docker socket access — Engine & Daemon: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-007",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Daemon configuration — Engine & Daemon: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective daemon configuration state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "daemon configuration",
      "Engine & Daemon",
      "Daemon configuration — Engine & Daemon: fails completely",
      "daemon",
      "configuration",
      "engine",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon configuration — Engine & Daemon: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon configuration; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon configuration — Engine & Daemon: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon configuration — Engine & Daemon: fails completely”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon configuration",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon configuration configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon configuration — Engine & Daemon: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the daemon configuration inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon configuration — Engine & Daemon: fails completely”",
      "Effective daemon configuration configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon configuration — Engine & Daemon: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Daemon configuration — Engine & Daemon: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Daemon configuration — Engine & Daemon: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon configuration — Engine & Daemon: fails completely”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon configuration — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon configuration",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon configuration\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon configuration\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon configuration",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon configuration — Engine & Daemon: fails completely”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon configuration — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon configuration configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon configuration configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon configuration configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The daemon configuration configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon configuration — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon configuration",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon configuration\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon configuration — Engine & Daemon: fails completely”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon configuration configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon configuration configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon configuration — Engine & Daemon: fails completely”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Daemon configuration — Engine & Daemon: fails completely: evidence found\nFor “Daemon configuration — Engine & Daemon: fails completely”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon configuration — Engine & Daemon: fails completely”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "check": "docker info",
        "output": "Daemon configuration — Engine & Daemon: fails completely: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Daemon configuration — Engine & Daemon: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-008",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Daemon configuration — Engine & Daemon: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective daemon configuration state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "daemon configuration",
      "Engine & Daemon",
      "Daemon configuration — Engine & Daemon: behaves differently than configured",
      "daemon",
      "configuration",
      "engine",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon configuration — Engine & Daemon: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon configuration; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon configuration — Engine & Daemon: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon configuration — Engine & Daemon: behaves differently than configured”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon configuration",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon configuration configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon configuration — Engine & Daemon: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the daemon configuration inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon configuration — Engine & Daemon: behaves differently than configured”",
      "Effective daemon configuration configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon configuration — Engine & Daemon: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Daemon configuration — Engine & Daemon: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Daemon configuration — Engine & Daemon: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon configuration — Engine & Daemon: behaves differently than configured”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon configuration — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon configuration",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon configuration\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon configuration\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon configuration",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon configuration — Engine & Daemon: behaves differently than configured”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon configuration — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon configuration configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon configuration configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon configuration configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The daemon configuration configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon configuration — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon configuration",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon configuration\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon configuration — Engine & Daemon: behaves differently than configured”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon configuration configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon configuration configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon configuration — Engine & Daemon: behaves differently than configured”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Daemon configuration — Engine & Daemon: behaves differently than configured: evidence found\nFor “Daemon configuration — Engine & Daemon: behaves differently than configured”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon configuration — Engine & Daemon: behaves differently than configured”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "check": "docker info",
        "output": "Daemon configuration — Engine & Daemon: behaves differently than configured: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Daemon configuration — Engine & Daemon: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-009",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Daemon configuration — Engine & Daemon: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective daemon configuration state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "daemon configuration",
      "Engine & Daemon",
      "Daemon configuration — Engine & Daemon: fails intermittently under load or restart",
      "daemon",
      "configuration",
      "engine",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon configuration — Engine & Daemon: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon configuration; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon configuration — Engine & Daemon: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon configuration — Engine & Daemon: fails intermittently under load or restart”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon configuration",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon configuration configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon configuration — Engine & Daemon: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the daemon configuration inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon configuration — Engine & Daemon: fails intermittently under load or restart”",
      "Effective daemon configuration configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon configuration — Engine & Daemon: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Daemon configuration — Engine & Daemon: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Daemon configuration — Engine & Daemon: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon configuration — Engine & Daemon: fails intermittently under load or restart”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon configuration — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon configuration",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon configuration\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon configuration\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon configuration",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon configuration — Engine & Daemon: fails intermittently under load or restart”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon configuration — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon configuration configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon configuration configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon configuration configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The daemon configuration configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon configuration — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon configuration",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon configuration\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon configuration — Engine & Daemon: fails intermittently under load or restart”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon configuration configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon configuration configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon configuration — Engine & Daemon: fails intermittently under load or restart”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Daemon configuration — Engine & Daemon: fails intermittently under load or restart: evidence found\nFor “Daemon configuration — Engine & Daemon: fails intermittently under load or restart”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon configuration — Engine & Daemon: fails intermittently under load or restart”, the effective daemon configuration state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "check": "docker info",
        "output": "Daemon configuration — Engine & Daemon: fails intermittently under load or restart: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon configuration path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Daemon configuration — Engine & Daemon: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-010",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Docker context endpoint — Engine & Daemon: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Docker context endpoint state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "Docker context endpoint",
      "Engine & Daemon",
      "Docker context endpoint — Engine & Daemon: fails completely",
      "docker",
      "context",
      "endpoint",
      "engine",
      "daemon",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker context endpoint — Engine & Daemon: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker context endpoint; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker context endpoint — Engine & Daemon: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker context endpoint — Engine & Daemon: fails completely”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker context endpoint",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker context endpoint configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker context endpoint — Engine & Daemon: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Docker context endpoint inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker context endpoint — Engine & Daemon: fails completely”",
      "Effective Docker context endpoint configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker context endpoint — Engine & Daemon: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Docker context endpoint — Engine & Daemon: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker context endpoint — Engine & Daemon: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker context endpoint — Engine & Daemon: fails completely”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker context endpoint — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker context endpoint",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker context endpoint\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker context endpoint\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker context endpoint",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker context endpoint — Engine & Daemon: fails completely”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker context endpoint — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker context endpoint configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker context endpoint configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker context endpoint configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The Docker context endpoint configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker context endpoint — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker context endpoint",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker context endpoint\nThe effective runtime value differs from expectation.",
        "root": "For “Docker context endpoint — Engine & Daemon: fails completely”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker context endpoint configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker context endpoint configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker context endpoint — Engine & Daemon: fails completely”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Docker context endpoint — Engine & Daemon: fails completely: evidence found\nFor “Docker context endpoint — Engine & Daemon: fails completely”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker context endpoint — Engine & Daemon: fails completely”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "check": "docker info",
        "output": "Docker context endpoint — Engine & Daemon: fails completely: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Docker context endpoint — Engine & Daemon: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-011",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Docker context endpoint — Engine & Daemon: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Docker context endpoint state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "Docker context endpoint",
      "Engine & Daemon",
      "Docker context endpoint — Engine & Daemon: behaves differently than configured",
      "docker",
      "context",
      "endpoint",
      "engine",
      "daemon",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker context endpoint — Engine & Daemon: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker context endpoint; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker context endpoint — Engine & Daemon: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker context endpoint — Engine & Daemon: behaves differently than configured”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker context endpoint",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker context endpoint configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker context endpoint — Engine & Daemon: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Docker context endpoint inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker context endpoint — Engine & Daemon: behaves differently than configured”",
      "Effective Docker context endpoint configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker context endpoint — Engine & Daemon: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Docker context endpoint — Engine & Daemon: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker context endpoint — Engine & Daemon: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker context endpoint — Engine & Daemon: behaves differently than configured”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker context endpoint — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker context endpoint",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker context endpoint\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker context endpoint\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker context endpoint",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker context endpoint — Engine & Daemon: behaves differently than configured”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker context endpoint — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker context endpoint configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker context endpoint configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker context endpoint configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The Docker context endpoint configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker context endpoint — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker context endpoint",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker context endpoint\nThe effective runtime value differs from expectation.",
        "root": "For “Docker context endpoint — Engine & Daemon: behaves differently than configured”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker context endpoint configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker context endpoint configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker context endpoint — Engine & Daemon: behaves differently than configured”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Docker context endpoint — Engine & Daemon: behaves differently than configured: evidence found\nFor “Docker context endpoint — Engine & Daemon: behaves differently than configured”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker context endpoint — Engine & Daemon: behaves differently than configured”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "check": "docker info",
        "output": "Docker context endpoint — Engine & Daemon: behaves differently than configured: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Docker context endpoint — Engine & Daemon: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-012",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Docker context endpoint — Engine & Daemon: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Docker context endpoint state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "Docker context endpoint",
      "Engine & Daemon",
      "Docker context endpoint — Engine & Daemon: fails intermittently under load or restart",
      "docker",
      "context",
      "endpoint",
      "engine",
      "daemon",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker context endpoint — Engine & Daemon: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker context endpoint; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker context endpoint — Engine & Daemon: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker context endpoint — Engine & Daemon: fails intermittently under load or restart”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker context endpoint",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker context endpoint configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker context endpoint — Engine & Daemon: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Docker context endpoint inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker context endpoint — Engine & Daemon: fails intermittently under load or restart”",
      "Effective Docker context endpoint configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker context endpoint — Engine & Daemon: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Docker context endpoint — Engine & Daemon: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker context endpoint — Engine & Daemon: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker context endpoint — Engine & Daemon: fails intermittently under load or restart”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker context endpoint — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker context endpoint",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker context endpoint\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker context endpoint\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker context endpoint",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker context endpoint — Engine & Daemon: fails intermittently under load or restart”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker context endpoint — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker context endpoint configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker context endpoint configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker context endpoint configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The Docker context endpoint configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker context endpoint — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker context endpoint",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker context endpoint\nThe effective runtime value differs from expectation.",
        "root": "For “Docker context endpoint — Engine & Daemon: fails intermittently under load or restart”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker context endpoint configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker context endpoint configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker context endpoint — Engine & Daemon: fails intermittently under load or restart”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Docker context endpoint — Engine & Daemon: fails intermittently under load or restart: evidence found\nFor “Docker context endpoint — Engine & Daemon: fails intermittently under load or restart”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker context endpoint — Engine & Daemon: fails intermittently under load or restart”, the effective Docker context endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "check": "docker info",
        "output": "Docker context endpoint — Engine & Daemon: fails intermittently under load or restart: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker context endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Docker context endpoint — Engine & Daemon: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-013",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Data-root access — Engine & Daemon: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective data-root access state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "data-root access",
      "Engine & Daemon",
      "Data-root access — Engine & Daemon: fails completely",
      "data",
      "root",
      "access",
      "engine",
      "daemon",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Data-root access — Engine & Daemon: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected data-root access; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Data-root access — Engine & Daemon: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Data-root access — Engine & Daemon: fails completely”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for data-root access",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The data-root access configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Data-root access — Engine & Daemon: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the data-root access inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Data-root access — Engine & Daemon: fails completely”",
      "Effective data-root access configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Data-root access — Engine & Daemon: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Data-root access — Engine & Daemon: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Data-root access — Engine & Daemon: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Data-root access — Engine & Daemon: fails completely”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Data-root access — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for data-root access",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for data-root access\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for data-root access\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for data-root access",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Data-root access — Engine & Daemon: fails completely”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Data-root access — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The data-root access configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The data-root access configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The data-root access configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The data-root access configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Data-root access — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for data-root access",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for data-root access\nThe effective runtime value differs from expectation.",
        "root": "For “Data-root access — Engine & Daemon: fails completely”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The data-root access configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The data-root access configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Data-root access — Engine & Daemon: fails completely”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Data-root access — Engine & Daemon: fails completely: evidence found\nFor “Data-root access — Engine & Daemon: fails completely”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Data-root access — Engine & Daemon: fails completely”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "check": "docker info",
        "output": "Data-root access — Engine & Daemon: fails completely: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Data-root access — Engine & Daemon: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-014",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Data-root access — Engine & Daemon: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective data-root access state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "data-root access",
      "Engine & Daemon",
      "Data-root access — Engine & Daemon: behaves differently than configured",
      "data",
      "root",
      "access",
      "engine",
      "daemon",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Data-root access — Engine & Daemon: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected data-root access; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Data-root access — Engine & Daemon: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Data-root access — Engine & Daemon: behaves differently than configured”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for data-root access",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The data-root access configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Data-root access — Engine & Daemon: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the data-root access inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Data-root access — Engine & Daemon: behaves differently than configured”",
      "Effective data-root access configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Data-root access — Engine & Daemon: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Data-root access — Engine & Daemon: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Data-root access — Engine & Daemon: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Data-root access — Engine & Daemon: behaves differently than configured”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Data-root access — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for data-root access",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for data-root access\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for data-root access\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for data-root access",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Data-root access — Engine & Daemon: behaves differently than configured”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Data-root access — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The data-root access configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The data-root access configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The data-root access configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The data-root access configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Data-root access — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for data-root access",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for data-root access\nThe effective runtime value differs from expectation.",
        "root": "For “Data-root access — Engine & Daemon: behaves differently than configured”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The data-root access configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The data-root access configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Data-root access — Engine & Daemon: behaves differently than configured”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Data-root access — Engine & Daemon: behaves differently than configured: evidence found\nFor “Data-root access — Engine & Daemon: behaves differently than configured”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Data-root access — Engine & Daemon: behaves differently than configured”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "check": "docker info",
        "output": "Data-root access — Engine & Daemon: behaves differently than configured: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Data-root access — Engine & Daemon: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-015",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Data-root access — Engine & Daemon: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective data-root access state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "data-root access",
      "Engine & Daemon",
      "Data-root access — Engine & Daemon: fails intermittently under load or restart",
      "data",
      "root",
      "access",
      "engine",
      "daemon",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Data-root access — Engine & Daemon: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected data-root access; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Data-root access — Engine & Daemon: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Data-root access — Engine & Daemon: fails intermittently under load or restart”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for data-root access",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The data-root access configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Data-root access — Engine & Daemon: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the data-root access inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Data-root access — Engine & Daemon: fails intermittently under load or restart”",
      "Effective data-root access configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Data-root access — Engine & Daemon: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Data-root access — Engine & Daemon: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Data-root access — Engine & Daemon: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Data-root access — Engine & Daemon: fails intermittently under load or restart”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Data-root access — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for data-root access",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for data-root access\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for data-root access\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for data-root access",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Data-root access — Engine & Daemon: fails intermittently under load or restart”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Data-root access — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The data-root access configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The data-root access configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The data-root access configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The data-root access configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Data-root access — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for data-root access",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for data-root access\nThe effective runtime value differs from expectation.",
        "root": "For “Data-root access — Engine & Daemon: fails intermittently under load or restart”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The data-root access configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The data-root access configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Data-root access — Engine & Daemon: fails intermittently under load or restart”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Data-root access — Engine & Daemon: fails intermittently under load or restart: evidence found\nFor “Data-root access — Engine & Daemon: fails intermittently under load or restart”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Data-root access — Engine & Daemon: fails intermittently under load or restart”, the effective data-root access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "check": "docker info",
        "output": "Data-root access — Engine & Daemon: fails intermittently under load or restart: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the data-root access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Data-root access — Engine & Daemon: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-016",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Daemon API responsiveness — Engine & Daemon: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective daemon API responsiveness state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "daemon API responsiveness",
      "Engine & Daemon",
      "Daemon API responsiveness — Engine & Daemon: fails completely",
      "daemon",
      "api",
      "responsiveness",
      "engine",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon API responsiveness — Engine & Daemon: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon API responsiveness; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon API responsiveness — Engine & Daemon: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon API responsiveness — Engine & Daemon: fails completely”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon API responsiveness",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon API responsiveness configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon API responsiveness — Engine & Daemon: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the daemon API responsiveness inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon API responsiveness — Engine & Daemon: fails completely”",
      "Effective daemon API responsiveness configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon API responsiveness — Engine & Daemon: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Daemon API responsiveness — Engine & Daemon: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Daemon API responsiveness — Engine & Daemon: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon API responsiveness — Engine & Daemon: fails completely”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon API responsiveness — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon API responsiveness",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon API responsiveness\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon API responsiveness\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon API responsiveness",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon API responsiveness — Engine & Daemon: fails completely”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon API responsiveness — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon API responsiveness configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon API responsiveness configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon API responsiveness configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The daemon API responsiveness configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon API responsiveness — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon API responsiveness",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon API responsiveness\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon API responsiveness — Engine & Daemon: fails completely”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon API responsiveness configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon API responsiveness configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon API responsiveness — Engine & Daemon: fails completely”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Daemon API responsiveness — Engine & Daemon: fails completely: evidence found\nFor “Daemon API responsiveness — Engine & Daemon: fails completely”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon API responsiveness — Engine & Daemon: fails completely”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "check": "docker info",
        "output": "Daemon API responsiveness — Engine & Daemon: fails completely: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Daemon API responsiveness — Engine & Daemon: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-017",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Daemon API responsiveness — Engine & Daemon: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective daemon API responsiveness state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "daemon API responsiveness",
      "Engine & Daemon",
      "Daemon API responsiveness — Engine & Daemon: behaves differently than configured",
      "daemon",
      "api",
      "responsiveness",
      "engine",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon API responsiveness — Engine & Daemon: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon API responsiveness; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon API responsiveness — Engine & Daemon: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon API responsiveness — Engine & Daemon: behaves differently than configured”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon API responsiveness",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon API responsiveness configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon API responsiveness — Engine & Daemon: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the daemon API responsiveness inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon API responsiveness — Engine & Daemon: behaves differently than configured”",
      "Effective daemon API responsiveness configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon API responsiveness — Engine & Daemon: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Daemon API responsiveness — Engine & Daemon: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Daemon API responsiveness — Engine & Daemon: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon API responsiveness — Engine & Daemon: behaves differently than configured”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon API responsiveness — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon API responsiveness",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon API responsiveness\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon API responsiveness\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon API responsiveness",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon API responsiveness — Engine & Daemon: behaves differently than configured”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon API responsiveness — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon API responsiveness configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon API responsiveness configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon API responsiveness configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The daemon API responsiveness configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon API responsiveness — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon API responsiveness",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon API responsiveness\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon API responsiveness — Engine & Daemon: behaves differently than configured”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon API responsiveness configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon API responsiveness configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon API responsiveness — Engine & Daemon: behaves differently than configured”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Daemon API responsiveness — Engine & Daemon: behaves differently than configured: evidence found\nFor “Daemon API responsiveness — Engine & Daemon: behaves differently than configured”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon API responsiveness — Engine & Daemon: behaves differently than configured”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "check": "docker info",
        "output": "Daemon API responsiveness — Engine & Daemon: behaves differently than configured: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Daemon API responsiveness — Engine & Daemon: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-018",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective daemon API responsiveness state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "daemon API responsiveness",
      "Engine & Daemon",
      "Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart",
      "daemon",
      "api",
      "responsiveness",
      "engine",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon API responsiveness; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon API responsiveness",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon API responsiveness configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the daemon API responsiveness inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart”",
      "Effective daemon API responsiveness configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "Observed Docker state for Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon API responsiveness",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon API responsiveness\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon API responsiveness\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon API responsiveness",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon API responsiveness configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon API responsiveness configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon API responsiveness configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The daemon API responsiveness configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon API responsiveness",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon API responsiveness\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon API responsiveness configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon API responsiveness configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart: evidence found\nFor “Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart”, the effective daemon API responsiveness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "check": "docker info",
        "output": "Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon API responsiveness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Daemon API responsiveness — Engine & Daemon: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-019",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Daemon proxy/DNS — Engine & Daemon: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective daemon proxy/DNS state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "daemon proxy/DNS",
      "Engine & Daemon",
      "Daemon proxy/DNS — Engine & Daemon: fails completely",
      "daemon",
      "proxy",
      "dns",
      "engine",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon proxy/DNS — Engine & Daemon: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon proxy/DNS; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon proxy/DNS — Engine & Daemon: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon proxy/DNS — Engine & Daemon: fails completely”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon proxy/DNS",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon proxy/DNS configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon proxy/DNS — Engine & Daemon: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the daemon proxy/DNS inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon proxy/DNS — Engine & Daemon: fails completely”",
      "Effective daemon proxy/DNS configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon proxy/DNS — Engine & Daemon: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon proxy/DNS — Engine & Daemon: fails completely”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon proxy/DNS — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon proxy/DNS — Engine & Daemon: fails completely”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon proxy/DNS — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon proxy/DNS configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon proxy/DNS configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon proxy/DNS configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The daemon proxy/DNS configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon proxy/DNS — Engine & Daemon: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon proxy/DNS",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon proxy/DNS — Engine & Daemon: fails completely”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon proxy/DNS configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon proxy/DNS configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon proxy/DNS — Engine & Daemon: fails completely”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Daemon proxy/DNS — Engine & Daemon: fails completely: evidence found\nFor “Daemon proxy/DNS — Engine & Daemon: fails completely”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon proxy/DNS — Engine & Daemon: fails completely”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "check": "docker info",
        "output": "Daemon proxy/DNS — Engine & Daemon: fails completely: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Daemon proxy/DNS — Engine & Daemon: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-020",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Daemon proxy/DNS — Engine & Daemon: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective daemon proxy/DNS state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "daemon proxy/DNS",
      "Engine & Daemon",
      "Daemon proxy/DNS — Engine & Daemon: behaves differently than configured",
      "daemon",
      "proxy",
      "dns",
      "engine",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon proxy/DNS — Engine & Daemon: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon proxy/DNS; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon proxy/DNS — Engine & Daemon: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon proxy/DNS — Engine & Daemon: behaves differently than configured”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon proxy/DNS",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon proxy/DNS configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon proxy/DNS — Engine & Daemon: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the daemon proxy/DNS inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon proxy/DNS — Engine & Daemon: behaves differently than configured”",
      "Effective daemon proxy/DNS configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon proxy/DNS — Engine & Daemon: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon proxy/DNS — Engine & Daemon: behaves differently than configured”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon proxy/DNS — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon proxy/DNS — Engine & Daemon: behaves differently than configured”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon proxy/DNS — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon proxy/DNS configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon proxy/DNS configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon proxy/DNS configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The daemon proxy/DNS configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon proxy/DNS — Engine & Daemon: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon proxy/DNS",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon proxy/DNS — Engine & Daemon: behaves differently than configured”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon proxy/DNS configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon proxy/DNS configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon proxy/DNS — Engine & Daemon: behaves differently than configured”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Daemon proxy/DNS — Engine & Daemon: behaves differently than configured: evidence found\nFor “Daemon proxy/DNS — Engine & Daemon: behaves differently than configured”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon proxy/DNS — Engine & Daemon: behaves differently than configured”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "check": "docker info",
        "output": "Daemon proxy/DNS — Engine & Daemon: behaves differently than configured: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Daemon proxy/DNS — Engine & Daemon: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-021",
    "technology": "Docker",
    "category": "Engine & Daemon",
    "title": "Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective daemon proxy/DNS state first, then narrow the failure through the engine & daemon dependency chain.",
    "aliases": [
      "daemon proxy/DNS",
      "Engine & Daemon",
      "Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart",
      "daemon",
      "proxy",
      "dns",
      "engine",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect client/server versions",
        "command": "docker version",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon proxy/DNS; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List contexts/endpoints",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
      "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon proxy/DNS",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon proxy/DNS configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the daemon proxy/DNS inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart”",
      "Effective daemon proxy/DNS configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Engine & Daemon Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker version",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context show",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Engine & Daemon path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS",
        "command": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker version",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon proxy/DNS configuration is correct but the failure remains",
        "command": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon proxy/DNS configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon proxy/DNS configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The daemon proxy/DNS configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon proxy/DNS",
        "check": "docker version",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon proxy/DNS\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon proxy/DNS configuration is correct but the failure remains",
        "check": "docker info",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon proxy/DNS configuration is correct but the failure remains",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context show",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker version",
        "output": "Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart: evidence found\nFor “Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart”, the effective daemon proxy/DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "check": "docker info",
        "output": "Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart: evidence found\nA engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "root": "A engine & daemon dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon proxy/DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context show",
        "output": "Daemon proxy/DNS — Engine & Daemon: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-022",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container startup — Container Lifecycle: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective container startup state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container startup",
      "Container Lifecycle",
      "Container startup — Container Lifecycle: fails completely",
      "container",
      "startup",
      "lifecycle",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container startup — Container Lifecycle: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container startup; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container startup — Container Lifecycle: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container startup — Container Lifecycle: fails completely”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container startup",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container startup configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container startup — Container Lifecycle: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the container startup inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container startup — Container Lifecycle: fails completely”",
      "Effective container startup configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container startup — Container Lifecycle: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container startup — Container Lifecycle: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container startup — Container Lifecycle: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container startup — Container Lifecycle: fails completely”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container startup — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container startup",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container startup\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container startup\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container startup",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container startup — Container Lifecycle: fails completely”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container startup — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container startup configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container startup configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container startup configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container startup configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container startup — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container startup",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container startup\nThe effective runtime value differs from expectation.",
        "root": "For “Container startup — Container Lifecycle: fails completely”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container startup configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container startup configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container startup — Container Lifecycle: fails completely”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container startup — Container Lifecycle: fails completely: evidence found\nFor “Container startup — Container Lifecycle: fails completely”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container startup — Container Lifecycle: fails completely”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container startup — Container Lifecycle: fails completely: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container startup — Container Lifecycle: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-023",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container startup — Container Lifecycle: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective container startup state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container startup",
      "Container Lifecycle",
      "Container startup — Container Lifecycle: behaves differently than configured",
      "container",
      "startup",
      "lifecycle",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container startup — Container Lifecycle: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container startup; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container startup — Container Lifecycle: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container startup — Container Lifecycle: behaves differently than configured”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container startup",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container startup configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container startup — Container Lifecycle: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the container startup inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container startup — Container Lifecycle: behaves differently than configured”",
      "Effective container startup configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container startup — Container Lifecycle: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container startup — Container Lifecycle: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container startup — Container Lifecycle: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container startup — Container Lifecycle: behaves differently than configured”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container startup — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container startup",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container startup\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container startup\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container startup",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container startup — Container Lifecycle: behaves differently than configured”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container startup — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container startup configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container startup configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container startup configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container startup configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container startup — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container startup",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container startup\nThe effective runtime value differs from expectation.",
        "root": "For “Container startup — Container Lifecycle: behaves differently than configured”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container startup configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container startup configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container startup — Container Lifecycle: behaves differently than configured”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container startup — Container Lifecycle: behaves differently than configured: evidence found\nFor “Container startup — Container Lifecycle: behaves differently than configured”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container startup — Container Lifecycle: behaves differently than configured”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container startup — Container Lifecycle: behaves differently than configured: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container startup — Container Lifecycle: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-024",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container startup — Container Lifecycle: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective container startup state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container startup",
      "Container Lifecycle",
      "Container startup — Container Lifecycle: fails intermittently under load or restart",
      "container",
      "startup",
      "lifecycle",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container startup — Container Lifecycle: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container startup; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container startup — Container Lifecycle: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container startup — Container Lifecycle: fails intermittently under load or restart”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container startup",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container startup configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container startup — Container Lifecycle: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the container startup inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container startup — Container Lifecycle: fails intermittently under load or restart”",
      "Effective container startup configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container startup — Container Lifecycle: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container startup — Container Lifecycle: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container startup — Container Lifecycle: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container startup — Container Lifecycle: fails intermittently under load or restart”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container startup — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container startup",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container startup\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container startup\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container startup",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container startup — Container Lifecycle: fails intermittently under load or restart”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container startup — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container startup configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container startup configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container startup configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container startup configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container startup — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container startup",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container startup\nThe effective runtime value differs from expectation.",
        "root": "For “Container startup — Container Lifecycle: fails intermittently under load or restart”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container startup configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container startup configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container startup — Container Lifecycle: fails intermittently under load or restart”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container startup — Container Lifecycle: fails intermittently under load or restart: evidence found\nFor “Container startup — Container Lifecycle: fails intermittently under load or restart”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container startup — Container Lifecycle: fails intermittently under load or restart”, the effective container startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container startup — Container Lifecycle: fails intermittently under load or restart: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container startup — Container Lifecycle: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-025",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container shutdown — Container Lifecycle: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective container shutdown state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container shutdown",
      "Container Lifecycle",
      "Container shutdown — Container Lifecycle: fails completely",
      "container",
      "shutdown",
      "lifecycle",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container shutdown — Container Lifecycle: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container shutdown; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container shutdown — Container Lifecycle: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container shutdown — Container Lifecycle: fails completely”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container shutdown",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container shutdown configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container shutdown — Container Lifecycle: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the container shutdown inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container shutdown — Container Lifecycle: fails completely”",
      "Effective container shutdown configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container shutdown — Container Lifecycle: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container shutdown — Container Lifecycle: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container shutdown — Container Lifecycle: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container shutdown — Container Lifecycle: fails completely”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container shutdown — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container shutdown",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container shutdown\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container shutdown\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container shutdown",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container shutdown — Container Lifecycle: fails completely”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container shutdown — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container shutdown configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container shutdown configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container shutdown configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container shutdown configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container shutdown — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container shutdown",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container shutdown\nThe effective runtime value differs from expectation.",
        "root": "For “Container shutdown — Container Lifecycle: fails completely”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container shutdown configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container shutdown configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container shutdown — Container Lifecycle: fails completely”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container shutdown — Container Lifecycle: fails completely: evidence found\nFor “Container shutdown — Container Lifecycle: fails completely”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container shutdown — Container Lifecycle: fails completely”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container shutdown — Container Lifecycle: fails completely: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container shutdown — Container Lifecycle: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-026",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container shutdown — Container Lifecycle: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective container shutdown state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container shutdown",
      "Container Lifecycle",
      "Container shutdown — Container Lifecycle: behaves differently than configured",
      "container",
      "shutdown",
      "lifecycle",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container shutdown — Container Lifecycle: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container shutdown; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container shutdown — Container Lifecycle: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container shutdown — Container Lifecycle: behaves differently than configured”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container shutdown",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container shutdown configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container shutdown — Container Lifecycle: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the container shutdown inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container shutdown — Container Lifecycle: behaves differently than configured”",
      "Effective container shutdown configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container shutdown — Container Lifecycle: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container shutdown — Container Lifecycle: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container shutdown — Container Lifecycle: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container shutdown — Container Lifecycle: behaves differently than configured”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container shutdown — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container shutdown",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container shutdown\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container shutdown\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container shutdown",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container shutdown — Container Lifecycle: behaves differently than configured”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container shutdown — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container shutdown configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container shutdown configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container shutdown configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container shutdown configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container shutdown — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container shutdown",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container shutdown\nThe effective runtime value differs from expectation.",
        "root": "For “Container shutdown — Container Lifecycle: behaves differently than configured”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container shutdown configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container shutdown configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container shutdown — Container Lifecycle: behaves differently than configured”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container shutdown — Container Lifecycle: behaves differently than configured: evidence found\nFor “Container shutdown — Container Lifecycle: behaves differently than configured”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container shutdown — Container Lifecycle: behaves differently than configured”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container shutdown — Container Lifecycle: behaves differently than configured: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container shutdown — Container Lifecycle: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-027",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container shutdown — Container Lifecycle: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective container shutdown state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container shutdown",
      "Container Lifecycle",
      "Container shutdown — Container Lifecycle: fails intermittently under load or restart",
      "container",
      "shutdown",
      "lifecycle",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container shutdown — Container Lifecycle: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container shutdown; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container shutdown — Container Lifecycle: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container shutdown — Container Lifecycle: fails intermittently under load or restart”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container shutdown",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container shutdown configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container shutdown — Container Lifecycle: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the container shutdown inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container shutdown — Container Lifecycle: fails intermittently under load or restart”",
      "Effective container shutdown configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container shutdown — Container Lifecycle: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container shutdown — Container Lifecycle: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container shutdown — Container Lifecycle: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container shutdown — Container Lifecycle: fails intermittently under load or restart”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container shutdown — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container shutdown",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container shutdown\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container shutdown\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container shutdown",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container shutdown — Container Lifecycle: fails intermittently under load or restart”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container shutdown — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container shutdown configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container shutdown configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container shutdown configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container shutdown configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container shutdown — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container shutdown",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container shutdown\nThe effective runtime value differs from expectation.",
        "root": "For “Container shutdown — Container Lifecycle: fails intermittently under load or restart”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container shutdown configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container shutdown configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container shutdown — Container Lifecycle: fails intermittently under load or restart”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container shutdown — Container Lifecycle: fails intermittently under load or restart: evidence found\nFor “Container shutdown — Container Lifecycle: fails intermittently under load or restart”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container shutdown — Container Lifecycle: fails intermittently under load or restart”, the effective container shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container shutdown — Container Lifecycle: fails intermittently under load or restart: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container shutdown — Container Lifecycle: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-028",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container restart loop — Container Lifecycle: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective container restart loop state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container restart loop",
      "Container Lifecycle",
      "Container restart loop — Container Lifecycle: fails completely",
      "container",
      "restart",
      "loop",
      "lifecycle",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container restart loop — Container Lifecycle: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container restart loop; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container restart loop — Container Lifecycle: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container restart loop — Container Lifecycle: fails completely”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container restart loop",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container restart loop configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container restart loop — Container Lifecycle: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the container restart loop inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container restart loop — Container Lifecycle: fails completely”",
      "Effective container restart loop configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container restart loop — Container Lifecycle: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container restart loop — Container Lifecycle: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container restart loop — Container Lifecycle: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container restart loop — Container Lifecycle: fails completely”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container restart loop — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container restart loop",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container restart loop\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container restart loop\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container restart loop",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container restart loop — Container Lifecycle: fails completely”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container restart loop — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container restart loop configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container restart loop configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container restart loop configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container restart loop configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container restart loop — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container restart loop",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container restart loop\nThe effective runtime value differs from expectation.",
        "root": "For “Container restart loop — Container Lifecycle: fails completely”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container restart loop configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container restart loop configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container restart loop — Container Lifecycle: fails completely”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container restart loop — Container Lifecycle: fails completely: evidence found\nFor “Container restart loop — Container Lifecycle: fails completely”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container restart loop — Container Lifecycle: fails completely”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container restart loop — Container Lifecycle: fails completely: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container restart loop — Container Lifecycle: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-029",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container restart loop — Container Lifecycle: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective container restart loop state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container restart loop",
      "Container Lifecycle",
      "Container restart loop — Container Lifecycle: behaves differently than configured",
      "container",
      "restart",
      "loop",
      "lifecycle",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container restart loop — Container Lifecycle: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container restart loop; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container restart loop — Container Lifecycle: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container restart loop — Container Lifecycle: behaves differently than configured”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container restart loop",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container restart loop configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container restart loop — Container Lifecycle: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the container restart loop inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container restart loop — Container Lifecycle: behaves differently than configured”",
      "Effective container restart loop configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container restart loop — Container Lifecycle: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container restart loop — Container Lifecycle: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container restart loop — Container Lifecycle: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container restart loop — Container Lifecycle: behaves differently than configured”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container restart loop — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container restart loop",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container restart loop\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container restart loop\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container restart loop",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container restart loop — Container Lifecycle: behaves differently than configured”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container restart loop — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container restart loop configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container restart loop configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container restart loop configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container restart loop configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container restart loop — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container restart loop",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container restart loop\nThe effective runtime value differs from expectation.",
        "root": "For “Container restart loop — Container Lifecycle: behaves differently than configured”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container restart loop configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container restart loop configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container restart loop — Container Lifecycle: behaves differently than configured”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container restart loop — Container Lifecycle: behaves differently than configured: evidence found\nFor “Container restart loop — Container Lifecycle: behaves differently than configured”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container restart loop — Container Lifecycle: behaves differently than configured”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container restart loop — Container Lifecycle: behaves differently than configured: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container restart loop — Container Lifecycle: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-030",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container restart loop — Container Lifecycle: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective container restart loop state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container restart loop",
      "Container Lifecycle",
      "Container restart loop — Container Lifecycle: fails intermittently under load or restart",
      "container",
      "restart",
      "loop",
      "lifecycle",
      "fails",
      "intermittently",
      "under",
      "load",
      "or"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container restart loop — Container Lifecycle: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container restart loop; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container restart loop — Container Lifecycle: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container restart loop — Container Lifecycle: fails intermittently under load or restart”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container restart loop",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container restart loop configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container restart loop — Container Lifecycle: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the container restart loop inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container restart loop — Container Lifecycle: fails intermittently under load or restart”",
      "Effective container restart loop configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container restart loop — Container Lifecycle: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container restart loop — Container Lifecycle: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container restart loop — Container Lifecycle: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container restart loop — Container Lifecycle: fails intermittently under load or restart”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container restart loop — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container restart loop",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container restart loop\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container restart loop\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container restart loop",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container restart loop — Container Lifecycle: fails intermittently under load or restart”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container restart loop — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container restart loop configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container restart loop configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container restart loop configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container restart loop configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container restart loop — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container restart loop",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container restart loop\nThe effective runtime value differs from expectation.",
        "root": "For “Container restart loop — Container Lifecycle: fails intermittently under load or restart”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container restart loop configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container restart loop configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container restart loop — Container Lifecycle: fails intermittently under load or restart”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container restart loop — Container Lifecycle: fails intermittently under load or restart: evidence found\nFor “Container restart loop — Container Lifecycle: fails intermittently under load or restart”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container restart loop — Container Lifecycle: fails intermittently under load or restart”, the effective container restart loop state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container restart loop — Container Lifecycle: fails intermittently under load or restart: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container restart loop path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container restart loop — Container Lifecycle: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-031",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container removal — Container Lifecycle: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective container removal state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container removal",
      "Container Lifecycle",
      "Container removal — Container Lifecycle: fails completely",
      "container",
      "removal",
      "lifecycle",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container removal — Container Lifecycle: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container removal; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container removal — Container Lifecycle: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container removal — Container Lifecycle: fails completely”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container removal",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container removal configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container removal — Container Lifecycle: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the container removal inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container removal — Container Lifecycle: fails completely”",
      "Effective container removal configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container removal — Container Lifecycle: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container removal — Container Lifecycle: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container removal — Container Lifecycle: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container removal — Container Lifecycle: fails completely”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container removal — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container removal",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container removal\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container removal\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container removal",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container removal — Container Lifecycle: fails completely”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container removal — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container removal configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container removal configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container removal configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container removal configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container removal — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container removal",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container removal\nThe effective runtime value differs from expectation.",
        "root": "For “Container removal — Container Lifecycle: fails completely”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container removal configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container removal configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container removal — Container Lifecycle: fails completely”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container removal — Container Lifecycle: fails completely: evidence found\nFor “Container removal — Container Lifecycle: fails completely”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container removal — Container Lifecycle: fails completely”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container removal — Container Lifecycle: fails completely: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container removal — Container Lifecycle: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-032",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container removal — Container Lifecycle: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective container removal state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container removal",
      "Container Lifecycle",
      "Container removal — Container Lifecycle: behaves differently than configured",
      "container",
      "removal",
      "lifecycle",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container removal — Container Lifecycle: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container removal; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container removal — Container Lifecycle: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container removal — Container Lifecycle: behaves differently than configured”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container removal",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container removal configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container removal — Container Lifecycle: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the container removal inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container removal — Container Lifecycle: behaves differently than configured”",
      "Effective container removal configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container removal — Container Lifecycle: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container removal — Container Lifecycle: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container removal — Container Lifecycle: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container removal — Container Lifecycle: behaves differently than configured”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container removal — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container removal",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container removal\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container removal\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container removal",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container removal — Container Lifecycle: behaves differently than configured”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container removal — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container removal configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container removal configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container removal configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container removal configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container removal — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container removal",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container removal\nThe effective runtime value differs from expectation.",
        "root": "For “Container removal — Container Lifecycle: behaves differently than configured”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container removal configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container removal configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container removal — Container Lifecycle: behaves differently than configured”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container removal — Container Lifecycle: behaves differently than configured: evidence found\nFor “Container removal — Container Lifecycle: behaves differently than configured”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container removal — Container Lifecycle: behaves differently than configured”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container removal — Container Lifecycle: behaves differently than configured: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container removal — Container Lifecycle: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-033",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container removal — Container Lifecycle: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective container removal state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container removal",
      "Container Lifecycle",
      "Container removal — Container Lifecycle: fails intermittently under load or restart",
      "container",
      "removal",
      "lifecycle",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container removal — Container Lifecycle: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container removal; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container removal — Container Lifecycle: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container removal — Container Lifecycle: fails intermittently under load or restart”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container removal",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container removal configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container removal — Container Lifecycle: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the container removal inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container removal — Container Lifecycle: fails intermittently under load or restart”",
      "Effective container removal configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container removal — Container Lifecycle: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container removal — Container Lifecycle: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container removal — Container Lifecycle: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container removal — Container Lifecycle: fails intermittently under load or restart”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container removal — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container removal",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container removal\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container removal\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container removal",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container removal — Container Lifecycle: fails intermittently under load or restart”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container removal — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container removal configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container removal configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container removal configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container removal configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container removal — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container removal",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container removal\nThe effective runtime value differs from expectation.",
        "root": "For “Container removal — Container Lifecycle: fails intermittently under load or restart”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container removal configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container removal configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container removal — Container Lifecycle: fails intermittently under load or restart”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container removal — Container Lifecycle: fails intermittently under load or restart: evidence found\nFor “Container removal — Container Lifecycle: fails intermittently under load or restart”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container removal — Container Lifecycle: fails intermittently under load or restart”, the effective container removal state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container removal — Container Lifecycle: fails intermittently under load or restart: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container removal path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container removal — Container Lifecycle: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-034",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container pause/unpause — Container Lifecycle: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective container pause/unpause state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container pause/unpause",
      "Container Lifecycle",
      "Container pause/unpause — Container Lifecycle: fails completely",
      "container",
      "pause",
      "unpause",
      "lifecycle",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container pause/unpause — Container Lifecycle: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container pause/unpause; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container pause/unpause — Container Lifecycle: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container pause/unpause — Container Lifecycle: fails completely”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container pause/unpause",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container pause/unpause configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container pause/unpause — Container Lifecycle: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the container pause/unpause inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container pause/unpause — Container Lifecycle: fails completely”",
      "Effective container pause/unpause configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container pause/unpause — Container Lifecycle: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container pause/unpause — Container Lifecycle: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container pause/unpause — Container Lifecycle: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container pause/unpause — Container Lifecycle: fails completely”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container pause/unpause — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container pause/unpause",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container pause/unpause\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container pause/unpause\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container pause/unpause",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container pause/unpause — Container Lifecycle: fails completely”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container pause/unpause — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container pause/unpause configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container pause/unpause configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container pause/unpause configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container pause/unpause configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container pause/unpause — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container pause/unpause",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container pause/unpause\nThe effective runtime value differs from expectation.",
        "root": "For “Container pause/unpause — Container Lifecycle: fails completely”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container pause/unpause configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container pause/unpause configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container pause/unpause — Container Lifecycle: fails completely”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container pause/unpause — Container Lifecycle: fails completely: evidence found\nFor “Container pause/unpause — Container Lifecycle: fails completely”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container pause/unpause — Container Lifecycle: fails completely”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container pause/unpause — Container Lifecycle: fails completely: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container pause/unpause — Container Lifecycle: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-035",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container pause/unpause — Container Lifecycle: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective container pause/unpause state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container pause/unpause",
      "Container Lifecycle",
      "Container pause/unpause — Container Lifecycle: behaves differently than configured",
      "container",
      "pause",
      "unpause",
      "lifecycle",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container pause/unpause — Container Lifecycle: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container pause/unpause; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container pause/unpause — Container Lifecycle: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container pause/unpause — Container Lifecycle: behaves differently than configured”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container pause/unpause",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container pause/unpause configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container pause/unpause — Container Lifecycle: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the container pause/unpause inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container pause/unpause — Container Lifecycle: behaves differently than configured”",
      "Effective container pause/unpause configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container pause/unpause — Container Lifecycle: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container pause/unpause — Container Lifecycle: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container pause/unpause — Container Lifecycle: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container pause/unpause — Container Lifecycle: behaves differently than configured”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container pause/unpause — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container pause/unpause",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container pause/unpause\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container pause/unpause\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container pause/unpause",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container pause/unpause — Container Lifecycle: behaves differently than configured”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container pause/unpause — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container pause/unpause configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container pause/unpause configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container pause/unpause configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container pause/unpause configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container pause/unpause — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container pause/unpause",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container pause/unpause\nThe effective runtime value differs from expectation.",
        "root": "For “Container pause/unpause — Container Lifecycle: behaves differently than configured”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container pause/unpause configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container pause/unpause configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container pause/unpause — Container Lifecycle: behaves differently than configured”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container pause/unpause — Container Lifecycle: behaves differently than configured: evidence found\nFor “Container pause/unpause — Container Lifecycle: behaves differently than configured”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container pause/unpause — Container Lifecycle: behaves differently than configured”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container pause/unpause — Container Lifecycle: behaves differently than configured: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container pause/unpause — Container Lifecycle: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-036",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Container pause/unpause — Container Lifecycle: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective container pause/unpause state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "container pause/unpause",
      "Container Lifecycle",
      "Container pause/unpause — Container Lifecycle: fails intermittently under load or restart",
      "container",
      "pause",
      "unpause",
      "lifecycle",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container pause/unpause — Container Lifecycle: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container pause/unpause; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container pause/unpause — Container Lifecycle: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container pause/unpause — Container Lifecycle: fails intermittently under load or restart”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container pause/unpause",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container pause/unpause configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container pause/unpause — Container Lifecycle: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the container pause/unpause inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container pause/unpause — Container Lifecycle: fails intermittently under load or restart”",
      "Effective container pause/unpause configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container pause/unpause — Container Lifecycle: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Container pause/unpause — Container Lifecycle: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container pause/unpause — Container Lifecycle: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container pause/unpause — Container Lifecycle: fails intermittently under load or restart”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container pause/unpause — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container pause/unpause",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container pause/unpause\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container pause/unpause\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container pause/unpause",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container pause/unpause — Container Lifecycle: fails intermittently under load or restart”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container pause/unpause — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container pause/unpause configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container pause/unpause configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container pause/unpause configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The container pause/unpause configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container pause/unpause — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container pause/unpause",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container pause/unpause\nThe effective runtime value differs from expectation.",
        "root": "For “Container pause/unpause — Container Lifecycle: fails intermittently under load or restart”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container pause/unpause configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container pause/unpause configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container pause/unpause — Container Lifecycle: fails intermittently under load or restart”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Container pause/unpause — Container Lifecycle: fails intermittently under load or restart: evidence found\nFor “Container pause/unpause — Container Lifecycle: fails intermittently under load or restart”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container pause/unpause — Container Lifecycle: fails intermittently under load or restart”, the effective container pause/unpause state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container pause/unpause — Container Lifecycle: fails intermittently under load or restart: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container pause/unpause path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container pause/unpause — Container Lifecycle: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-037",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Runtime command — Container Lifecycle: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective runtime command state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "runtime command",
      "Container Lifecycle",
      "Runtime command — Container Lifecycle: fails completely",
      "runtime",
      "command",
      "container",
      "lifecycle",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Runtime command — Container Lifecycle: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected runtime command; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Runtime command — Container Lifecycle: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Runtime command — Container Lifecycle: fails completely”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for runtime command",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The runtime command configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Runtime command — Container Lifecycle: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the runtime command inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Runtime command — Container Lifecycle: fails completely”",
      "Effective runtime command configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Runtime command — Container Lifecycle: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Runtime command — Container Lifecycle: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Runtime command — Container Lifecycle: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Runtime command — Container Lifecycle: fails completely”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Runtime command — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for runtime command",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime command\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime command\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for runtime command",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Runtime command — Container Lifecycle: fails completely”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Runtime command — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The runtime command configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime command configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime command configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The runtime command configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Runtime command — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for runtime command",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime command\nThe effective runtime value differs from expectation.",
        "root": "For “Runtime command — Container Lifecycle: fails completely”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The runtime command configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime command configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Runtime command — Container Lifecycle: fails completely”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Runtime command — Container Lifecycle: fails completely: evidence found\nFor “Runtime command — Container Lifecycle: fails completely”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Runtime command — Container Lifecycle: fails completely”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Runtime command — Container Lifecycle: fails completely: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Runtime command — Container Lifecycle: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-038",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Runtime command — Container Lifecycle: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective runtime command state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "runtime command",
      "Container Lifecycle",
      "Runtime command — Container Lifecycle: behaves differently than configured",
      "runtime",
      "command",
      "container",
      "lifecycle",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Runtime command — Container Lifecycle: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected runtime command; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Runtime command — Container Lifecycle: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Runtime command — Container Lifecycle: behaves differently than configured”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for runtime command",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The runtime command configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Runtime command — Container Lifecycle: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the runtime command inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Runtime command — Container Lifecycle: behaves differently than configured”",
      "Effective runtime command configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Runtime command — Container Lifecycle: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Runtime command — Container Lifecycle: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Runtime command — Container Lifecycle: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Runtime command — Container Lifecycle: behaves differently than configured”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Runtime command — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for runtime command",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime command\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime command\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for runtime command",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Runtime command — Container Lifecycle: behaves differently than configured”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Runtime command — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The runtime command configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime command configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime command configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The runtime command configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Runtime command — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for runtime command",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime command\nThe effective runtime value differs from expectation.",
        "root": "For “Runtime command — Container Lifecycle: behaves differently than configured”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The runtime command configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime command configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Runtime command — Container Lifecycle: behaves differently than configured”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Runtime command — Container Lifecycle: behaves differently than configured: evidence found\nFor “Runtime command — Container Lifecycle: behaves differently than configured”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Runtime command — Container Lifecycle: behaves differently than configured”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Runtime command — Container Lifecycle: behaves differently than configured: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Runtime command — Container Lifecycle: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-039",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Runtime command — Container Lifecycle: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective runtime command state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "runtime command",
      "Container Lifecycle",
      "Runtime command — Container Lifecycle: fails intermittently under load or restart",
      "runtime",
      "command",
      "container",
      "lifecycle",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Runtime command — Container Lifecycle: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected runtime command; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Runtime command — Container Lifecycle: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Runtime command — Container Lifecycle: fails intermittently under load or restart”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for runtime command",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The runtime command configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Runtime command — Container Lifecycle: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the runtime command inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Runtime command — Container Lifecycle: fails intermittently under load or restart”",
      "Effective runtime command configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Runtime command — Container Lifecycle: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Runtime command — Container Lifecycle: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Runtime command — Container Lifecycle: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Runtime command — Container Lifecycle: fails intermittently under load or restart”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Runtime command — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for runtime command",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime command\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime command\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for runtime command",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Runtime command — Container Lifecycle: fails intermittently under load or restart”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Runtime command — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The runtime command configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime command configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime command configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The runtime command configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Runtime command — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for runtime command",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime command\nThe effective runtime value differs from expectation.",
        "root": "For “Runtime command — Container Lifecycle: fails intermittently under load or restart”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The runtime command configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime command configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Runtime command — Container Lifecycle: fails intermittently under load or restart”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Runtime command — Container Lifecycle: fails intermittently under load or restart: evidence found\nFor “Runtime command — Container Lifecycle: fails intermittently under load or restart”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Runtime command — Container Lifecycle: fails intermittently under load or restart”, the effective runtime command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Runtime command — Container Lifecycle: fails intermittently under load or restart: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Runtime command — Container Lifecycle: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-040",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Runtime working directory — Container Lifecycle: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective runtime working directory state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "runtime working directory",
      "Container Lifecycle",
      "Runtime working directory — Container Lifecycle: fails completely",
      "runtime",
      "working",
      "directory",
      "container",
      "lifecycle",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Runtime working directory — Container Lifecycle: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected runtime working directory; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Runtime working directory — Container Lifecycle: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Runtime working directory — Container Lifecycle: fails completely”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for runtime working directory",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The runtime working directory configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Runtime working directory — Container Lifecycle: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the runtime working directory inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Runtime working directory — Container Lifecycle: fails completely”",
      "Effective runtime working directory configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Runtime working directory — Container Lifecycle: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Runtime working directory — Container Lifecycle: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Runtime working directory — Container Lifecycle: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Runtime working directory — Container Lifecycle: fails completely”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Runtime working directory — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for runtime working directory",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime working directory\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime working directory\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for runtime working directory",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Runtime working directory — Container Lifecycle: fails completely”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Runtime working directory — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The runtime working directory configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime working directory configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime working directory configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The runtime working directory configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Runtime working directory — Container Lifecycle: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for runtime working directory",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime working directory\nThe effective runtime value differs from expectation.",
        "root": "For “Runtime working directory — Container Lifecycle: fails completely”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The runtime working directory configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime working directory configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Runtime working directory — Container Lifecycle: fails completely”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Runtime working directory — Container Lifecycle: fails completely: evidence found\nFor “Runtime working directory — Container Lifecycle: fails completely”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Runtime working directory — Container Lifecycle: fails completely”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Runtime working directory — Container Lifecycle: fails completely: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Runtime working directory — Container Lifecycle: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-041",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Runtime working directory — Container Lifecycle: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective runtime working directory state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "runtime working directory",
      "Container Lifecycle",
      "Runtime working directory — Container Lifecycle: behaves differently than configured",
      "runtime",
      "working",
      "directory",
      "container",
      "lifecycle",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Runtime working directory — Container Lifecycle: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected runtime working directory; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Runtime working directory — Container Lifecycle: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Runtime working directory — Container Lifecycle: behaves differently than configured”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for runtime working directory",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The runtime working directory configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Runtime working directory — Container Lifecycle: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the runtime working directory inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Runtime working directory — Container Lifecycle: behaves differently than configured”",
      "Effective runtime working directory configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Runtime working directory — Container Lifecycle: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Runtime working directory — Container Lifecycle: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Runtime working directory — Container Lifecycle: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Runtime working directory — Container Lifecycle: behaves differently than configured”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Runtime working directory — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for runtime working directory",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime working directory\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime working directory\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for runtime working directory",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Runtime working directory — Container Lifecycle: behaves differently than configured”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Runtime working directory — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The runtime working directory configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime working directory configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime working directory configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The runtime working directory configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Runtime working directory — Container Lifecycle: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for runtime working directory",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime working directory\nThe effective runtime value differs from expectation.",
        "root": "For “Runtime working directory — Container Lifecycle: behaves differently than configured”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The runtime working directory configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime working directory configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Runtime working directory — Container Lifecycle: behaves differently than configured”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Runtime working directory — Container Lifecycle: behaves differently than configured: evidence found\nFor “Runtime working directory — Container Lifecycle: behaves differently than configured”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Runtime working directory — Container Lifecycle: behaves differently than configured”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Runtime working directory — Container Lifecycle: behaves differently than configured: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Runtime working directory — Container Lifecycle: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-042",
    "technology": "Docker",
    "category": "Container Lifecycle",
    "title": "Runtime working directory — Container Lifecycle: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective runtime working directory state first, then narrow the failure through the container lifecycle dependency chain.",
    "aliases": [
      "runtime working directory",
      "Container Lifecycle",
      "Runtime working directory — Container Lifecycle: fails intermittently under load or restart",
      "runtime",
      "working",
      "directory",
      "container",
      "lifecycle",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect state",
        "command": "docker inspect <container> --format '{{json .State}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Runtime working directory — Container Lifecycle: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read recent logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected runtime working directory; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect runtime command",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Runtime working directory — Container Lifecycle: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect restart policy",
        "command": "docker inspect <container> --format '{{json .HostConfig.RestartPolicy}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Runtime working directory — Container Lifecycle: fails intermittently under load or restart”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
      "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for runtime working directory",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The runtime working directory configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Runtime working directory — Container Lifecycle: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the runtime working directory inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Runtime working directory — Container Lifecycle: fails intermittently under load or restart”",
      "Effective runtime working directory configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Container Lifecycle Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Runtime working directory — Container Lifecycle: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State}}'",
          "output": "Observed Docker state for Runtime working directory — Container Lifecycle: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Runtime working directory — Container Lifecycle: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Runtime working directory — Container Lifecycle: fails intermittently under load or restart”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Container Lifecycle path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Runtime working directory — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for runtime working directory",
        "command": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime working directory\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime working directory\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for runtime working directory",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Runtime working directory — Container Lifecycle: fails intermittently under load or restart”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Runtime working directory — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The runtime working directory configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime working directory configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime working directory configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The runtime working directory configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Runtime working directory — Container Lifecycle: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for runtime working directory",
        "check": "docker inspect payments-api --format '{{json .State}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime working directory\nThe effective runtime value differs from expectation.",
        "root": "For “Runtime working directory — Container Lifecycle: fails intermittently under load or restart”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The runtime working directory configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime working directory configuration is correct but the failure remains",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Runtime working directory — Container Lifecycle: fails intermittently under load or restart”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State}}'",
        "output": "Runtime working directory — Container Lifecycle: fails intermittently under load or restart: evidence found\nFor “Runtime working directory — Container Lifecycle: fails intermittently under load or restart”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Runtime working directory — Container Lifecycle: fails intermittently under load or restart”, the effective runtime working directory state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Runtime working directory — Container Lifecycle: fails intermittently under load or restart: evidence found\nA container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "root": "A container lifecycle dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime working directory path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Runtime working directory — Container Lifecycle: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .HostConfig.RestartPolicy}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-043",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective ENTRYPOINT executable state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "ENTRYPOINT executable",
      "Entrypoint, PID 1 & Signals",
      "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely",
      "entrypoint",
      "executable",
      "pid",
      "1",
      "signals",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected ENTRYPOINT executable; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The ENTRYPOINT executable configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the ENTRYPOINT executable inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely”",
      "Effective ENTRYPOINT executable configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The ENTRYPOINT executable configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ENTRYPOINT executable configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ENTRYPOINT executable configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The ENTRYPOINT executable configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable\nThe effective runtime value differs from expectation.",
        "root": "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The ENTRYPOINT executable configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ENTRYPOINT executable configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely: evidence found\nFor “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-044",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective ENTRYPOINT executable state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "ENTRYPOINT executable",
      "Entrypoint, PID 1 & Signals",
      "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured",
      "entrypoint",
      "executable",
      "pid",
      "1",
      "signals",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected ENTRYPOINT executable; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The ENTRYPOINT executable configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the ENTRYPOINT executable inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured”",
      "Effective ENTRYPOINT executable configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The ENTRYPOINT executable configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ENTRYPOINT executable configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ENTRYPOINT executable configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The ENTRYPOINT executable configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable\nThe effective runtime value differs from expectation.",
        "root": "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The ENTRYPOINT executable configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ENTRYPOINT executable configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nFor “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-045",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective ENTRYPOINT executable state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "ENTRYPOINT executable",
      "Entrypoint, PID 1 & Signals",
      "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
      "entrypoint",
      "executable",
      "pid",
      "1",
      "signals",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected ENTRYPOINT executable; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The ENTRYPOINT executable configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the ENTRYPOINT executable inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”",
      "Effective ENTRYPOINT executable configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The ENTRYPOINT executable configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ENTRYPOINT executable configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ENTRYPOINT executable configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The ENTRYPOINT executable configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ENTRYPOINT executable\nThe effective runtime value differs from expectation.",
        "root": "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The ENTRYPOINT executable configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ENTRYPOINT executable configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nFor “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective ENTRYPOINT executable state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ENTRYPOINT executable path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "ENTRYPOINT executable — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-046",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "CMD arguments — Entrypoint, PID 1 & Signals: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective CMD arguments state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "CMD arguments",
      "Entrypoint, PID 1 & Signals",
      "CMD arguments — Entrypoint, PID 1 & Signals: fails completely",
      "cmd",
      "arguments",
      "entrypoint",
      "pid",
      "1",
      "signals",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “CMD arguments — Entrypoint, PID 1 & Signals: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected CMD arguments; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “CMD arguments — Entrypoint, PID 1 & Signals: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “CMD arguments — Entrypoint, PID 1 & Signals: fails completely”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for CMD arguments",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The CMD arguments configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “CMD arguments — Entrypoint, PID 1 & Signals: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the CMD arguments inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “CMD arguments — Entrypoint, PID 1 & Signals: fails completely”",
      "Effective CMD arguments configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: CMD arguments — Entrypoint, PID 1 & Signals: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for CMD arguments — Entrypoint, PID 1 & Signals: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “CMD arguments — Entrypoint, PID 1 & Signals: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “CMD arguments — Entrypoint, PID 1 & Signals: fails completely”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "CMD arguments — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for CMD arguments",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD arguments\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD arguments\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for CMD arguments",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “CMD arguments — Entrypoint, PID 1 & Signals: fails completely”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "CMD arguments — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The CMD arguments configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD arguments configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD arguments configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The CMD arguments configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "CMD arguments — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for CMD arguments",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD arguments\nThe effective runtime value differs from expectation.",
        "root": "For “CMD arguments — Entrypoint, PID 1 & Signals: fails completely”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The CMD arguments configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD arguments configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “CMD arguments — Entrypoint, PID 1 & Signals: fails completely”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "CMD arguments — Entrypoint, PID 1 & Signals: fails completely: evidence found\nFor “CMD arguments — Entrypoint, PID 1 & Signals: fails completely”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “CMD arguments — Entrypoint, PID 1 & Signals: fails completely”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "CMD arguments — Entrypoint, PID 1 & Signals: fails completely: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "CMD arguments — Entrypoint, PID 1 & Signals: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-047",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective CMD arguments state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "CMD arguments",
      "Entrypoint, PID 1 & Signals",
      "CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured",
      "cmd",
      "arguments",
      "entrypoint",
      "pid",
      "1",
      "signals",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected CMD arguments; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for CMD arguments",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The CMD arguments configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the CMD arguments inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured”",
      "Effective CMD arguments configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for CMD arguments",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD arguments\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD arguments\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for CMD arguments",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The CMD arguments configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD arguments configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD arguments configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The CMD arguments configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for CMD arguments",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD arguments\nThe effective runtime value differs from expectation.",
        "root": "For “CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The CMD arguments configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD arguments configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nFor “CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "CMD arguments — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-048",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective CMD arguments state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "CMD arguments",
      "Entrypoint, PID 1 & Signals",
      "CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
      "cmd",
      "arguments",
      "entrypoint",
      "pid",
      "1",
      "signals",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected CMD arguments; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for CMD arguments",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The CMD arguments configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the CMD arguments inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”",
      "Effective CMD arguments configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for CMD arguments",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD arguments\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD arguments\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for CMD arguments",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The CMD arguments configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD arguments configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD arguments configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The CMD arguments configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for CMD arguments",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD arguments\nThe effective runtime value differs from expectation.",
        "root": "For “CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The CMD arguments configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD arguments configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nFor “CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective CMD arguments state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD arguments path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "CMD arguments — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-049",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective PID 1 signal handling state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "PID 1 signal handling",
      "Entrypoint, PID 1 & Signals",
      "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely",
      "pid",
      "1",
      "signal",
      "handling",
      "entrypoint",
      "signals",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected PID 1 signal handling; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for PID 1 signal handling",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The PID 1 signal handling configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the PID 1 signal handling inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely”",
      "Effective PID 1 signal handling configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for PID 1 signal handling",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID 1 signal handling\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID 1 signal handling\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for PID 1 signal handling",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The PID 1 signal handling configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID 1 signal handling configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID 1 signal handling configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The PID 1 signal handling configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for PID 1 signal handling",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID 1 signal handling\nThe effective runtime value differs from expectation.",
        "root": "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The PID 1 signal handling configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID 1 signal handling configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely: evidence found\nFor “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-050",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective PID 1 signal handling state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "PID 1 signal handling",
      "Entrypoint, PID 1 & Signals",
      "PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured",
      "pid",
      "1",
      "signal",
      "handling",
      "entrypoint",
      "signals",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected PID 1 signal handling; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for PID 1 signal handling",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The PID 1 signal handling configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the PID 1 signal handling inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured”",
      "Effective PID 1 signal handling configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for PID 1 signal handling",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID 1 signal handling\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID 1 signal handling\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for PID 1 signal handling",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The PID 1 signal handling configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID 1 signal handling configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID 1 signal handling configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The PID 1 signal handling configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for PID 1 signal handling",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID 1 signal handling\nThe effective runtime value differs from expectation.",
        "root": "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The PID 1 signal handling configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID 1 signal handling configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nFor “PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "PID 1 signal handling — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-051",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective PID 1 signal handling state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "PID 1 signal handling",
      "Entrypoint, PID 1 & Signals",
      "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
      "pid",
      "1",
      "signal",
      "handling",
      "entrypoint",
      "signals",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected PID 1 signal handling; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for PID 1 signal handling",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The PID 1 signal handling configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the PID 1 signal handling inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”",
      "Effective PID 1 signal handling configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for PID 1 signal handling",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID 1 signal handling\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID 1 signal handling\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for PID 1 signal handling",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The PID 1 signal handling configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID 1 signal handling configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID 1 signal handling configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The PID 1 signal handling configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for PID 1 signal handling",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID 1 signal handling\nThe effective runtime value differs from expectation.",
        "root": "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The PID 1 signal handling configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID 1 signal handling configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nFor “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective PID 1 signal handling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID 1 signal handling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "PID 1 signal handling — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-052",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "Child process reaping — Entrypoint, PID 1 & Signals: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective child process reaping state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "child process reaping",
      "Entrypoint, PID 1 & Signals",
      "Child process reaping — Entrypoint, PID 1 & Signals: fails completely",
      "child",
      "process",
      "reaping",
      "entrypoint",
      "pid",
      "1",
      "signals",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Child process reaping — Entrypoint, PID 1 & Signals: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected child process reaping; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Child process reaping — Entrypoint, PID 1 & Signals: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Child process reaping — Entrypoint, PID 1 & Signals: fails completely”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for child process reaping",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The child process reaping configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Child process reaping — Entrypoint, PID 1 & Signals: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the child process reaping inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Child process reaping — Entrypoint, PID 1 & Signals: fails completely”",
      "Effective child process reaping configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Child process reaping — Entrypoint, PID 1 & Signals: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for Child process reaping — Entrypoint, PID 1 & Signals: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Child process reaping — Entrypoint, PID 1 & Signals: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Child process reaping — Entrypoint, PID 1 & Signals: fails completely”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Child process reaping — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for child process reaping",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for child process reaping\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for child process reaping\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for child process reaping",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Child process reaping — Entrypoint, PID 1 & Signals: fails completely”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Child process reaping — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The child process reaping configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The child process reaping configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The child process reaping configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The child process reaping configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Child process reaping — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for child process reaping",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for child process reaping\nThe effective runtime value differs from expectation.",
        "root": "For “Child process reaping — Entrypoint, PID 1 & Signals: fails completely”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The child process reaping configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The child process reaping configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Child process reaping — Entrypoint, PID 1 & Signals: fails completely”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Child process reaping — Entrypoint, PID 1 & Signals: fails completely: evidence found\nFor “Child process reaping — Entrypoint, PID 1 & Signals: fails completely”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Child process reaping — Entrypoint, PID 1 & Signals: fails completely”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Child process reaping — Entrypoint, PID 1 & Signals: fails completely: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Child process reaping — Entrypoint, PID 1 & Signals: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-053",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective child process reaping state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "child process reaping",
      "Entrypoint, PID 1 & Signals",
      "Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured",
      "child",
      "process",
      "reaping",
      "entrypoint",
      "pid",
      "1",
      "signals",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected child process reaping; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for child process reaping",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The child process reaping configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the child process reaping inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured”",
      "Effective child process reaping configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for child process reaping",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for child process reaping\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for child process reaping\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for child process reaping",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The child process reaping configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The child process reaping configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The child process reaping configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The child process reaping configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for child process reaping",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for child process reaping\nThe effective runtime value differs from expectation.",
        "root": "For “Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The child process reaping configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The child process reaping configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nFor “Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Child process reaping — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-054",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective child process reaping state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "child process reaping",
      "Entrypoint, PID 1 & Signals",
      "Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
      "child",
      "process",
      "reaping",
      "entrypoint",
      "pid",
      "1",
      "signals",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected child process reaping; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for child process reaping",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The child process reaping configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the child process reaping inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”",
      "Effective child process reaping configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for child process reaping",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for child process reaping\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for child process reaping\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for child process reaping",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The child process reaping configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The child process reaping configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The child process reaping configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The child process reaping configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for child process reaping",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for child process reaping\nThe effective runtime value differs from expectation.",
        "root": "For “Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The child process reaping configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The child process reaping configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nFor “Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective child process reaping state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the child process reaping path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Child process reaping — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-055",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective STOPSIGNAL behavior state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "STOPSIGNAL behavior",
      "Entrypoint, PID 1 & Signals",
      "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely",
      "stopsignal",
      "behavior",
      "entrypoint",
      "pid",
      "1",
      "signals",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected STOPSIGNAL behavior; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The STOPSIGNAL behavior configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the STOPSIGNAL behavior inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely”",
      "Effective STOPSIGNAL behavior configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The STOPSIGNAL behavior configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The STOPSIGNAL behavior configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The STOPSIGNAL behavior configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The STOPSIGNAL behavior configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior\nThe effective runtime value differs from expectation.",
        "root": "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The STOPSIGNAL behavior configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The STOPSIGNAL behavior configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely: evidence found\nFor “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-056",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective STOPSIGNAL behavior state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "STOPSIGNAL behavior",
      "Entrypoint, PID 1 & Signals",
      "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured",
      "stopsignal",
      "behavior",
      "entrypoint",
      "pid",
      "1",
      "signals",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected STOPSIGNAL behavior; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The STOPSIGNAL behavior configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the STOPSIGNAL behavior inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured”",
      "Effective STOPSIGNAL behavior configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The STOPSIGNAL behavior configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The STOPSIGNAL behavior configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The STOPSIGNAL behavior configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The STOPSIGNAL behavior configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior\nThe effective runtime value differs from expectation.",
        "root": "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The STOPSIGNAL behavior configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The STOPSIGNAL behavior configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nFor “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-057",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective STOPSIGNAL behavior state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "STOPSIGNAL behavior",
      "Entrypoint, PID 1 & Signals",
      "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
      "stopsignal",
      "behavior",
      "entrypoint",
      "pid",
      "1",
      "signals",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected STOPSIGNAL behavior; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The STOPSIGNAL behavior configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the STOPSIGNAL behavior inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”",
      "Effective STOPSIGNAL behavior configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The STOPSIGNAL behavior configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The STOPSIGNAL behavior configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The STOPSIGNAL behavior configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The STOPSIGNAL behavior configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for STOPSIGNAL behavior\nThe effective runtime value differs from expectation.",
        "root": "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The STOPSIGNAL behavior configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The STOPSIGNAL behavior configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nFor “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective STOPSIGNAL behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the STOPSIGNAL behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "STOPSIGNAL behavior — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-058",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective entrypoint script permissions state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "entrypoint script permissions",
      "Entrypoint, PID 1 & Signals",
      "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely",
      "entrypoint",
      "script",
      "permissions",
      "pid",
      "1",
      "signals",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected entrypoint script permissions; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for entrypoint script permissions",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The entrypoint script permissions configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the entrypoint script permissions inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely”",
      "Effective entrypoint script permissions configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for entrypoint script permissions",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for entrypoint script permissions\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for entrypoint script permissions\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for entrypoint script permissions",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The entrypoint script permissions configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The entrypoint script permissions configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The entrypoint script permissions configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The entrypoint script permissions configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for entrypoint script permissions",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for entrypoint script permissions\nThe effective runtime value differs from expectation.",
        "root": "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The entrypoint script permissions configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The entrypoint script permissions configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely: evidence found\nFor “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-059",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective entrypoint script permissions state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "entrypoint script permissions",
      "Entrypoint, PID 1 & Signals",
      "Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured",
      "entrypoint",
      "script",
      "permissions",
      "pid",
      "1",
      "signals",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected entrypoint script permissions; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for entrypoint script permissions",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The entrypoint script permissions configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the entrypoint script permissions inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured”",
      "Effective entrypoint script permissions configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for entrypoint script permissions",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for entrypoint script permissions\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for entrypoint script permissions\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for entrypoint script permissions",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The entrypoint script permissions configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The entrypoint script permissions configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The entrypoint script permissions configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The entrypoint script permissions configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for entrypoint script permissions",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for entrypoint script permissions\nThe effective runtime value differs from expectation.",
        "root": "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The entrypoint script permissions configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The entrypoint script permissions configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nFor “Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-060",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective entrypoint script permissions state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "entrypoint script permissions",
      "Entrypoint, PID 1 & Signals",
      "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
      "entrypoint",
      "script",
      "permissions",
      "pid",
      "1",
      "signals",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected entrypoint script permissions; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for entrypoint script permissions",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The entrypoint script permissions configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the entrypoint script permissions inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”",
      "Effective entrypoint script permissions configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for entrypoint script permissions",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for entrypoint script permissions\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for entrypoint script permissions\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for entrypoint script permissions",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The entrypoint script permissions configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The entrypoint script permissions configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The entrypoint script permissions configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The entrypoint script permissions configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for entrypoint script permissions",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for entrypoint script permissions\nThe effective runtime value differs from expectation.",
        "root": "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The entrypoint script permissions configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The entrypoint script permissions configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nFor “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective entrypoint script permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the entrypoint script permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Entrypoint script permissions — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-061",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective shell versus exec form state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "shell versus exec form",
      "Entrypoint, PID 1 & Signals",
      "Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely",
      "shell",
      "versus",
      "exec",
      "form",
      "entrypoint",
      "pid",
      "1",
      "signals",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected shell versus exec form; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for shell versus exec form",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The shell versus exec form configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the shell versus exec form inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely”",
      "Effective shell versus exec form configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for shell versus exec form",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for shell versus exec form\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for shell versus exec form\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for shell versus exec form",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The shell versus exec form configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The shell versus exec form configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The shell versus exec form configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The shell versus exec form configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for shell versus exec form",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for shell versus exec form\nThe effective runtime value differs from expectation.",
        "root": "For “Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The shell versus exec form configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The shell versus exec form configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely: evidence found\nFor “Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-062",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective shell versus exec form state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "shell versus exec form",
      "Entrypoint, PID 1 & Signals",
      "Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured",
      "shell",
      "versus",
      "exec",
      "form",
      "entrypoint",
      "pid",
      "1",
      "signals",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected shell versus exec form; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for shell versus exec form",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The shell versus exec form configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the shell versus exec form inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured”",
      "Effective shell versus exec form configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for shell versus exec form",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for shell versus exec form\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for shell versus exec form\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for shell versus exec form",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The shell versus exec form configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The shell versus exec form configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The shell versus exec form configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The shell versus exec form configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for shell versus exec form",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for shell versus exec form\nThe effective runtime value differs from expectation.",
        "root": "For “Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The shell versus exec form configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The shell versus exec form configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nFor “Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Shell versus exec form — Entrypoint, PID 1 & Signals: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-063",
    "technology": "Docker",
    "category": "Entrypoint, PID 1 & Signals",
    "title": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective shell versus exec form state first, then narrow the failure through the entrypoint, pid 1 & signals dependency chain.",
    "aliases": [
      "shell versus exec form",
      "Entrypoint, PID 1 & Signals",
      "Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart",
      "shell",
      "versus",
      "exec",
      "form",
      "entrypoint",
      "pid",
      "1",
      "signals",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image process defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect launched process",
        "command": "docker inspect <container> --format 'Path={{.Path}} Args={{json .Args}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected shell versus exec form; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect process tree",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect stop settings",
        "command": "docker inspect <container> --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
      "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for shell versus exec form",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The shell versus exec form configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the shell versus exec form inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”",
      "Effective shell versus exec form configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Entrypoint, PID 1 & Signals Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "Observed Docker state for Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Entrypoint, PID 1 & Signals path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for shell versus exec form",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for shell versus exec form\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for shell versus exec form\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for shell versus exec form",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The shell versus exec form configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The shell versus exec form configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The shell versus exec form configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The shell versus exec form configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for shell versus exec form",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for shell versus exec form\nThe effective runtime value differs from expectation.",
        "root": "For “Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The shell versus exec form configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The shell versus exec form configuration is correct but the failure remains",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} StopSignal={{.Config.StopSignal}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nFor “Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart”, the effective shell versus exec form state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Path={{.Path}} Args={{json .Args}}'",
        "output": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nA entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "root": "A entrypoint, pid 1 & signals dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the shell versus exec form path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Shell versus exec form — Entrypoint, PID 1 & Signals: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'StopSignal={{.Config.StopSignal}} StopTimeout={{.Config.StopTimeout}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-064",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Healthcheck command — Health & Restart Policies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective healthcheck command state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "healthcheck command",
      "Health & Restart Policies",
      "Healthcheck command — Health & Restart Policies: fails completely",
      "healthcheck",
      "command",
      "health",
      "restart",
      "policies",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Healthcheck command — Health & Restart Policies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected healthcheck command; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Healthcheck command — Health & Restart Policies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Healthcheck command — Health & Restart Policies: fails completely”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for healthcheck command",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The healthcheck command configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Healthcheck command — Health & Restart Policies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the healthcheck command inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Healthcheck command — Health & Restart Policies: fails completely”",
      "Effective healthcheck command configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Healthcheck command — Health & Restart Policies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Healthcheck command — Health & Restart Policies: fails completely”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Healthcheck command — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for healthcheck command",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck command\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck command\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for healthcheck command",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Healthcheck command — Health & Restart Policies: fails completely”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Healthcheck command — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The healthcheck command configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck command configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck command configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The healthcheck command configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Healthcheck command — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for healthcheck command",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck command\nThe effective runtime value differs from expectation.",
        "root": "For “Healthcheck command — Health & Restart Policies: fails completely”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The healthcheck command configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck command configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Healthcheck command — Health & Restart Policies: fails completely”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Healthcheck command — Health & Restart Policies: fails completely: evidence found\nFor “Healthcheck command — Health & Restart Policies: fails completely”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Healthcheck command — Health & Restart Policies: fails completely”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Healthcheck command — Health & Restart Policies: fails completely: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Healthcheck command — Health & Restart Policies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-065",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Healthcheck command — Health & Restart Policies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective healthcheck command state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "healthcheck command",
      "Health & Restart Policies",
      "Healthcheck command — Health & Restart Policies: behaves differently than configured",
      "healthcheck",
      "command",
      "health",
      "restart",
      "policies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Healthcheck command — Health & Restart Policies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected healthcheck command; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Healthcheck command — Health & Restart Policies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Healthcheck command — Health & Restart Policies: behaves differently than configured”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for healthcheck command",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The healthcheck command configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Healthcheck command — Health & Restart Policies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the healthcheck command inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Healthcheck command — Health & Restart Policies: behaves differently than configured”",
      "Effective healthcheck command configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Healthcheck command — Health & Restart Policies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Healthcheck command — Health & Restart Policies: behaves differently than configured”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Healthcheck command — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for healthcheck command",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck command\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck command\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for healthcheck command",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Healthcheck command — Health & Restart Policies: behaves differently than configured”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Healthcheck command — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The healthcheck command configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck command configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck command configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The healthcheck command configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Healthcheck command — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for healthcheck command",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck command\nThe effective runtime value differs from expectation.",
        "root": "For “Healthcheck command — Health & Restart Policies: behaves differently than configured”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The healthcheck command configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck command configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Healthcheck command — Health & Restart Policies: behaves differently than configured”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Healthcheck command — Health & Restart Policies: behaves differently than configured: evidence found\nFor “Healthcheck command — Health & Restart Policies: behaves differently than configured”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Healthcheck command — Health & Restart Policies: behaves differently than configured”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Healthcheck command — Health & Restart Policies: behaves differently than configured: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Healthcheck command — Health & Restart Policies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-066",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Healthcheck command — Health & Restart Policies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective healthcheck command state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "healthcheck command",
      "Health & Restart Policies",
      "Healthcheck command — Health & Restart Policies: fails intermittently under load or restart",
      "healthcheck",
      "command",
      "health",
      "restart",
      "policies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Healthcheck command — Health & Restart Policies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected healthcheck command; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Healthcheck command — Health & Restart Policies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Healthcheck command — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for healthcheck command",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The healthcheck command configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Healthcheck command — Health & Restart Policies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the healthcheck command inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Healthcheck command — Health & Restart Policies: fails intermittently under load or restart”",
      "Effective healthcheck command configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Healthcheck command — Health & Restart Policies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Healthcheck command — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Healthcheck command — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for healthcheck command",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck command\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck command\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for healthcheck command",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Healthcheck command — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Healthcheck command — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The healthcheck command configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck command configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck command configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The healthcheck command configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Healthcheck command — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for healthcheck command",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck command\nThe effective runtime value differs from expectation.",
        "root": "For “Healthcheck command — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The healthcheck command configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck command configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Healthcheck command — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Healthcheck command — Health & Restart Policies: fails intermittently under load or restart: evidence found\nFor “Healthcheck command — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Healthcheck command — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck command state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Healthcheck command — Health & Restart Policies: fails intermittently under load or restart: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck command path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Healthcheck command — Health & Restart Policies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-067",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Healthcheck endpoint — Health & Restart Policies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective healthcheck endpoint state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "healthcheck endpoint",
      "Health & Restart Policies",
      "Healthcheck endpoint — Health & Restart Policies: fails completely",
      "healthcheck",
      "endpoint",
      "health",
      "restart",
      "policies",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Healthcheck endpoint — Health & Restart Policies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected healthcheck endpoint; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Healthcheck endpoint — Health & Restart Policies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Healthcheck endpoint — Health & Restart Policies: fails completely”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for healthcheck endpoint",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The healthcheck endpoint configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Healthcheck endpoint — Health & Restart Policies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the healthcheck endpoint inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Healthcheck endpoint — Health & Restart Policies: fails completely”",
      "Effective healthcheck endpoint configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Healthcheck endpoint — Health & Restart Policies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Healthcheck endpoint — Health & Restart Policies: fails completely”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Healthcheck endpoint — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for healthcheck endpoint",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck endpoint\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck endpoint\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for healthcheck endpoint",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Healthcheck endpoint — Health & Restart Policies: fails completely”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Healthcheck endpoint — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The healthcheck endpoint configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck endpoint configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck endpoint configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The healthcheck endpoint configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Healthcheck endpoint — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for healthcheck endpoint",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck endpoint\nThe effective runtime value differs from expectation.",
        "root": "For “Healthcheck endpoint — Health & Restart Policies: fails completely”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The healthcheck endpoint configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck endpoint configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Healthcheck endpoint — Health & Restart Policies: fails completely”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Healthcheck endpoint — Health & Restart Policies: fails completely: evidence found\nFor “Healthcheck endpoint — Health & Restart Policies: fails completely”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Healthcheck endpoint — Health & Restart Policies: fails completely”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Healthcheck endpoint — Health & Restart Policies: fails completely: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Healthcheck endpoint — Health & Restart Policies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-068",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Healthcheck endpoint — Health & Restart Policies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective healthcheck endpoint state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "healthcheck endpoint",
      "Health & Restart Policies",
      "Healthcheck endpoint — Health & Restart Policies: behaves differently than configured",
      "healthcheck",
      "endpoint",
      "health",
      "restart",
      "policies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Healthcheck endpoint — Health & Restart Policies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected healthcheck endpoint; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Healthcheck endpoint — Health & Restart Policies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Healthcheck endpoint — Health & Restart Policies: behaves differently than configured”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for healthcheck endpoint",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The healthcheck endpoint configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Healthcheck endpoint — Health & Restart Policies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the healthcheck endpoint inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Healthcheck endpoint — Health & Restart Policies: behaves differently than configured”",
      "Effective healthcheck endpoint configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Healthcheck endpoint — Health & Restart Policies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Healthcheck endpoint — Health & Restart Policies: behaves differently than configured”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Healthcheck endpoint — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for healthcheck endpoint",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck endpoint\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck endpoint\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for healthcheck endpoint",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Healthcheck endpoint — Health & Restart Policies: behaves differently than configured”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Healthcheck endpoint — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The healthcheck endpoint configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck endpoint configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck endpoint configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The healthcheck endpoint configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Healthcheck endpoint — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for healthcheck endpoint",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck endpoint\nThe effective runtime value differs from expectation.",
        "root": "For “Healthcheck endpoint — Health & Restart Policies: behaves differently than configured”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The healthcheck endpoint configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck endpoint configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Healthcheck endpoint — Health & Restart Policies: behaves differently than configured”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Healthcheck endpoint — Health & Restart Policies: behaves differently than configured: evidence found\nFor “Healthcheck endpoint — Health & Restart Policies: behaves differently than configured”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Healthcheck endpoint — Health & Restart Policies: behaves differently than configured”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Healthcheck endpoint — Health & Restart Policies: behaves differently than configured: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Healthcheck endpoint — Health & Restart Policies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-069",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective healthcheck endpoint state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "healthcheck endpoint",
      "Health & Restart Policies",
      "Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart",
      "healthcheck",
      "endpoint",
      "health",
      "restart",
      "policies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected healthcheck endpoint; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for healthcheck endpoint",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The healthcheck endpoint configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the healthcheck endpoint inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart”",
      "Effective healthcheck endpoint configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for healthcheck endpoint",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck endpoint\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck endpoint\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for healthcheck endpoint",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The healthcheck endpoint configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck endpoint configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck endpoint configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The healthcheck endpoint configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for healthcheck endpoint",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck endpoint\nThe effective runtime value differs from expectation.",
        "root": "For “Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The healthcheck endpoint configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck endpoint configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart: evidence found\nFor “Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck endpoint state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck endpoint path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Healthcheck endpoint — Health & Restart Policies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-070",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Healthcheck timing — Health & Restart Policies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective healthcheck timing state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "healthcheck timing",
      "Health & Restart Policies",
      "Healthcheck timing — Health & Restart Policies: fails completely",
      "healthcheck",
      "timing",
      "health",
      "restart",
      "policies",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Healthcheck timing — Health & Restart Policies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected healthcheck timing; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Healthcheck timing — Health & Restart Policies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Healthcheck timing — Health & Restart Policies: fails completely”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for healthcheck timing",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The healthcheck timing configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Healthcheck timing — Health & Restart Policies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the healthcheck timing inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Healthcheck timing — Health & Restart Policies: fails completely”",
      "Effective healthcheck timing configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Healthcheck timing — Health & Restart Policies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Healthcheck timing — Health & Restart Policies: fails completely”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Healthcheck timing — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for healthcheck timing",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck timing\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck timing\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for healthcheck timing",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Healthcheck timing — Health & Restart Policies: fails completely”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Healthcheck timing — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The healthcheck timing configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck timing configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck timing configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The healthcheck timing configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Healthcheck timing — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for healthcheck timing",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck timing\nThe effective runtime value differs from expectation.",
        "root": "For “Healthcheck timing — Health & Restart Policies: fails completely”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The healthcheck timing configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck timing configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Healthcheck timing — Health & Restart Policies: fails completely”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Healthcheck timing — Health & Restart Policies: fails completely: evidence found\nFor “Healthcheck timing — Health & Restart Policies: fails completely”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Healthcheck timing — Health & Restart Policies: fails completely”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Healthcheck timing — Health & Restart Policies: fails completely: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Healthcheck timing — Health & Restart Policies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-071",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Healthcheck timing — Health & Restart Policies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective healthcheck timing state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "healthcheck timing",
      "Health & Restart Policies",
      "Healthcheck timing — Health & Restart Policies: behaves differently than configured",
      "healthcheck",
      "timing",
      "health",
      "restart",
      "policies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Healthcheck timing — Health & Restart Policies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected healthcheck timing; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Healthcheck timing — Health & Restart Policies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Healthcheck timing — Health & Restart Policies: behaves differently than configured”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for healthcheck timing",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The healthcheck timing configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Healthcheck timing — Health & Restart Policies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the healthcheck timing inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Healthcheck timing — Health & Restart Policies: behaves differently than configured”",
      "Effective healthcheck timing configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Healthcheck timing — Health & Restart Policies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Healthcheck timing — Health & Restart Policies: behaves differently than configured”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Healthcheck timing — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for healthcheck timing",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck timing\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck timing\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for healthcheck timing",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Healthcheck timing — Health & Restart Policies: behaves differently than configured”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Healthcheck timing — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The healthcheck timing configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck timing configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck timing configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The healthcheck timing configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Healthcheck timing — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for healthcheck timing",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck timing\nThe effective runtime value differs from expectation.",
        "root": "For “Healthcheck timing — Health & Restart Policies: behaves differently than configured”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The healthcheck timing configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck timing configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Healthcheck timing — Health & Restart Policies: behaves differently than configured”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Healthcheck timing — Health & Restart Policies: behaves differently than configured: evidence found\nFor “Healthcheck timing — Health & Restart Policies: behaves differently than configured”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Healthcheck timing — Health & Restart Policies: behaves differently than configured”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Healthcheck timing — Health & Restart Policies: behaves differently than configured: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Healthcheck timing — Health & Restart Policies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-072",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective healthcheck timing state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "healthcheck timing",
      "Health & Restart Policies",
      "Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart",
      "healthcheck",
      "timing",
      "health",
      "restart",
      "policies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected healthcheck timing; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for healthcheck timing",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The healthcheck timing configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the healthcheck timing inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart”",
      "Effective healthcheck timing configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for healthcheck timing",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck timing\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck timing\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for healthcheck timing",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The healthcheck timing configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck timing configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck timing configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The healthcheck timing configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for healthcheck timing",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for healthcheck timing\nThe effective runtime value differs from expectation.",
        "root": "For “Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The healthcheck timing configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The healthcheck timing configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart: evidence found\nFor “Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart”, the effective healthcheck timing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the healthcheck timing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Healthcheck timing — Health & Restart Policies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-073",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Restart policy — Health & Restart Policies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective restart policy state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "restart policy",
      "Health & Restart Policies",
      "Restart policy — Health & Restart Policies: fails completely",
      "restart",
      "policy",
      "health",
      "policies",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Restart policy — Health & Restart Policies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected restart policy; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Restart policy — Health & Restart Policies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Restart policy — Health & Restart Policies: fails completely”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for restart policy",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The restart policy configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Restart policy — Health & Restart Policies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the restart policy inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Restart policy — Health & Restart Policies: fails completely”",
      "Effective restart policy configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Restart policy — Health & Restart Policies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Restart policy — Health & Restart Policies: fails completely”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Restart policy — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for restart policy",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart policy\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart policy\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for restart policy",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Restart policy — Health & Restart Policies: fails completely”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Restart policy — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The restart policy configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart policy configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart policy configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The restart policy configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Restart policy — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for restart policy",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart policy\nThe effective runtime value differs from expectation.",
        "root": "For “Restart policy — Health & Restart Policies: fails completely”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The restart policy configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart policy configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Restart policy — Health & Restart Policies: fails completely”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Restart policy — Health & Restart Policies: fails completely: evidence found\nFor “Restart policy — Health & Restart Policies: fails completely”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Restart policy — Health & Restart Policies: fails completely”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Restart policy — Health & Restart Policies: fails completely: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Restart policy — Health & Restart Policies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-074",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Restart policy — Health & Restart Policies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective restart policy state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "restart policy",
      "Health & Restart Policies",
      "Restart policy — Health & Restart Policies: behaves differently than configured",
      "restart",
      "policy",
      "health",
      "policies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Restart policy — Health & Restart Policies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected restart policy; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Restart policy — Health & Restart Policies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Restart policy — Health & Restart Policies: behaves differently than configured”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for restart policy",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The restart policy configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Restart policy — Health & Restart Policies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the restart policy inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Restart policy — Health & Restart Policies: behaves differently than configured”",
      "Effective restart policy configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Restart policy — Health & Restart Policies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Restart policy — Health & Restart Policies: behaves differently than configured”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Restart policy — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for restart policy",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart policy\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart policy\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for restart policy",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Restart policy — Health & Restart Policies: behaves differently than configured”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Restart policy — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The restart policy configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart policy configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart policy configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The restart policy configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Restart policy — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for restart policy",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart policy\nThe effective runtime value differs from expectation.",
        "root": "For “Restart policy — Health & Restart Policies: behaves differently than configured”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The restart policy configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart policy configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Restart policy — Health & Restart Policies: behaves differently than configured”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Restart policy — Health & Restart Policies: behaves differently than configured: evidence found\nFor “Restart policy — Health & Restart Policies: behaves differently than configured”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Restart policy — Health & Restart Policies: behaves differently than configured”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Restart policy — Health & Restart Policies: behaves differently than configured: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Restart policy — Health & Restart Policies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-075",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Restart policy — Health & Restart Policies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective restart policy state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "restart policy",
      "Health & Restart Policies",
      "Restart policy — Health & Restart Policies: fails intermittently under load or restart",
      "restart",
      "policy",
      "health",
      "policies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Restart policy — Health & Restart Policies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected restart policy; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Restart policy — Health & Restart Policies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Restart policy — Health & Restart Policies: fails intermittently under load or restart”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for restart policy",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The restart policy configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Restart policy — Health & Restart Policies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the restart policy inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Restart policy — Health & Restart Policies: fails intermittently under load or restart”",
      "Effective restart policy configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Restart policy — Health & Restart Policies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Restart policy — Health & Restart Policies: fails intermittently under load or restart”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Restart policy — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for restart policy",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart policy\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart policy\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for restart policy",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Restart policy — Health & Restart Policies: fails intermittently under load or restart”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Restart policy — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The restart policy configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart policy configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart policy configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The restart policy configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Restart policy — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for restart policy",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart policy\nThe effective runtime value differs from expectation.",
        "root": "For “Restart policy — Health & Restart Policies: fails intermittently under load or restart”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The restart policy configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart policy configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Restart policy — Health & Restart Policies: fails intermittently under load or restart”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Restart policy — Health & Restart Policies: fails intermittently under load or restart: evidence found\nFor “Restart policy — Health & Restart Policies: fails intermittently under load or restart”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Restart policy — Health & Restart Policies: fails intermittently under load or restart”, the effective restart policy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Restart policy — Health & Restart Policies: fails intermittently under load or restart: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart policy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Restart policy — Health & Restart Policies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-076",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Restart count — Health & Restart Policies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective restart count state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "restart count",
      "Health & Restart Policies",
      "Restart count — Health & Restart Policies: fails completely",
      "restart",
      "count",
      "health",
      "policies",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Restart count — Health & Restart Policies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected restart count; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Restart count — Health & Restart Policies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Restart count — Health & Restart Policies: fails completely”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for restart count",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The restart count configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Restart count — Health & Restart Policies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the restart count inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Restart count — Health & Restart Policies: fails completely”",
      "Effective restart count configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Restart count — Health & Restart Policies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Restart count — Health & Restart Policies: fails completely”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Restart count — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for restart count",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart count\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart count\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for restart count",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Restart count — Health & Restart Policies: fails completely”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Restart count — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The restart count configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart count configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart count configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The restart count configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Restart count — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for restart count",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart count\nThe effective runtime value differs from expectation.",
        "root": "For “Restart count — Health & Restart Policies: fails completely”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The restart count configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart count configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Restart count — Health & Restart Policies: fails completely”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Restart count — Health & Restart Policies: fails completely: evidence found\nFor “Restart count — Health & Restart Policies: fails completely”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Restart count — Health & Restart Policies: fails completely”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Restart count — Health & Restart Policies: fails completely: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Restart count — Health & Restart Policies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-077",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Restart count — Health & Restart Policies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective restart count state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "restart count",
      "Health & Restart Policies",
      "Restart count — Health & Restart Policies: behaves differently than configured",
      "restart",
      "count",
      "health",
      "policies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Restart count — Health & Restart Policies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected restart count; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Restart count — Health & Restart Policies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Restart count — Health & Restart Policies: behaves differently than configured”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for restart count",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The restart count configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Restart count — Health & Restart Policies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the restart count inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Restart count — Health & Restart Policies: behaves differently than configured”",
      "Effective restart count configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Restart count — Health & Restart Policies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Restart count — Health & Restart Policies: behaves differently than configured”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Restart count — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for restart count",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart count\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart count\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for restart count",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Restart count — Health & Restart Policies: behaves differently than configured”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Restart count — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The restart count configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart count configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart count configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The restart count configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Restart count — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for restart count",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart count\nThe effective runtime value differs from expectation.",
        "root": "For “Restart count — Health & Restart Policies: behaves differently than configured”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The restart count configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart count configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Restart count — Health & Restart Policies: behaves differently than configured”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Restart count — Health & Restart Policies: behaves differently than configured: evidence found\nFor “Restart count — Health & Restart Policies: behaves differently than configured”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Restart count — Health & Restart Policies: behaves differently than configured”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Restart count — Health & Restart Policies: behaves differently than configured: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Restart count — Health & Restart Policies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-078",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Restart count — Health & Restart Policies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective restart count state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "restart count",
      "Health & Restart Policies",
      "Restart count — Health & Restart Policies: fails intermittently under load or restart",
      "restart",
      "count",
      "health",
      "policies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Restart count — Health & Restart Policies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected restart count; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Restart count — Health & Restart Policies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Restart count — Health & Restart Policies: fails intermittently under load or restart”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for restart count",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The restart count configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Restart count — Health & Restart Policies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the restart count inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Restart count — Health & Restart Policies: fails intermittently under load or restart”",
      "Effective restart count configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Restart count — Health & Restart Policies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Restart count — Health & Restart Policies: fails intermittently under load or restart”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Restart count — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for restart count",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart count\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart count\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for restart count",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Restart count — Health & Restart Policies: fails intermittently under load or restart”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Restart count — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The restart count configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart count configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart count configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The restart count configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Restart count — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for restart count",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for restart count\nThe effective runtime value differs from expectation.",
        "root": "For “Restart count — Health & Restart Policies: fails intermittently under load or restart”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The restart count configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The restart count configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Restart count — Health & Restart Policies: fails intermittently under load or restart”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Restart count — Health & Restart Policies: fails intermittently under load or restart: evidence found\nFor “Restart count — Health & Restart Policies: fails intermittently under load or restart”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Restart count — Health & Restart Policies: fails intermittently under load or restart”, the effective restart count state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Restart count — Health & Restart Policies: fails intermittently under load or restart: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the restart count path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Restart count — Health & Restart Policies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-079",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Dependency readiness — Health & Restart Policies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective dependency readiness state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "dependency readiness",
      "Health & Restart Policies",
      "Dependency readiness — Health & Restart Policies: fails completely",
      "dependency",
      "readiness",
      "health",
      "restart",
      "policies",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Dependency readiness — Health & Restart Policies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected dependency readiness; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Dependency readiness — Health & Restart Policies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Dependency readiness — Health & Restart Policies: fails completely”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for dependency readiness",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The dependency readiness configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Dependency readiness — Health & Restart Policies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the dependency readiness inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Dependency readiness — Health & Restart Policies: fails completely”",
      "Effective dependency readiness configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Dependency readiness — Health & Restart Policies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Dependency readiness — Health & Restart Policies: fails completely”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Dependency readiness — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for dependency readiness",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for dependency readiness\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for dependency readiness\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for dependency readiness",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Dependency readiness — Health & Restart Policies: fails completely”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Dependency readiness — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The dependency readiness configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The dependency readiness configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The dependency readiness configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The dependency readiness configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Dependency readiness — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for dependency readiness",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for dependency readiness\nThe effective runtime value differs from expectation.",
        "root": "For “Dependency readiness — Health & Restart Policies: fails completely”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The dependency readiness configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The dependency readiness configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Dependency readiness — Health & Restart Policies: fails completely”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Dependency readiness — Health & Restart Policies: fails completely: evidence found\nFor “Dependency readiness — Health & Restart Policies: fails completely”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Dependency readiness — Health & Restart Policies: fails completely”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Dependency readiness — Health & Restart Policies: fails completely: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Dependency readiness — Health & Restart Policies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-080",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Dependency readiness — Health & Restart Policies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective dependency readiness state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "dependency readiness",
      "Health & Restart Policies",
      "Dependency readiness — Health & Restart Policies: behaves differently than configured",
      "dependency",
      "readiness",
      "health",
      "restart",
      "policies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Dependency readiness — Health & Restart Policies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected dependency readiness; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Dependency readiness — Health & Restart Policies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Dependency readiness — Health & Restart Policies: behaves differently than configured”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for dependency readiness",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The dependency readiness configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Dependency readiness — Health & Restart Policies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the dependency readiness inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Dependency readiness — Health & Restart Policies: behaves differently than configured”",
      "Effective dependency readiness configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Dependency readiness — Health & Restart Policies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Dependency readiness — Health & Restart Policies: behaves differently than configured”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Dependency readiness — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for dependency readiness",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for dependency readiness\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for dependency readiness\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for dependency readiness",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Dependency readiness — Health & Restart Policies: behaves differently than configured”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Dependency readiness — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The dependency readiness configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The dependency readiness configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The dependency readiness configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The dependency readiness configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Dependency readiness — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for dependency readiness",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for dependency readiness\nThe effective runtime value differs from expectation.",
        "root": "For “Dependency readiness — Health & Restart Policies: behaves differently than configured”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The dependency readiness configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The dependency readiness configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Dependency readiness — Health & Restart Policies: behaves differently than configured”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Dependency readiness — Health & Restart Policies: behaves differently than configured: evidence found\nFor “Dependency readiness — Health & Restart Policies: behaves differently than configured”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Dependency readiness — Health & Restart Policies: behaves differently than configured”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Dependency readiness — Health & Restart Policies: behaves differently than configured: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Dependency readiness — Health & Restart Policies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-081",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Dependency readiness — Health & Restart Policies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective dependency readiness state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "dependency readiness",
      "Health & Restart Policies",
      "Dependency readiness — Health & Restart Policies: fails intermittently under load or restart",
      "dependency",
      "readiness",
      "health",
      "restart",
      "policies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Dependency readiness — Health & Restart Policies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected dependency readiness; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Dependency readiness — Health & Restart Policies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Dependency readiness — Health & Restart Policies: fails intermittently under load or restart”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for dependency readiness",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The dependency readiness configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Dependency readiness — Health & Restart Policies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the dependency readiness inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Dependency readiness — Health & Restart Policies: fails intermittently under load or restart”",
      "Effective dependency readiness configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Dependency readiness — Health & Restart Policies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Dependency readiness — Health & Restart Policies: fails intermittently under load or restart”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Dependency readiness — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for dependency readiness",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for dependency readiness\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for dependency readiness\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for dependency readiness",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Dependency readiness — Health & Restart Policies: fails intermittently under load or restart”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Dependency readiness — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The dependency readiness configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The dependency readiness configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The dependency readiness configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The dependency readiness configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Dependency readiness — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for dependency readiness",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for dependency readiness\nThe effective runtime value differs from expectation.",
        "root": "For “Dependency readiness — Health & Restart Policies: fails intermittently under load or restart”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The dependency readiness configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The dependency readiness configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Dependency readiness — Health & Restart Policies: fails intermittently under load or restart”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Dependency readiness — Health & Restart Policies: fails intermittently under load or restart: evidence found\nFor “Dependency readiness — Health & Restart Policies: fails intermittently under load or restart”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Dependency readiness — Health & Restart Policies: fails intermittently under load or restart”, the effective dependency readiness state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Dependency readiness — Health & Restart Policies: fails intermittently under load or restart: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the dependency readiness path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Dependency readiness — Health & Restart Policies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-082",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Inherited image healthcheck — Health & Restart Policies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective inherited image healthcheck state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "inherited image healthcheck",
      "Health & Restart Policies",
      "Inherited image healthcheck — Health & Restart Policies: fails completely",
      "inherited",
      "image",
      "healthcheck",
      "health",
      "restart",
      "policies",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Inherited image healthcheck — Health & Restart Policies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected inherited image healthcheck; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Inherited image healthcheck — Health & Restart Policies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Inherited image healthcheck — Health & Restart Policies: fails completely”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for inherited image healthcheck",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The inherited image healthcheck configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Inherited image healthcheck — Health & Restart Policies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the inherited image healthcheck inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Inherited image healthcheck — Health & Restart Policies: fails completely”",
      "Effective inherited image healthcheck configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Inherited image healthcheck — Health & Restart Policies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Inherited image healthcheck — Health & Restart Policies: fails completely”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Inherited image healthcheck — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for inherited image healthcheck",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inherited image healthcheck\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inherited image healthcheck\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for inherited image healthcheck",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Inherited image healthcheck — Health & Restart Policies: fails completely”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Inherited image healthcheck — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inherited image healthcheck configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inherited image healthcheck configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inherited image healthcheck configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The inherited image healthcheck configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Inherited image healthcheck — Health & Restart Policies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for inherited image healthcheck",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inherited image healthcheck\nThe effective runtime value differs from expectation.",
        "root": "For “Inherited image healthcheck — Health & Restart Policies: fails completely”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The inherited image healthcheck configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inherited image healthcheck configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Inherited image healthcheck — Health & Restart Policies: fails completely”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Inherited image healthcheck — Health & Restart Policies: fails completely: evidence found\nFor “Inherited image healthcheck — Health & Restart Policies: fails completely”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Inherited image healthcheck — Health & Restart Policies: fails completely”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Inherited image healthcheck — Health & Restart Policies: fails completely: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Inherited image healthcheck — Health & Restart Policies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-083",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Inherited image healthcheck — Health & Restart Policies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective inherited image healthcheck state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "inherited image healthcheck",
      "Health & Restart Policies",
      "Inherited image healthcheck — Health & Restart Policies: behaves differently than configured",
      "inherited",
      "image",
      "healthcheck",
      "health",
      "restart",
      "policies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Inherited image healthcheck — Health & Restart Policies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected inherited image healthcheck; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Inherited image healthcheck — Health & Restart Policies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Inherited image healthcheck — Health & Restart Policies: behaves differently than configured”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for inherited image healthcheck",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The inherited image healthcheck configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Inherited image healthcheck — Health & Restart Policies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the inherited image healthcheck inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Inherited image healthcheck — Health & Restart Policies: behaves differently than configured”",
      "Effective inherited image healthcheck configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Inherited image healthcheck — Health & Restart Policies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Inherited image healthcheck — Health & Restart Policies: behaves differently than configured”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Inherited image healthcheck — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for inherited image healthcheck",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inherited image healthcheck\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inherited image healthcheck\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for inherited image healthcheck",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Inherited image healthcheck — Health & Restart Policies: behaves differently than configured”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Inherited image healthcheck — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inherited image healthcheck configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inherited image healthcheck configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inherited image healthcheck configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The inherited image healthcheck configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Inherited image healthcheck — Health & Restart Policies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for inherited image healthcheck",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inherited image healthcheck\nThe effective runtime value differs from expectation.",
        "root": "For “Inherited image healthcheck — Health & Restart Policies: behaves differently than configured”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The inherited image healthcheck configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inherited image healthcheck configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Inherited image healthcheck — Health & Restart Policies: behaves differently than configured”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Inherited image healthcheck — Health & Restart Policies: behaves differently than configured: evidence found\nFor “Inherited image healthcheck — Health & Restart Policies: behaves differently than configured”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Inherited image healthcheck — Health & Restart Policies: behaves differently than configured”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Inherited image healthcheck — Health & Restart Policies: behaves differently than configured: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Inherited image healthcheck — Health & Restart Policies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-084",
    "technology": "Docker",
    "category": "Health & Restart Policies",
    "title": "Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective inherited image healthcheck state first, then narrow the failure through the health & restart policies dependency chain.",
    "aliases": [
      "inherited image healthcheck",
      "Health & Restart Policies",
      "Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart",
      "inherited",
      "image",
      "healthcheck",
      "health",
      "restart",
      "policies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect health history",
        "command": "docker inspect <container> --format '{{json .State.Health}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect health configuration",
        "command": "docker inspect <container> --format '{{json .Config.Healthcheck}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected inherited image healthcheck; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect restart state",
        "command": "docker inspect <container> --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Read application logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
      "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for inherited image healthcheck",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The inherited image healthcheck configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the inherited image healthcheck inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart”",
      "Effective inherited image healthcheck configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Health & Restart Policies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .State.Health}}'",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Health & Restart Policies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for inherited image healthcheck",
        "command": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inherited image healthcheck\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .State.Health}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inherited image healthcheck\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for inherited image healthcheck",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inherited image healthcheck configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inherited image healthcheck configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inherited image healthcheck configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Evidence matches this path: The inherited image healthcheck configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for inherited image healthcheck",
        "check": "docker inspect payments-api --format '{{json .State.Health}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inherited image healthcheck\nThe effective runtime value differs from expectation.",
        "root": "For “Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The inherited image healthcheck configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Config.Healthcheck}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inherited image healthcheck configuration is correct but the failure remains",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .State.Health}}'",
        "output": "Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart: evidence found\nFor “Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart”, the effective inherited image healthcheck state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Config.Healthcheck}}'",
        "output": "Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart: evidence found\nA health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "root": "A health & restart policies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inherited image healthcheck path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'Restarting={{.State.Restarting}} RestartCount={{.RestartCount}} ExitCode={{.State.ExitCode}} OOMKilled={{.State.OOMKilled}}'",
        "output": "Inherited image healthcheck — Health & Restart Policies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-085",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Docker logs output — Logs, Exec & Debugging: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective docker logs output state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "docker logs output",
      "Logs, Exec & Debugging",
      "Docker logs output — Logs, Exec & Debugging: fails completely",
      "docker",
      "logs",
      "output",
      "exec",
      "debugging",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker logs output — Logs, Exec & Debugging: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected docker logs output; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker logs output — Logs, Exec & Debugging: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker logs output — Logs, Exec & Debugging: fails completely”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for docker logs output",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The docker logs output configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker logs output — Logs, Exec & Debugging: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the docker logs output inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker logs output — Logs, Exec & Debugging: fails completely”",
      "Effective docker logs output configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker logs output — Logs, Exec & Debugging: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Docker logs output — Logs, Exec & Debugging: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker logs output — Logs, Exec & Debugging: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker logs output — Logs, Exec & Debugging: fails completely”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker logs output — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for docker logs output",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker logs output\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker logs output\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for docker logs output",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker logs output — Logs, Exec & Debugging: fails completely”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker logs output — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The docker logs output configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker logs output configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker logs output configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The docker logs output configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker logs output — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for docker logs output",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker logs output\nThe effective runtime value differs from expectation.",
        "root": "For “Docker logs output — Logs, Exec & Debugging: fails completely”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The docker logs output configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker logs output configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker logs output — Logs, Exec & Debugging: fails completely”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker logs output — Logs, Exec & Debugging: fails completely: evidence found\nFor “Docker logs output — Logs, Exec & Debugging: fails completely”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker logs output — Logs, Exec & Debugging: fails completely”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Docker logs output — Logs, Exec & Debugging: fails completely: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Docker logs output — Logs, Exec & Debugging: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-086",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Docker logs output — Logs, Exec & Debugging: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective docker logs output state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "docker logs output",
      "Logs, Exec & Debugging",
      "Docker logs output — Logs, Exec & Debugging: behaves differently than configured",
      "docker",
      "logs",
      "output",
      "exec",
      "debugging",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker logs output — Logs, Exec & Debugging: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected docker logs output; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker logs output — Logs, Exec & Debugging: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker logs output — Logs, Exec & Debugging: behaves differently than configured”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for docker logs output",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The docker logs output configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker logs output — Logs, Exec & Debugging: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the docker logs output inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker logs output — Logs, Exec & Debugging: behaves differently than configured”",
      "Effective docker logs output configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker logs output — Logs, Exec & Debugging: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Docker logs output — Logs, Exec & Debugging: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker logs output — Logs, Exec & Debugging: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker logs output — Logs, Exec & Debugging: behaves differently than configured”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker logs output — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for docker logs output",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker logs output\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker logs output\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for docker logs output",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker logs output — Logs, Exec & Debugging: behaves differently than configured”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker logs output — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The docker logs output configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker logs output configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker logs output configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The docker logs output configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker logs output — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for docker logs output",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker logs output\nThe effective runtime value differs from expectation.",
        "root": "For “Docker logs output — Logs, Exec & Debugging: behaves differently than configured”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The docker logs output configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker logs output configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker logs output — Logs, Exec & Debugging: behaves differently than configured”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker logs output — Logs, Exec & Debugging: behaves differently than configured: evidence found\nFor “Docker logs output — Logs, Exec & Debugging: behaves differently than configured”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker logs output — Logs, Exec & Debugging: behaves differently than configured”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Docker logs output — Logs, Exec & Debugging: behaves differently than configured: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Docker logs output — Logs, Exec & Debugging: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-087",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective docker logs output state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "docker logs output",
      "Logs, Exec & Debugging",
      "Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart",
      "docker",
      "logs",
      "output",
      "exec",
      "debugging",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected docker logs output; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for docker logs output",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The docker logs output configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the docker logs output inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart”",
      "Effective docker logs output configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for docker logs output",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker logs output\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker logs output\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for docker logs output",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The docker logs output configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker logs output configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker logs output configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The docker logs output configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for docker logs output",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker logs output\nThe effective runtime value differs from expectation.",
        "root": "For “Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The docker logs output configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker logs output configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nFor “Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker logs output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker logs output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Docker logs output — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-088",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Logging driver — Logs, Exec & Debugging: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective logging driver state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "logging driver",
      "Logs, Exec & Debugging",
      "Logging driver — Logs, Exec & Debugging: fails completely",
      "logging",
      "driver",
      "logs",
      "exec",
      "debugging",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Logging driver — Logs, Exec & Debugging: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected logging driver; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Logging driver — Logs, Exec & Debugging: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Logging driver — Logs, Exec & Debugging: fails completely”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for logging driver",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The logging driver configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Logging driver — Logs, Exec & Debugging: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the logging driver inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Logging driver — Logs, Exec & Debugging: fails completely”",
      "Effective logging driver configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Logging driver — Logs, Exec & Debugging: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Logging driver — Logs, Exec & Debugging: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Logging driver — Logs, Exec & Debugging: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Logging driver — Logs, Exec & Debugging: fails completely”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Logging driver — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for logging driver",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for logging driver\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for logging driver\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for logging driver",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Logging driver — Logs, Exec & Debugging: fails completely”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Logging driver — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The logging driver configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The logging driver configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The logging driver configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The logging driver configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Logging driver — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for logging driver",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for logging driver\nThe effective runtime value differs from expectation.",
        "root": "For “Logging driver — Logs, Exec & Debugging: fails completely”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The logging driver configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The logging driver configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Logging driver — Logs, Exec & Debugging: fails completely”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Logging driver — Logs, Exec & Debugging: fails completely: evidence found\nFor “Logging driver — Logs, Exec & Debugging: fails completely”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Logging driver — Logs, Exec & Debugging: fails completely”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Logging driver — Logs, Exec & Debugging: fails completely: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Logging driver — Logs, Exec & Debugging: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-089",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Logging driver — Logs, Exec & Debugging: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective logging driver state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "logging driver",
      "Logs, Exec & Debugging",
      "Logging driver — Logs, Exec & Debugging: behaves differently than configured",
      "logging",
      "driver",
      "logs",
      "exec",
      "debugging",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Logging driver — Logs, Exec & Debugging: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected logging driver; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Logging driver — Logs, Exec & Debugging: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Logging driver — Logs, Exec & Debugging: behaves differently than configured”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for logging driver",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The logging driver configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Logging driver — Logs, Exec & Debugging: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the logging driver inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Logging driver — Logs, Exec & Debugging: behaves differently than configured”",
      "Effective logging driver configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Logging driver — Logs, Exec & Debugging: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Logging driver — Logs, Exec & Debugging: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Logging driver — Logs, Exec & Debugging: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Logging driver — Logs, Exec & Debugging: behaves differently than configured”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Logging driver — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for logging driver",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for logging driver\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for logging driver\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for logging driver",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Logging driver — Logs, Exec & Debugging: behaves differently than configured”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Logging driver — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The logging driver configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The logging driver configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The logging driver configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The logging driver configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Logging driver — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for logging driver",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for logging driver\nThe effective runtime value differs from expectation.",
        "root": "For “Logging driver — Logs, Exec & Debugging: behaves differently than configured”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The logging driver configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The logging driver configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Logging driver — Logs, Exec & Debugging: behaves differently than configured”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Logging driver — Logs, Exec & Debugging: behaves differently than configured: evidence found\nFor “Logging driver — Logs, Exec & Debugging: behaves differently than configured”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Logging driver — Logs, Exec & Debugging: behaves differently than configured”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Logging driver — Logs, Exec & Debugging: behaves differently than configured: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Logging driver — Logs, Exec & Debugging: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-090",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective logging driver state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "logging driver",
      "Logs, Exec & Debugging",
      "Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart",
      "logging",
      "driver",
      "logs",
      "exec",
      "debugging",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected logging driver; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for logging driver",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The logging driver configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the logging driver inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart”",
      "Effective logging driver configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for logging driver",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for logging driver\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for logging driver\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for logging driver",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The logging driver configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The logging driver configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The logging driver configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The logging driver configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for logging driver",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for logging driver\nThe effective runtime value differs from expectation.",
        "root": "For “Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The logging driver configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The logging driver configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nFor “Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective logging driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the logging driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Logging driver — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-091",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Docker exec — Logs, Exec & Debugging: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective docker exec state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "docker exec",
      "Logs, Exec & Debugging",
      "Docker exec — Logs, Exec & Debugging: fails completely",
      "docker",
      "exec",
      "logs",
      "debugging",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker exec — Logs, Exec & Debugging: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected docker exec; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker exec — Logs, Exec & Debugging: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker exec — Logs, Exec & Debugging: fails completely”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for docker exec",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The docker exec configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker exec — Logs, Exec & Debugging: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the docker exec inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker exec — Logs, Exec & Debugging: fails completely”",
      "Effective docker exec configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker exec — Logs, Exec & Debugging: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Docker exec — Logs, Exec & Debugging: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker exec — Logs, Exec & Debugging: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker exec — Logs, Exec & Debugging: fails completely”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker exec — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for docker exec",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker exec\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker exec\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for docker exec",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker exec — Logs, Exec & Debugging: fails completely”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker exec — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The docker exec configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker exec configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker exec configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The docker exec configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker exec — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for docker exec",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker exec\nThe effective runtime value differs from expectation.",
        "root": "For “Docker exec — Logs, Exec & Debugging: fails completely”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The docker exec configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker exec configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker exec — Logs, Exec & Debugging: fails completely”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker exec — Logs, Exec & Debugging: fails completely: evidence found\nFor “Docker exec — Logs, Exec & Debugging: fails completely”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker exec — Logs, Exec & Debugging: fails completely”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Docker exec — Logs, Exec & Debugging: fails completely: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Docker exec — Logs, Exec & Debugging: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-092",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Docker exec — Logs, Exec & Debugging: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective docker exec state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "docker exec",
      "Logs, Exec & Debugging",
      "Docker exec — Logs, Exec & Debugging: behaves differently than configured",
      "docker",
      "exec",
      "logs",
      "debugging",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker exec — Logs, Exec & Debugging: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected docker exec; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker exec — Logs, Exec & Debugging: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker exec — Logs, Exec & Debugging: behaves differently than configured”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for docker exec",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The docker exec configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker exec — Logs, Exec & Debugging: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the docker exec inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker exec — Logs, Exec & Debugging: behaves differently than configured”",
      "Effective docker exec configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker exec — Logs, Exec & Debugging: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Docker exec — Logs, Exec & Debugging: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker exec — Logs, Exec & Debugging: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker exec — Logs, Exec & Debugging: behaves differently than configured”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker exec — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for docker exec",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker exec\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker exec\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for docker exec",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker exec — Logs, Exec & Debugging: behaves differently than configured”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker exec — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The docker exec configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker exec configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker exec configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The docker exec configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker exec — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for docker exec",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker exec\nThe effective runtime value differs from expectation.",
        "root": "For “Docker exec — Logs, Exec & Debugging: behaves differently than configured”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The docker exec configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker exec configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker exec — Logs, Exec & Debugging: behaves differently than configured”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker exec — Logs, Exec & Debugging: behaves differently than configured: evidence found\nFor “Docker exec — Logs, Exec & Debugging: behaves differently than configured”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker exec — Logs, Exec & Debugging: behaves differently than configured”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Docker exec — Logs, Exec & Debugging: behaves differently than configured: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Docker exec — Logs, Exec & Debugging: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-093",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective docker exec state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "docker exec",
      "Logs, Exec & Debugging",
      "Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart",
      "docker",
      "exec",
      "logs",
      "debugging",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected docker exec; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for docker exec",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The docker exec configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the docker exec inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart”",
      "Effective docker exec configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for docker exec",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker exec\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker exec\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for docker exec",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The docker exec configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker exec configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker exec configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The docker exec configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for docker exec",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for docker exec\nThe effective runtime value differs from expectation.",
        "root": "For “Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The docker exec configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The docker exec configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nFor “Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective docker exec state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the docker exec path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Docker exec — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-094",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Interactive TTY — Logs, Exec & Debugging: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective interactive TTY state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "interactive TTY",
      "Logs, Exec & Debugging",
      "Interactive TTY — Logs, Exec & Debugging: fails completely",
      "interactive",
      "tty",
      "logs",
      "exec",
      "debugging",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Interactive TTY — Logs, Exec & Debugging: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected interactive TTY; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Interactive TTY — Logs, Exec & Debugging: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Interactive TTY — Logs, Exec & Debugging: fails completely”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for interactive TTY",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The interactive TTY configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Interactive TTY — Logs, Exec & Debugging: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the interactive TTY inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Interactive TTY — Logs, Exec & Debugging: fails completely”",
      "Effective interactive TTY configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Interactive TTY — Logs, Exec & Debugging: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Interactive TTY — Logs, Exec & Debugging: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Interactive TTY — Logs, Exec & Debugging: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Interactive TTY — Logs, Exec & Debugging: fails completely”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Interactive TTY — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for interactive TTY",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for interactive TTY\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for interactive TTY\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for interactive TTY",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Interactive TTY — Logs, Exec & Debugging: fails completely”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Interactive TTY — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The interactive TTY configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The interactive TTY configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The interactive TTY configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The interactive TTY configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Interactive TTY — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for interactive TTY",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for interactive TTY\nThe effective runtime value differs from expectation.",
        "root": "For “Interactive TTY — Logs, Exec & Debugging: fails completely”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The interactive TTY configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The interactive TTY configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Interactive TTY — Logs, Exec & Debugging: fails completely”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Interactive TTY — Logs, Exec & Debugging: fails completely: evidence found\nFor “Interactive TTY — Logs, Exec & Debugging: fails completely”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Interactive TTY — Logs, Exec & Debugging: fails completely”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Interactive TTY — Logs, Exec & Debugging: fails completely: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Interactive TTY — Logs, Exec & Debugging: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-095",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Interactive TTY — Logs, Exec & Debugging: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective interactive TTY state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "interactive TTY",
      "Logs, Exec & Debugging",
      "Interactive TTY — Logs, Exec & Debugging: behaves differently than configured",
      "interactive",
      "tty",
      "logs",
      "exec",
      "debugging",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Interactive TTY — Logs, Exec & Debugging: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected interactive TTY; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Interactive TTY — Logs, Exec & Debugging: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Interactive TTY — Logs, Exec & Debugging: behaves differently than configured”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for interactive TTY",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The interactive TTY configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Interactive TTY — Logs, Exec & Debugging: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the interactive TTY inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Interactive TTY — Logs, Exec & Debugging: behaves differently than configured”",
      "Effective interactive TTY configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Interactive TTY — Logs, Exec & Debugging: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Interactive TTY — Logs, Exec & Debugging: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Interactive TTY — Logs, Exec & Debugging: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Interactive TTY — Logs, Exec & Debugging: behaves differently than configured”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Interactive TTY — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for interactive TTY",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for interactive TTY\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for interactive TTY\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for interactive TTY",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Interactive TTY — Logs, Exec & Debugging: behaves differently than configured”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Interactive TTY — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The interactive TTY configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The interactive TTY configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The interactive TTY configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The interactive TTY configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Interactive TTY — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for interactive TTY",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for interactive TTY\nThe effective runtime value differs from expectation.",
        "root": "For “Interactive TTY — Logs, Exec & Debugging: behaves differently than configured”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The interactive TTY configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The interactive TTY configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Interactive TTY — Logs, Exec & Debugging: behaves differently than configured”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Interactive TTY — Logs, Exec & Debugging: behaves differently than configured: evidence found\nFor “Interactive TTY — Logs, Exec & Debugging: behaves differently than configured”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Interactive TTY — Logs, Exec & Debugging: behaves differently than configured”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Interactive TTY — Logs, Exec & Debugging: behaves differently than configured: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Interactive TTY — Logs, Exec & Debugging: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-096",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective interactive TTY state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "interactive TTY",
      "Logs, Exec & Debugging",
      "Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart",
      "interactive",
      "tty",
      "logs",
      "exec",
      "debugging",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected interactive TTY; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for interactive TTY",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The interactive TTY configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the interactive TTY inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart”",
      "Effective interactive TTY configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for interactive TTY",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for interactive TTY\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for interactive TTY\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for interactive TTY",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The interactive TTY configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The interactive TTY configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The interactive TTY configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The interactive TTY configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for interactive TTY",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for interactive TTY\nThe effective runtime value differs from expectation.",
        "root": "For “Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The interactive TTY configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The interactive TTY configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nFor “Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective interactive TTY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the interactive TTY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Interactive TTY — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-097",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Container process visibility — Logs, Exec & Debugging: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective container process visibility state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "container process visibility",
      "Logs, Exec & Debugging",
      "Container process visibility — Logs, Exec & Debugging: fails completely",
      "container",
      "process",
      "visibility",
      "logs",
      "exec",
      "debugging",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container process visibility — Logs, Exec & Debugging: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container process visibility; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container process visibility — Logs, Exec & Debugging: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container process visibility — Logs, Exec & Debugging: fails completely”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container process visibility",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container process visibility configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container process visibility — Logs, Exec & Debugging: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the container process visibility inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container process visibility — Logs, Exec & Debugging: fails completely”",
      "Effective container process visibility configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container process visibility — Logs, Exec & Debugging: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Container process visibility — Logs, Exec & Debugging: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container process visibility — Logs, Exec & Debugging: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container process visibility — Logs, Exec & Debugging: fails completely”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container process visibility — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container process visibility",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container process visibility\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container process visibility\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container process visibility",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container process visibility — Logs, Exec & Debugging: fails completely”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container process visibility — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container process visibility configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container process visibility configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container process visibility configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The container process visibility configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container process visibility — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container process visibility",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container process visibility\nThe effective runtime value differs from expectation.",
        "root": "For “Container process visibility — Logs, Exec & Debugging: fails completely”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container process visibility configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container process visibility configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container process visibility — Logs, Exec & Debugging: fails completely”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Container process visibility — Logs, Exec & Debugging: fails completely: evidence found\nFor “Container process visibility — Logs, Exec & Debugging: fails completely”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container process visibility — Logs, Exec & Debugging: fails completely”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container process visibility — Logs, Exec & Debugging: fails completely: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Container process visibility — Logs, Exec & Debugging: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-098",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Container process visibility — Logs, Exec & Debugging: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective container process visibility state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "container process visibility",
      "Logs, Exec & Debugging",
      "Container process visibility — Logs, Exec & Debugging: behaves differently than configured",
      "container",
      "process",
      "visibility",
      "logs",
      "exec",
      "debugging",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container process visibility — Logs, Exec & Debugging: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container process visibility; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container process visibility — Logs, Exec & Debugging: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container process visibility — Logs, Exec & Debugging: behaves differently than configured”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container process visibility",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container process visibility configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container process visibility — Logs, Exec & Debugging: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the container process visibility inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container process visibility — Logs, Exec & Debugging: behaves differently than configured”",
      "Effective container process visibility configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container process visibility — Logs, Exec & Debugging: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Container process visibility — Logs, Exec & Debugging: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container process visibility — Logs, Exec & Debugging: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container process visibility — Logs, Exec & Debugging: behaves differently than configured”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container process visibility — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container process visibility",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container process visibility\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container process visibility\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container process visibility",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container process visibility — Logs, Exec & Debugging: behaves differently than configured”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container process visibility — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container process visibility configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container process visibility configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container process visibility configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The container process visibility configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container process visibility — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container process visibility",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container process visibility\nThe effective runtime value differs from expectation.",
        "root": "For “Container process visibility — Logs, Exec & Debugging: behaves differently than configured”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container process visibility configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container process visibility configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container process visibility — Logs, Exec & Debugging: behaves differently than configured”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Container process visibility — Logs, Exec & Debugging: behaves differently than configured: evidence found\nFor “Container process visibility — Logs, Exec & Debugging: behaves differently than configured”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container process visibility — Logs, Exec & Debugging: behaves differently than configured”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container process visibility — Logs, Exec & Debugging: behaves differently than configured: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Container process visibility — Logs, Exec & Debugging: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-099",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective container process visibility state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "container process visibility",
      "Logs, Exec & Debugging",
      "Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart",
      "container",
      "process",
      "visibility",
      "logs",
      "exec",
      "debugging",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container process visibility; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container process visibility",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container process visibility configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the container process visibility inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart”",
      "Effective container process visibility configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container process visibility",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container process visibility\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container process visibility\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container process visibility",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container process visibility configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container process visibility configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container process visibility configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The container process visibility configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container process visibility",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container process visibility\nThe effective runtime value differs from expectation.",
        "root": "For “Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container process visibility configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container process visibility configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nFor “Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective container process visibility state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container process visibility path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Container process visibility — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-100",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Runtime filesystem inspection — Logs, Exec & Debugging: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective runtime filesystem inspection state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "runtime filesystem inspection",
      "Logs, Exec & Debugging",
      "Runtime filesystem inspection — Logs, Exec & Debugging: fails completely",
      "runtime",
      "filesystem",
      "inspection",
      "logs",
      "exec",
      "debugging",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Runtime filesystem inspection — Logs, Exec & Debugging: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected runtime filesystem inspection; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Runtime filesystem inspection — Logs, Exec & Debugging: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Runtime filesystem inspection — Logs, Exec & Debugging: fails completely”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for runtime filesystem inspection",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The runtime filesystem inspection configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Runtime filesystem inspection — Logs, Exec & Debugging: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the runtime filesystem inspection inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Runtime filesystem inspection — Logs, Exec & Debugging: fails completely”",
      "Effective runtime filesystem inspection configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Runtime filesystem inspection — Logs, Exec & Debugging: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Runtime filesystem inspection — Logs, Exec & Debugging: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Runtime filesystem inspection — Logs, Exec & Debugging: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Runtime filesystem inspection — Logs, Exec & Debugging: fails completely”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Runtime filesystem inspection — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Runtime filesystem inspection — Logs, Exec & Debugging: fails completely”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Runtime filesystem inspection — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The runtime filesystem inspection configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime filesystem inspection configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime filesystem inspection configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The runtime filesystem inspection configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Runtime filesystem inspection — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for runtime filesystem inspection",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection\nThe effective runtime value differs from expectation.",
        "root": "For “Runtime filesystem inspection — Logs, Exec & Debugging: fails completely”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The runtime filesystem inspection configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime filesystem inspection configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Runtime filesystem inspection — Logs, Exec & Debugging: fails completely”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Runtime filesystem inspection — Logs, Exec & Debugging: fails completely: evidence found\nFor “Runtime filesystem inspection — Logs, Exec & Debugging: fails completely”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Runtime filesystem inspection — Logs, Exec & Debugging: fails completely”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Runtime filesystem inspection — Logs, Exec & Debugging: fails completely: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Runtime filesystem inspection — Logs, Exec & Debugging: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-101",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective runtime filesystem inspection state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "runtime filesystem inspection",
      "Logs, Exec & Debugging",
      "Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured",
      "runtime",
      "filesystem",
      "inspection",
      "logs",
      "exec",
      "debugging",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected runtime filesystem inspection; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for runtime filesystem inspection",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The runtime filesystem inspection configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the runtime filesystem inspection inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured”",
      "Effective runtime filesystem inspection configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The runtime filesystem inspection configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime filesystem inspection configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime filesystem inspection configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The runtime filesystem inspection configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for runtime filesystem inspection",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection\nThe effective runtime value differs from expectation.",
        "root": "For “Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The runtime filesystem inspection configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime filesystem inspection configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured: evidence found\nFor “Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Runtime filesystem inspection — Logs, Exec & Debugging: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-102",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective runtime filesystem inspection state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "runtime filesystem inspection",
      "Logs, Exec & Debugging",
      "Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart",
      "runtime",
      "filesystem",
      "inspection",
      "logs",
      "exec",
      "debugging",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected runtime filesystem inspection; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for runtime filesystem inspection",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The runtime filesystem inspection configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the runtime filesystem inspection inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart”",
      "Effective runtime filesystem inspection configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The runtime filesystem inspection configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime filesystem inspection configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime filesystem inspection configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The runtime filesystem inspection configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for runtime filesystem inspection",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime filesystem inspection\nThe effective runtime value differs from expectation.",
        "root": "For “Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The runtime filesystem inspection configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime filesystem inspection configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nFor “Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective runtime filesystem inspection state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime filesystem inspection path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Runtime filesystem inspection — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-103",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Log rotation — Logs, Exec & Debugging: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective log rotation state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "log rotation",
      "Logs, Exec & Debugging",
      "Log rotation — Logs, Exec & Debugging: fails completely",
      "log",
      "rotation",
      "logs",
      "exec",
      "debugging",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Log rotation — Logs, Exec & Debugging: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected log rotation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Log rotation — Logs, Exec & Debugging: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Log rotation — Logs, Exec & Debugging: fails completely”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for log rotation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The log rotation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Log rotation — Logs, Exec & Debugging: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the log rotation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Log rotation — Logs, Exec & Debugging: fails completely”",
      "Effective log rotation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Log rotation — Logs, Exec & Debugging: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Log rotation — Logs, Exec & Debugging: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Log rotation — Logs, Exec & Debugging: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Log rotation — Logs, Exec & Debugging: fails completely”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Log rotation — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for log rotation",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for log rotation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for log rotation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for log rotation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Log rotation — Logs, Exec & Debugging: fails completely”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Log rotation — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The log rotation configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The log rotation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The log rotation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The log rotation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Log rotation — Logs, Exec & Debugging: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for log rotation",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for log rotation\nThe effective runtime value differs from expectation.",
        "root": "For “Log rotation — Logs, Exec & Debugging: fails completely”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The log rotation configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The log rotation configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Log rotation — Logs, Exec & Debugging: fails completely”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Log rotation — Logs, Exec & Debugging: fails completely: evidence found\nFor “Log rotation — Logs, Exec & Debugging: fails completely”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Log rotation — Logs, Exec & Debugging: fails completely”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Log rotation — Logs, Exec & Debugging: fails completely: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Log rotation — Logs, Exec & Debugging: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-104",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Log rotation — Logs, Exec & Debugging: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective log rotation state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "log rotation",
      "Logs, Exec & Debugging",
      "Log rotation — Logs, Exec & Debugging: behaves differently than configured",
      "log",
      "rotation",
      "logs",
      "exec",
      "debugging",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Log rotation — Logs, Exec & Debugging: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected log rotation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Log rotation — Logs, Exec & Debugging: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Log rotation — Logs, Exec & Debugging: behaves differently than configured”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for log rotation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The log rotation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Log rotation — Logs, Exec & Debugging: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the log rotation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Log rotation — Logs, Exec & Debugging: behaves differently than configured”",
      "Effective log rotation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Log rotation — Logs, Exec & Debugging: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Log rotation — Logs, Exec & Debugging: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Log rotation — Logs, Exec & Debugging: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Log rotation — Logs, Exec & Debugging: behaves differently than configured”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Log rotation — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for log rotation",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for log rotation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for log rotation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for log rotation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Log rotation — Logs, Exec & Debugging: behaves differently than configured”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Log rotation — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The log rotation configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The log rotation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The log rotation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The log rotation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Log rotation — Logs, Exec & Debugging: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for log rotation",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for log rotation\nThe effective runtime value differs from expectation.",
        "root": "For “Log rotation — Logs, Exec & Debugging: behaves differently than configured”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The log rotation configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The log rotation configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Log rotation — Logs, Exec & Debugging: behaves differently than configured”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Log rotation — Logs, Exec & Debugging: behaves differently than configured: evidence found\nFor “Log rotation — Logs, Exec & Debugging: behaves differently than configured”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Log rotation — Logs, Exec & Debugging: behaves differently than configured”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Log rotation — Logs, Exec & Debugging: behaves differently than configured: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Log rotation — Logs, Exec & Debugging: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-105",
    "technology": "Docker",
    "category": "Logs, Exec & Debugging",
    "title": "Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective log rotation state first, then narrow the failure through the logs, exec & debugging dependency chain.",
    "aliases": [
      "log rotation",
      "Logs, Exec & Debugging",
      "Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart",
      "log",
      "rotation",
      "logs",
      "exec",
      "debugging",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect logging driver",
        "command": "docker inspect <container> --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read logs",
        "command": "docker logs --tail 200 --timestamps <container>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected log rotation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect processes",
        "command": "docker top <container> -eo pid,ppid,user,stat,args",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect filesystem changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
      "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for log rotation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The log rotation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the log rotation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart”",
      "Effective log rotation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Logs, Exec & Debugging Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
          "output": "Observed Docker state for Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker logs --tail 200 --timestamps payments-api",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker top payments-api -eo pid,ppid,user,stat,args",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Logs, Exec & Debugging path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for log rotation",
        "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for log rotation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for log rotation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker logs --tail 200 --timestamps checkout-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for log rotation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The log rotation configuration is correct but the failure remains",
        "command": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The log rotation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The log rotation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,stat,args",
            "output": "Evidence matches this path: The log rotation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker top payments-api -eo pid,ppid,user,stat,args",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for log rotation",
        "check": "docker inspect payments-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for log rotation\nThe effective runtime value differs from expectation.",
        "root": "For “Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The log rotation configuration is correct but the failure remains",
        "check": "docker logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The log rotation configuration is correct but the failure remains",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker top payments-api -eo pid,ppid,user,stat,args",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{.HostConfig.LogConfig.Type}} {{json .HostConfig.LogConfig.Config}}'",
        "output": "Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nFor “Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart”, the effective log rotation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker logs --tail 200 --timestamps checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "check": "docker logs --tail 200 --timestamps checkout-api",
        "output": "Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nA logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "root": "A logs, exec & debugging dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the log rotation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker top checkout-api -eo pid,ppid,user,stat,args",
        "output": "Log rotation — Logs, Exec & Debugging: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-106",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "Memory limit — CPU, Memory & Cgroups: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective memory limit state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "memory limit",
      "CPU, Memory & Cgroups",
      "Memory limit — CPU, Memory & Cgroups: fails completely",
      "memory",
      "limit",
      "cpu",
      "cgroups",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Memory limit — CPU, Memory & Cgroups: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected memory limit; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Memory limit — CPU, Memory & Cgroups: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Memory limit — CPU, Memory & Cgroups: fails completely”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for memory limit",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The memory limit configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Memory limit — CPU, Memory & Cgroups: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the memory limit inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Memory limit — CPU, Memory & Cgroups: fails completely”",
      "Effective memory limit configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Memory limit — CPU, Memory & Cgroups: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Memory limit — CPU, Memory & Cgroups: fails completely”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Memory limit — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for memory limit",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for memory limit\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for memory limit\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for memory limit",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Memory limit — CPU, Memory & Cgroups: fails completely”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Memory limit — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The memory limit configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The memory limit configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The memory limit configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The memory limit configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Memory limit — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for memory limit",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for memory limit\nThe effective runtime value differs from expectation.",
        "root": "For “Memory limit — CPU, Memory & Cgroups: fails completely”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The memory limit configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The memory limit configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Memory limit — CPU, Memory & Cgroups: fails completely”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "Memory limit — CPU, Memory & Cgroups: fails completely: evidence found\nFor “Memory limit — CPU, Memory & Cgroups: fails completely”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Memory limit — CPU, Memory & Cgroups: fails completely”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Memory limit — CPU, Memory & Cgroups: fails completely: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Memory limit — CPU, Memory & Cgroups: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-107",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "Memory limit — CPU, Memory & Cgroups: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective memory limit state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "memory limit",
      "CPU, Memory & Cgroups",
      "Memory limit — CPU, Memory & Cgroups: behaves differently than configured",
      "memory",
      "limit",
      "cpu",
      "cgroups",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Memory limit — CPU, Memory & Cgroups: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected memory limit; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Memory limit — CPU, Memory & Cgroups: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Memory limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for memory limit",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The memory limit configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Memory limit — CPU, Memory & Cgroups: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the memory limit inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Memory limit — CPU, Memory & Cgroups: behaves differently than configured”",
      "Effective memory limit configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Memory limit — CPU, Memory & Cgroups: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Memory limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Memory limit — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for memory limit",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for memory limit\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for memory limit\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for memory limit",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Memory limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Memory limit — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The memory limit configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The memory limit configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The memory limit configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The memory limit configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Memory limit — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for memory limit",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for memory limit\nThe effective runtime value differs from expectation.",
        "root": "For “Memory limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The memory limit configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The memory limit configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Memory limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "Memory limit — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nFor “Memory limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Memory limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Memory limit — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Memory limit — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-108",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective memory limit state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "memory limit",
      "CPU, Memory & Cgroups",
      "Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart",
      "memory",
      "limit",
      "cpu",
      "cgroups",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected memory limit; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for memory limit",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The memory limit configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the memory limit inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart”",
      "Effective memory limit configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for memory limit",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for memory limit\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for memory limit\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for memory limit",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The memory limit configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The memory limit configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The memory limit configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The memory limit configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for memory limit",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for memory limit\nThe effective runtime value differs from expectation.",
        "root": "For “Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The memory limit configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The memory limit configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nFor “Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective memory limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the memory limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Memory limit — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-109",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "OOM behavior — CPU, Memory & Cgroups: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective OOM behavior state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "OOM behavior",
      "CPU, Memory & Cgroups",
      "OOM behavior — CPU, Memory & Cgroups: fails completely",
      "oom",
      "behavior",
      "cpu",
      "memory",
      "cgroups",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “OOM behavior — CPU, Memory & Cgroups: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected OOM behavior; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “OOM behavior — CPU, Memory & Cgroups: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “OOM behavior — CPU, Memory & Cgroups: fails completely”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for OOM behavior",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The OOM behavior configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “OOM behavior — CPU, Memory & Cgroups: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the OOM behavior inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “OOM behavior — CPU, Memory & Cgroups: fails completely”",
      "Effective OOM behavior configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: OOM behavior — CPU, Memory & Cgroups: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “OOM behavior — CPU, Memory & Cgroups: fails completely”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "OOM behavior — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for OOM behavior",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for OOM behavior\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for OOM behavior\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for OOM behavior",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “OOM behavior — CPU, Memory & Cgroups: fails completely”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "OOM behavior — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The OOM behavior configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The OOM behavior configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The OOM behavior configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The OOM behavior configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "OOM behavior — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for OOM behavior",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for OOM behavior\nThe effective runtime value differs from expectation.",
        "root": "For “OOM behavior — CPU, Memory & Cgroups: fails completely”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The OOM behavior configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The OOM behavior configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “OOM behavior — CPU, Memory & Cgroups: fails completely”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "OOM behavior — CPU, Memory & Cgroups: fails completely: evidence found\nFor “OOM behavior — CPU, Memory & Cgroups: fails completely”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “OOM behavior — CPU, Memory & Cgroups: fails completely”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "OOM behavior — CPU, Memory & Cgroups: fails completely: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "OOM behavior — CPU, Memory & Cgroups: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-110",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "OOM behavior — CPU, Memory & Cgroups: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective OOM behavior state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "OOM behavior",
      "CPU, Memory & Cgroups",
      "OOM behavior — CPU, Memory & Cgroups: behaves differently than configured",
      "oom",
      "behavior",
      "cpu",
      "memory",
      "cgroups",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “OOM behavior — CPU, Memory & Cgroups: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected OOM behavior; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “OOM behavior — CPU, Memory & Cgroups: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “OOM behavior — CPU, Memory & Cgroups: behaves differently than configured”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for OOM behavior",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The OOM behavior configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “OOM behavior — CPU, Memory & Cgroups: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the OOM behavior inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “OOM behavior — CPU, Memory & Cgroups: behaves differently than configured”",
      "Effective OOM behavior configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: OOM behavior — CPU, Memory & Cgroups: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “OOM behavior — CPU, Memory & Cgroups: behaves differently than configured”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "OOM behavior — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for OOM behavior",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for OOM behavior\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for OOM behavior\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for OOM behavior",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “OOM behavior — CPU, Memory & Cgroups: behaves differently than configured”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "OOM behavior — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The OOM behavior configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The OOM behavior configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The OOM behavior configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The OOM behavior configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "OOM behavior — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for OOM behavior",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for OOM behavior\nThe effective runtime value differs from expectation.",
        "root": "For “OOM behavior — CPU, Memory & Cgroups: behaves differently than configured”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The OOM behavior configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The OOM behavior configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “OOM behavior — CPU, Memory & Cgroups: behaves differently than configured”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "OOM behavior — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nFor “OOM behavior — CPU, Memory & Cgroups: behaves differently than configured”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “OOM behavior — CPU, Memory & Cgroups: behaves differently than configured”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "OOM behavior — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "OOM behavior — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-111",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective OOM behavior state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "OOM behavior",
      "CPU, Memory & Cgroups",
      "OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart",
      "oom",
      "behavior",
      "cpu",
      "memory",
      "cgroups",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected OOM behavior; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for OOM behavior",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The OOM behavior configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the OOM behavior inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart”",
      "Effective OOM behavior configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for OOM behavior",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for OOM behavior\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for OOM behavior\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for OOM behavior",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The OOM behavior configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The OOM behavior configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The OOM behavior configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The OOM behavior configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for OOM behavior",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for OOM behavior\nThe effective runtime value differs from expectation.",
        "root": "For “OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The OOM behavior configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The OOM behavior configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nFor “OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective OOM behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the OOM behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "OOM behavior — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-112",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "CPU limit — CPU, Memory & Cgroups: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective CPU limit state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "CPU limit",
      "CPU, Memory & Cgroups",
      "CPU limit — CPU, Memory & Cgroups: fails completely",
      "cpu",
      "limit",
      "memory",
      "cgroups",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “CPU limit — CPU, Memory & Cgroups: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected CPU limit; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “CPU limit — CPU, Memory & Cgroups: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “CPU limit — CPU, Memory & Cgroups: fails completely”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for CPU limit",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The CPU limit configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “CPU limit — CPU, Memory & Cgroups: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the CPU limit inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “CPU limit — CPU, Memory & Cgroups: fails completely”",
      "Effective CPU limit configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: CPU limit — CPU, Memory & Cgroups: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “CPU limit — CPU, Memory & Cgroups: fails completely”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "CPU limit — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for CPU limit",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU limit\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU limit\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for CPU limit",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “CPU limit — CPU, Memory & Cgroups: fails completely”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "CPU limit — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The CPU limit configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU limit configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU limit configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The CPU limit configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "CPU limit — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for CPU limit",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU limit\nThe effective runtime value differs from expectation.",
        "root": "For “CPU limit — CPU, Memory & Cgroups: fails completely”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The CPU limit configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU limit configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “CPU limit — CPU, Memory & Cgroups: fails completely”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "CPU limit — CPU, Memory & Cgroups: fails completely: evidence found\nFor “CPU limit — CPU, Memory & Cgroups: fails completely”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “CPU limit — CPU, Memory & Cgroups: fails completely”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "CPU limit — CPU, Memory & Cgroups: fails completely: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "CPU limit — CPU, Memory & Cgroups: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-113",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "CPU limit — CPU, Memory & Cgroups: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective CPU limit state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "CPU limit",
      "CPU, Memory & Cgroups",
      "CPU limit — CPU, Memory & Cgroups: behaves differently than configured",
      "cpu",
      "limit",
      "memory",
      "cgroups",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “CPU limit — CPU, Memory & Cgroups: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected CPU limit; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “CPU limit — CPU, Memory & Cgroups: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “CPU limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for CPU limit",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The CPU limit configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “CPU limit — CPU, Memory & Cgroups: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the CPU limit inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “CPU limit — CPU, Memory & Cgroups: behaves differently than configured”",
      "Effective CPU limit configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: CPU limit — CPU, Memory & Cgroups: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “CPU limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "CPU limit — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for CPU limit",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU limit\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU limit\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for CPU limit",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “CPU limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "CPU limit — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The CPU limit configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU limit configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU limit configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The CPU limit configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "CPU limit — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for CPU limit",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU limit\nThe effective runtime value differs from expectation.",
        "root": "For “CPU limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The CPU limit configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU limit configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “CPU limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "CPU limit — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nFor “CPU limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “CPU limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "CPU limit — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "CPU limit — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-114",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective CPU limit state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "CPU limit",
      "CPU, Memory & Cgroups",
      "CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart",
      "cpu",
      "limit",
      "memory",
      "cgroups",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected CPU limit; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for CPU limit",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The CPU limit configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the CPU limit inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart”",
      "Effective CPU limit configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for CPU limit",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU limit\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU limit\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for CPU limit",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The CPU limit configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU limit configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU limit configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The CPU limit configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for CPU limit",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU limit\nThe effective runtime value differs from expectation.",
        "root": "For “CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The CPU limit configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU limit configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nFor “CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "CPU limit — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-115",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "CPU throttling — CPU, Memory & Cgroups: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective CPU throttling state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "CPU throttling",
      "CPU, Memory & Cgroups",
      "CPU throttling — CPU, Memory & Cgroups: fails completely",
      "cpu",
      "throttling",
      "memory",
      "cgroups",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “CPU throttling — CPU, Memory & Cgroups: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected CPU throttling; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “CPU throttling — CPU, Memory & Cgroups: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “CPU throttling — CPU, Memory & Cgroups: fails completely”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for CPU throttling",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The CPU throttling configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “CPU throttling — CPU, Memory & Cgroups: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the CPU throttling inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “CPU throttling — CPU, Memory & Cgroups: fails completely”",
      "Effective CPU throttling configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: CPU throttling — CPU, Memory & Cgroups: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “CPU throttling — CPU, Memory & Cgroups: fails completely”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "CPU throttling — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for CPU throttling",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU throttling\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU throttling\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for CPU throttling",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “CPU throttling — CPU, Memory & Cgroups: fails completely”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "CPU throttling — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The CPU throttling configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU throttling configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU throttling configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The CPU throttling configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "CPU throttling — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for CPU throttling",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU throttling\nThe effective runtime value differs from expectation.",
        "root": "For “CPU throttling — CPU, Memory & Cgroups: fails completely”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The CPU throttling configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU throttling configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “CPU throttling — CPU, Memory & Cgroups: fails completely”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "CPU throttling — CPU, Memory & Cgroups: fails completely: evidence found\nFor “CPU throttling — CPU, Memory & Cgroups: fails completely”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “CPU throttling — CPU, Memory & Cgroups: fails completely”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "CPU throttling — CPU, Memory & Cgroups: fails completely: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "CPU throttling — CPU, Memory & Cgroups: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-116",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "CPU throttling — CPU, Memory & Cgroups: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective CPU throttling state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "CPU throttling",
      "CPU, Memory & Cgroups",
      "CPU throttling — CPU, Memory & Cgroups: behaves differently than configured",
      "cpu",
      "throttling",
      "memory",
      "cgroups",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “CPU throttling — CPU, Memory & Cgroups: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected CPU throttling; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “CPU throttling — CPU, Memory & Cgroups: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “CPU throttling — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for CPU throttling",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The CPU throttling configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “CPU throttling — CPU, Memory & Cgroups: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the CPU throttling inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “CPU throttling — CPU, Memory & Cgroups: behaves differently than configured”",
      "Effective CPU throttling configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: CPU throttling — CPU, Memory & Cgroups: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “CPU throttling — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "CPU throttling — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for CPU throttling",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU throttling\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU throttling\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for CPU throttling",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “CPU throttling — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "CPU throttling — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The CPU throttling configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU throttling configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU throttling configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The CPU throttling configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "CPU throttling — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for CPU throttling",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU throttling\nThe effective runtime value differs from expectation.",
        "root": "For “CPU throttling — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The CPU throttling configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU throttling configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “CPU throttling — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "CPU throttling — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nFor “CPU throttling — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “CPU throttling — CPU, Memory & Cgroups: behaves differently than configured”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "CPU throttling — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "CPU throttling — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-117",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective CPU throttling state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "CPU throttling",
      "CPU, Memory & Cgroups",
      "CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart",
      "cpu",
      "throttling",
      "memory",
      "cgroups",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected CPU throttling; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for CPU throttling",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The CPU throttling configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the CPU throttling inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart”",
      "Effective CPU throttling configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for CPU throttling",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU throttling\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU throttling\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for CPU throttling",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The CPU throttling configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU throttling configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU throttling configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The CPU throttling configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for CPU throttling",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CPU throttling\nThe effective runtime value differs from expectation.",
        "root": "For “CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The CPU throttling configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CPU throttling configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nFor “CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective CPU throttling state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CPU throttling path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "CPU throttling — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-118",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "PID limit — CPU, Memory & Cgroups: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective PID limit state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "PID limit",
      "CPU, Memory & Cgroups",
      "PID limit — CPU, Memory & Cgroups: fails completely",
      "pid",
      "limit",
      "cpu",
      "memory",
      "cgroups",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “PID limit — CPU, Memory & Cgroups: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected PID limit; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “PID limit — CPU, Memory & Cgroups: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “PID limit — CPU, Memory & Cgroups: fails completely”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for PID limit",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The PID limit configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “PID limit — CPU, Memory & Cgroups: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the PID limit inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “PID limit — CPU, Memory & Cgroups: fails completely”",
      "Effective PID limit configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: PID limit — CPU, Memory & Cgroups: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “PID limit — CPU, Memory & Cgroups: fails completely”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "PID limit — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for PID limit",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID limit\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID limit\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for PID limit",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “PID limit — CPU, Memory & Cgroups: fails completely”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "PID limit — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The PID limit configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID limit configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID limit configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The PID limit configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "PID limit — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for PID limit",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID limit\nThe effective runtime value differs from expectation.",
        "root": "For “PID limit — CPU, Memory & Cgroups: fails completely”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The PID limit configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID limit configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “PID limit — CPU, Memory & Cgroups: fails completely”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "PID limit — CPU, Memory & Cgroups: fails completely: evidence found\nFor “PID limit — CPU, Memory & Cgroups: fails completely”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “PID limit — CPU, Memory & Cgroups: fails completely”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "PID limit — CPU, Memory & Cgroups: fails completely: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "PID limit — CPU, Memory & Cgroups: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-119",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "PID limit — CPU, Memory & Cgroups: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective PID limit state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "PID limit",
      "CPU, Memory & Cgroups",
      "PID limit — CPU, Memory & Cgroups: behaves differently than configured",
      "pid",
      "limit",
      "cpu",
      "memory",
      "cgroups",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “PID limit — CPU, Memory & Cgroups: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected PID limit; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “PID limit — CPU, Memory & Cgroups: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “PID limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for PID limit",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The PID limit configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “PID limit — CPU, Memory & Cgroups: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the PID limit inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “PID limit — CPU, Memory & Cgroups: behaves differently than configured”",
      "Effective PID limit configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: PID limit — CPU, Memory & Cgroups: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “PID limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "PID limit — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for PID limit",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID limit\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID limit\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for PID limit",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “PID limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "PID limit — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The PID limit configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID limit configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID limit configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The PID limit configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "PID limit — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for PID limit",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID limit\nThe effective runtime value differs from expectation.",
        "root": "For “PID limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The PID limit configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID limit configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “PID limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "PID limit — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nFor “PID limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “PID limit — CPU, Memory & Cgroups: behaves differently than configured”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "PID limit — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "PID limit — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-120",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective PID limit state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "PID limit",
      "CPU, Memory & Cgroups",
      "PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart",
      "pid",
      "limit",
      "cpu",
      "memory",
      "cgroups",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected PID limit; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for PID limit",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The PID limit configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the PID limit inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart”",
      "Effective PID limit configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for PID limit",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID limit\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID limit\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for PID limit",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The PID limit configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID limit configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID limit configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The PID limit configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for PID limit",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for PID limit\nThe effective runtime value differs from expectation.",
        "root": "For “PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The PID limit configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The PID limit configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nFor “PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective PID limit state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the PID limit path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "PID limit — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-121",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "Cgroup controller — CPU, Memory & Cgroups: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective cgroup controller state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "cgroup controller",
      "CPU, Memory & Cgroups",
      "Cgroup controller — CPU, Memory & Cgroups: fails completely",
      "cgroup",
      "controller",
      "cpu",
      "memory",
      "cgroups",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Cgroup controller — CPU, Memory & Cgroups: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected cgroup controller; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Cgroup controller — CPU, Memory & Cgroups: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Cgroup controller — CPU, Memory & Cgroups: fails completely”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for cgroup controller",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The cgroup controller configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Cgroup controller — CPU, Memory & Cgroups: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the cgroup controller inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Cgroup controller — CPU, Memory & Cgroups: fails completely”",
      "Effective cgroup controller configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Cgroup controller — CPU, Memory & Cgroups: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Cgroup controller — CPU, Memory & Cgroups: fails completely”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Cgroup controller — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for cgroup controller",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cgroup controller\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cgroup controller\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for cgroup controller",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Cgroup controller — CPU, Memory & Cgroups: fails completely”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Cgroup controller — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The cgroup controller configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cgroup controller configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cgroup controller configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The cgroup controller configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Cgroup controller — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for cgroup controller",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cgroup controller\nThe effective runtime value differs from expectation.",
        "root": "For “Cgroup controller — CPU, Memory & Cgroups: fails completely”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The cgroup controller configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cgroup controller configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Cgroup controller — CPU, Memory & Cgroups: fails completely”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "Cgroup controller — CPU, Memory & Cgroups: fails completely: evidence found\nFor “Cgroup controller — CPU, Memory & Cgroups: fails completely”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Cgroup controller — CPU, Memory & Cgroups: fails completely”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Cgroup controller — CPU, Memory & Cgroups: fails completely: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Cgroup controller — CPU, Memory & Cgroups: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-122",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective cgroup controller state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "cgroup controller",
      "CPU, Memory & Cgroups",
      "Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured",
      "cgroup",
      "controller",
      "cpu",
      "memory",
      "cgroups",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected cgroup controller; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for cgroup controller",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The cgroup controller configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the cgroup controller inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured”",
      "Effective cgroup controller configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for cgroup controller",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cgroup controller\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cgroup controller\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for cgroup controller",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The cgroup controller configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cgroup controller configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cgroup controller configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The cgroup controller configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for cgroup controller",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cgroup controller\nThe effective runtime value differs from expectation.",
        "root": "For “Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The cgroup controller configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cgroup controller configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nFor “Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Cgroup controller — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-123",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective cgroup controller state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "cgroup controller",
      "CPU, Memory & Cgroups",
      "Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart",
      "cgroup",
      "controller",
      "cpu",
      "memory",
      "cgroups",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected cgroup controller; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for cgroup controller",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The cgroup controller configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the cgroup controller inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart”",
      "Effective cgroup controller configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for cgroup controller",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cgroup controller\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cgroup controller\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for cgroup controller",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The cgroup controller configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cgroup controller configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cgroup controller configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The cgroup controller configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for cgroup controller",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cgroup controller\nThe effective runtime value differs from expectation.",
        "root": "For “Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The cgroup controller configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cgroup controller configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nFor “Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective cgroup controller state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cgroup controller path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Cgroup controller — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-124",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "Block I/O pressure — CPU, Memory & Cgroups: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective block I/O pressure state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "block I/O pressure",
      "CPU, Memory & Cgroups",
      "Block I/O pressure — CPU, Memory & Cgroups: fails completely",
      "block",
      "i",
      "o",
      "pressure",
      "cpu",
      "memory",
      "cgroups",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Block I/O pressure — CPU, Memory & Cgroups: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected block I/O pressure; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Block I/O pressure — CPU, Memory & Cgroups: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Block I/O pressure — CPU, Memory & Cgroups: fails completely”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for block I/O pressure",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The block I/O pressure configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Block I/O pressure — CPU, Memory & Cgroups: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the block I/O pressure inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Block I/O pressure — CPU, Memory & Cgroups: fails completely”",
      "Effective block I/O pressure configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Block I/O pressure — CPU, Memory & Cgroups: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Block I/O pressure — CPU, Memory & Cgroups: fails completely”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Block I/O pressure — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for block I/O pressure",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for block I/O pressure\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for block I/O pressure\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for block I/O pressure",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Block I/O pressure — CPU, Memory & Cgroups: fails completely”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Block I/O pressure — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The block I/O pressure configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The block I/O pressure configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The block I/O pressure configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The block I/O pressure configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Block I/O pressure — CPU, Memory & Cgroups: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for block I/O pressure",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for block I/O pressure\nThe effective runtime value differs from expectation.",
        "root": "For “Block I/O pressure — CPU, Memory & Cgroups: fails completely”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The block I/O pressure configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The block I/O pressure configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Block I/O pressure — CPU, Memory & Cgroups: fails completely”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "Block I/O pressure — CPU, Memory & Cgroups: fails completely: evidence found\nFor “Block I/O pressure — CPU, Memory & Cgroups: fails completely”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Block I/O pressure — CPU, Memory & Cgroups: fails completely”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Block I/O pressure — CPU, Memory & Cgroups: fails completely: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Block I/O pressure — CPU, Memory & Cgroups: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-125",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective block I/O pressure state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "block I/O pressure",
      "CPU, Memory & Cgroups",
      "Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured",
      "block",
      "i",
      "o",
      "pressure",
      "cpu",
      "memory",
      "cgroups",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected block I/O pressure; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for block I/O pressure",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The block I/O pressure configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the block I/O pressure inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured”",
      "Effective block I/O pressure configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for block I/O pressure",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for block I/O pressure\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for block I/O pressure\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for block I/O pressure",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The block I/O pressure configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The block I/O pressure configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The block I/O pressure configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The block I/O pressure configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for block I/O pressure",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for block I/O pressure\nThe effective runtime value differs from expectation.",
        "root": "For “Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The block I/O pressure configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The block I/O pressure configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nFor “Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Block I/O pressure — CPU, Memory & Cgroups: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-126",
    "technology": "Docker",
    "category": "CPU, Memory & Cgroups",
    "title": "Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective block I/O pressure state first, then narrow the failure through the cpu, memory & cgroups dependency chain.",
    "aliases": [
      "block I/O pressure",
      "CPU, Memory & Cgroups",
      "Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart",
      "block",
      "i",
      "o",
      "pressure",
      "cpu",
      "memory",
      "cgroups",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect limits",
        "command": "docker inspect <container> --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected block I/O pressure; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect OOM/exit state",
        "command": "docker inspect <container> --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect cgroup mode",
        "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
      "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for block I/O pressure",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The block I/O pressure configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the block I/O pressure inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart”",
      "Effective block I/O pressure configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant CPU, Memory & Cgroups Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker stats --no-stream payments-api",
          "output": "{\"Status\":\"exited\",\"ExitCode\":137,\"OOMKilled\":true}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
          "output": "payments-api  94.2% CPU  510MiB / 512MiB  99.6% MEM",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
          "output": "Memory=536870912 NanoCpus=1000000000 PidsLimit=256",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The container reaches its 512MiB memory limit and Docker records OOMKilled=true.",
      "remediation": "Profile/fix the application's memory behavior and adjust the configured memory limit only when the workload requirement justifies it.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Use OOMKilled plus the configured limit and usage evidence; exit code 137 by itself is ambiguous.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the CPU, Memory & Cgroups path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for block I/O pressure",
        "command": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for block I/O pressure\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for block I/O pressure\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for block I/O pressure",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The block I/O pressure configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The block I/O pressure configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The block I/O pressure configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Evidence matches this path: The block I/O pressure configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for block I/O pressure",
        "check": "docker stats --no-stream payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for block I/O pressure\nThe effective runtime value differs from expectation.",
        "root": "For “Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The block I/O pressure configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The block I/O pressure configuration is correct but the failure remains",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker stats --no-stream checkout-api",
        "output": "Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nFor “Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart”, the effective block I/O pressure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format 'Memory={{.HostConfig.Memory}} NanoCpus={{.HostConfig.NanoCpus}} PidsLimit={{.HostConfig.PidsLimit}}'",
        "output": "Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nA cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "root": "A cpu, memory & cgroups dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the block I/O pressure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format 'OOMKilled={{.State.OOMKilled}} ExitCode={{.State.ExitCode}} Error={{.State.Error}}'",
        "output": "Block I/O pressure — CPU, Memory & Cgroups: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info --format 'CgroupDriver={{.CgroupDriver}} CgroupVersion={{.CgroupVersion}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-127",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Published TCP port — Networking & Port Publishing: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective published TCP port state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "published TCP port",
      "Networking & Port Publishing",
      "Published TCP port — Networking & Port Publishing: fails completely",
      "published",
      "tcp",
      "port",
      "networking",
      "publishing",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Published TCP port — Networking & Port Publishing: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected published TCP port; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Published TCP port — Networking & Port Publishing: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Published TCP port — Networking & Port Publishing: fails completely”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for published TCP port",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The published TCP port configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Published TCP port — Networking & Port Publishing: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the published TCP port inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Published TCP port — Networking & Port Publishing: fails completely”",
      "Effective published TCP port configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Published TCP port — Networking & Port Publishing: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Published TCP port — Networking & Port Publishing: fails completely”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Published TCP port — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for published TCP port",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published TCP port\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published TCP port\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for published TCP port",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Published TCP port — Networking & Port Publishing: fails completely”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Published TCP port — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The published TCP port configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published TCP port configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published TCP port configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The published TCP port configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Published TCP port — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for published TCP port",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published TCP port\nThe effective runtime value differs from expectation.",
        "root": "For “Published TCP port — Networking & Port Publishing: fails completely”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The published TCP port configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published TCP port configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Published TCP port — Networking & Port Publishing: fails completely”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Published TCP port — Networking & Port Publishing: fails completely: evidence found\nFor “Published TCP port — Networking & Port Publishing: fails completely”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Published TCP port — Networking & Port Publishing: fails completely”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Published TCP port — Networking & Port Publishing: fails completely: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Published TCP port — Networking & Port Publishing: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-128",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Published TCP port — Networking & Port Publishing: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective published TCP port state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "published TCP port",
      "Networking & Port Publishing",
      "Published TCP port — Networking & Port Publishing: behaves differently than configured",
      "published",
      "tcp",
      "port",
      "networking",
      "publishing",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Published TCP port — Networking & Port Publishing: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected published TCP port; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Published TCP port — Networking & Port Publishing: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Published TCP port — Networking & Port Publishing: behaves differently than configured”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for published TCP port",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The published TCP port configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Published TCP port — Networking & Port Publishing: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the published TCP port inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Published TCP port — Networking & Port Publishing: behaves differently than configured”",
      "Effective published TCP port configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Published TCP port — Networking & Port Publishing: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Published TCP port — Networking & Port Publishing: behaves differently than configured”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Published TCP port — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for published TCP port",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published TCP port\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published TCP port\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for published TCP port",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Published TCP port — Networking & Port Publishing: behaves differently than configured”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Published TCP port — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The published TCP port configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published TCP port configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published TCP port configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The published TCP port configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Published TCP port — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for published TCP port",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published TCP port\nThe effective runtime value differs from expectation.",
        "root": "For “Published TCP port — Networking & Port Publishing: behaves differently than configured”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The published TCP port configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published TCP port configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Published TCP port — Networking & Port Publishing: behaves differently than configured”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Published TCP port — Networking & Port Publishing: behaves differently than configured: evidence found\nFor “Published TCP port — Networking & Port Publishing: behaves differently than configured”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Published TCP port — Networking & Port Publishing: behaves differently than configured”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Published TCP port — Networking & Port Publishing: behaves differently than configured: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Published TCP port — Networking & Port Publishing: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-129",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Published TCP port — Networking & Port Publishing: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective published TCP port state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "published TCP port",
      "Networking & Port Publishing",
      "Published TCP port — Networking & Port Publishing: fails intermittently under load or restart",
      "published",
      "tcp",
      "port",
      "networking",
      "publishing",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Published TCP port — Networking & Port Publishing: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected published TCP port; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Published TCP port — Networking & Port Publishing: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Published TCP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for published TCP port",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The published TCP port configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Published TCP port — Networking & Port Publishing: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the published TCP port inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Published TCP port — Networking & Port Publishing: fails intermittently under load or restart”",
      "Effective published TCP port configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Published TCP port — Networking & Port Publishing: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Published TCP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Published TCP port — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for published TCP port",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published TCP port\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published TCP port\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for published TCP port",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Published TCP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Published TCP port — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The published TCP port configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published TCP port configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published TCP port configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The published TCP port configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Published TCP port — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for published TCP port",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published TCP port\nThe effective runtime value differs from expectation.",
        "root": "For “Published TCP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The published TCP port configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published TCP port configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Published TCP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Published TCP port — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nFor “Published TCP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Published TCP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published TCP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Published TCP port — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published TCP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Published TCP port — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-130",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Published UDP port — Networking & Port Publishing: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective published UDP port state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "published UDP port",
      "Networking & Port Publishing",
      "Published UDP port — Networking & Port Publishing: fails completely",
      "published",
      "udp",
      "port",
      "networking",
      "publishing",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Published UDP port — Networking & Port Publishing: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected published UDP port; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Published UDP port — Networking & Port Publishing: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Published UDP port — Networking & Port Publishing: fails completely”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for published UDP port",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The published UDP port configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Published UDP port — Networking & Port Publishing: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the published UDP port inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Published UDP port — Networking & Port Publishing: fails completely”",
      "Effective published UDP port configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Published UDP port — Networking & Port Publishing: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Published UDP port — Networking & Port Publishing: fails completely”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Published UDP port — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for published UDP port",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published UDP port\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published UDP port\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for published UDP port",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Published UDP port — Networking & Port Publishing: fails completely”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Published UDP port — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The published UDP port configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published UDP port configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published UDP port configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The published UDP port configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Published UDP port — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for published UDP port",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published UDP port\nThe effective runtime value differs from expectation.",
        "root": "For “Published UDP port — Networking & Port Publishing: fails completely”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The published UDP port configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published UDP port configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Published UDP port — Networking & Port Publishing: fails completely”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Published UDP port — Networking & Port Publishing: fails completely: evidence found\nFor “Published UDP port — Networking & Port Publishing: fails completely”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Published UDP port — Networking & Port Publishing: fails completely”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Published UDP port — Networking & Port Publishing: fails completely: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Published UDP port — Networking & Port Publishing: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-131",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Published UDP port — Networking & Port Publishing: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective published UDP port state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "published UDP port",
      "Networking & Port Publishing",
      "Published UDP port — Networking & Port Publishing: behaves differently than configured",
      "published",
      "udp",
      "port",
      "networking",
      "publishing",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Published UDP port — Networking & Port Publishing: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected published UDP port; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Published UDP port — Networking & Port Publishing: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Published UDP port — Networking & Port Publishing: behaves differently than configured”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for published UDP port",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The published UDP port configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Published UDP port — Networking & Port Publishing: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the published UDP port inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Published UDP port — Networking & Port Publishing: behaves differently than configured”",
      "Effective published UDP port configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Published UDP port — Networking & Port Publishing: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Published UDP port — Networking & Port Publishing: behaves differently than configured”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Published UDP port — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for published UDP port",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published UDP port\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published UDP port\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for published UDP port",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Published UDP port — Networking & Port Publishing: behaves differently than configured”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Published UDP port — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The published UDP port configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published UDP port configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published UDP port configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The published UDP port configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Published UDP port — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for published UDP port",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published UDP port\nThe effective runtime value differs from expectation.",
        "root": "For “Published UDP port — Networking & Port Publishing: behaves differently than configured”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The published UDP port configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published UDP port configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Published UDP port — Networking & Port Publishing: behaves differently than configured”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Published UDP port — Networking & Port Publishing: behaves differently than configured: evidence found\nFor “Published UDP port — Networking & Port Publishing: behaves differently than configured”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Published UDP port — Networking & Port Publishing: behaves differently than configured”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Published UDP port — Networking & Port Publishing: behaves differently than configured: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Published UDP port — Networking & Port Publishing: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-132",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Published UDP port — Networking & Port Publishing: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective published UDP port state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "published UDP port",
      "Networking & Port Publishing",
      "Published UDP port — Networking & Port Publishing: fails intermittently under load or restart",
      "published",
      "udp",
      "port",
      "networking",
      "publishing",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Published UDP port — Networking & Port Publishing: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected published UDP port; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Published UDP port — Networking & Port Publishing: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Published UDP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for published UDP port",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The published UDP port configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Published UDP port — Networking & Port Publishing: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the published UDP port inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Published UDP port — Networking & Port Publishing: fails intermittently under load or restart”",
      "Effective published UDP port configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Published UDP port — Networking & Port Publishing: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Published UDP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Published UDP port — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for published UDP port",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published UDP port\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published UDP port\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for published UDP port",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Published UDP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Published UDP port — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The published UDP port configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published UDP port configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published UDP port configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The published UDP port configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Published UDP port — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for published UDP port",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for published UDP port\nThe effective runtime value differs from expectation.",
        "root": "For “Published UDP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The published UDP port configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The published UDP port configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Published UDP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Published UDP port — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nFor “Published UDP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Published UDP port — Networking & Port Publishing: fails intermittently under load or restart”, the effective published UDP port state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Published UDP port — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the published UDP port path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Published UDP port — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-133",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Container listener address — Networking & Port Publishing: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective container listener address state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "container listener address",
      "Networking & Port Publishing",
      "Container listener address — Networking & Port Publishing: fails completely",
      "container",
      "listener",
      "address",
      "networking",
      "port",
      "publishing",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container listener address — Networking & Port Publishing: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container listener address; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container listener address — Networking & Port Publishing: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container listener address — Networking & Port Publishing: fails completely”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container listener address",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container listener address configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container listener address — Networking & Port Publishing: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the container listener address inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container listener address — Networking & Port Publishing: fails completely”",
      "Effective container listener address configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container listener address — Networking & Port Publishing: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container listener address — Networking & Port Publishing: fails completely”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container listener address — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container listener address",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container listener address\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container listener address\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container listener address",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container listener address — Networking & Port Publishing: fails completely”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container listener address — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container listener address configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container listener address configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container listener address configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The container listener address configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container listener address — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container listener address",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container listener address\nThe effective runtime value differs from expectation.",
        "root": "For “Container listener address — Networking & Port Publishing: fails completely”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container listener address configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container listener address configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container listener address — Networking & Port Publishing: fails completely”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Container listener address — Networking & Port Publishing: fails completely: evidence found\nFor “Container listener address — Networking & Port Publishing: fails completely”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container listener address — Networking & Port Publishing: fails completely”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container listener address — Networking & Port Publishing: fails completely: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Container listener address — Networking & Port Publishing: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-134",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Container listener address — Networking & Port Publishing: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective container listener address state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "container listener address",
      "Networking & Port Publishing",
      "Container listener address — Networking & Port Publishing: behaves differently than configured",
      "container",
      "listener",
      "address",
      "networking",
      "port",
      "publishing",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container listener address — Networking & Port Publishing: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container listener address; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container listener address — Networking & Port Publishing: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container listener address — Networking & Port Publishing: behaves differently than configured”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container listener address",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container listener address configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container listener address — Networking & Port Publishing: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the container listener address inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container listener address — Networking & Port Publishing: behaves differently than configured”",
      "Effective container listener address configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container listener address — Networking & Port Publishing: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container listener address — Networking & Port Publishing: behaves differently than configured”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container listener address — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container listener address",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container listener address\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container listener address\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container listener address",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container listener address — Networking & Port Publishing: behaves differently than configured”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container listener address — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container listener address configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container listener address configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container listener address configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The container listener address configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container listener address — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container listener address",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container listener address\nThe effective runtime value differs from expectation.",
        "root": "For “Container listener address — Networking & Port Publishing: behaves differently than configured”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container listener address configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container listener address configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container listener address — Networking & Port Publishing: behaves differently than configured”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Container listener address — Networking & Port Publishing: behaves differently than configured: evidence found\nFor “Container listener address — Networking & Port Publishing: behaves differently than configured”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container listener address — Networking & Port Publishing: behaves differently than configured”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container listener address — Networking & Port Publishing: behaves differently than configured: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Container listener address — Networking & Port Publishing: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-135",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Container listener address — Networking & Port Publishing: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective container listener address state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "container listener address",
      "Networking & Port Publishing",
      "Container listener address — Networking & Port Publishing: fails intermittently under load or restart",
      "container",
      "listener",
      "address",
      "networking",
      "port",
      "publishing",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container listener address — Networking & Port Publishing: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container listener address; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container listener address — Networking & Port Publishing: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container listener address — Networking & Port Publishing: fails intermittently under load or restart”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container listener address",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container listener address configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container listener address — Networking & Port Publishing: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the container listener address inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container listener address — Networking & Port Publishing: fails intermittently under load or restart”",
      "Effective container listener address configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container listener address — Networking & Port Publishing: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container listener address — Networking & Port Publishing: fails intermittently under load or restart”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container listener address — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container listener address",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container listener address\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container listener address\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container listener address",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container listener address — Networking & Port Publishing: fails intermittently under load or restart”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container listener address — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container listener address configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container listener address configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container listener address configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The container listener address configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container listener address — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container listener address",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container listener address\nThe effective runtime value differs from expectation.",
        "root": "For “Container listener address — Networking & Port Publishing: fails intermittently under load or restart”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container listener address configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container listener address configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container listener address — Networking & Port Publishing: fails intermittently under load or restart”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Container listener address — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nFor “Container listener address — Networking & Port Publishing: fails intermittently under load or restart”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container listener address — Networking & Port Publishing: fails intermittently under load or restart”, the effective container listener address state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container listener address — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container listener address path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Container listener address — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-136",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Bridge connectivity — Networking & Port Publishing: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective bridge connectivity state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "bridge connectivity",
      "Networking & Port Publishing",
      "Bridge connectivity — Networking & Port Publishing: fails completely",
      "bridge",
      "connectivity",
      "networking",
      "port",
      "publishing",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Bridge connectivity — Networking & Port Publishing: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected bridge connectivity; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Bridge connectivity — Networking & Port Publishing: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Bridge connectivity — Networking & Port Publishing: fails completely”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for bridge connectivity",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The bridge connectivity configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Bridge connectivity — Networking & Port Publishing: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the bridge connectivity inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Bridge connectivity — Networking & Port Publishing: fails completely”",
      "Effective bridge connectivity configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Bridge connectivity — Networking & Port Publishing: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Bridge connectivity — Networking & Port Publishing: fails completely”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Bridge connectivity — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for bridge connectivity",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bridge connectivity\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bridge connectivity\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for bridge connectivity",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Bridge connectivity — Networking & Port Publishing: fails completely”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Bridge connectivity — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The bridge connectivity configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bridge connectivity configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bridge connectivity configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The bridge connectivity configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Bridge connectivity — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for bridge connectivity",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bridge connectivity\nThe effective runtime value differs from expectation.",
        "root": "For “Bridge connectivity — Networking & Port Publishing: fails completely”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The bridge connectivity configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bridge connectivity configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Bridge connectivity — Networking & Port Publishing: fails completely”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Bridge connectivity — Networking & Port Publishing: fails completely: evidence found\nFor “Bridge connectivity — Networking & Port Publishing: fails completely”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Bridge connectivity — Networking & Port Publishing: fails completely”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Bridge connectivity — Networking & Port Publishing: fails completely: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Bridge connectivity — Networking & Port Publishing: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-137",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Bridge connectivity — Networking & Port Publishing: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective bridge connectivity state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "bridge connectivity",
      "Networking & Port Publishing",
      "Bridge connectivity — Networking & Port Publishing: behaves differently than configured",
      "bridge",
      "connectivity",
      "networking",
      "port",
      "publishing",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Bridge connectivity — Networking & Port Publishing: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected bridge connectivity; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Bridge connectivity — Networking & Port Publishing: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Bridge connectivity — Networking & Port Publishing: behaves differently than configured”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for bridge connectivity",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The bridge connectivity configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Bridge connectivity — Networking & Port Publishing: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the bridge connectivity inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Bridge connectivity — Networking & Port Publishing: behaves differently than configured”",
      "Effective bridge connectivity configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Bridge connectivity — Networking & Port Publishing: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Bridge connectivity — Networking & Port Publishing: behaves differently than configured”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Bridge connectivity — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for bridge connectivity",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bridge connectivity\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bridge connectivity\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for bridge connectivity",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Bridge connectivity — Networking & Port Publishing: behaves differently than configured”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Bridge connectivity — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The bridge connectivity configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bridge connectivity configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bridge connectivity configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The bridge connectivity configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Bridge connectivity — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for bridge connectivity",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bridge connectivity\nThe effective runtime value differs from expectation.",
        "root": "For “Bridge connectivity — Networking & Port Publishing: behaves differently than configured”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The bridge connectivity configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bridge connectivity configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Bridge connectivity — Networking & Port Publishing: behaves differently than configured”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Bridge connectivity — Networking & Port Publishing: behaves differently than configured: evidence found\nFor “Bridge connectivity — Networking & Port Publishing: behaves differently than configured”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Bridge connectivity — Networking & Port Publishing: behaves differently than configured”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Bridge connectivity — Networking & Port Publishing: behaves differently than configured: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Bridge connectivity — Networking & Port Publishing: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-138",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective bridge connectivity state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "bridge connectivity",
      "Networking & Port Publishing",
      "Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart",
      "bridge",
      "connectivity",
      "networking",
      "port",
      "publishing",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected bridge connectivity; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for bridge connectivity",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The bridge connectivity configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the bridge connectivity inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart”",
      "Effective bridge connectivity configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for bridge connectivity",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bridge connectivity\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bridge connectivity\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for bridge connectivity",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The bridge connectivity configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bridge connectivity configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bridge connectivity configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The bridge connectivity configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for bridge connectivity",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bridge connectivity\nThe effective runtime value differs from expectation.",
        "root": "For “Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The bridge connectivity configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bridge connectivity configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nFor “Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart”, the effective bridge connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bridge connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Bridge connectivity — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-139",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "User-defined network — Networking & Port Publishing: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective user-defined network state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "user-defined network",
      "Networking & Port Publishing",
      "User-defined network — Networking & Port Publishing: fails completely",
      "user",
      "defined",
      "network",
      "networking",
      "port",
      "publishing",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “User-defined network — Networking & Port Publishing: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected user-defined network; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “User-defined network — Networking & Port Publishing: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “User-defined network — Networking & Port Publishing: fails completely”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for user-defined network",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The user-defined network configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “User-defined network — Networking & Port Publishing: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the user-defined network inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “User-defined network — Networking & Port Publishing: fails completely”",
      "Effective user-defined network configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: User-defined network — Networking & Port Publishing: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “User-defined network — Networking & Port Publishing: fails completely”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "User-defined network — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for user-defined network",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for user-defined network\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for user-defined network\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for user-defined network",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “User-defined network — Networking & Port Publishing: fails completely”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "User-defined network — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The user-defined network configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The user-defined network configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The user-defined network configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The user-defined network configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "User-defined network — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for user-defined network",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for user-defined network\nThe effective runtime value differs from expectation.",
        "root": "For “User-defined network — Networking & Port Publishing: fails completely”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The user-defined network configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The user-defined network configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “User-defined network — Networking & Port Publishing: fails completely”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "User-defined network — Networking & Port Publishing: fails completely: evidence found\nFor “User-defined network — Networking & Port Publishing: fails completely”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “User-defined network — Networking & Port Publishing: fails completely”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "User-defined network — Networking & Port Publishing: fails completely: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "User-defined network — Networking & Port Publishing: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-140",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "User-defined network — Networking & Port Publishing: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective user-defined network state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "user-defined network",
      "Networking & Port Publishing",
      "User-defined network — Networking & Port Publishing: behaves differently than configured",
      "user",
      "defined",
      "network",
      "networking",
      "port",
      "publishing",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “User-defined network — Networking & Port Publishing: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected user-defined network; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “User-defined network — Networking & Port Publishing: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “User-defined network — Networking & Port Publishing: behaves differently than configured”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for user-defined network",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The user-defined network configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “User-defined network — Networking & Port Publishing: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the user-defined network inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “User-defined network — Networking & Port Publishing: behaves differently than configured”",
      "Effective user-defined network configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: User-defined network — Networking & Port Publishing: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “User-defined network — Networking & Port Publishing: behaves differently than configured”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "User-defined network — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for user-defined network",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for user-defined network\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for user-defined network\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for user-defined network",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “User-defined network — Networking & Port Publishing: behaves differently than configured”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "User-defined network — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The user-defined network configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The user-defined network configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The user-defined network configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The user-defined network configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "User-defined network — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for user-defined network",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for user-defined network\nThe effective runtime value differs from expectation.",
        "root": "For “User-defined network — Networking & Port Publishing: behaves differently than configured”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The user-defined network configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The user-defined network configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “User-defined network — Networking & Port Publishing: behaves differently than configured”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "User-defined network — Networking & Port Publishing: behaves differently than configured: evidence found\nFor “User-defined network — Networking & Port Publishing: behaves differently than configured”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “User-defined network — Networking & Port Publishing: behaves differently than configured”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "User-defined network — Networking & Port Publishing: behaves differently than configured: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "User-defined network — Networking & Port Publishing: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-141",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "User-defined network — Networking & Port Publishing: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective user-defined network state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "user-defined network",
      "Networking & Port Publishing",
      "User-defined network — Networking & Port Publishing: fails intermittently under load or restart",
      "user",
      "defined",
      "network",
      "networking",
      "port",
      "publishing",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “User-defined network — Networking & Port Publishing: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected user-defined network; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “User-defined network — Networking & Port Publishing: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “User-defined network — Networking & Port Publishing: fails intermittently under load or restart”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for user-defined network",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The user-defined network configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “User-defined network — Networking & Port Publishing: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the user-defined network inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “User-defined network — Networking & Port Publishing: fails intermittently under load or restart”",
      "Effective user-defined network configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: User-defined network — Networking & Port Publishing: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “User-defined network — Networking & Port Publishing: fails intermittently under load or restart”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "User-defined network — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for user-defined network",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for user-defined network\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for user-defined network\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for user-defined network",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “User-defined network — Networking & Port Publishing: fails intermittently under load or restart”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "User-defined network — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The user-defined network configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The user-defined network configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The user-defined network configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The user-defined network configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "User-defined network — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for user-defined network",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for user-defined network\nThe effective runtime value differs from expectation.",
        "root": "For “User-defined network — Networking & Port Publishing: fails intermittently under load or restart”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The user-defined network configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The user-defined network configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “User-defined network — Networking & Port Publishing: fails intermittently under load or restart”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "User-defined network — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nFor “User-defined network — Networking & Port Publishing: fails intermittently under load or restart”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “User-defined network — Networking & Port Publishing: fails intermittently under load or restart”, the effective user-defined network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "User-defined network — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the user-defined network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "User-defined network — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-142",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Host-to-container path — Networking & Port Publishing: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective host-to-container path state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "host-to-container path",
      "Networking & Port Publishing",
      "Host-to-container path — Networking & Port Publishing: fails completely",
      "host",
      "to",
      "container",
      "path",
      "networking",
      "port",
      "publishing",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host-to-container path — Networking & Port Publishing: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host-to-container path; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host-to-container path — Networking & Port Publishing: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host-to-container path — Networking & Port Publishing: fails completely”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host-to-container path",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host-to-container path configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host-to-container path — Networking & Port Publishing: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the host-to-container path inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host-to-container path — Networking & Port Publishing: fails completely”",
      "Effective host-to-container path configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host-to-container path — Networking & Port Publishing: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host-to-container path — Networking & Port Publishing: fails completely”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host-to-container path — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host-to-container path",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host-to-container path\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host-to-container path\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host-to-container path",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host-to-container path — Networking & Port Publishing: fails completely”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host-to-container path — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host-to-container path configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host-to-container path configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host-to-container path configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The host-to-container path configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host-to-container path — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host-to-container path",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host-to-container path\nThe effective runtime value differs from expectation.",
        "root": "For “Host-to-container path — Networking & Port Publishing: fails completely”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host-to-container path configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host-to-container path configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host-to-container path — Networking & Port Publishing: fails completely”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Host-to-container path — Networking & Port Publishing: fails completely: evidence found\nFor “Host-to-container path — Networking & Port Publishing: fails completely”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host-to-container path — Networking & Port Publishing: fails completely”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Host-to-container path — Networking & Port Publishing: fails completely: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Host-to-container path — Networking & Port Publishing: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-143",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Host-to-container path — Networking & Port Publishing: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective host-to-container path state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "host-to-container path",
      "Networking & Port Publishing",
      "Host-to-container path — Networking & Port Publishing: behaves differently than configured",
      "host",
      "to",
      "container",
      "path",
      "networking",
      "port",
      "publishing",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host-to-container path — Networking & Port Publishing: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host-to-container path; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host-to-container path — Networking & Port Publishing: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host-to-container path — Networking & Port Publishing: behaves differently than configured”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host-to-container path",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host-to-container path configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host-to-container path — Networking & Port Publishing: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the host-to-container path inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host-to-container path — Networking & Port Publishing: behaves differently than configured”",
      "Effective host-to-container path configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host-to-container path — Networking & Port Publishing: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host-to-container path — Networking & Port Publishing: behaves differently than configured”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host-to-container path — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host-to-container path",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host-to-container path\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host-to-container path\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host-to-container path",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host-to-container path — Networking & Port Publishing: behaves differently than configured”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host-to-container path — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host-to-container path configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host-to-container path configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host-to-container path configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The host-to-container path configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host-to-container path — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host-to-container path",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host-to-container path\nThe effective runtime value differs from expectation.",
        "root": "For “Host-to-container path — Networking & Port Publishing: behaves differently than configured”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host-to-container path configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host-to-container path configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host-to-container path — Networking & Port Publishing: behaves differently than configured”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Host-to-container path — Networking & Port Publishing: behaves differently than configured: evidence found\nFor “Host-to-container path — Networking & Port Publishing: behaves differently than configured”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host-to-container path — Networking & Port Publishing: behaves differently than configured”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Host-to-container path — Networking & Port Publishing: behaves differently than configured: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Host-to-container path — Networking & Port Publishing: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-144",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective host-to-container path state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "host-to-container path",
      "Networking & Port Publishing",
      "Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart",
      "host",
      "to",
      "container",
      "path",
      "networking",
      "port",
      "publishing",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host-to-container path; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host-to-container path",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host-to-container path configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the host-to-container path inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart”",
      "Effective host-to-container path configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host-to-container path",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host-to-container path\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host-to-container path\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host-to-container path",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host-to-container path configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host-to-container path configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host-to-container path configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The host-to-container path configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host-to-container path",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host-to-container path\nThe effective runtime value differs from expectation.",
        "root": "For “Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host-to-container path configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host-to-container path configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nFor “Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart”, the effective host-to-container path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host-to-container path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Host-to-container path — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-145",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Multi-network routing — Networking & Port Publishing: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective multi-network routing state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "multi-network routing",
      "Networking & Port Publishing",
      "Multi-network routing — Networking & Port Publishing: fails completely",
      "multi",
      "network",
      "routing",
      "networking",
      "port",
      "publishing",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Multi-network routing — Networking & Port Publishing: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected multi-network routing; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Multi-network routing — Networking & Port Publishing: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Multi-network routing — Networking & Port Publishing: fails completely”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for multi-network routing",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The multi-network routing configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Multi-network routing — Networking & Port Publishing: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the multi-network routing inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Multi-network routing — Networking & Port Publishing: fails completely”",
      "Effective multi-network routing configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Multi-network routing — Networking & Port Publishing: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Multi-network routing — Networking & Port Publishing: fails completely”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Multi-network routing — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for multi-network routing",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-network routing\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-network routing\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for multi-network routing",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Multi-network routing — Networking & Port Publishing: fails completely”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Multi-network routing — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The multi-network routing configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-network routing configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-network routing configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The multi-network routing configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Multi-network routing — Networking & Port Publishing: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for multi-network routing",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-network routing\nThe effective runtime value differs from expectation.",
        "root": "For “Multi-network routing — Networking & Port Publishing: fails completely”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The multi-network routing configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-network routing configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Multi-network routing — Networking & Port Publishing: fails completely”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Multi-network routing — Networking & Port Publishing: fails completely: evidence found\nFor “Multi-network routing — Networking & Port Publishing: fails completely”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Multi-network routing — Networking & Port Publishing: fails completely”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Multi-network routing — Networking & Port Publishing: fails completely: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Multi-network routing — Networking & Port Publishing: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-146",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Multi-network routing — Networking & Port Publishing: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective multi-network routing state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "multi-network routing",
      "Networking & Port Publishing",
      "Multi-network routing — Networking & Port Publishing: behaves differently than configured",
      "multi",
      "network",
      "routing",
      "networking",
      "port",
      "publishing",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Multi-network routing — Networking & Port Publishing: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected multi-network routing; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Multi-network routing — Networking & Port Publishing: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Multi-network routing — Networking & Port Publishing: behaves differently than configured”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for multi-network routing",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The multi-network routing configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Multi-network routing — Networking & Port Publishing: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the multi-network routing inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Multi-network routing — Networking & Port Publishing: behaves differently than configured”",
      "Effective multi-network routing configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Multi-network routing — Networking & Port Publishing: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Multi-network routing — Networking & Port Publishing: behaves differently than configured”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Multi-network routing — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for multi-network routing",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-network routing\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-network routing\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for multi-network routing",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Multi-network routing — Networking & Port Publishing: behaves differently than configured”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Multi-network routing — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The multi-network routing configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-network routing configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-network routing configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The multi-network routing configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Multi-network routing — Networking & Port Publishing: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for multi-network routing",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-network routing\nThe effective runtime value differs from expectation.",
        "root": "For “Multi-network routing — Networking & Port Publishing: behaves differently than configured”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The multi-network routing configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-network routing configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Multi-network routing — Networking & Port Publishing: behaves differently than configured”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Multi-network routing — Networking & Port Publishing: behaves differently than configured: evidence found\nFor “Multi-network routing — Networking & Port Publishing: behaves differently than configured”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Multi-network routing — Networking & Port Publishing: behaves differently than configured”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Multi-network routing — Networking & Port Publishing: behaves differently than configured: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Multi-network routing — Networking & Port Publishing: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-147",
    "technology": "Docker",
    "category": "Networking & Port Publishing",
    "title": "Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective multi-network routing state first, then narrow the failure through the networking & port publishing dependency chain.",
    "aliases": [
      "multi-network routing",
      "Networking & Port Publishing",
      "Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart",
      "multi",
      "network",
      "routing",
      "networking",
      "port",
      "publishing",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "network"
    ],
    "steps": [
      {
        "title": "Inspect published ports",
        "command": "docker port <container>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect container networks",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected multi-network routing; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect network",
        "command": "docker network inspect <network>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect port bindings",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Ports}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
      "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for multi-network routing",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The multi-network routing configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the multi-network routing inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart”",
      "Effective multi-network routing configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Networking & Port Publishing Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker port payments-api",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker network inspect payments-net",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Networking & Port Publishing path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for multi-network routing",
        "command": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-network routing\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker port payments-api",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-network routing\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for multi-network routing",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The multi-network routing configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-network routing configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-network routing configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker network inspect payments-net",
            "output": "Evidence matches this path: The multi-network routing configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker network inspect payments-net",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for multi-network routing",
        "check": "docker port payments-api",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-network routing\nThe effective runtime value differs from expectation.",
        "root": "For “Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The multi-network routing configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-network routing configuration is correct but the failure remains",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker network inspect payments-net",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker port checkout-api",
        "output": "Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nFor “Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart”, the effective multi-network routing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nA networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "root": "A networking & port publishing dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-network routing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker network inspect payments-net",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker network inspect payments-net",
        "output": "Multi-network routing — Networking & Port Publishing: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Ports}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-148",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "Container DNS — DNS, Proxy & Egress: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective container DNS state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "container DNS",
      "DNS, Proxy & Egress",
      "Container DNS — DNS, Proxy & Egress: fails completely",
      "container",
      "dns",
      "proxy",
      "egress",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container DNS — DNS, Proxy & Egress: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container DNS; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Container DNS — DNS, Proxy & Egress: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container DNS — DNS, Proxy & Egress: fails completely”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container DNS",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container DNS configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container DNS — DNS, Proxy & Egress: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the container DNS inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container DNS — DNS, Proxy & Egress: fails completely”",
      "Effective container DNS configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container DNS — DNS, Proxy & Egress: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container DNS — DNS, Proxy & Egress: fails completely”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container DNS — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container DNS",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container DNS\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container DNS\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container DNS",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container DNS — DNS, Proxy & Egress: fails completely”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container DNS — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container DNS configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container DNS configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container DNS configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The container DNS configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container DNS — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container DNS",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container DNS\nThe effective runtime value differs from expectation.",
        "root": "For “Container DNS — DNS, Proxy & Egress: fails completely”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container DNS configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container DNS configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container DNS — DNS, Proxy & Egress: fails completely”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Container DNS — DNS, Proxy & Egress: fails completely: evidence found\nFor “Container DNS — DNS, Proxy & Egress: fails completely”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container DNS — DNS, Proxy & Egress: fails completely”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container DNS — DNS, Proxy & Egress: fails completely: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Container DNS — DNS, Proxy & Egress: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-149",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "Container DNS — DNS, Proxy & Egress: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective container DNS state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "container DNS",
      "DNS, Proxy & Egress",
      "Container DNS — DNS, Proxy & Egress: behaves differently than configured",
      "container",
      "dns",
      "proxy",
      "egress",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container DNS — DNS, Proxy & Egress: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container DNS; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Container DNS — DNS, Proxy & Egress: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container DNS — DNS, Proxy & Egress: behaves differently than configured”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container DNS",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container DNS configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container DNS — DNS, Proxy & Egress: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the container DNS inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container DNS — DNS, Proxy & Egress: behaves differently than configured”",
      "Effective container DNS configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container DNS — DNS, Proxy & Egress: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container DNS — DNS, Proxy & Egress: behaves differently than configured”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container DNS — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container DNS",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container DNS\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container DNS\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container DNS",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container DNS — DNS, Proxy & Egress: behaves differently than configured”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container DNS — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container DNS configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container DNS configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container DNS configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The container DNS configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container DNS — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container DNS",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container DNS\nThe effective runtime value differs from expectation.",
        "root": "For “Container DNS — DNS, Proxy & Egress: behaves differently than configured”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container DNS configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container DNS configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container DNS — DNS, Proxy & Egress: behaves differently than configured”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Container DNS — DNS, Proxy & Egress: behaves differently than configured: evidence found\nFor “Container DNS — DNS, Proxy & Egress: behaves differently than configured”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container DNS — DNS, Proxy & Egress: behaves differently than configured”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container DNS — DNS, Proxy & Egress: behaves differently than configured: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Container DNS — DNS, Proxy & Egress: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-150",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective container DNS state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "container DNS",
      "DNS, Proxy & Egress",
      "Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart",
      "container",
      "dns",
      "proxy",
      "egress",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container DNS; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container DNS",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container DNS configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the container DNS inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart”",
      "Effective container DNS configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container DNS",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container DNS\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container DNS\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container DNS",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container DNS configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container DNS configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container DNS configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The container DNS configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container DNS",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container DNS\nThe effective runtime value differs from expectation.",
        "root": "For “Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container DNS configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container DNS configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nFor “Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective container DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Container DNS — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-151",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "Embedded DNS service discovery — DNS, Proxy & Egress: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective embedded DNS service discovery state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "embedded DNS service discovery",
      "DNS, Proxy & Egress",
      "Embedded DNS service discovery — DNS, Proxy & Egress: fails completely",
      "embedded",
      "dns",
      "service",
      "discovery",
      "proxy",
      "egress",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Embedded DNS service discovery — DNS, Proxy & Egress: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected embedded DNS service discovery; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Embedded DNS service discovery — DNS, Proxy & Egress: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Embedded DNS service discovery — DNS, Proxy & Egress: fails completely”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for embedded DNS service discovery",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The embedded DNS service discovery configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Embedded DNS service discovery — DNS, Proxy & Egress: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the embedded DNS service discovery inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Embedded DNS service discovery — DNS, Proxy & Egress: fails completely”",
      "Effective embedded DNS service discovery configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Embedded DNS service discovery — DNS, Proxy & Egress: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Embedded DNS service discovery — DNS, Proxy & Egress: fails completely”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Embedded DNS service discovery — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Embedded DNS service discovery — DNS, Proxy & Egress: fails completely”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Embedded DNS service discovery — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The embedded DNS service discovery configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The embedded DNS service discovery configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The embedded DNS service discovery configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The embedded DNS service discovery configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Embedded DNS service discovery — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for embedded DNS service discovery",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery\nThe effective runtime value differs from expectation.",
        "root": "For “Embedded DNS service discovery — DNS, Proxy & Egress: fails completely”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The embedded DNS service discovery configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The embedded DNS service discovery configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Embedded DNS service discovery — DNS, Proxy & Egress: fails completely”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Embedded DNS service discovery — DNS, Proxy & Egress: fails completely: evidence found\nFor “Embedded DNS service discovery — DNS, Proxy & Egress: fails completely”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Embedded DNS service discovery — DNS, Proxy & Egress: fails completely”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Embedded DNS service discovery — DNS, Proxy & Egress: fails completely: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Embedded DNS service discovery — DNS, Proxy & Egress: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-152",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective embedded DNS service discovery state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "embedded DNS service discovery",
      "DNS, Proxy & Egress",
      "Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured",
      "embedded",
      "dns",
      "service",
      "discovery",
      "proxy",
      "egress",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected embedded DNS service discovery; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for embedded DNS service discovery",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The embedded DNS service discovery configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the embedded DNS service discovery inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured”",
      "Effective embedded DNS service discovery configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The embedded DNS service discovery configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The embedded DNS service discovery configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The embedded DNS service discovery configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The embedded DNS service discovery configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for embedded DNS service discovery",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery\nThe effective runtime value differs from expectation.",
        "root": "For “Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The embedded DNS service discovery configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The embedded DNS service discovery configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured: evidence found\nFor “Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Embedded DNS service discovery — DNS, Proxy & Egress: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-153",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective embedded DNS service discovery state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "embedded DNS service discovery",
      "DNS, Proxy & Egress",
      "Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart",
      "embedded",
      "dns",
      "service",
      "discovery",
      "proxy",
      "egress",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected embedded DNS service discovery; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for embedded DNS service discovery",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The embedded DNS service discovery configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the embedded DNS service discovery inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart”",
      "Effective embedded DNS service discovery configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The embedded DNS service discovery configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The embedded DNS service discovery configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The embedded DNS service discovery configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The embedded DNS service discovery configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for embedded DNS service discovery",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for embedded DNS service discovery\nThe effective runtime value differs from expectation.",
        "root": "For “Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The embedded DNS service discovery configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The embedded DNS service discovery configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nFor “Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective embedded DNS service discovery state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the embedded DNS service discovery path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Embedded DNS service discovery — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-154",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "External egress — DNS, Proxy & Egress: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective external egress state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "external egress",
      "DNS, Proxy & Egress",
      "External egress — DNS, Proxy & Egress: fails completely",
      "external",
      "egress",
      "dns",
      "proxy",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “External egress — DNS, Proxy & Egress: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected external egress; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “External egress — DNS, Proxy & Egress: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “External egress — DNS, Proxy & Egress: fails completely”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for external egress",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The external egress configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “External egress — DNS, Proxy & Egress: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the external egress inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “External egress — DNS, Proxy & Egress: fails completely”",
      "Effective external egress configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: External egress — DNS, Proxy & Egress: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “External egress — DNS, Proxy & Egress: fails completely”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "External egress — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for external egress",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external egress\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external egress\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for external egress",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “External egress — DNS, Proxy & Egress: fails completely”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "External egress — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The external egress configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external egress configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external egress configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The external egress configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "External egress — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for external egress",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external egress\nThe effective runtime value differs from expectation.",
        "root": "For “External egress — DNS, Proxy & Egress: fails completely”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The external egress configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external egress configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “External egress — DNS, Proxy & Egress: fails completely”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "External egress — DNS, Proxy & Egress: fails completely: evidence found\nFor “External egress — DNS, Proxy & Egress: fails completely”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “External egress — DNS, Proxy & Egress: fails completely”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "External egress — DNS, Proxy & Egress: fails completely: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "External egress — DNS, Proxy & Egress: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-155",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "External egress — DNS, Proxy & Egress: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective external egress state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "external egress",
      "DNS, Proxy & Egress",
      "External egress — DNS, Proxy & Egress: behaves differently than configured",
      "external",
      "egress",
      "dns",
      "proxy",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “External egress — DNS, Proxy & Egress: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected external egress; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “External egress — DNS, Proxy & Egress: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “External egress — DNS, Proxy & Egress: behaves differently than configured”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for external egress",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The external egress configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “External egress — DNS, Proxy & Egress: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the external egress inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “External egress — DNS, Proxy & Egress: behaves differently than configured”",
      "Effective external egress configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: External egress — DNS, Proxy & Egress: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “External egress — DNS, Proxy & Egress: behaves differently than configured”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "External egress — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for external egress",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external egress\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external egress\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for external egress",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “External egress — DNS, Proxy & Egress: behaves differently than configured”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "External egress — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The external egress configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external egress configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external egress configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The external egress configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "External egress — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for external egress",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external egress\nThe effective runtime value differs from expectation.",
        "root": "For “External egress — DNS, Proxy & Egress: behaves differently than configured”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The external egress configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external egress configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “External egress — DNS, Proxy & Egress: behaves differently than configured”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "External egress — DNS, Proxy & Egress: behaves differently than configured: evidence found\nFor “External egress — DNS, Proxy & Egress: behaves differently than configured”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “External egress — DNS, Proxy & Egress: behaves differently than configured”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "External egress — DNS, Proxy & Egress: behaves differently than configured: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "External egress — DNS, Proxy & Egress: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-156",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "External egress — DNS, Proxy & Egress: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective external egress state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "external egress",
      "DNS, Proxy & Egress",
      "External egress — DNS, Proxy & Egress: fails intermittently under load or restart",
      "external",
      "egress",
      "dns",
      "proxy",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “External egress — DNS, Proxy & Egress: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected external egress; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “External egress — DNS, Proxy & Egress: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “External egress — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for external egress",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The external egress configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “External egress — DNS, Proxy & Egress: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the external egress inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “External egress — DNS, Proxy & Egress: fails intermittently under load or restart”",
      "Effective external egress configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: External egress — DNS, Proxy & Egress: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “External egress — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "External egress — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for external egress",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external egress\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external egress\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for external egress",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “External egress — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "External egress — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The external egress configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external egress configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external egress configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The external egress configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "External egress — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for external egress",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external egress\nThe effective runtime value differs from expectation.",
        "root": "For “External egress — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The external egress configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external egress configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “External egress — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "External egress — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nFor “External egress — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “External egress — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective external egress state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "External egress — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external egress path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "External egress — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-157",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "HTTP proxy — DNS, Proxy & Egress: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective HTTP proxy state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "HTTP proxy",
      "DNS, Proxy & Egress",
      "HTTP proxy — DNS, Proxy & Egress: fails completely",
      "http",
      "proxy",
      "dns",
      "egress",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “HTTP proxy — DNS, Proxy & Egress: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected HTTP proxy; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “HTTP proxy — DNS, Proxy & Egress: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “HTTP proxy — DNS, Proxy & Egress: fails completely”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for HTTP proxy",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The HTTP proxy configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “HTTP proxy — DNS, Proxy & Egress: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the HTTP proxy inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “HTTP proxy — DNS, Proxy & Egress: fails completely”",
      "Effective HTTP proxy configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: HTTP proxy — DNS, Proxy & Egress: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “HTTP proxy — DNS, Proxy & Egress: fails completely”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "HTTP proxy — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for HTTP proxy",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTP proxy\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTP proxy\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for HTTP proxy",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “HTTP proxy — DNS, Proxy & Egress: fails completely”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "HTTP proxy — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The HTTP proxy configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTP proxy configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTP proxy configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The HTTP proxy configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "HTTP proxy — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for HTTP proxy",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTP proxy\nThe effective runtime value differs from expectation.",
        "root": "For “HTTP proxy — DNS, Proxy & Egress: fails completely”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The HTTP proxy configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTP proxy configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “HTTP proxy — DNS, Proxy & Egress: fails completely”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "HTTP proxy — DNS, Proxy & Egress: fails completely: evidence found\nFor “HTTP proxy — DNS, Proxy & Egress: fails completely”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “HTTP proxy — DNS, Proxy & Egress: fails completely”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "HTTP proxy — DNS, Proxy & Egress: fails completely: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "HTTP proxy — DNS, Proxy & Egress: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-158",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "HTTP proxy — DNS, Proxy & Egress: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective HTTP proxy state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "HTTP proxy",
      "DNS, Proxy & Egress",
      "HTTP proxy — DNS, Proxy & Egress: behaves differently than configured",
      "http",
      "proxy",
      "dns",
      "egress",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “HTTP proxy — DNS, Proxy & Egress: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected HTTP proxy; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “HTTP proxy — DNS, Proxy & Egress: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “HTTP proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for HTTP proxy",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The HTTP proxy configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “HTTP proxy — DNS, Proxy & Egress: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the HTTP proxy inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “HTTP proxy — DNS, Proxy & Egress: behaves differently than configured”",
      "Effective HTTP proxy configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: HTTP proxy — DNS, Proxy & Egress: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “HTTP proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "HTTP proxy — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for HTTP proxy",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTP proxy\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTP proxy\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for HTTP proxy",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “HTTP proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "HTTP proxy — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The HTTP proxy configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTP proxy configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTP proxy configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The HTTP proxy configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "HTTP proxy — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for HTTP proxy",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTP proxy\nThe effective runtime value differs from expectation.",
        "root": "For “HTTP proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The HTTP proxy configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTP proxy configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “HTTP proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "HTTP proxy — DNS, Proxy & Egress: behaves differently than configured: evidence found\nFor “HTTP proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “HTTP proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "HTTP proxy — DNS, Proxy & Egress: behaves differently than configured: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "HTTP proxy — DNS, Proxy & Egress: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-159",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective HTTP proxy state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "HTTP proxy",
      "DNS, Proxy & Egress",
      "HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart",
      "http",
      "proxy",
      "dns",
      "egress",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected HTTP proxy; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for HTTP proxy",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The HTTP proxy configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the HTTP proxy inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart”",
      "Effective HTTP proxy configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for HTTP proxy",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTP proxy\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTP proxy\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for HTTP proxy",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The HTTP proxy configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTP proxy configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTP proxy configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The HTTP proxy configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for HTTP proxy",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTP proxy\nThe effective runtime value differs from expectation.",
        "root": "For “HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The HTTP proxy configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTP proxy configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nFor “HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTP proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTP proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "HTTP proxy — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-160",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "HTTPS proxy — DNS, Proxy & Egress: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective HTTPS proxy state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "HTTPS proxy",
      "DNS, Proxy & Egress",
      "HTTPS proxy — DNS, Proxy & Egress: fails completely",
      "https",
      "proxy",
      "dns",
      "egress",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “HTTPS proxy — DNS, Proxy & Egress: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected HTTPS proxy; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “HTTPS proxy — DNS, Proxy & Egress: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “HTTPS proxy — DNS, Proxy & Egress: fails completely”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for HTTPS proxy",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The HTTPS proxy configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “HTTPS proxy — DNS, Proxy & Egress: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the HTTPS proxy inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “HTTPS proxy — DNS, Proxy & Egress: fails completely”",
      "Effective HTTPS proxy configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: HTTPS proxy — DNS, Proxy & Egress: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “HTTPS proxy — DNS, Proxy & Egress: fails completely”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "HTTPS proxy — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for HTTPS proxy",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTPS proxy\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTPS proxy\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for HTTPS proxy",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “HTTPS proxy — DNS, Proxy & Egress: fails completely”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "HTTPS proxy — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The HTTPS proxy configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTPS proxy configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTPS proxy configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The HTTPS proxy configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "HTTPS proxy — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for HTTPS proxy",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTPS proxy\nThe effective runtime value differs from expectation.",
        "root": "For “HTTPS proxy — DNS, Proxy & Egress: fails completely”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The HTTPS proxy configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTPS proxy configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “HTTPS proxy — DNS, Proxy & Egress: fails completely”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "HTTPS proxy — DNS, Proxy & Egress: fails completely: evidence found\nFor “HTTPS proxy — DNS, Proxy & Egress: fails completely”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “HTTPS proxy — DNS, Proxy & Egress: fails completely”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "HTTPS proxy — DNS, Proxy & Egress: fails completely: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "HTTPS proxy — DNS, Proxy & Egress: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-161",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective HTTPS proxy state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "HTTPS proxy",
      "DNS, Proxy & Egress",
      "HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured",
      "https",
      "proxy",
      "dns",
      "egress",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected HTTPS proxy; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for HTTPS proxy",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The HTTPS proxy configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the HTTPS proxy inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured”",
      "Effective HTTPS proxy configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for HTTPS proxy",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTPS proxy\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTPS proxy\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for HTTPS proxy",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The HTTPS proxy configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTPS proxy configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTPS proxy configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The HTTPS proxy configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for HTTPS proxy",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTPS proxy\nThe effective runtime value differs from expectation.",
        "root": "For “HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The HTTPS proxy configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTPS proxy configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured: evidence found\nFor “HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "HTTPS proxy — DNS, Proxy & Egress: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-162",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective HTTPS proxy state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "HTTPS proxy",
      "DNS, Proxy & Egress",
      "HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart",
      "https",
      "proxy",
      "dns",
      "egress",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected HTTPS proxy; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for HTTPS proxy",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The HTTPS proxy configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the HTTPS proxy inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart”",
      "Effective HTTPS proxy configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for HTTPS proxy",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTPS proxy\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTPS proxy\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for HTTPS proxy",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The HTTPS proxy configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTPS proxy configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTPS proxy configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The HTTPS proxy configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for HTTPS proxy",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for HTTPS proxy\nThe effective runtime value differs from expectation.",
        "root": "For “HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The HTTPS proxy configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The HTTPS proxy configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nFor “HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective HTTPS proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the HTTPS proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "HTTPS proxy — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-163",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "NO_PROXY — DNS, Proxy & Egress: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective NO_PROXY state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "NO_PROXY",
      "DNS, Proxy & Egress",
      "NO_PROXY — DNS, Proxy & Egress: fails completely",
      "no",
      "proxy",
      "dns",
      "egress",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “NO_PROXY — DNS, Proxy & Egress: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected NO_PROXY; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “NO_PROXY — DNS, Proxy & Egress: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “NO_PROXY — DNS, Proxy & Egress: fails completely”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for NO_PROXY",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The NO_PROXY configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “NO_PROXY — DNS, Proxy & Egress: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the NO_PROXY inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “NO_PROXY — DNS, Proxy & Egress: fails completely”",
      "Effective NO_PROXY configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: NO_PROXY — DNS, Proxy & Egress: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “NO_PROXY — DNS, Proxy & Egress: fails completely”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "NO_PROXY — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for NO_PROXY",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for NO_PROXY\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for NO_PROXY\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for NO_PROXY",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “NO_PROXY — DNS, Proxy & Egress: fails completely”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "NO_PROXY — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The NO_PROXY configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The NO_PROXY configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The NO_PROXY configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The NO_PROXY configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "NO_PROXY — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for NO_PROXY",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for NO_PROXY\nThe effective runtime value differs from expectation.",
        "root": "For “NO_PROXY — DNS, Proxy & Egress: fails completely”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The NO_PROXY configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The NO_PROXY configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “NO_PROXY — DNS, Proxy & Egress: fails completely”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "NO_PROXY — DNS, Proxy & Egress: fails completely: evidence found\nFor “NO_PROXY — DNS, Proxy & Egress: fails completely”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “NO_PROXY — DNS, Proxy & Egress: fails completely”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "NO_PROXY — DNS, Proxy & Egress: fails completely: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "NO_PROXY — DNS, Proxy & Egress: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-164",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "NO_PROXY — DNS, Proxy & Egress: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective NO_PROXY state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "NO_PROXY",
      "DNS, Proxy & Egress",
      "NO_PROXY — DNS, Proxy & Egress: behaves differently than configured",
      "no",
      "proxy",
      "dns",
      "egress",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “NO_PROXY — DNS, Proxy & Egress: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected NO_PROXY; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “NO_PROXY — DNS, Proxy & Egress: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “NO_PROXY — DNS, Proxy & Egress: behaves differently than configured”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for NO_PROXY",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The NO_PROXY configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “NO_PROXY — DNS, Proxy & Egress: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the NO_PROXY inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “NO_PROXY — DNS, Proxy & Egress: behaves differently than configured”",
      "Effective NO_PROXY configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: NO_PROXY — DNS, Proxy & Egress: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “NO_PROXY — DNS, Proxy & Egress: behaves differently than configured”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "NO_PROXY — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for NO_PROXY",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for NO_PROXY\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for NO_PROXY\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for NO_PROXY",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “NO_PROXY — DNS, Proxy & Egress: behaves differently than configured”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "NO_PROXY — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The NO_PROXY configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The NO_PROXY configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The NO_PROXY configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The NO_PROXY configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "NO_PROXY — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for NO_PROXY",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for NO_PROXY\nThe effective runtime value differs from expectation.",
        "root": "For “NO_PROXY — DNS, Proxy & Egress: behaves differently than configured”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The NO_PROXY configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The NO_PROXY configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “NO_PROXY — DNS, Proxy & Egress: behaves differently than configured”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "NO_PROXY — DNS, Proxy & Egress: behaves differently than configured: evidence found\nFor “NO_PROXY — DNS, Proxy & Egress: behaves differently than configured”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “NO_PROXY — DNS, Proxy & Egress: behaves differently than configured”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "NO_PROXY — DNS, Proxy & Egress: behaves differently than configured: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "NO_PROXY — DNS, Proxy & Egress: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-165",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective NO_PROXY state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "NO_PROXY",
      "DNS, Proxy & Egress",
      "NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart",
      "no",
      "proxy",
      "dns",
      "egress",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected NO_PROXY; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for NO_PROXY",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The NO_PROXY configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the NO_PROXY inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart”",
      "Effective NO_PROXY configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for NO_PROXY",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for NO_PROXY\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for NO_PROXY\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for NO_PROXY",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The NO_PROXY configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The NO_PROXY configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The NO_PROXY configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The NO_PROXY configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for NO_PROXY",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for NO_PROXY\nThe effective runtime value differs from expectation.",
        "root": "For “NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The NO_PROXY configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The NO_PROXY configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nFor “NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective NO_PROXY state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the NO_PROXY path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "NO_PROXY — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-166",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "VPN/MTU path — DNS, Proxy & Egress: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective VPN/MTU path state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "VPN/MTU path",
      "DNS, Proxy & Egress",
      "VPN/MTU path — DNS, Proxy & Egress: fails completely",
      "vpn",
      "mtu",
      "path",
      "dns",
      "proxy",
      "egress",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “VPN/MTU path — DNS, Proxy & Egress: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected VPN/MTU path; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “VPN/MTU path — DNS, Proxy & Egress: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “VPN/MTU path — DNS, Proxy & Egress: fails completely”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for VPN/MTU path",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The VPN/MTU path configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “VPN/MTU path — DNS, Proxy & Egress: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the VPN/MTU path inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “VPN/MTU path — DNS, Proxy & Egress: fails completely”",
      "Effective VPN/MTU path configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: VPN/MTU path — DNS, Proxy & Egress: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “VPN/MTU path — DNS, Proxy & Egress: fails completely”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "VPN/MTU path — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for VPN/MTU path",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VPN/MTU path\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VPN/MTU path\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for VPN/MTU path",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “VPN/MTU path — DNS, Proxy & Egress: fails completely”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "VPN/MTU path — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The VPN/MTU path configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VPN/MTU path configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VPN/MTU path configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The VPN/MTU path configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "VPN/MTU path — DNS, Proxy & Egress: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for VPN/MTU path",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VPN/MTU path\nThe effective runtime value differs from expectation.",
        "root": "For “VPN/MTU path — DNS, Proxy & Egress: fails completely”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The VPN/MTU path configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VPN/MTU path configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “VPN/MTU path — DNS, Proxy & Egress: fails completely”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "VPN/MTU path — DNS, Proxy & Egress: fails completely: evidence found\nFor “VPN/MTU path — DNS, Proxy & Egress: fails completely”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “VPN/MTU path — DNS, Proxy & Egress: fails completely”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "VPN/MTU path — DNS, Proxy & Egress: fails completely: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "VPN/MTU path — DNS, Proxy & Egress: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-167",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective VPN/MTU path state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "VPN/MTU path",
      "DNS, Proxy & Egress",
      "VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured",
      "vpn",
      "mtu",
      "path",
      "dns",
      "proxy",
      "egress",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected VPN/MTU path; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for VPN/MTU path",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The VPN/MTU path configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the VPN/MTU path inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured”",
      "Effective VPN/MTU path configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for VPN/MTU path",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VPN/MTU path\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VPN/MTU path\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for VPN/MTU path",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The VPN/MTU path configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VPN/MTU path configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VPN/MTU path configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The VPN/MTU path configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for VPN/MTU path",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VPN/MTU path\nThe effective runtime value differs from expectation.",
        "root": "For “VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The VPN/MTU path configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VPN/MTU path configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured: evidence found\nFor “VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "VPN/MTU path — DNS, Proxy & Egress: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-168",
    "technology": "Docker",
    "category": "DNS, Proxy & Egress",
    "title": "VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective VPN/MTU path state first, then narrow the failure through the dns, proxy & egress dependency chain.",
    "aliases": [
      "VPN/MTU path",
      "DNS, Proxy & Egress",
      "VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart",
      "vpn",
      "mtu",
      "path",
      "dns",
      "proxy",
      "egress",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect DNS overrides",
        "command": "docker inspect <container> --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect network attachment",
        "command": "docker inspect <container> --format '{{json .NetworkSettings.Networks}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected VPN/MTU path; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment/proxy values",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect engine networking",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
      "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for VPN/MTU path",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The VPN/MTU path configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the VPN/MTU path inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart”",
      "Effective VPN/MTU path configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant DNS, Proxy & Egress Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the DNS, Proxy & Egress path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for VPN/MTU path",
        "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VPN/MTU path\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VPN/MTU path\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for VPN/MTU path",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The VPN/MTU path configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VPN/MTU path configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VPN/MTU path configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The VPN/MTU path configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for VPN/MTU path",
        "check": "docker inspect payments-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VPN/MTU path\nThe effective runtime value differs from expectation.",
        "root": "For “VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The VPN/MTU path configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VPN/MTU path configuration is correct but the failure remains",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'DNS={{json .HostConfig.Dns}} Search={{json .HostConfig.DnsSearch}} ExtraHosts={{json .HostConfig.ExtraHosts}}'",
        "output": "VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nFor “VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart”, the effective VPN/MTU path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .NetworkSettings.Networks}}'",
        "output": "VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nA dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "root": "A dns, proxy & egress dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VPN/MTU path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "VPN/MTU path — DNS, Proxy & Egress: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-169",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Named volume — Volumes & Bind Mounts: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective named volume state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "named volume",
      "Volumes & Bind Mounts",
      "Named volume — Volumes & Bind Mounts: fails completely",
      "named",
      "volume",
      "volumes",
      "bind",
      "mounts",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Named volume — Volumes & Bind Mounts: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected named volume; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Named volume — Volumes & Bind Mounts: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Named volume — Volumes & Bind Mounts: fails completely”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for named volume",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The named volume configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Named volume — Volumes & Bind Mounts: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the named volume inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Named volume — Volumes & Bind Mounts: fails completely”",
      "Effective named volume configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Named volume — Volumes & Bind Mounts: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Named volume — Volumes & Bind Mounts: fails completely”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Named volume — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for named volume",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named volume\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named volume\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for named volume",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Named volume — Volumes & Bind Mounts: fails completely”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Named volume — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The named volume configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named volume configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named volume configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The named volume configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Named volume — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for named volume",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named volume\nThe effective runtime value differs from expectation.",
        "root": "For “Named volume — Volumes & Bind Mounts: fails completely”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The named volume configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named volume configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Named volume — Volumes & Bind Mounts: fails completely”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Named volume — Volumes & Bind Mounts: fails completely: evidence found\nFor “Named volume — Volumes & Bind Mounts: fails completely”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Named volume — Volumes & Bind Mounts: fails completely”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Named volume — Volumes & Bind Mounts: fails completely: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Named volume — Volumes & Bind Mounts: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-170",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Named volume — Volumes & Bind Mounts: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective named volume state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "named volume",
      "Volumes & Bind Mounts",
      "Named volume — Volumes & Bind Mounts: behaves differently than configured",
      "named",
      "volume",
      "volumes",
      "bind",
      "mounts",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Named volume — Volumes & Bind Mounts: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected named volume; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Named volume — Volumes & Bind Mounts: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Named volume — Volumes & Bind Mounts: behaves differently than configured”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for named volume",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The named volume configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Named volume — Volumes & Bind Mounts: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the named volume inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Named volume — Volumes & Bind Mounts: behaves differently than configured”",
      "Effective named volume configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Named volume — Volumes & Bind Mounts: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Named volume — Volumes & Bind Mounts: behaves differently than configured”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Named volume — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for named volume",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named volume\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named volume\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for named volume",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Named volume — Volumes & Bind Mounts: behaves differently than configured”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Named volume — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The named volume configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named volume configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named volume configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The named volume configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Named volume — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for named volume",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named volume\nThe effective runtime value differs from expectation.",
        "root": "For “Named volume — Volumes & Bind Mounts: behaves differently than configured”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The named volume configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named volume configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Named volume — Volumes & Bind Mounts: behaves differently than configured”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Named volume — Volumes & Bind Mounts: behaves differently than configured: evidence found\nFor “Named volume — Volumes & Bind Mounts: behaves differently than configured”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Named volume — Volumes & Bind Mounts: behaves differently than configured”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Named volume — Volumes & Bind Mounts: behaves differently than configured: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Named volume — Volumes & Bind Mounts: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-171",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Named volume — Volumes & Bind Mounts: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective named volume state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "named volume",
      "Volumes & Bind Mounts",
      "Named volume — Volumes & Bind Mounts: fails intermittently under load or restart",
      "named",
      "volume",
      "volumes",
      "bind",
      "mounts",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Named volume — Volumes & Bind Mounts: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected named volume; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Named volume — Volumes & Bind Mounts: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Named volume — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for named volume",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The named volume configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Named volume — Volumes & Bind Mounts: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the named volume inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Named volume — Volumes & Bind Mounts: fails intermittently under load or restart”",
      "Effective named volume configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Named volume — Volumes & Bind Mounts: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Named volume — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Named volume — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for named volume",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named volume\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named volume\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for named volume",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Named volume — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Named volume — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The named volume configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named volume configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named volume configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The named volume configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Named volume — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for named volume",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named volume\nThe effective runtime value differs from expectation.",
        "root": "For “Named volume — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The named volume configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named volume configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Named volume — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Named volume — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nFor “Named volume — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Named volume — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective named volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Named volume — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Named volume — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-172",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Bind mount source — Volumes & Bind Mounts: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective bind mount source state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "bind mount source",
      "Volumes & Bind Mounts",
      "Bind mount source — Volumes & Bind Mounts: fails completely",
      "bind",
      "mount",
      "source",
      "volumes",
      "mounts",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Bind mount source — Volumes & Bind Mounts: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected bind mount source; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Bind mount source — Volumes & Bind Mounts: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Bind mount source — Volumes & Bind Mounts: fails completely”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for bind mount source",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The bind mount source configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Bind mount source — Volumes & Bind Mounts: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the bind mount source inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Bind mount source — Volumes & Bind Mounts: fails completely”",
      "Effective bind mount source configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Bind mount source — Volumes & Bind Mounts: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Bind mount source — Volumes & Bind Mounts: fails completely”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Bind mount source — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for bind mount source",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind mount source\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind mount source\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for bind mount source",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Bind mount source — Volumes & Bind Mounts: fails completely”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Bind mount source — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The bind mount source configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind mount source configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind mount source configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The bind mount source configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Bind mount source — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for bind mount source",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind mount source\nThe effective runtime value differs from expectation.",
        "root": "For “Bind mount source — Volumes & Bind Mounts: fails completely”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The bind mount source configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind mount source configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Bind mount source — Volumes & Bind Mounts: fails completely”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Bind mount source — Volumes & Bind Mounts: fails completely: evidence found\nFor “Bind mount source — Volumes & Bind Mounts: fails completely”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Bind mount source — Volumes & Bind Mounts: fails completely”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Bind mount source — Volumes & Bind Mounts: fails completely: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Bind mount source — Volumes & Bind Mounts: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-173",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Bind mount source — Volumes & Bind Mounts: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective bind mount source state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "bind mount source",
      "Volumes & Bind Mounts",
      "Bind mount source — Volumes & Bind Mounts: behaves differently than configured",
      "bind",
      "mount",
      "source",
      "volumes",
      "mounts",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Bind mount source — Volumes & Bind Mounts: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected bind mount source; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Bind mount source — Volumes & Bind Mounts: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Bind mount source — Volumes & Bind Mounts: behaves differently than configured”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for bind mount source",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The bind mount source configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Bind mount source — Volumes & Bind Mounts: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the bind mount source inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Bind mount source — Volumes & Bind Mounts: behaves differently than configured”",
      "Effective bind mount source configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Bind mount source — Volumes & Bind Mounts: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Bind mount source — Volumes & Bind Mounts: behaves differently than configured”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Bind mount source — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for bind mount source",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind mount source\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind mount source\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for bind mount source",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Bind mount source — Volumes & Bind Mounts: behaves differently than configured”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Bind mount source — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The bind mount source configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind mount source configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind mount source configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The bind mount source configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Bind mount source — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for bind mount source",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind mount source\nThe effective runtime value differs from expectation.",
        "root": "For “Bind mount source — Volumes & Bind Mounts: behaves differently than configured”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The bind mount source configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind mount source configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Bind mount source — Volumes & Bind Mounts: behaves differently than configured”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Bind mount source — Volumes & Bind Mounts: behaves differently than configured: evidence found\nFor “Bind mount source — Volumes & Bind Mounts: behaves differently than configured”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Bind mount source — Volumes & Bind Mounts: behaves differently than configured”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Bind mount source — Volumes & Bind Mounts: behaves differently than configured: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Bind mount source — Volumes & Bind Mounts: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-174",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective bind mount source state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "bind mount source",
      "Volumes & Bind Mounts",
      "Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart",
      "bind",
      "mount",
      "source",
      "volumes",
      "mounts",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected bind mount source; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for bind mount source",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The bind mount source configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the bind mount source inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart”",
      "Effective bind mount source configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for bind mount source",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind mount source\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind mount source\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for bind mount source",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The bind mount source configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind mount source configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind mount source configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The bind mount source configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for bind mount source",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind mount source\nThe effective runtime value differs from expectation.",
        "root": "For “Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The bind mount source configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind mount source configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nFor “Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind mount source state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind mount source path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Bind mount source — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-175",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Mount destination — Volumes & Bind Mounts: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective mount destination state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "mount destination",
      "Volumes & Bind Mounts",
      "Mount destination — Volumes & Bind Mounts: fails completely",
      "mount",
      "destination",
      "volumes",
      "bind",
      "mounts",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Mount destination — Volumes & Bind Mounts: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected mount destination; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Mount destination — Volumes & Bind Mounts: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Mount destination — Volumes & Bind Mounts: fails completely”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for mount destination",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The mount destination configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Mount destination — Volumes & Bind Mounts: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the mount destination inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Mount destination — Volumes & Bind Mounts: fails completely”",
      "Effective mount destination configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Mount destination — Volumes & Bind Mounts: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Mount destination — Volumes & Bind Mounts: fails completely”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Mount destination — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for mount destination",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount destination\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount destination\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for mount destination",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Mount destination — Volumes & Bind Mounts: fails completely”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Mount destination — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The mount destination configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount destination configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount destination configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The mount destination configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Mount destination — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for mount destination",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount destination\nThe effective runtime value differs from expectation.",
        "root": "For “Mount destination — Volumes & Bind Mounts: fails completely”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The mount destination configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount destination configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Mount destination — Volumes & Bind Mounts: fails completely”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Mount destination — Volumes & Bind Mounts: fails completely: evidence found\nFor “Mount destination — Volumes & Bind Mounts: fails completely”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Mount destination — Volumes & Bind Mounts: fails completely”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Mount destination — Volumes & Bind Mounts: fails completely: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Mount destination — Volumes & Bind Mounts: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-176",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Mount destination — Volumes & Bind Mounts: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective mount destination state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "mount destination",
      "Volumes & Bind Mounts",
      "Mount destination — Volumes & Bind Mounts: behaves differently than configured",
      "mount",
      "destination",
      "volumes",
      "bind",
      "mounts",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Mount destination — Volumes & Bind Mounts: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected mount destination; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Mount destination — Volumes & Bind Mounts: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Mount destination — Volumes & Bind Mounts: behaves differently than configured”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for mount destination",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The mount destination configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Mount destination — Volumes & Bind Mounts: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the mount destination inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Mount destination — Volumes & Bind Mounts: behaves differently than configured”",
      "Effective mount destination configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Mount destination — Volumes & Bind Mounts: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Mount destination — Volumes & Bind Mounts: behaves differently than configured”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Mount destination — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for mount destination",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount destination\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount destination\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for mount destination",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Mount destination — Volumes & Bind Mounts: behaves differently than configured”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Mount destination — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The mount destination configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount destination configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount destination configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The mount destination configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Mount destination — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for mount destination",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount destination\nThe effective runtime value differs from expectation.",
        "root": "For “Mount destination — Volumes & Bind Mounts: behaves differently than configured”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The mount destination configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount destination configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Mount destination — Volumes & Bind Mounts: behaves differently than configured”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Mount destination — Volumes & Bind Mounts: behaves differently than configured: evidence found\nFor “Mount destination — Volumes & Bind Mounts: behaves differently than configured”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Mount destination — Volumes & Bind Mounts: behaves differently than configured”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Mount destination — Volumes & Bind Mounts: behaves differently than configured: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Mount destination — Volumes & Bind Mounts: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-177",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective mount destination state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "mount destination",
      "Volumes & Bind Mounts",
      "Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart",
      "mount",
      "destination",
      "volumes",
      "bind",
      "mounts",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected mount destination; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for mount destination",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The mount destination configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the mount destination inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart”",
      "Effective mount destination configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for mount destination",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount destination\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount destination\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for mount destination",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The mount destination configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount destination configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount destination configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The mount destination configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for mount destination",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount destination\nThe effective runtime value differs from expectation.",
        "root": "For “Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The mount destination configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount destination configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nFor “Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount destination state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount destination path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Mount destination — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-178",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Mount permissions — Volumes & Bind Mounts: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective mount permissions state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "mount permissions",
      "Volumes & Bind Mounts",
      "Mount permissions — Volumes & Bind Mounts: fails completely",
      "mount",
      "permissions",
      "volumes",
      "bind",
      "mounts",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Mount permissions — Volumes & Bind Mounts: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected mount permissions; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Mount permissions — Volumes & Bind Mounts: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Mount permissions — Volumes & Bind Mounts: fails completely”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for mount permissions",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The mount permissions configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Mount permissions — Volumes & Bind Mounts: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the mount permissions inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Mount permissions — Volumes & Bind Mounts: fails completely”",
      "Effective mount permissions configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Mount permissions — Volumes & Bind Mounts: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Mount permissions — Volumes & Bind Mounts: fails completely”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Mount permissions — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for mount permissions",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount permissions\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount permissions\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for mount permissions",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Mount permissions — Volumes & Bind Mounts: fails completely”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Mount permissions — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The mount permissions configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount permissions configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount permissions configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The mount permissions configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Mount permissions — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for mount permissions",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount permissions\nThe effective runtime value differs from expectation.",
        "root": "For “Mount permissions — Volumes & Bind Mounts: fails completely”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The mount permissions configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount permissions configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Mount permissions — Volumes & Bind Mounts: fails completely”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Mount permissions — Volumes & Bind Mounts: fails completely: evidence found\nFor “Mount permissions — Volumes & Bind Mounts: fails completely”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Mount permissions — Volumes & Bind Mounts: fails completely”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Mount permissions — Volumes & Bind Mounts: fails completely: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Mount permissions — Volumes & Bind Mounts: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-179",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Mount permissions — Volumes & Bind Mounts: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective mount permissions state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "mount permissions",
      "Volumes & Bind Mounts",
      "Mount permissions — Volumes & Bind Mounts: behaves differently than configured",
      "mount",
      "permissions",
      "volumes",
      "bind",
      "mounts",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Mount permissions — Volumes & Bind Mounts: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected mount permissions; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Mount permissions — Volumes & Bind Mounts: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Mount permissions — Volumes & Bind Mounts: behaves differently than configured”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for mount permissions",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The mount permissions configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Mount permissions — Volumes & Bind Mounts: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the mount permissions inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Mount permissions — Volumes & Bind Mounts: behaves differently than configured”",
      "Effective mount permissions configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Mount permissions — Volumes & Bind Mounts: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Mount permissions — Volumes & Bind Mounts: behaves differently than configured”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Mount permissions — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for mount permissions",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount permissions\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount permissions\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for mount permissions",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Mount permissions — Volumes & Bind Mounts: behaves differently than configured”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Mount permissions — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The mount permissions configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount permissions configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount permissions configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The mount permissions configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Mount permissions — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for mount permissions",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount permissions\nThe effective runtime value differs from expectation.",
        "root": "For “Mount permissions — Volumes & Bind Mounts: behaves differently than configured”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The mount permissions configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount permissions configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Mount permissions — Volumes & Bind Mounts: behaves differently than configured”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Mount permissions — Volumes & Bind Mounts: behaves differently than configured: evidence found\nFor “Mount permissions — Volumes & Bind Mounts: behaves differently than configured”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Mount permissions — Volumes & Bind Mounts: behaves differently than configured”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Mount permissions — Volumes & Bind Mounts: behaves differently than configured: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Mount permissions — Volumes & Bind Mounts: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-180",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective mount permissions state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "mount permissions",
      "Volumes & Bind Mounts",
      "Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart",
      "mount",
      "permissions",
      "volumes",
      "bind",
      "mounts",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected mount permissions; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for mount permissions",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The mount permissions configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the mount permissions inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart”",
      "Effective mount permissions configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for mount permissions",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount permissions\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount permissions\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for mount permissions",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The mount permissions configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount permissions configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount permissions configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The mount permissions configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for mount permissions",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for mount permissions\nThe effective runtime value differs from expectation.",
        "root": "For “Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The mount permissions configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The mount permissions configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nFor “Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective mount permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the mount permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Mount permissions — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-181",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Volume ownership — Volumes & Bind Mounts: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective volume ownership state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "volume ownership",
      "Volumes & Bind Mounts",
      "Volume ownership — Volumes & Bind Mounts: fails completely",
      "volume",
      "ownership",
      "volumes",
      "bind",
      "mounts",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Volume ownership — Volumes & Bind Mounts: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected volume ownership; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Volume ownership — Volumes & Bind Mounts: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Volume ownership — Volumes & Bind Mounts: fails completely”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for volume ownership",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The volume ownership configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Volume ownership — Volumes & Bind Mounts: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the volume ownership inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Volume ownership — Volumes & Bind Mounts: fails completely”",
      "Effective volume ownership configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Volume ownership — Volumes & Bind Mounts: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Volume ownership — Volumes & Bind Mounts: fails completely”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Volume ownership — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for volume ownership",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for volume ownership\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for volume ownership\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for volume ownership",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Volume ownership — Volumes & Bind Mounts: fails completely”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Volume ownership — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The volume ownership configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The volume ownership configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The volume ownership configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The volume ownership configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Volume ownership — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for volume ownership",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for volume ownership\nThe effective runtime value differs from expectation.",
        "root": "For “Volume ownership — Volumes & Bind Mounts: fails completely”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The volume ownership configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The volume ownership configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Volume ownership — Volumes & Bind Mounts: fails completely”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Volume ownership — Volumes & Bind Mounts: fails completely: evidence found\nFor “Volume ownership — Volumes & Bind Mounts: fails completely”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Volume ownership — Volumes & Bind Mounts: fails completely”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Volume ownership — Volumes & Bind Mounts: fails completely: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Volume ownership — Volumes & Bind Mounts: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-182",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Volume ownership — Volumes & Bind Mounts: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective volume ownership state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "volume ownership",
      "Volumes & Bind Mounts",
      "Volume ownership — Volumes & Bind Mounts: behaves differently than configured",
      "volume",
      "ownership",
      "volumes",
      "bind",
      "mounts",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Volume ownership — Volumes & Bind Mounts: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected volume ownership; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Volume ownership — Volumes & Bind Mounts: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Volume ownership — Volumes & Bind Mounts: behaves differently than configured”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for volume ownership",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The volume ownership configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Volume ownership — Volumes & Bind Mounts: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the volume ownership inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Volume ownership — Volumes & Bind Mounts: behaves differently than configured”",
      "Effective volume ownership configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Volume ownership — Volumes & Bind Mounts: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Volume ownership — Volumes & Bind Mounts: behaves differently than configured”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Volume ownership — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for volume ownership",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for volume ownership\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for volume ownership\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for volume ownership",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Volume ownership — Volumes & Bind Mounts: behaves differently than configured”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Volume ownership — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The volume ownership configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The volume ownership configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The volume ownership configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The volume ownership configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Volume ownership — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for volume ownership",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for volume ownership\nThe effective runtime value differs from expectation.",
        "root": "For “Volume ownership — Volumes & Bind Mounts: behaves differently than configured”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The volume ownership configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The volume ownership configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Volume ownership — Volumes & Bind Mounts: behaves differently than configured”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Volume ownership — Volumes & Bind Mounts: behaves differently than configured: evidence found\nFor “Volume ownership — Volumes & Bind Mounts: behaves differently than configured”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Volume ownership — Volumes & Bind Mounts: behaves differently than configured”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Volume ownership — Volumes & Bind Mounts: behaves differently than configured: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Volume ownership — Volumes & Bind Mounts: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-183",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective volume ownership state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "volume ownership",
      "Volumes & Bind Mounts",
      "Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart",
      "volume",
      "ownership",
      "volumes",
      "bind",
      "mounts",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected volume ownership; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for volume ownership",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The volume ownership configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the volume ownership inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart”",
      "Effective volume ownership configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for volume ownership",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for volume ownership\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for volume ownership\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for volume ownership",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The volume ownership configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The volume ownership configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The volume ownership configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The volume ownership configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for volume ownership",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for volume ownership\nThe effective runtime value differs from expectation.",
        "root": "For “Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The volume ownership configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The volume ownership configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nFor “Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Volume ownership — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-184",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Bind propagation — Volumes & Bind Mounts: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective bind propagation state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "bind propagation",
      "Volumes & Bind Mounts",
      "Bind propagation — Volumes & Bind Mounts: fails completely",
      "bind",
      "propagation",
      "volumes",
      "mounts",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Bind propagation — Volumes & Bind Mounts: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected bind propagation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Bind propagation — Volumes & Bind Mounts: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Bind propagation — Volumes & Bind Mounts: fails completely”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for bind propagation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The bind propagation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Bind propagation — Volumes & Bind Mounts: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the bind propagation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Bind propagation — Volumes & Bind Mounts: fails completely”",
      "Effective bind propagation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Bind propagation — Volumes & Bind Mounts: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Bind propagation — Volumes & Bind Mounts: fails completely”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Bind propagation — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for bind propagation",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind propagation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind propagation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for bind propagation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Bind propagation — Volumes & Bind Mounts: fails completely”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Bind propagation — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The bind propagation configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind propagation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind propagation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The bind propagation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Bind propagation — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for bind propagation",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind propagation\nThe effective runtime value differs from expectation.",
        "root": "For “Bind propagation — Volumes & Bind Mounts: fails completely”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The bind propagation configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind propagation configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Bind propagation — Volumes & Bind Mounts: fails completely”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Bind propagation — Volumes & Bind Mounts: fails completely: evidence found\nFor “Bind propagation — Volumes & Bind Mounts: fails completely”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Bind propagation — Volumes & Bind Mounts: fails completely”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Bind propagation — Volumes & Bind Mounts: fails completely: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Bind propagation — Volumes & Bind Mounts: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-185",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Bind propagation — Volumes & Bind Mounts: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective bind propagation state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "bind propagation",
      "Volumes & Bind Mounts",
      "Bind propagation — Volumes & Bind Mounts: behaves differently than configured",
      "bind",
      "propagation",
      "volumes",
      "mounts",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Bind propagation — Volumes & Bind Mounts: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected bind propagation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Bind propagation — Volumes & Bind Mounts: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Bind propagation — Volumes & Bind Mounts: behaves differently than configured”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for bind propagation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The bind propagation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Bind propagation — Volumes & Bind Mounts: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the bind propagation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Bind propagation — Volumes & Bind Mounts: behaves differently than configured”",
      "Effective bind propagation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Bind propagation — Volumes & Bind Mounts: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Bind propagation — Volumes & Bind Mounts: behaves differently than configured”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Bind propagation — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for bind propagation",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind propagation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind propagation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for bind propagation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Bind propagation — Volumes & Bind Mounts: behaves differently than configured”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Bind propagation — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The bind propagation configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind propagation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind propagation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The bind propagation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Bind propagation — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for bind propagation",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind propagation\nThe effective runtime value differs from expectation.",
        "root": "For “Bind propagation — Volumes & Bind Mounts: behaves differently than configured”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The bind propagation configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind propagation configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Bind propagation — Volumes & Bind Mounts: behaves differently than configured”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Bind propagation — Volumes & Bind Mounts: behaves differently than configured: evidence found\nFor “Bind propagation — Volumes & Bind Mounts: behaves differently than configured”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Bind propagation — Volumes & Bind Mounts: behaves differently than configured”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Bind propagation — Volumes & Bind Mounts: behaves differently than configured: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Bind propagation — Volumes & Bind Mounts: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-186",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective bind propagation state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "bind propagation",
      "Volumes & Bind Mounts",
      "Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart",
      "bind",
      "propagation",
      "volumes",
      "mounts",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected bind propagation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for bind propagation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The bind propagation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the bind propagation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart”",
      "Effective bind propagation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for bind propagation",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind propagation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind propagation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for bind propagation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The bind propagation configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind propagation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind propagation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The bind propagation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for bind propagation",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for bind propagation\nThe effective runtime value differs from expectation.",
        "root": "For “Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The bind propagation configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The bind propagation configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nFor “Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective bind propagation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the bind propagation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Bind propagation — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-187",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Runtime data persistence — Volumes & Bind Mounts: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective runtime data persistence state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "runtime data persistence",
      "Volumes & Bind Mounts",
      "Runtime data persistence — Volumes & Bind Mounts: fails completely",
      "runtime",
      "data",
      "persistence",
      "volumes",
      "bind",
      "mounts",
      "fails",
      "completely"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Runtime data persistence — Volumes & Bind Mounts: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected runtime data persistence; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Runtime data persistence — Volumes & Bind Mounts: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Runtime data persistence — Volumes & Bind Mounts: fails completely”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for runtime data persistence",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The runtime data persistence configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Runtime data persistence — Volumes & Bind Mounts: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the runtime data persistence inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Runtime data persistence — Volumes & Bind Mounts: fails completely”",
      "Effective runtime data persistence configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Runtime data persistence — Volumes & Bind Mounts: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Runtime data persistence — Volumes & Bind Mounts: fails completely”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Runtime data persistence — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for runtime data persistence",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime data persistence\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime data persistence\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for runtime data persistence",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Runtime data persistence — Volumes & Bind Mounts: fails completely”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Runtime data persistence — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The runtime data persistence configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime data persistence configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime data persistence configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The runtime data persistence configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Runtime data persistence — Volumes & Bind Mounts: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for runtime data persistence",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime data persistence\nThe effective runtime value differs from expectation.",
        "root": "For “Runtime data persistence — Volumes & Bind Mounts: fails completely”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The runtime data persistence configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime data persistence configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Runtime data persistence — Volumes & Bind Mounts: fails completely”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Runtime data persistence — Volumes & Bind Mounts: fails completely: evidence found\nFor “Runtime data persistence — Volumes & Bind Mounts: fails completely”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Runtime data persistence — Volumes & Bind Mounts: fails completely”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Runtime data persistence — Volumes & Bind Mounts: fails completely: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Runtime data persistence — Volumes & Bind Mounts: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-188",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective runtime data persistence state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "runtime data persistence",
      "Volumes & Bind Mounts",
      "Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured",
      "runtime",
      "data",
      "persistence",
      "volumes",
      "bind",
      "mounts",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected runtime data persistence; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for runtime data persistence",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The runtime data persistence configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the runtime data persistence inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured”",
      "Effective runtime data persistence configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for runtime data persistence",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime data persistence\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime data persistence\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for runtime data persistence",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The runtime data persistence configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime data persistence configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime data persistence configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The runtime data persistence configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for runtime data persistence",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime data persistence\nThe effective runtime value differs from expectation.",
        "root": "For “Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The runtime data persistence configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime data persistence configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured: evidence found\nFor “Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Runtime data persistence — Volumes & Bind Mounts: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-189",
    "technology": "Docker",
    "category": "Volumes & Bind Mounts",
    "title": "Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective runtime data persistence state first, then narrow the failure through the volumes & bind mounts dependency chain.",
    "aliases": [
      "runtime data persistence",
      "Volumes & Bind Mounts",
      "Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart",
      "runtime",
      "data",
      "persistence",
      "volumes",
      "bind",
      "mounts",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container",
      "volume"
    ],
    "steps": [
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect volume",
        "command": "docker volume inspect <volume>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected runtime data persistence; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List volumes",
        "command": "docker volume ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect writable-layer changes",
        "command": "docker diff <container>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
      "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for runtime data persistence",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The runtime data persistence configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the runtime data persistence inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart”",
      "Effective runtime data persistence configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Volumes & Bind Mounts Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker volume inspect payments-data",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker volume ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Volumes & Bind Mounts path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for runtime data persistence",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime data persistence\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime data persistence\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker volume inspect payments-data",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for runtime data persistence",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The runtime data persistence configuration is correct but the failure remains",
        "command": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime data persistence configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume inspect payments-data",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime data persistence configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker volume ls",
            "output": "Evidence matches this path: The runtime data persistence configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker volume ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker diff checkout-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker diff checkout-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for runtime data persistence",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for runtime data persistence\nThe effective runtime value differs from expectation.",
        "root": "For “Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The runtime data persistence configuration is correct but the failure remains",
        "check": "docker volume inspect payments-data",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The runtime data persistence configuration is correct but the failure remains",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker volume ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nFor “Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart”, the effective runtime data persistence state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker volume inspect payments-data",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "check": "docker volume inspect payments-data",
        "output": "Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nA volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "root": "A volumes & bind mounts dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the runtime data persistence path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker volume ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker volume ls",
        "output": "Runtime data persistence — Volumes & Bind Mounts: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker diff checkout-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-190",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Docker data-root capacity — Storage Driver & Disk: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Docker data-root capacity state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "Docker data-root capacity",
      "Storage Driver & Disk",
      "Docker data-root capacity — Storage Driver & Disk: fails completely",
      "docker",
      "data",
      "root",
      "capacity",
      "storage",
      "driver",
      "disk",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker data-root capacity — Storage Driver & Disk: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker data-root capacity; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker data-root capacity — Storage Driver & Disk: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker data-root capacity — Storage Driver & Disk: fails completely”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker data-root capacity",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker data-root capacity configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker data-root capacity — Storage Driver & Disk: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Docker data-root capacity inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker data-root capacity — Storage Driver & Disk: fails completely”",
      "Effective Docker data-root capacity configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker data-root capacity — Storage Driver & Disk: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker data-root capacity — Storage Driver & Disk: fails completely”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker data-root capacity — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker data-root capacity",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker data-root capacity\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker data-root capacity\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker data-root capacity",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker data-root capacity — Storage Driver & Disk: fails completely”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker data-root capacity — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker data-root capacity configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker data-root capacity configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker data-root capacity configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The Docker data-root capacity configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker data-root capacity — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker data-root capacity",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker data-root capacity\nThe effective runtime value differs from expectation.",
        "root": "For “Docker data-root capacity — Storage Driver & Disk: fails completely”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker data-root capacity configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker data-root capacity configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker data-root capacity — Storage Driver & Disk: fails completely”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Docker data-root capacity — Storage Driver & Disk: fails completely: evidence found\nFor “Docker data-root capacity — Storage Driver & Disk: fails completely”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker data-root capacity — Storage Driver & Disk: fails completely”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Docker data-root capacity — Storage Driver & Disk: fails completely: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Docker data-root capacity — Storage Driver & Disk: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-191",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Docker data-root capacity — Storage Driver & Disk: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Docker data-root capacity state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "Docker data-root capacity",
      "Storage Driver & Disk",
      "Docker data-root capacity — Storage Driver & Disk: behaves differently than configured",
      "docker",
      "data",
      "root",
      "capacity",
      "storage",
      "driver",
      "disk",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker data-root capacity — Storage Driver & Disk: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker data-root capacity; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker data-root capacity — Storage Driver & Disk: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker data-root capacity — Storage Driver & Disk: behaves differently than configured”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker data-root capacity",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker data-root capacity configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker data-root capacity — Storage Driver & Disk: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Docker data-root capacity inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker data-root capacity — Storage Driver & Disk: behaves differently than configured”",
      "Effective Docker data-root capacity configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker data-root capacity — Storage Driver & Disk: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker data-root capacity — Storage Driver & Disk: behaves differently than configured”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker data-root capacity — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker data-root capacity",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker data-root capacity\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker data-root capacity\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker data-root capacity",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker data-root capacity — Storage Driver & Disk: behaves differently than configured”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker data-root capacity — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker data-root capacity configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker data-root capacity configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker data-root capacity configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The Docker data-root capacity configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker data-root capacity — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker data-root capacity",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker data-root capacity\nThe effective runtime value differs from expectation.",
        "root": "For “Docker data-root capacity — Storage Driver & Disk: behaves differently than configured”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker data-root capacity configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker data-root capacity configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker data-root capacity — Storage Driver & Disk: behaves differently than configured”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Docker data-root capacity — Storage Driver & Disk: behaves differently than configured: evidence found\nFor “Docker data-root capacity — Storage Driver & Disk: behaves differently than configured”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker data-root capacity — Storage Driver & Disk: behaves differently than configured”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Docker data-root capacity — Storage Driver & Disk: behaves differently than configured: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Docker data-root capacity — Storage Driver & Disk: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-192",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Docker data-root capacity state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "Docker data-root capacity",
      "Storage Driver & Disk",
      "Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart",
      "docker",
      "data",
      "root",
      "capacity",
      "storage",
      "driver",
      "disk",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker data-root capacity; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker data-root capacity",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker data-root capacity configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Docker data-root capacity inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart”",
      "Effective Docker data-root capacity configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker data-root capacity",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker data-root capacity\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker data-root capacity\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker data-root capacity",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker data-root capacity configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker data-root capacity configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker data-root capacity configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The Docker data-root capacity configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker data-root capacity",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker data-root capacity\nThe effective runtime value differs from expectation.",
        "root": "For “Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker data-root capacity configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker data-root capacity configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nFor “Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective Docker data-root capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker data-root capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Docker data-root capacity — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-193",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Inode capacity — Storage Driver & Disk: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective inode capacity state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "inode capacity",
      "Storage Driver & Disk",
      "Inode capacity — Storage Driver & Disk: fails completely",
      "inode",
      "capacity",
      "storage",
      "driver",
      "disk",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Inode capacity — Storage Driver & Disk: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected inode capacity; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Inode capacity — Storage Driver & Disk: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Inode capacity — Storage Driver & Disk: fails completely”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for inode capacity",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The inode capacity configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Inode capacity — Storage Driver & Disk: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the inode capacity inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Inode capacity — Storage Driver & Disk: fails completely”",
      "Effective inode capacity configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Inode capacity — Storage Driver & Disk: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Inode capacity — Storage Driver & Disk: fails completely”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Inode capacity — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for inode capacity",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inode capacity\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inode capacity\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for inode capacity",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Inode capacity — Storage Driver & Disk: fails completely”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Inode capacity — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inode capacity configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inode capacity configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inode capacity configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The inode capacity configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Inode capacity — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for inode capacity",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inode capacity\nThe effective runtime value differs from expectation.",
        "root": "For “Inode capacity — Storage Driver & Disk: fails completely”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The inode capacity configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inode capacity configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Inode capacity — Storage Driver & Disk: fails completely”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Inode capacity — Storage Driver & Disk: fails completely: evidence found\nFor “Inode capacity — Storage Driver & Disk: fails completely”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Inode capacity — Storage Driver & Disk: fails completely”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Inode capacity — Storage Driver & Disk: fails completely: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Inode capacity — Storage Driver & Disk: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-194",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Inode capacity — Storage Driver & Disk: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective inode capacity state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "inode capacity",
      "Storage Driver & Disk",
      "Inode capacity — Storage Driver & Disk: behaves differently than configured",
      "inode",
      "capacity",
      "storage",
      "driver",
      "disk",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Inode capacity — Storage Driver & Disk: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected inode capacity; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Inode capacity — Storage Driver & Disk: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Inode capacity — Storage Driver & Disk: behaves differently than configured”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for inode capacity",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The inode capacity configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Inode capacity — Storage Driver & Disk: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the inode capacity inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Inode capacity — Storage Driver & Disk: behaves differently than configured”",
      "Effective inode capacity configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Inode capacity — Storage Driver & Disk: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Inode capacity — Storage Driver & Disk: behaves differently than configured”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Inode capacity — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for inode capacity",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inode capacity\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inode capacity\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for inode capacity",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Inode capacity — Storage Driver & Disk: behaves differently than configured”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Inode capacity — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inode capacity configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inode capacity configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inode capacity configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The inode capacity configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Inode capacity — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for inode capacity",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inode capacity\nThe effective runtime value differs from expectation.",
        "root": "For “Inode capacity — Storage Driver & Disk: behaves differently than configured”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The inode capacity configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inode capacity configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Inode capacity — Storage Driver & Disk: behaves differently than configured”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Inode capacity — Storage Driver & Disk: behaves differently than configured: evidence found\nFor “Inode capacity — Storage Driver & Disk: behaves differently than configured”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Inode capacity — Storage Driver & Disk: behaves differently than configured”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Inode capacity — Storage Driver & Disk: behaves differently than configured: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Inode capacity — Storage Driver & Disk: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-195",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Inode capacity — Storage Driver & Disk: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective inode capacity state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "inode capacity",
      "Storage Driver & Disk",
      "Inode capacity — Storage Driver & Disk: fails intermittently under load or restart",
      "inode",
      "capacity",
      "storage",
      "driver",
      "disk",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Inode capacity — Storage Driver & Disk: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected inode capacity; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Inode capacity — Storage Driver & Disk: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Inode capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for inode capacity",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The inode capacity configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Inode capacity — Storage Driver & Disk: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the inode capacity inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Inode capacity — Storage Driver & Disk: fails intermittently under load or restart”",
      "Effective inode capacity configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Inode capacity — Storage Driver & Disk: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Inode capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Inode capacity — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for inode capacity",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inode capacity\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inode capacity\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for inode capacity",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Inode capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Inode capacity — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inode capacity configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inode capacity configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inode capacity configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The inode capacity configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Inode capacity — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for inode capacity",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for inode capacity\nThe effective runtime value differs from expectation.",
        "root": "For “Inode capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The inode capacity configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The inode capacity configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Inode capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Inode capacity — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nFor “Inode capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Inode capacity — Storage Driver & Disk: fails intermittently under load or restart”, the effective inode capacity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Inode capacity — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the inode capacity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Inode capacity — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-196",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Overlay2 mount — Storage Driver & Disk: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective overlay2 mount state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "overlay2 mount",
      "Storage Driver & Disk",
      "Overlay2 mount — Storage Driver & Disk: fails completely",
      "overlay2",
      "mount",
      "storage",
      "driver",
      "disk",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Overlay2 mount — Storage Driver & Disk: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected overlay2 mount; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Overlay2 mount — Storage Driver & Disk: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Overlay2 mount — Storage Driver & Disk: fails completely”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for overlay2 mount",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The overlay2 mount configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Overlay2 mount — Storage Driver & Disk: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the overlay2 mount inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Overlay2 mount — Storage Driver & Disk: fails completely”",
      "Effective overlay2 mount configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Overlay2 mount — Storage Driver & Disk: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Overlay2 mount — Storage Driver & Disk: fails completely”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Overlay2 mount — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for overlay2 mount",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for overlay2 mount\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for overlay2 mount\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for overlay2 mount",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Overlay2 mount — Storage Driver & Disk: fails completely”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Overlay2 mount — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The overlay2 mount configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The overlay2 mount configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The overlay2 mount configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The overlay2 mount configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Overlay2 mount — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for overlay2 mount",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for overlay2 mount\nThe effective runtime value differs from expectation.",
        "root": "For “Overlay2 mount — Storage Driver & Disk: fails completely”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The overlay2 mount configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The overlay2 mount configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Overlay2 mount — Storage Driver & Disk: fails completely”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Overlay2 mount — Storage Driver & Disk: fails completely: evidence found\nFor “Overlay2 mount — Storage Driver & Disk: fails completely”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Overlay2 mount — Storage Driver & Disk: fails completely”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Overlay2 mount — Storage Driver & Disk: fails completely: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Overlay2 mount — Storage Driver & Disk: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-197",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Overlay2 mount — Storage Driver & Disk: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective overlay2 mount state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "overlay2 mount",
      "Storage Driver & Disk",
      "Overlay2 mount — Storage Driver & Disk: behaves differently than configured",
      "overlay2",
      "mount",
      "storage",
      "driver",
      "disk",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Overlay2 mount — Storage Driver & Disk: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected overlay2 mount; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Overlay2 mount — Storage Driver & Disk: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Overlay2 mount — Storage Driver & Disk: behaves differently than configured”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for overlay2 mount",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The overlay2 mount configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Overlay2 mount — Storage Driver & Disk: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the overlay2 mount inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Overlay2 mount — Storage Driver & Disk: behaves differently than configured”",
      "Effective overlay2 mount configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Overlay2 mount — Storage Driver & Disk: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Overlay2 mount — Storage Driver & Disk: behaves differently than configured”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Overlay2 mount — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for overlay2 mount",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for overlay2 mount\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for overlay2 mount\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for overlay2 mount",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Overlay2 mount — Storage Driver & Disk: behaves differently than configured”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Overlay2 mount — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The overlay2 mount configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The overlay2 mount configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The overlay2 mount configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The overlay2 mount configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Overlay2 mount — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for overlay2 mount",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for overlay2 mount\nThe effective runtime value differs from expectation.",
        "root": "For “Overlay2 mount — Storage Driver & Disk: behaves differently than configured”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The overlay2 mount configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The overlay2 mount configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Overlay2 mount — Storage Driver & Disk: behaves differently than configured”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Overlay2 mount — Storage Driver & Disk: behaves differently than configured: evidence found\nFor “Overlay2 mount — Storage Driver & Disk: behaves differently than configured”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Overlay2 mount — Storage Driver & Disk: behaves differently than configured”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Overlay2 mount — Storage Driver & Disk: behaves differently than configured: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Overlay2 mount — Storage Driver & Disk: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-198",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective overlay2 mount state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "overlay2 mount",
      "Storage Driver & Disk",
      "Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart",
      "overlay2",
      "mount",
      "storage",
      "driver",
      "disk",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected overlay2 mount; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for overlay2 mount",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The overlay2 mount configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the overlay2 mount inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart”",
      "Effective overlay2 mount configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for overlay2 mount",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for overlay2 mount\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for overlay2 mount\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for overlay2 mount",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The overlay2 mount configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The overlay2 mount configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The overlay2 mount configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The overlay2 mount configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for overlay2 mount",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for overlay2 mount\nThe effective runtime value differs from expectation.",
        "root": "For “Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The overlay2 mount configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The overlay2 mount configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nFor “Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart”, the effective overlay2 mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the overlay2 mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Overlay2 mount — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-199",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Container writable layer — Storage Driver & Disk: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective container writable layer state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "container writable layer",
      "Storage Driver & Disk",
      "Container writable layer — Storage Driver & Disk: fails completely",
      "container",
      "writable",
      "layer",
      "storage",
      "driver",
      "disk",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container writable layer — Storage Driver & Disk: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container writable layer; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container writable layer — Storage Driver & Disk: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container writable layer — Storage Driver & Disk: fails completely”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container writable layer",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container writable layer configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container writable layer — Storage Driver & Disk: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the container writable layer inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container writable layer — Storage Driver & Disk: fails completely”",
      "Effective container writable layer configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container writable layer — Storage Driver & Disk: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container writable layer — Storage Driver & Disk: fails completely”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container writable layer — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container writable layer",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container writable layer\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container writable layer\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container writable layer",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container writable layer — Storage Driver & Disk: fails completely”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container writable layer — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container writable layer configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container writable layer configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container writable layer configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The container writable layer configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container writable layer — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container writable layer",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container writable layer\nThe effective runtime value differs from expectation.",
        "root": "For “Container writable layer — Storage Driver & Disk: fails completely”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container writable layer configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container writable layer configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container writable layer — Storage Driver & Disk: fails completely”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Container writable layer — Storage Driver & Disk: fails completely: evidence found\nFor “Container writable layer — Storage Driver & Disk: fails completely”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container writable layer — Storage Driver & Disk: fails completely”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container writable layer — Storage Driver & Disk: fails completely: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Container writable layer — Storage Driver & Disk: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-200",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Container writable layer — Storage Driver & Disk: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective container writable layer state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "container writable layer",
      "Storage Driver & Disk",
      "Container writable layer — Storage Driver & Disk: behaves differently than configured",
      "container",
      "writable",
      "layer",
      "storage",
      "driver",
      "disk",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container writable layer — Storage Driver & Disk: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container writable layer; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container writable layer — Storage Driver & Disk: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container writable layer — Storage Driver & Disk: behaves differently than configured”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container writable layer",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container writable layer configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container writable layer — Storage Driver & Disk: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the container writable layer inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container writable layer — Storage Driver & Disk: behaves differently than configured”",
      "Effective container writable layer configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container writable layer — Storage Driver & Disk: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container writable layer — Storage Driver & Disk: behaves differently than configured”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container writable layer — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container writable layer",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container writable layer\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container writable layer\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container writable layer",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container writable layer — Storage Driver & Disk: behaves differently than configured”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container writable layer — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container writable layer configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container writable layer configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container writable layer configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The container writable layer configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container writable layer — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container writable layer",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container writable layer\nThe effective runtime value differs from expectation.",
        "root": "For “Container writable layer — Storage Driver & Disk: behaves differently than configured”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container writable layer configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container writable layer configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container writable layer — Storage Driver & Disk: behaves differently than configured”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Container writable layer — Storage Driver & Disk: behaves differently than configured: evidence found\nFor “Container writable layer — Storage Driver & Disk: behaves differently than configured”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container writable layer — Storage Driver & Disk: behaves differently than configured”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container writable layer — Storage Driver & Disk: behaves differently than configured: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Container writable layer — Storage Driver & Disk: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-201",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Container writable layer — Storage Driver & Disk: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective container writable layer state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "container writable layer",
      "Storage Driver & Disk",
      "Container writable layer — Storage Driver & Disk: fails intermittently under load or restart",
      "container",
      "writable",
      "layer",
      "storage",
      "driver",
      "disk",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container writable layer — Storage Driver & Disk: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container writable layer; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container writable layer — Storage Driver & Disk: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container writable layer — Storage Driver & Disk: fails intermittently under load or restart”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container writable layer",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container writable layer configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container writable layer — Storage Driver & Disk: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the container writable layer inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container writable layer — Storage Driver & Disk: fails intermittently under load or restart”",
      "Effective container writable layer configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container writable layer — Storage Driver & Disk: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container writable layer — Storage Driver & Disk: fails intermittently under load or restart”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container writable layer — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container writable layer",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container writable layer\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container writable layer\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container writable layer",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container writable layer — Storage Driver & Disk: fails intermittently under load or restart”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container writable layer — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container writable layer configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container writable layer configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container writable layer configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The container writable layer configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container writable layer — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container writable layer",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container writable layer\nThe effective runtime value differs from expectation.",
        "root": "For “Container writable layer — Storage Driver & Disk: fails intermittently under load or restart”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container writable layer configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container writable layer configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container writable layer — Storage Driver & Disk: fails intermittently under load or restart”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Container writable layer — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nFor “Container writable layer — Storage Driver & Disk: fails intermittently under load or restart”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container writable layer — Storage Driver & Disk: fails intermittently under load or restart”, the effective container writable layer state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container writable layer — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container writable layer path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Container writable layer — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-202",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Image layer extraction — Storage Driver & Disk: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective image layer extraction state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "image layer extraction",
      "Storage Driver & Disk",
      "Image layer extraction — Storage Driver & Disk: fails completely",
      "image",
      "layer",
      "extraction",
      "storage",
      "driver",
      "disk",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image layer extraction — Storage Driver & Disk: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image layer extraction; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image layer extraction — Storage Driver & Disk: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image layer extraction — Storage Driver & Disk: fails completely”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image layer extraction",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image layer extraction configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image layer extraction — Storage Driver & Disk: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the image layer extraction inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image layer extraction — Storage Driver & Disk: fails completely”",
      "Effective image layer extraction configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image layer extraction — Storage Driver & Disk: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image layer extraction — Storage Driver & Disk: fails completely”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image layer extraction — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image layer extraction",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image layer extraction\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image layer extraction\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image layer extraction",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image layer extraction — Storage Driver & Disk: fails completely”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image layer extraction — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image layer extraction configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image layer extraction configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image layer extraction configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The image layer extraction configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image layer extraction — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image layer extraction",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image layer extraction\nThe effective runtime value differs from expectation.",
        "root": "For “Image layer extraction — Storage Driver & Disk: fails completely”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image layer extraction configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image layer extraction configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image layer extraction — Storage Driver & Disk: fails completely”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Image layer extraction — Storage Driver & Disk: fails completely: evidence found\nFor “Image layer extraction — Storage Driver & Disk: fails completely”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image layer extraction — Storage Driver & Disk: fails completely”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Image layer extraction — Storage Driver & Disk: fails completely: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Image layer extraction — Storage Driver & Disk: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-203",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Image layer extraction — Storage Driver & Disk: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective image layer extraction state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "image layer extraction",
      "Storage Driver & Disk",
      "Image layer extraction — Storage Driver & Disk: behaves differently than configured",
      "image",
      "layer",
      "extraction",
      "storage",
      "driver",
      "disk",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image layer extraction — Storage Driver & Disk: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image layer extraction; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image layer extraction — Storage Driver & Disk: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image layer extraction — Storage Driver & Disk: behaves differently than configured”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image layer extraction",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image layer extraction configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image layer extraction — Storage Driver & Disk: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the image layer extraction inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image layer extraction — Storage Driver & Disk: behaves differently than configured”",
      "Effective image layer extraction configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image layer extraction — Storage Driver & Disk: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image layer extraction — Storage Driver & Disk: behaves differently than configured”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image layer extraction — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image layer extraction",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image layer extraction\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image layer extraction\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image layer extraction",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image layer extraction — Storage Driver & Disk: behaves differently than configured”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image layer extraction — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image layer extraction configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image layer extraction configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image layer extraction configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The image layer extraction configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image layer extraction — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image layer extraction",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image layer extraction\nThe effective runtime value differs from expectation.",
        "root": "For “Image layer extraction — Storage Driver & Disk: behaves differently than configured”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image layer extraction configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image layer extraction configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image layer extraction — Storage Driver & Disk: behaves differently than configured”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Image layer extraction — Storage Driver & Disk: behaves differently than configured: evidence found\nFor “Image layer extraction — Storage Driver & Disk: behaves differently than configured”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image layer extraction — Storage Driver & Disk: behaves differently than configured”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Image layer extraction — Storage Driver & Disk: behaves differently than configured: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Image layer extraction — Storage Driver & Disk: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-204",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective image layer extraction state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "image layer extraction",
      "Storage Driver & Disk",
      "Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart",
      "image",
      "layer",
      "extraction",
      "storage",
      "driver",
      "disk",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image layer extraction; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image layer extraction",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image layer extraction configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the image layer extraction inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart”",
      "Effective image layer extraction configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image layer extraction",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image layer extraction\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image layer extraction\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image layer extraction",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image layer extraction configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image layer extraction configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image layer extraction configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The image layer extraction configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image layer extraction",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image layer extraction\nThe effective runtime value differs from expectation.",
        "root": "For “Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image layer extraction configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image layer extraction configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nFor “Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart”, the effective image layer extraction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image layer extraction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Image layer extraction — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-205",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Build cache disk usage — Storage Driver & Disk: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective build cache disk usage state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "build cache disk usage",
      "Storage Driver & Disk",
      "Build cache disk usage — Storage Driver & Disk: fails completely",
      "build",
      "cache",
      "disk",
      "usage",
      "storage",
      "driver",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Build cache disk usage — Storage Driver & Disk: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected build cache disk usage; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Build cache disk usage — Storage Driver & Disk: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Build cache disk usage — Storage Driver & Disk: fails completely”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for build cache disk usage",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The build cache disk usage configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Build cache disk usage — Storage Driver & Disk: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the build cache disk usage inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Build cache disk usage — Storage Driver & Disk: fails completely”",
      "Effective build cache disk usage configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Build cache disk usage — Storage Driver & Disk: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Build cache disk usage — Storage Driver & Disk: fails completely”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Build cache disk usage — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for build cache disk usage",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build cache disk usage\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build cache disk usage\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for build cache disk usage",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Build cache disk usage — Storage Driver & Disk: fails completely”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Build cache disk usage — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The build cache disk usage configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build cache disk usage configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build cache disk usage configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The build cache disk usage configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Build cache disk usage — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for build cache disk usage",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build cache disk usage\nThe effective runtime value differs from expectation.",
        "root": "For “Build cache disk usage — Storage Driver & Disk: fails completely”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The build cache disk usage configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build cache disk usage configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Build cache disk usage — Storage Driver & Disk: fails completely”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Build cache disk usage — Storage Driver & Disk: fails completely: evidence found\nFor “Build cache disk usage — Storage Driver & Disk: fails completely”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Build cache disk usage — Storage Driver & Disk: fails completely”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Build cache disk usage — Storage Driver & Disk: fails completely: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Build cache disk usage — Storage Driver & Disk: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-206",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Build cache disk usage — Storage Driver & Disk: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective build cache disk usage state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "build cache disk usage",
      "Storage Driver & Disk",
      "Build cache disk usage — Storage Driver & Disk: behaves differently than configured",
      "build",
      "cache",
      "disk",
      "usage",
      "storage",
      "driver",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Build cache disk usage — Storage Driver & Disk: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected build cache disk usage; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Build cache disk usage — Storage Driver & Disk: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Build cache disk usage — Storage Driver & Disk: behaves differently than configured”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for build cache disk usage",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The build cache disk usage configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Build cache disk usage — Storage Driver & Disk: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the build cache disk usage inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Build cache disk usage — Storage Driver & Disk: behaves differently than configured”",
      "Effective build cache disk usage configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Build cache disk usage — Storage Driver & Disk: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Build cache disk usage — Storage Driver & Disk: behaves differently than configured”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Build cache disk usage — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for build cache disk usage",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build cache disk usage\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build cache disk usage\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for build cache disk usage",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Build cache disk usage — Storage Driver & Disk: behaves differently than configured”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Build cache disk usage — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The build cache disk usage configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build cache disk usage configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build cache disk usage configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The build cache disk usage configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Build cache disk usage — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for build cache disk usage",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build cache disk usage\nThe effective runtime value differs from expectation.",
        "root": "For “Build cache disk usage — Storage Driver & Disk: behaves differently than configured”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The build cache disk usage configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build cache disk usage configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Build cache disk usage — Storage Driver & Disk: behaves differently than configured”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Build cache disk usage — Storage Driver & Disk: behaves differently than configured: evidence found\nFor “Build cache disk usage — Storage Driver & Disk: behaves differently than configured”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Build cache disk usage — Storage Driver & Disk: behaves differently than configured”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Build cache disk usage — Storage Driver & Disk: behaves differently than configured: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Build cache disk usage — Storage Driver & Disk: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-207",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective build cache disk usage state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "build cache disk usage",
      "Storage Driver & Disk",
      "Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart",
      "build",
      "cache",
      "disk",
      "usage",
      "storage",
      "driver",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected build cache disk usage; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for build cache disk usage",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The build cache disk usage configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the build cache disk usage inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart”",
      "Effective build cache disk usage configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for build cache disk usage",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build cache disk usage\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build cache disk usage\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for build cache disk usage",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The build cache disk usage configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build cache disk usage configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build cache disk usage configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The build cache disk usage configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for build cache disk usage",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build cache disk usage\nThe effective runtime value differs from expectation.",
        "root": "For “Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The build cache disk usage configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build cache disk usage configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nFor “Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective build cache disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build cache disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Build cache disk usage — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-208",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Container log disk usage — Storage Driver & Disk: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective container log disk usage state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "container log disk usage",
      "Storage Driver & Disk",
      "Container log disk usage — Storage Driver & Disk: fails completely",
      "container",
      "log",
      "disk",
      "usage",
      "storage",
      "driver",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container log disk usage — Storage Driver & Disk: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container log disk usage; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container log disk usage — Storage Driver & Disk: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container log disk usage — Storage Driver & Disk: fails completely”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container log disk usage",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container log disk usage configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container log disk usage — Storage Driver & Disk: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the container log disk usage inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container log disk usage — Storage Driver & Disk: fails completely”",
      "Effective container log disk usage configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container log disk usage — Storage Driver & Disk: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container log disk usage — Storage Driver & Disk: fails completely”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container log disk usage — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container log disk usage",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container log disk usage\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container log disk usage\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container log disk usage",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container log disk usage — Storage Driver & Disk: fails completely”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container log disk usage — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container log disk usage configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container log disk usage configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container log disk usage configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The container log disk usage configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container log disk usage — Storage Driver & Disk: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container log disk usage",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container log disk usage\nThe effective runtime value differs from expectation.",
        "root": "For “Container log disk usage — Storage Driver & Disk: fails completely”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container log disk usage configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container log disk usage configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container log disk usage — Storage Driver & Disk: fails completely”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Container log disk usage — Storage Driver & Disk: fails completely: evidence found\nFor “Container log disk usage — Storage Driver & Disk: fails completely”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container log disk usage — Storage Driver & Disk: fails completely”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container log disk usage — Storage Driver & Disk: fails completely: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Container log disk usage — Storage Driver & Disk: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-209",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Container log disk usage — Storage Driver & Disk: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective container log disk usage state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "container log disk usage",
      "Storage Driver & Disk",
      "Container log disk usage — Storage Driver & Disk: behaves differently than configured",
      "container",
      "log",
      "disk",
      "usage",
      "storage",
      "driver",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container log disk usage — Storage Driver & Disk: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container log disk usage; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container log disk usage — Storage Driver & Disk: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container log disk usage — Storage Driver & Disk: behaves differently than configured”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container log disk usage",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container log disk usage configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container log disk usage — Storage Driver & Disk: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the container log disk usage inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container log disk usage — Storage Driver & Disk: behaves differently than configured”",
      "Effective container log disk usage configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container log disk usage — Storage Driver & Disk: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container log disk usage — Storage Driver & Disk: behaves differently than configured”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container log disk usage — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container log disk usage",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container log disk usage\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container log disk usage\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container log disk usage",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container log disk usage — Storage Driver & Disk: behaves differently than configured”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container log disk usage — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container log disk usage configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container log disk usage configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container log disk usage configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The container log disk usage configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container log disk usage — Storage Driver & Disk: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container log disk usage",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container log disk usage\nThe effective runtime value differs from expectation.",
        "root": "For “Container log disk usage — Storage Driver & Disk: behaves differently than configured”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container log disk usage configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container log disk usage configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container log disk usage — Storage Driver & Disk: behaves differently than configured”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Container log disk usage — Storage Driver & Disk: behaves differently than configured: evidence found\nFor “Container log disk usage — Storage Driver & Disk: behaves differently than configured”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container log disk usage — Storage Driver & Disk: behaves differently than configured”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container log disk usage — Storage Driver & Disk: behaves differently than configured: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Container log disk usage — Storage Driver & Disk: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-210",
    "technology": "Docker",
    "category": "Storage Driver & Disk",
    "title": "Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective container log disk usage state first, then narrow the failure through the storage driver & disk dependency chain.",
    "aliases": [
      "container log disk usage",
      "Storage Driver & Disk",
      "Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart",
      "container",
      "log",
      "disk",
      "usage",
      "storage",
      "driver",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect storage driver/root",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container log disk usage; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect container writable sizes",
        "command": "docker ps -a --size",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder cache",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
      "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container log disk usage",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container log disk usage configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the container log disk usage inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart”",
      "Effective container log disk usage configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Storage Driver & Disk Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker system df -v",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker ps -a --size",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Storage Driver & Disk path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container log disk usage",
        "command": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container log disk usage\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container log disk usage\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container log disk usage",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container log disk usage configuration is correct but the failure remains",
        "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container log disk usage configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container log disk usage configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker ps -a --size",
            "output": "Evidence matches this path: The container log disk usage configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker ps -a --size",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx du",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container log disk usage",
        "check": "docker system df -v",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container log disk usage\nThe effective runtime value differs from expectation.",
        "root": "For “Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container log disk usage configuration is correct but the failure remains",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container log disk usage configuration is correct but the failure remains",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker ps -a --size",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker system df -v",
        "output": "Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nFor “Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart”, the effective container log disk usage state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "check": "docker info --format 'Driver={{.Driver}} DockerRootDir={{.DockerRootDir}}'",
        "output": "Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nA storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "root": "A storage driver & disk dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container log disk usage path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker ps -a --size",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker ps -a --size",
        "output": "Container log disk usage — Storage Driver & Disk: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-211",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image tag — Images & Local Image Store: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective image tag state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image tag",
      "Images & Local Image Store",
      "Image tag — Images & Local Image Store: fails completely",
      "image",
      "tag",
      "images",
      "local",
      "store",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image tag — Images & Local Image Store: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image tag; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image tag — Images & Local Image Store: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image tag — Images & Local Image Store: fails completely”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image tag",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image tag configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image tag — Images & Local Image Store: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the image tag inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image tag — Images & Local Image Store: fails completely”",
      "Effective image tag configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image tag — Images & Local Image Store: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image tag — Images & Local Image Store: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image tag — Images & Local Image Store: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image tag — Images & Local Image Store: fails completely”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image tag — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image tag",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image tag\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image tag\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image tag",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image tag — Images & Local Image Store: fails completely”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image tag — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image tag configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image tag configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image tag configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image tag configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image tag — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image tag",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image tag\nThe effective runtime value differs from expectation.",
        "root": "For “Image tag — Images & Local Image Store: fails completely”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image tag configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image tag configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image tag — Images & Local Image Store: fails completely”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image tag — Images & Local Image Store: fails completely: evidence found\nFor “Image tag — Images & Local Image Store: fails completely”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image tag — Images & Local Image Store: fails completely”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image tag — Images & Local Image Store: fails completely: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image tag — Images & Local Image Store: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-212",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image tag — Images & Local Image Store: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective image tag state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image tag",
      "Images & Local Image Store",
      "Image tag — Images & Local Image Store: behaves differently than configured",
      "image",
      "tag",
      "images",
      "local",
      "store",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image tag — Images & Local Image Store: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image tag; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image tag — Images & Local Image Store: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image tag — Images & Local Image Store: behaves differently than configured”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image tag",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image tag configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image tag — Images & Local Image Store: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the image tag inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image tag — Images & Local Image Store: behaves differently than configured”",
      "Effective image tag configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image tag — Images & Local Image Store: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image tag — Images & Local Image Store: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image tag — Images & Local Image Store: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image tag — Images & Local Image Store: behaves differently than configured”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image tag — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image tag",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image tag\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image tag\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image tag",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image tag — Images & Local Image Store: behaves differently than configured”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image tag — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image tag configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image tag configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image tag configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image tag configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image tag — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image tag",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image tag\nThe effective runtime value differs from expectation.",
        "root": "For “Image tag — Images & Local Image Store: behaves differently than configured”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image tag configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image tag configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image tag — Images & Local Image Store: behaves differently than configured”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image tag — Images & Local Image Store: behaves differently than configured: evidence found\nFor “Image tag — Images & Local Image Store: behaves differently than configured”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image tag — Images & Local Image Store: behaves differently than configured”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image tag — Images & Local Image Store: behaves differently than configured: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image tag — Images & Local Image Store: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-213",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image tag — Images & Local Image Store: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective image tag state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image tag",
      "Images & Local Image Store",
      "Image tag — Images & Local Image Store: fails intermittently under load or restart",
      "image",
      "tag",
      "images",
      "local",
      "store",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image tag — Images & Local Image Store: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image tag; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image tag — Images & Local Image Store: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image tag — Images & Local Image Store: fails intermittently under load or restart”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image tag",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image tag configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image tag — Images & Local Image Store: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the image tag inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image tag — Images & Local Image Store: fails intermittently under load or restart”",
      "Effective image tag configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image tag — Images & Local Image Store: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image tag — Images & Local Image Store: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image tag — Images & Local Image Store: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image tag — Images & Local Image Store: fails intermittently under load or restart”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image tag — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image tag",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image tag\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image tag\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image tag",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image tag — Images & Local Image Store: fails intermittently under load or restart”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image tag — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image tag configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image tag configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image tag configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image tag configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image tag — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image tag",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image tag\nThe effective runtime value differs from expectation.",
        "root": "For “Image tag — Images & Local Image Store: fails intermittently under load or restart”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image tag configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image tag configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image tag — Images & Local Image Store: fails intermittently under load or restart”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image tag — Images & Local Image Store: fails intermittently under load or restart: evidence found\nFor “Image tag — Images & Local Image Store: fails intermittently under load or restart”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image tag — Images & Local Image Store: fails intermittently under load or restart”, the effective image tag state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image tag — Images & Local Image Store: fails intermittently under load or restart: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image tag path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image tag — Images & Local Image Store: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-214",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image digest — Images & Local Image Store: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective image digest state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image digest",
      "Images & Local Image Store",
      "Image digest — Images & Local Image Store: fails completely",
      "image",
      "digest",
      "images",
      "local",
      "store",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image digest — Images & Local Image Store: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image digest; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image digest — Images & Local Image Store: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image digest — Images & Local Image Store: fails completely”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image digest",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image digest configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image digest — Images & Local Image Store: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the image digest inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image digest — Images & Local Image Store: fails completely”",
      "Effective image digest configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image digest — Images & Local Image Store: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image digest — Images & Local Image Store: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image digest — Images & Local Image Store: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image digest — Images & Local Image Store: fails completely”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image digest — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image digest",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image digest\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image digest\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image digest",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image digest — Images & Local Image Store: fails completely”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image digest — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image digest configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image digest configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image digest configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image digest configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image digest — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image digest",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image digest\nThe effective runtime value differs from expectation.",
        "root": "For “Image digest — Images & Local Image Store: fails completely”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image digest configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image digest configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image digest — Images & Local Image Store: fails completely”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image digest — Images & Local Image Store: fails completely: evidence found\nFor “Image digest — Images & Local Image Store: fails completely”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image digest — Images & Local Image Store: fails completely”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image digest — Images & Local Image Store: fails completely: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image digest — Images & Local Image Store: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-215",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image digest — Images & Local Image Store: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective image digest state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image digest",
      "Images & Local Image Store",
      "Image digest — Images & Local Image Store: behaves differently than configured",
      "image",
      "digest",
      "images",
      "local",
      "store",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image digest — Images & Local Image Store: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image digest; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image digest — Images & Local Image Store: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image digest — Images & Local Image Store: behaves differently than configured”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image digest",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image digest configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image digest — Images & Local Image Store: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the image digest inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image digest — Images & Local Image Store: behaves differently than configured”",
      "Effective image digest configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image digest — Images & Local Image Store: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image digest — Images & Local Image Store: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image digest — Images & Local Image Store: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image digest — Images & Local Image Store: behaves differently than configured”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image digest — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image digest",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image digest\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image digest\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image digest",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image digest — Images & Local Image Store: behaves differently than configured”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image digest — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image digest configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image digest configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image digest configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image digest configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image digest — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image digest",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image digest\nThe effective runtime value differs from expectation.",
        "root": "For “Image digest — Images & Local Image Store: behaves differently than configured”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image digest configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image digest configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image digest — Images & Local Image Store: behaves differently than configured”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image digest — Images & Local Image Store: behaves differently than configured: evidence found\nFor “Image digest — Images & Local Image Store: behaves differently than configured”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image digest — Images & Local Image Store: behaves differently than configured”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image digest — Images & Local Image Store: behaves differently than configured: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image digest — Images & Local Image Store: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-216",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image digest — Images & Local Image Store: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective image digest state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image digest",
      "Images & Local Image Store",
      "Image digest — Images & Local Image Store: fails intermittently under load or restart",
      "image",
      "digest",
      "images",
      "local",
      "store",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image digest — Images & Local Image Store: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image digest; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image digest — Images & Local Image Store: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image digest — Images & Local Image Store: fails intermittently under load or restart”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image digest",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image digest configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image digest — Images & Local Image Store: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the image digest inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image digest — Images & Local Image Store: fails intermittently under load or restart”",
      "Effective image digest configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image digest — Images & Local Image Store: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image digest — Images & Local Image Store: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image digest — Images & Local Image Store: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image digest — Images & Local Image Store: fails intermittently under load or restart”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image digest — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image digest",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image digest\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image digest\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image digest",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image digest — Images & Local Image Store: fails intermittently under load or restart”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image digest — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image digest configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image digest configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image digest configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image digest configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image digest — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image digest",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image digest\nThe effective runtime value differs from expectation.",
        "root": "For “Image digest — Images & Local Image Store: fails intermittently under load or restart”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image digest configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image digest configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image digest — Images & Local Image Store: fails intermittently under load or restart”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image digest — Images & Local Image Store: fails intermittently under load or restart: evidence found\nFor “Image digest — Images & Local Image Store: fails intermittently under load or restart”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image digest — Images & Local Image Store: fails intermittently under load or restart”, the effective image digest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image digest — Images & Local Image Store: fails intermittently under load or restart: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image digest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image digest — Images & Local Image Store: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-217",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image architecture — Images & Local Image Store: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective image architecture state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image architecture",
      "Images & Local Image Store",
      "Image architecture — Images & Local Image Store: fails completely",
      "image",
      "architecture",
      "images",
      "local",
      "store",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image architecture — Images & Local Image Store: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image architecture; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image architecture — Images & Local Image Store: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image architecture — Images & Local Image Store: fails completely”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image architecture",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image architecture configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image architecture — Images & Local Image Store: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the image architecture inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image architecture — Images & Local Image Store: fails completely”",
      "Effective image architecture configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image architecture — Images & Local Image Store: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image architecture — Images & Local Image Store: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image architecture — Images & Local Image Store: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image architecture — Images & Local Image Store: fails completely”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image architecture — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image architecture",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image architecture\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image architecture\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image architecture",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image architecture — Images & Local Image Store: fails completely”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image architecture — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image architecture configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image architecture configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image architecture configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image architecture configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image architecture — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image architecture",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image architecture\nThe effective runtime value differs from expectation.",
        "root": "For “Image architecture — Images & Local Image Store: fails completely”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image architecture configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image architecture configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image architecture — Images & Local Image Store: fails completely”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image architecture — Images & Local Image Store: fails completely: evidence found\nFor “Image architecture — Images & Local Image Store: fails completely”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image architecture — Images & Local Image Store: fails completely”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image architecture — Images & Local Image Store: fails completely: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image architecture — Images & Local Image Store: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-218",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image architecture — Images & Local Image Store: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective image architecture state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image architecture",
      "Images & Local Image Store",
      "Image architecture — Images & Local Image Store: behaves differently than configured",
      "image",
      "architecture",
      "images",
      "local",
      "store",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image architecture — Images & Local Image Store: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image architecture; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image architecture — Images & Local Image Store: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image architecture — Images & Local Image Store: behaves differently than configured”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image architecture",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image architecture configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image architecture — Images & Local Image Store: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the image architecture inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image architecture — Images & Local Image Store: behaves differently than configured”",
      "Effective image architecture configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image architecture — Images & Local Image Store: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image architecture — Images & Local Image Store: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image architecture — Images & Local Image Store: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image architecture — Images & Local Image Store: behaves differently than configured”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image architecture — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image architecture",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image architecture\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image architecture\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image architecture",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image architecture — Images & Local Image Store: behaves differently than configured”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image architecture — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image architecture configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image architecture configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image architecture configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image architecture configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image architecture — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image architecture",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image architecture\nThe effective runtime value differs from expectation.",
        "root": "For “Image architecture — Images & Local Image Store: behaves differently than configured”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image architecture configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image architecture configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image architecture — Images & Local Image Store: behaves differently than configured”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image architecture — Images & Local Image Store: behaves differently than configured: evidence found\nFor “Image architecture — Images & Local Image Store: behaves differently than configured”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image architecture — Images & Local Image Store: behaves differently than configured”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image architecture — Images & Local Image Store: behaves differently than configured: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image architecture — Images & Local Image Store: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-219",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image architecture — Images & Local Image Store: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective image architecture state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image architecture",
      "Images & Local Image Store",
      "Image architecture — Images & Local Image Store: fails intermittently under load or restart",
      "image",
      "architecture",
      "images",
      "local",
      "store",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image architecture — Images & Local Image Store: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image architecture; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image architecture — Images & Local Image Store: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image architecture — Images & Local Image Store: fails intermittently under load or restart”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image architecture",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image architecture configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image architecture — Images & Local Image Store: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the image architecture inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image architecture — Images & Local Image Store: fails intermittently under load or restart”",
      "Effective image architecture configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image architecture — Images & Local Image Store: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image architecture — Images & Local Image Store: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image architecture — Images & Local Image Store: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image architecture — Images & Local Image Store: fails intermittently under load or restart”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image architecture — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image architecture",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image architecture\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image architecture\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image architecture",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image architecture — Images & Local Image Store: fails intermittently under load or restart”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image architecture — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image architecture configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image architecture configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image architecture configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image architecture configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image architecture — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image architecture",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image architecture\nThe effective runtime value differs from expectation.",
        "root": "For “Image architecture — Images & Local Image Store: fails intermittently under load or restart”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image architecture configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image architecture configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image architecture — Images & Local Image Store: fails intermittently under load or restart”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image architecture — Images & Local Image Store: fails intermittently under load or restart: evidence found\nFor “Image architecture — Images & Local Image Store: fails intermittently under load or restart”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image architecture — Images & Local Image Store: fails intermittently under load or restart”, the effective image architecture state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image architecture — Images & Local Image Store: fails intermittently under load or restart: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image architecture path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image architecture — Images & Local Image Store: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-220",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image ENTRYPOINT metadata — Images & Local Image Store: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective image ENTRYPOINT metadata state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image ENTRYPOINT metadata",
      "Images & Local Image Store",
      "Image ENTRYPOINT metadata — Images & Local Image Store: fails completely",
      "image",
      "entrypoint",
      "metadata",
      "images",
      "local",
      "store",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image ENTRYPOINT metadata — Images & Local Image Store: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image ENTRYPOINT metadata; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image ENTRYPOINT metadata — Images & Local Image Store: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image ENTRYPOINT metadata — Images & Local Image Store: fails completely”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image ENTRYPOINT metadata — Images & Local Image Store: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the image ENTRYPOINT metadata inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image ENTRYPOINT metadata — Images & Local Image Store: fails completely”",
      "Effective image ENTRYPOINT metadata configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image ENTRYPOINT metadata — Images & Local Image Store: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image ENTRYPOINT metadata — Images & Local Image Store: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image ENTRYPOINT metadata — Images & Local Image Store: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image ENTRYPOINT metadata — Images & Local Image Store: fails completely”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image ENTRYPOINT metadata — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image ENTRYPOINT metadata — Images & Local Image Store: fails completely”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image ENTRYPOINT metadata — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image ENTRYPOINT metadata configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image ENTRYPOINT metadata configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image ENTRYPOINT metadata — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata\nThe effective runtime value differs from expectation.",
        "root": "For “Image ENTRYPOINT metadata — Images & Local Image Store: fails completely”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image ENTRYPOINT metadata — Images & Local Image Store: fails completely”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image ENTRYPOINT metadata — Images & Local Image Store: fails completely: evidence found\nFor “Image ENTRYPOINT metadata — Images & Local Image Store: fails completely”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image ENTRYPOINT metadata — Images & Local Image Store: fails completely”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image ENTRYPOINT metadata — Images & Local Image Store: fails completely: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image ENTRYPOINT metadata — Images & Local Image Store: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-221",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective image ENTRYPOINT metadata state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image ENTRYPOINT metadata",
      "Images & Local Image Store",
      "Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured",
      "image",
      "entrypoint",
      "metadata",
      "images",
      "local",
      "store",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image ENTRYPOINT metadata; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the image ENTRYPOINT metadata inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured”",
      "Effective image ENTRYPOINT metadata configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image ENTRYPOINT metadata configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image ENTRYPOINT metadata configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata\nThe effective runtime value differs from expectation.",
        "root": "For “Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured: evidence found\nFor “Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image ENTRYPOINT metadata — Images & Local Image Store: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-222",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective image ENTRYPOINT metadata state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image ENTRYPOINT metadata",
      "Images & Local Image Store",
      "Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart",
      "image",
      "entrypoint",
      "metadata",
      "images",
      "local",
      "store",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image ENTRYPOINT metadata; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the image ENTRYPOINT metadata inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart”",
      "Effective image ENTRYPOINT metadata configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image ENTRYPOINT metadata configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image ENTRYPOINT metadata configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image ENTRYPOINT metadata\nThe effective runtime value differs from expectation.",
        "root": "For “Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image ENTRYPOINT metadata configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart: evidence found\nFor “Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart”, the effective image ENTRYPOINT metadata state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image ENTRYPOINT metadata path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image ENTRYPOINT metadata — Images & Local Image Store: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-223",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image runtime libraries — Images & Local Image Store: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective image runtime libraries state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image runtime libraries",
      "Images & Local Image Store",
      "Image runtime libraries — Images & Local Image Store: fails completely",
      "image",
      "runtime",
      "libraries",
      "images",
      "local",
      "store",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image runtime libraries — Images & Local Image Store: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image runtime libraries; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image runtime libraries — Images & Local Image Store: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image runtime libraries — Images & Local Image Store: fails completely”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image runtime libraries",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image runtime libraries configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image runtime libraries — Images & Local Image Store: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the image runtime libraries inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image runtime libraries — Images & Local Image Store: fails completely”",
      "Effective image runtime libraries configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image runtime libraries — Images & Local Image Store: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image runtime libraries — Images & Local Image Store: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image runtime libraries — Images & Local Image Store: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image runtime libraries — Images & Local Image Store: fails completely”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image runtime libraries — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image runtime libraries",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image runtime libraries\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image runtime libraries\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image runtime libraries",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image runtime libraries — Images & Local Image Store: fails completely”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image runtime libraries — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image runtime libraries configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image runtime libraries configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image runtime libraries configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image runtime libraries configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image runtime libraries — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image runtime libraries",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image runtime libraries\nThe effective runtime value differs from expectation.",
        "root": "For “Image runtime libraries — Images & Local Image Store: fails completely”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image runtime libraries configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image runtime libraries configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image runtime libraries — Images & Local Image Store: fails completely”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image runtime libraries — Images & Local Image Store: fails completely: evidence found\nFor “Image runtime libraries — Images & Local Image Store: fails completely”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image runtime libraries — Images & Local Image Store: fails completely”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image runtime libraries — Images & Local Image Store: fails completely: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image runtime libraries — Images & Local Image Store: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-224",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image runtime libraries — Images & Local Image Store: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective image runtime libraries state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image runtime libraries",
      "Images & Local Image Store",
      "Image runtime libraries — Images & Local Image Store: behaves differently than configured",
      "image",
      "runtime",
      "libraries",
      "images",
      "local",
      "store",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image runtime libraries — Images & Local Image Store: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image runtime libraries; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image runtime libraries — Images & Local Image Store: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image runtime libraries — Images & Local Image Store: behaves differently than configured”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image runtime libraries",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image runtime libraries configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image runtime libraries — Images & Local Image Store: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the image runtime libraries inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image runtime libraries — Images & Local Image Store: behaves differently than configured”",
      "Effective image runtime libraries configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image runtime libraries — Images & Local Image Store: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image runtime libraries — Images & Local Image Store: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image runtime libraries — Images & Local Image Store: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image runtime libraries — Images & Local Image Store: behaves differently than configured”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image runtime libraries — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image runtime libraries",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image runtime libraries\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image runtime libraries\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image runtime libraries",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image runtime libraries — Images & Local Image Store: behaves differently than configured”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image runtime libraries — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image runtime libraries configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image runtime libraries configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image runtime libraries configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image runtime libraries configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image runtime libraries — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image runtime libraries",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image runtime libraries\nThe effective runtime value differs from expectation.",
        "root": "For “Image runtime libraries — Images & Local Image Store: behaves differently than configured”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image runtime libraries configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image runtime libraries configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image runtime libraries — Images & Local Image Store: behaves differently than configured”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image runtime libraries — Images & Local Image Store: behaves differently than configured: evidence found\nFor “Image runtime libraries — Images & Local Image Store: behaves differently than configured”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image runtime libraries — Images & Local Image Store: behaves differently than configured”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image runtime libraries — Images & Local Image Store: behaves differently than configured: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image runtime libraries — Images & Local Image Store: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-225",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective image runtime libraries state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image runtime libraries",
      "Images & Local Image Store",
      "Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart",
      "image",
      "runtime",
      "libraries",
      "images",
      "local",
      "store",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image runtime libraries; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image runtime libraries",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image runtime libraries configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the image runtime libraries inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart”",
      "Effective image runtime libraries configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image runtime libraries",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image runtime libraries\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image runtime libraries\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image runtime libraries",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image runtime libraries configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image runtime libraries configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image runtime libraries configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image runtime libraries configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image runtime libraries",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image runtime libraries\nThe effective runtime value differs from expectation.",
        "root": "For “Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image runtime libraries configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image runtime libraries configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart: evidence found\nFor “Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart”, the effective image runtime libraries state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image runtime libraries path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image runtime libraries — Images & Local Image Store: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-226",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image size/layers — Images & Local Image Store: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective image size/layers state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image size/layers",
      "Images & Local Image Store",
      "Image size/layers — Images & Local Image Store: fails completely",
      "image",
      "size",
      "layers",
      "images",
      "local",
      "store",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image size/layers — Images & Local Image Store: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image size/layers; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image size/layers — Images & Local Image Store: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image size/layers — Images & Local Image Store: fails completely”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image size/layers",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image size/layers configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image size/layers — Images & Local Image Store: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the image size/layers inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image size/layers — Images & Local Image Store: fails completely”",
      "Effective image size/layers configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image size/layers — Images & Local Image Store: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image size/layers — Images & Local Image Store: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image size/layers — Images & Local Image Store: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image size/layers — Images & Local Image Store: fails completely”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image size/layers — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image size/layers",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image size/layers\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image size/layers\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image size/layers",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image size/layers — Images & Local Image Store: fails completely”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image size/layers — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image size/layers configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image size/layers configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image size/layers configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image size/layers configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image size/layers — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image size/layers",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image size/layers\nThe effective runtime value differs from expectation.",
        "root": "For “Image size/layers — Images & Local Image Store: fails completely”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image size/layers configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image size/layers configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image size/layers — Images & Local Image Store: fails completely”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image size/layers — Images & Local Image Store: fails completely: evidence found\nFor “Image size/layers — Images & Local Image Store: fails completely”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image size/layers — Images & Local Image Store: fails completely”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image size/layers — Images & Local Image Store: fails completely: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image size/layers — Images & Local Image Store: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-227",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image size/layers — Images & Local Image Store: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective image size/layers state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image size/layers",
      "Images & Local Image Store",
      "Image size/layers — Images & Local Image Store: behaves differently than configured",
      "image",
      "size",
      "layers",
      "images",
      "local",
      "store",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image size/layers — Images & Local Image Store: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image size/layers; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image size/layers — Images & Local Image Store: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image size/layers — Images & Local Image Store: behaves differently than configured”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image size/layers",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image size/layers configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image size/layers — Images & Local Image Store: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the image size/layers inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image size/layers — Images & Local Image Store: behaves differently than configured”",
      "Effective image size/layers configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image size/layers — Images & Local Image Store: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image size/layers — Images & Local Image Store: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image size/layers — Images & Local Image Store: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image size/layers — Images & Local Image Store: behaves differently than configured”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image size/layers — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image size/layers",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image size/layers\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image size/layers\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image size/layers",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image size/layers — Images & Local Image Store: behaves differently than configured”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image size/layers — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image size/layers configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image size/layers configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image size/layers configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image size/layers configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image size/layers — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image size/layers",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image size/layers\nThe effective runtime value differs from expectation.",
        "root": "For “Image size/layers — Images & Local Image Store: behaves differently than configured”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image size/layers configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image size/layers configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image size/layers — Images & Local Image Store: behaves differently than configured”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image size/layers — Images & Local Image Store: behaves differently than configured: evidence found\nFor “Image size/layers — Images & Local Image Store: behaves differently than configured”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image size/layers — Images & Local Image Store: behaves differently than configured”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image size/layers — Images & Local Image Store: behaves differently than configured: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image size/layers — Images & Local Image Store: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-228",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image size/layers — Images & Local Image Store: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective image size/layers state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image size/layers",
      "Images & Local Image Store",
      "Image size/layers — Images & Local Image Store: fails intermittently under load or restart",
      "image",
      "size",
      "layers",
      "images",
      "local",
      "store",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image size/layers — Images & Local Image Store: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image size/layers; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image size/layers — Images & Local Image Store: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image size/layers — Images & Local Image Store: fails intermittently under load or restart”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image size/layers",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image size/layers configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image size/layers — Images & Local Image Store: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the image size/layers inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image size/layers — Images & Local Image Store: fails intermittently under load or restart”",
      "Effective image size/layers configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image size/layers — Images & Local Image Store: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image size/layers — Images & Local Image Store: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image size/layers — Images & Local Image Store: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image size/layers — Images & Local Image Store: fails intermittently under load or restart”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image size/layers — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image size/layers",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image size/layers\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image size/layers\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image size/layers",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image size/layers — Images & Local Image Store: fails intermittently under load or restart”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image size/layers — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image size/layers configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image size/layers configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image size/layers configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image size/layers configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image size/layers — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image size/layers",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image size/layers\nThe effective runtime value differs from expectation.",
        "root": "For “Image size/layers — Images & Local Image Store: fails intermittently under load or restart”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image size/layers configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image size/layers configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image size/layers — Images & Local Image Store: fails intermittently under load or restart”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image size/layers — Images & Local Image Store: fails intermittently under load or restart: evidence found\nFor “Image size/layers — Images & Local Image Store: fails intermittently under load or restart”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image size/layers — Images & Local Image Store: fails intermittently under load or restart”, the effective image size/layers state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image size/layers — Images & Local Image Store: fails intermittently under load or restart: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image size/layers path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image size/layers — Images & Local Image Store: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-229",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image save/load — Images & Local Image Store: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective image save/load state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image save/load",
      "Images & Local Image Store",
      "Image save/load — Images & Local Image Store: fails completely",
      "image",
      "save",
      "load",
      "images",
      "local",
      "store",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image save/load — Images & Local Image Store: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image save/load; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image save/load — Images & Local Image Store: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image save/load — Images & Local Image Store: fails completely”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image save/load",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image save/load configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image save/load — Images & Local Image Store: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the image save/load inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image save/load — Images & Local Image Store: fails completely”",
      "Effective image save/load configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image save/load — Images & Local Image Store: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image save/load — Images & Local Image Store: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image save/load — Images & Local Image Store: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image save/load — Images & Local Image Store: fails completely”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image save/load — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image save/load",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image save/load\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image save/load\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image save/load",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image save/load — Images & Local Image Store: fails completely”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image save/load — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image save/load configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image save/load configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image save/load configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image save/load configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image save/load — Images & Local Image Store: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image save/load",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image save/load\nThe effective runtime value differs from expectation.",
        "root": "For “Image save/load — Images & Local Image Store: fails completely”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image save/load configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image save/load configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image save/load — Images & Local Image Store: fails completely”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image save/load — Images & Local Image Store: fails completely: evidence found\nFor “Image save/load — Images & Local Image Store: fails completely”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image save/load — Images & Local Image Store: fails completely”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image save/load — Images & Local Image Store: fails completely: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image save/load — Images & Local Image Store: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-230",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image save/load — Images & Local Image Store: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective image save/load state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image save/load",
      "Images & Local Image Store",
      "Image save/load — Images & Local Image Store: behaves differently than configured",
      "image",
      "save",
      "load",
      "images",
      "local",
      "store",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image save/load — Images & Local Image Store: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image save/load; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image save/load — Images & Local Image Store: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image save/load — Images & Local Image Store: behaves differently than configured”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image save/load",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image save/load configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image save/load — Images & Local Image Store: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the image save/load inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image save/load — Images & Local Image Store: behaves differently than configured”",
      "Effective image save/load configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image save/load — Images & Local Image Store: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image save/load — Images & Local Image Store: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image save/load — Images & Local Image Store: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image save/load — Images & Local Image Store: behaves differently than configured”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image save/load — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image save/load",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image save/load\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image save/load\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image save/load",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image save/load — Images & Local Image Store: behaves differently than configured”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image save/load — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image save/load configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image save/load configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image save/load configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image save/load configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image save/load — Images & Local Image Store: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image save/load",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image save/load\nThe effective runtime value differs from expectation.",
        "root": "For “Image save/load — Images & Local Image Store: behaves differently than configured”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image save/load configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image save/load configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image save/load — Images & Local Image Store: behaves differently than configured”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image save/load — Images & Local Image Store: behaves differently than configured: evidence found\nFor “Image save/load — Images & Local Image Store: behaves differently than configured”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image save/load — Images & Local Image Store: behaves differently than configured”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image save/load — Images & Local Image Store: behaves differently than configured: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image save/load — Images & Local Image Store: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-231",
    "technology": "Docker",
    "category": "Images & Local Image Store",
    "title": "Image save/load — Images & Local Image Store: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective image save/load state first, then narrow the failure through the images & local image store dependency chain.",
    "aliases": [
      "image save/load",
      "Images & Local Image Store",
      "Image save/load — Images & Local Image Store: fails intermittently under load or restart",
      "image",
      "save",
      "load",
      "images",
      "local",
      "store",
      "fails",
      "intermittently",
      "under",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "container"
    ],
    "steps": [
      {
        "title": "Inspect image metadata",
        "command": "docker image inspect <image>",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Image save/load — Images & Local Image Store: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect image history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected image save/load; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List tags/digests",
        "command": "docker image ls --digests",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Image save/load — Images & Local Image Store: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect running image ID",
        "command": "docker inspect <container> --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Image save/load — Images & Local Image Store: fails intermittently under load or restart”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
      "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for image save/load",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The image save/load configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Image save/load — Images & Local Image Store: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the image save/load inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Image save/load — Images & Local Image Store: fails intermittently under load or restart”",
      "Effective image save/load configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Images & Local Image Store Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Image save/load — Images & Local Image Store: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
          "output": "Observed Docker state for Image save/load — Images & Local Image Store: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker image ls --digests",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Image save/load — Images & Local Image Store: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Image save/load — Images & Local Image Store: fails intermittently under load or restart”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Images & Local Image Store path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Image save/load — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for image save/load",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image save/load\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image save/load\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for image save/load",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Image save/load — Images & Local Image Store: fails intermittently under load or restart”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Image save/load — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The image save/load configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image save/load configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image save/load configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker image ls --digests",
            "output": "Evidence matches this path: The image save/load configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Image save/load — Images & Local Image Store: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image ls --digests",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for image save/load",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for image save/load\nThe effective runtime value differs from expectation.",
        "root": "For “Image save/load — Images & Local Image Store: fails intermittently under load or restart”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The image save/load configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The image save/load configuration is correct but the failure remains",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker image ls --digests",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Image save/load — Images & Local Image Store: fails intermittently under load or restart”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12",
        "output": "Image save/load — Images & Local Image Store: fails intermittently under load or restart: evidence found\nFor “Image save/load — Images & Local Image Store: fails intermittently under load or restart”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Image save/load — Images & Local Image Store: fails intermittently under load or restart”, the effective image save/load state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Image save/load — Images & Local Image Store: fails intermittently under load or restart: evidence found\nA images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "root": "A images & local image store dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the image save/load path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker image ls --digests",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker image ls --digests",
        "output": "Image save/load — Images & Local Image Store: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker inspect checkout-api --format 'ImageRef={{.Config.Image}} ImageID={{.Image}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-232",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry login — Registry, Auth & TLS: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective registry login state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry login",
      "Registry, Auth & TLS",
      "Registry login — Registry, Auth & TLS: fails completely",
      "registry",
      "login",
      "auth",
      "tls",
      "fails",
      "completely"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry login — Registry, Auth & TLS: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry login; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry login — Registry, Auth & TLS: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry login — Registry, Auth & TLS: fails completely”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry login",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry login configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry login — Registry, Auth & TLS: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the registry login inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry login — Registry, Auth & TLS: fails completely”",
      "Effective registry login configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry login — Registry, Auth & TLS: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry login — Registry, Auth & TLS: fails completely”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry login — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry login",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry login\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry login\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry login",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry login — Registry, Auth & TLS: fails completely”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry login — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry login configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry login configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry login configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry login configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry login — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry login",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry login\nThe effective runtime value differs from expectation.",
        "root": "For “Registry login — Registry, Auth & TLS: fails completely”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry login configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry login configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry login — Registry, Auth & TLS: fails completely”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry login — Registry, Auth & TLS: fails completely: evidence found\nFor “Registry login — Registry, Auth & TLS: fails completely”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry login — Registry, Auth & TLS: fails completely”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry login — Registry, Auth & TLS: fails completely: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry login — Registry, Auth & TLS: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-233",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry login — Registry, Auth & TLS: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective registry login state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry login",
      "Registry, Auth & TLS",
      "Registry login — Registry, Auth & TLS: behaves differently than configured",
      "registry",
      "login",
      "auth",
      "tls",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry login — Registry, Auth & TLS: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry login; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry login — Registry, Auth & TLS: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry login — Registry, Auth & TLS: behaves differently than configured”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry login",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry login configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry login — Registry, Auth & TLS: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the registry login inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry login — Registry, Auth & TLS: behaves differently than configured”",
      "Effective registry login configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry login — Registry, Auth & TLS: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry login — Registry, Auth & TLS: behaves differently than configured”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry login — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry login",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry login\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry login\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry login",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry login — Registry, Auth & TLS: behaves differently than configured”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry login — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry login configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry login configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry login configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry login configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry login — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry login",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry login\nThe effective runtime value differs from expectation.",
        "root": "For “Registry login — Registry, Auth & TLS: behaves differently than configured”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry login configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry login configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry login — Registry, Auth & TLS: behaves differently than configured”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry login — Registry, Auth & TLS: behaves differently than configured: evidence found\nFor “Registry login — Registry, Auth & TLS: behaves differently than configured”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry login — Registry, Auth & TLS: behaves differently than configured”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry login — Registry, Auth & TLS: behaves differently than configured: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry login — Registry, Auth & TLS: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-234",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry login — Registry, Auth & TLS: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective registry login state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry login",
      "Registry, Auth & TLS",
      "Registry login — Registry, Auth & TLS: fails intermittently under load or restart",
      "registry",
      "login",
      "auth",
      "tls",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry login — Registry, Auth & TLS: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry login; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry login — Registry, Auth & TLS: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry login — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry login",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry login configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry login — Registry, Auth & TLS: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the registry login inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry login — Registry, Auth & TLS: fails intermittently under load or restart”",
      "Effective registry login configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry login — Registry, Auth & TLS: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry login — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry login — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry login",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry login\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry login\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry login",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry login — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry login — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry login configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry login configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry login configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry login configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry login — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry login",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry login\nThe effective runtime value differs from expectation.",
        "root": "For “Registry login — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry login configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry login configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry login — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry login — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nFor “Registry login — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry login — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry login state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry login — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry login path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry login — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-235",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry pull authorization — Registry, Auth & TLS: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective registry pull authorization state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry pull authorization",
      "Registry, Auth & TLS",
      "Registry pull authorization — Registry, Auth & TLS: fails completely",
      "registry",
      "pull",
      "authorization",
      "auth",
      "tls",
      "fails",
      "completely"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry pull authorization — Registry, Auth & TLS: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry pull authorization; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry pull authorization — Registry, Auth & TLS: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry pull authorization — Registry, Auth & TLS: fails completely”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry pull authorization",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry pull authorization configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry pull authorization — Registry, Auth & TLS: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the registry pull authorization inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry pull authorization — Registry, Auth & TLS: fails completely”",
      "Effective registry pull authorization configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry pull authorization — Registry, Auth & TLS: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry pull authorization — Registry, Auth & TLS: fails completely”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry pull authorization — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry pull authorization",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry pull authorization\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry pull authorization\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry pull authorization",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry pull authorization — Registry, Auth & TLS: fails completely”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry pull authorization — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry pull authorization configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry pull authorization configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry pull authorization configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry pull authorization configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry pull authorization — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry pull authorization",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry pull authorization\nThe effective runtime value differs from expectation.",
        "root": "For “Registry pull authorization — Registry, Auth & TLS: fails completely”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry pull authorization configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry pull authorization configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry pull authorization — Registry, Auth & TLS: fails completely”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry pull authorization — Registry, Auth & TLS: fails completely: evidence found\nFor “Registry pull authorization — Registry, Auth & TLS: fails completely”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry pull authorization — Registry, Auth & TLS: fails completely”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry pull authorization — Registry, Auth & TLS: fails completely: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry pull authorization — Registry, Auth & TLS: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-236",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry pull authorization — Registry, Auth & TLS: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective registry pull authorization state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry pull authorization",
      "Registry, Auth & TLS",
      "Registry pull authorization — Registry, Auth & TLS: behaves differently than configured",
      "registry",
      "pull",
      "authorization",
      "auth",
      "tls",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry pull authorization — Registry, Auth & TLS: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry pull authorization; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry pull authorization — Registry, Auth & TLS: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry pull authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry pull authorization",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry pull authorization configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry pull authorization — Registry, Auth & TLS: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the registry pull authorization inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry pull authorization — Registry, Auth & TLS: behaves differently than configured”",
      "Effective registry pull authorization configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry pull authorization — Registry, Auth & TLS: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry pull authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry pull authorization — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry pull authorization",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry pull authorization\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry pull authorization\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry pull authorization",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry pull authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry pull authorization — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry pull authorization configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry pull authorization configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry pull authorization configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry pull authorization configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry pull authorization — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry pull authorization",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry pull authorization\nThe effective runtime value differs from expectation.",
        "root": "For “Registry pull authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry pull authorization configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry pull authorization configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry pull authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry pull authorization — Registry, Auth & TLS: behaves differently than configured: evidence found\nFor “Registry pull authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry pull authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry pull authorization — Registry, Auth & TLS: behaves differently than configured: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry pull authorization — Registry, Auth & TLS: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-237",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective registry pull authorization state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry pull authorization",
      "Registry, Auth & TLS",
      "Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart",
      "registry",
      "pull",
      "authorization",
      "auth",
      "tls",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry pull authorization; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry pull authorization",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry pull authorization configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the registry pull authorization inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart”",
      "Effective registry pull authorization configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry pull authorization",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry pull authorization\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry pull authorization\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry pull authorization",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry pull authorization configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry pull authorization configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry pull authorization configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry pull authorization configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry pull authorization",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry pull authorization\nThe effective runtime value differs from expectation.",
        "root": "For “Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry pull authorization configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry pull authorization configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nFor “Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry pull authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry pull authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry pull authorization — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-238",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry push authorization — Registry, Auth & TLS: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective registry push authorization state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry push authorization",
      "Registry, Auth & TLS",
      "Registry push authorization — Registry, Auth & TLS: fails completely",
      "registry",
      "push",
      "authorization",
      "auth",
      "tls",
      "fails",
      "completely"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry push authorization — Registry, Auth & TLS: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry push authorization; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry push authorization — Registry, Auth & TLS: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry push authorization — Registry, Auth & TLS: fails completely”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry push authorization",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry push authorization configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry push authorization — Registry, Auth & TLS: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the registry push authorization inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry push authorization — Registry, Auth & TLS: fails completely”",
      "Effective registry push authorization configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry push authorization — Registry, Auth & TLS: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry push authorization — Registry, Auth & TLS: fails completely”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry push authorization — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry push authorization",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry push authorization\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry push authorization\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry push authorization",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry push authorization — Registry, Auth & TLS: fails completely”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry push authorization — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry push authorization configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry push authorization configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry push authorization configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry push authorization configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry push authorization — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry push authorization",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry push authorization\nThe effective runtime value differs from expectation.",
        "root": "For “Registry push authorization — Registry, Auth & TLS: fails completely”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry push authorization configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry push authorization configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry push authorization — Registry, Auth & TLS: fails completely”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry push authorization — Registry, Auth & TLS: fails completely: evidence found\nFor “Registry push authorization — Registry, Auth & TLS: fails completely”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry push authorization — Registry, Auth & TLS: fails completely”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry push authorization — Registry, Auth & TLS: fails completely: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry push authorization — Registry, Auth & TLS: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-239",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry push authorization — Registry, Auth & TLS: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective registry push authorization state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry push authorization",
      "Registry, Auth & TLS",
      "Registry push authorization — Registry, Auth & TLS: behaves differently than configured",
      "registry",
      "push",
      "authorization",
      "auth",
      "tls",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry push authorization — Registry, Auth & TLS: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry push authorization; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry push authorization — Registry, Auth & TLS: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry push authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry push authorization",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry push authorization configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry push authorization — Registry, Auth & TLS: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the registry push authorization inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry push authorization — Registry, Auth & TLS: behaves differently than configured”",
      "Effective registry push authorization configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry push authorization — Registry, Auth & TLS: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry push authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry push authorization — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry push authorization",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry push authorization\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry push authorization\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry push authorization",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry push authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry push authorization — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry push authorization configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry push authorization configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry push authorization configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry push authorization configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry push authorization — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry push authorization",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry push authorization\nThe effective runtime value differs from expectation.",
        "root": "For “Registry push authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry push authorization configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry push authorization configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry push authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry push authorization — Registry, Auth & TLS: behaves differently than configured: evidence found\nFor “Registry push authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry push authorization — Registry, Auth & TLS: behaves differently than configured”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry push authorization — Registry, Auth & TLS: behaves differently than configured: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry push authorization — Registry, Auth & TLS: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-240",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective registry push authorization state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry push authorization",
      "Registry, Auth & TLS",
      "Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart",
      "registry",
      "push",
      "authorization",
      "auth",
      "tls",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry push authorization; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry push authorization",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry push authorization configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the registry push authorization inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart”",
      "Effective registry push authorization configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry push authorization",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry push authorization\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry push authorization\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry push authorization",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry push authorization configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry push authorization configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry push authorization configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry push authorization configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry push authorization",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry push authorization\nThe effective runtime value differs from expectation.",
        "root": "For “Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry push authorization configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry push authorization configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nFor “Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry push authorization state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry push authorization path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry push authorization — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-241",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry TLS trust — Registry, Auth & TLS: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective registry TLS trust state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry TLS trust",
      "Registry, Auth & TLS",
      "Registry TLS trust — Registry, Auth & TLS: fails completely",
      "registry",
      "tls",
      "trust",
      "auth",
      "fails",
      "completely"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry TLS trust — Registry, Auth & TLS: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry TLS trust; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry TLS trust — Registry, Auth & TLS: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry TLS trust — Registry, Auth & TLS: fails completely”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry TLS trust",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry TLS trust configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry TLS trust — Registry, Auth & TLS: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the registry TLS trust inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry TLS trust — Registry, Auth & TLS: fails completely”",
      "Effective registry TLS trust configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry TLS trust — Registry, Auth & TLS: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry TLS trust — Registry, Auth & TLS: fails completely”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry TLS trust — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry TLS trust",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry TLS trust\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry TLS trust\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry TLS trust",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry TLS trust — Registry, Auth & TLS: fails completely”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry TLS trust — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry TLS trust configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry TLS trust configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry TLS trust configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry TLS trust configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry TLS trust — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry TLS trust",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry TLS trust\nThe effective runtime value differs from expectation.",
        "root": "For “Registry TLS trust — Registry, Auth & TLS: fails completely”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry TLS trust configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry TLS trust configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry TLS trust — Registry, Auth & TLS: fails completely”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry TLS trust — Registry, Auth & TLS: fails completely: evidence found\nFor “Registry TLS trust — Registry, Auth & TLS: fails completely”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry TLS trust — Registry, Auth & TLS: fails completely”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry TLS trust — Registry, Auth & TLS: fails completely: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry TLS trust — Registry, Auth & TLS: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-242",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry TLS trust — Registry, Auth & TLS: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective registry TLS trust state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry TLS trust",
      "Registry, Auth & TLS",
      "Registry TLS trust — Registry, Auth & TLS: behaves differently than configured",
      "registry",
      "tls",
      "trust",
      "auth",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry TLS trust — Registry, Auth & TLS: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry TLS trust; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry TLS trust — Registry, Auth & TLS: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry TLS trust — Registry, Auth & TLS: behaves differently than configured”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry TLS trust",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry TLS trust configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry TLS trust — Registry, Auth & TLS: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the registry TLS trust inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry TLS trust — Registry, Auth & TLS: behaves differently than configured”",
      "Effective registry TLS trust configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry TLS trust — Registry, Auth & TLS: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry TLS trust — Registry, Auth & TLS: behaves differently than configured”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry TLS trust — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry TLS trust",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry TLS trust\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry TLS trust\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry TLS trust",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry TLS trust — Registry, Auth & TLS: behaves differently than configured”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry TLS trust — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry TLS trust configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry TLS trust configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry TLS trust configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry TLS trust configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry TLS trust — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry TLS trust",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry TLS trust\nThe effective runtime value differs from expectation.",
        "root": "For “Registry TLS trust — Registry, Auth & TLS: behaves differently than configured”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry TLS trust configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry TLS trust configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry TLS trust — Registry, Auth & TLS: behaves differently than configured”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry TLS trust — Registry, Auth & TLS: behaves differently than configured: evidence found\nFor “Registry TLS trust — Registry, Auth & TLS: behaves differently than configured”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry TLS trust — Registry, Auth & TLS: behaves differently than configured”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry TLS trust — Registry, Auth & TLS: behaves differently than configured: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry TLS trust — Registry, Auth & TLS: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-243",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective registry TLS trust state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry TLS trust",
      "Registry, Auth & TLS",
      "Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart",
      "registry",
      "tls",
      "trust",
      "auth",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry TLS trust; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry TLS trust",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry TLS trust configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the registry TLS trust inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart”",
      "Effective registry TLS trust configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry TLS trust",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry TLS trust\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry TLS trust\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry TLS trust",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry TLS trust configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry TLS trust configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry TLS trust configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry TLS trust configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry TLS trust",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry TLS trust\nThe effective runtime value differs from expectation.",
        "root": "For “Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry TLS trust configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry TLS trust configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nFor “Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry TLS trust state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry TLS trust path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry TLS trust — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-244",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry DNS/connectivity — Registry, Auth & TLS: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective registry DNS/connectivity state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry DNS/connectivity",
      "Registry, Auth & TLS",
      "Registry DNS/connectivity — Registry, Auth & TLS: fails completely",
      "registry",
      "dns",
      "connectivity",
      "auth",
      "tls",
      "fails",
      "completely"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry DNS/connectivity — Registry, Auth & TLS: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry DNS/connectivity; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry DNS/connectivity — Registry, Auth & TLS: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry DNS/connectivity — Registry, Auth & TLS: fails completely”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry DNS/connectivity",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry DNS/connectivity configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry DNS/connectivity — Registry, Auth & TLS: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the registry DNS/connectivity inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry DNS/connectivity — Registry, Auth & TLS: fails completely”",
      "Effective registry DNS/connectivity configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry DNS/connectivity — Registry, Auth & TLS: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry DNS/connectivity — Registry, Auth & TLS: fails completely”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry DNS/connectivity — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry DNS/connectivity — Registry, Auth & TLS: fails completely”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry DNS/connectivity — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry DNS/connectivity configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry DNS/connectivity configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry DNS/connectivity configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry DNS/connectivity configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry DNS/connectivity — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry DNS/connectivity",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity\nThe effective runtime value differs from expectation.",
        "root": "For “Registry DNS/connectivity — Registry, Auth & TLS: fails completely”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry DNS/connectivity configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry DNS/connectivity configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry DNS/connectivity — Registry, Auth & TLS: fails completely”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry DNS/connectivity — Registry, Auth & TLS: fails completely: evidence found\nFor “Registry DNS/connectivity — Registry, Auth & TLS: fails completely”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry DNS/connectivity — Registry, Auth & TLS: fails completely”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry DNS/connectivity — Registry, Auth & TLS: fails completely: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry DNS/connectivity — Registry, Auth & TLS: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-245",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective registry DNS/connectivity state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry DNS/connectivity",
      "Registry, Auth & TLS",
      "Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured",
      "registry",
      "dns",
      "connectivity",
      "auth",
      "tls",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry DNS/connectivity; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry DNS/connectivity",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry DNS/connectivity configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the registry DNS/connectivity inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured”",
      "Effective registry DNS/connectivity configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry DNS/connectivity configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry DNS/connectivity configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry DNS/connectivity configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry DNS/connectivity configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry DNS/connectivity",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity\nThe effective runtime value differs from expectation.",
        "root": "For “Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry DNS/connectivity configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry DNS/connectivity configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured: evidence found\nFor “Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry DNS/connectivity — Registry, Auth & TLS: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-246",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective registry DNS/connectivity state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "registry DNS/connectivity",
      "Registry, Auth & TLS",
      "Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart",
      "registry",
      "dns",
      "connectivity",
      "auth",
      "tls",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected registry DNS/connectivity; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for registry DNS/connectivity",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The registry DNS/connectivity configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the registry DNS/connectivity inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart”",
      "Effective registry DNS/connectivity configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The registry DNS/connectivity configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry DNS/connectivity configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry DNS/connectivity configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The registry DNS/connectivity configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for registry DNS/connectivity",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for registry DNS/connectivity\nThe effective runtime value differs from expectation.",
        "root": "For “Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The registry DNS/connectivity configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The registry DNS/connectivity configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nFor “Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart”, the effective registry DNS/connectivity state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the registry DNS/connectivity path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Registry DNS/connectivity — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-247",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Credential helper — Registry, Auth & TLS: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective credential helper state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "credential helper",
      "Registry, Auth & TLS",
      "Credential helper — Registry, Auth & TLS: fails completely",
      "credential",
      "helper",
      "registry",
      "auth",
      "tls",
      "fails",
      "completely"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Credential helper — Registry, Auth & TLS: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected credential helper; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Credential helper — Registry, Auth & TLS: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Credential helper — Registry, Auth & TLS: fails completely”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for credential helper",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The credential helper configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Credential helper — Registry, Auth & TLS: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the credential helper inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Credential helper — Registry, Auth & TLS: fails completely”",
      "Effective credential helper configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Credential helper — Registry, Auth & TLS: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Credential helper — Registry, Auth & TLS: fails completely”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Credential helper — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for credential helper",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for credential helper\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for credential helper\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for credential helper",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Credential helper — Registry, Auth & TLS: fails completely”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Credential helper — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The credential helper configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The credential helper configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The credential helper configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The credential helper configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Credential helper — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for credential helper",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for credential helper\nThe effective runtime value differs from expectation.",
        "root": "For “Credential helper — Registry, Auth & TLS: fails completely”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The credential helper configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The credential helper configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Credential helper — Registry, Auth & TLS: fails completely”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Credential helper — Registry, Auth & TLS: fails completely: evidence found\nFor “Credential helper — Registry, Auth & TLS: fails completely”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Credential helper — Registry, Auth & TLS: fails completely”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Credential helper — Registry, Auth & TLS: fails completely: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Credential helper — Registry, Auth & TLS: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-248",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Credential helper — Registry, Auth & TLS: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective credential helper state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "credential helper",
      "Registry, Auth & TLS",
      "Credential helper — Registry, Auth & TLS: behaves differently than configured",
      "credential",
      "helper",
      "registry",
      "auth",
      "tls",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Credential helper — Registry, Auth & TLS: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected credential helper; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Credential helper — Registry, Auth & TLS: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Credential helper — Registry, Auth & TLS: behaves differently than configured”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for credential helper",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The credential helper configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Credential helper — Registry, Auth & TLS: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the credential helper inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Credential helper — Registry, Auth & TLS: behaves differently than configured”",
      "Effective credential helper configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Credential helper — Registry, Auth & TLS: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Credential helper — Registry, Auth & TLS: behaves differently than configured”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Credential helper — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for credential helper",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for credential helper\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for credential helper\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for credential helper",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Credential helper — Registry, Auth & TLS: behaves differently than configured”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Credential helper — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The credential helper configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The credential helper configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The credential helper configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The credential helper configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Credential helper — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for credential helper",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for credential helper\nThe effective runtime value differs from expectation.",
        "root": "For “Credential helper — Registry, Auth & TLS: behaves differently than configured”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The credential helper configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The credential helper configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Credential helper — Registry, Auth & TLS: behaves differently than configured”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Credential helper — Registry, Auth & TLS: behaves differently than configured: evidence found\nFor “Credential helper — Registry, Auth & TLS: behaves differently than configured”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Credential helper — Registry, Auth & TLS: behaves differently than configured”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Credential helper — Registry, Auth & TLS: behaves differently than configured: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Credential helper — Registry, Auth & TLS: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-249",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Credential helper — Registry, Auth & TLS: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective credential helper state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "credential helper",
      "Registry, Auth & TLS",
      "Credential helper — Registry, Auth & TLS: fails intermittently under load or restart",
      "credential",
      "helper",
      "registry",
      "auth",
      "tls",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Credential helper — Registry, Auth & TLS: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected credential helper; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Credential helper — Registry, Auth & TLS: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Credential helper — Registry, Auth & TLS: fails intermittently under load or restart”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for credential helper",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The credential helper configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Credential helper — Registry, Auth & TLS: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the credential helper inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Credential helper — Registry, Auth & TLS: fails intermittently under load or restart”",
      "Effective credential helper configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Credential helper — Registry, Auth & TLS: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Credential helper — Registry, Auth & TLS: fails intermittently under load or restart”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Credential helper — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for credential helper",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for credential helper\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for credential helper\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for credential helper",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Credential helper — Registry, Auth & TLS: fails intermittently under load or restart”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Credential helper — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The credential helper configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The credential helper configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The credential helper configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The credential helper configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Credential helper — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for credential helper",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for credential helper\nThe effective runtime value differs from expectation.",
        "root": "For “Credential helper — Registry, Auth & TLS: fails intermittently under load or restart”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The credential helper configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The credential helper configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Credential helper — Registry, Auth & TLS: fails intermittently under load or restart”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Credential helper — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nFor “Credential helper — Registry, Auth & TLS: fails intermittently under load or restart”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Credential helper — Registry, Auth & TLS: fails intermittently under load or restart”, the effective credential helper state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Credential helper — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the credential helper path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Credential helper — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-250",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Multi-platform manifest — Registry, Auth & TLS: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective multi-platform manifest state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "multi-platform manifest",
      "Registry, Auth & TLS",
      "Multi-platform manifest — Registry, Auth & TLS: fails completely",
      "multi",
      "platform",
      "manifest",
      "registry",
      "auth",
      "tls",
      "fails",
      "completely"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Multi-platform manifest — Registry, Auth & TLS: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected multi-platform manifest; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Multi-platform manifest — Registry, Auth & TLS: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Multi-platform manifest — Registry, Auth & TLS: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The multi-platform manifest configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Multi-platform manifest — Registry, Auth & TLS: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the multi-platform manifest inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Multi-platform manifest — Registry, Auth & TLS: fails completely”",
      "Effective multi-platform manifest configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Multi-platform manifest — Registry, Auth & TLS: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Multi-platform manifest — Registry, Auth & TLS: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Multi-platform manifest — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for multi-platform manifest",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Multi-platform manifest — Registry, Auth & TLS: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Multi-platform manifest — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The multi-platform manifest configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The multi-platform manifest configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Multi-platform manifest — Registry, Auth & TLS: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
        "root": "For “Multi-platform manifest — Registry, Auth & TLS: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The multi-platform manifest configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Multi-platform manifest — Registry, Auth & TLS: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Multi-platform manifest — Registry, Auth & TLS: fails completely: evidence found\nFor “Multi-platform manifest — Registry, Auth & TLS: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Multi-platform manifest — Registry, Auth & TLS: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Multi-platform manifest — Registry, Auth & TLS: fails completely: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Multi-platform manifest — Registry, Auth & TLS: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-251",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective multi-platform manifest state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "multi-platform manifest",
      "Registry, Auth & TLS",
      "Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured",
      "multi",
      "platform",
      "manifest",
      "registry",
      "auth",
      "tls",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected multi-platform manifest; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The multi-platform manifest configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the multi-platform manifest inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured”",
      "Effective multi-platform manifest configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for multi-platform manifest",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The multi-platform manifest configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The multi-platform manifest configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
        "root": "For “Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The multi-platform manifest configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured: evidence found\nFor “Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Multi-platform manifest — Registry, Auth & TLS: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-252",
    "technology": "Docker",
    "category": "Registry, Auth & TLS",
    "title": "Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective multi-platform manifest state first, then narrow the failure through the registry, auth & tls dependency chain.",
    "aliases": [
      "multi-platform manifest",
      "Registry, Auth & TLS",
      "Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart",
      "multi",
      "platform",
      "manifest",
      "registry",
      "auth",
      "tls",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image"
    ],
    "steps": [
      {
        "title": "Reproduce registry pull",
        "command": "docker pull <image>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect local repo digests",
        "command": "docker image inspect <image> --format '{{json .RepoDigests}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected multi-platform manifest; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry/engine config",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Confirm Docker context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
      "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The multi-platform manifest configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the multi-platform manifest inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart”",
      "Effective multi-platform manifest configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Registry, Auth & TLS Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker pull registry.example.com/payments-api:2026.09.12",
          "output": "Error response from daemon: Head https://registry.example.com/v2/: x509: certificate signed by unknown authority",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
          "output": "[]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker info",
          "output": "Registry Mirrors: []\nInsecure Registries: 127.0.0.0/8",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Docker daemon does not trust the private registry's certificate chain.",
      "remediation": "Install the correct registry CA/trust configuration according to organizational policy and restart/reload the engine as required; do not disable TLS verification as a shortcut.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Registry TLS failures are daemon trust problems; repeated docker login attempts will not fix an untrusted certificate chain.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Registry, Auth & TLS path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "command": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker pull registry.example.com/payments-api:2026.09.12",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for multi-platform manifest",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The multi-platform manifest configuration is correct but the failure remains",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The multi-platform manifest configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker context show",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker context show",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
        "root": "For “Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The multi-platform manifest configuration is correct but the failure remains",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker info",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker pull registry.example.com/payments-api:2026.09.12",
        "output": "Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nFor “Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format '{{json .RepoDigests}}'",
        "output": "Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nA registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "root": "A registry, auth & tls dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker info",
        "output": "Multi-platform manifest — Registry, Auth & TLS: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker context show",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-253",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective CMD/ENTRYPOINT interaction state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "CMD/ENTRYPOINT interaction",
      "Dockerfile Semantics",
      "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely",
      "cmd",
      "entrypoint",
      "interaction",
      "dockerfile",
      "semantics",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected CMD/ENTRYPOINT interaction; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the CMD/ENTRYPOINT interaction inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely”",
      "Effective CMD/ENTRYPOINT interaction configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction\nThe effective runtime value differs from expectation.",
        "root": "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely: evidence found\nFor “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-254",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective CMD/ENTRYPOINT interaction state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "CMD/ENTRYPOINT interaction",
      "Dockerfile Semantics",
      "CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured",
      "cmd",
      "entrypoint",
      "interaction",
      "dockerfile",
      "semantics",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected CMD/ENTRYPOINT interaction; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the CMD/ENTRYPOINT interaction inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured”",
      "Effective CMD/ENTRYPOINT interaction configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction\nThe effective runtime value differs from expectation.",
        "root": "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured: evidence found\nFor “CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-255",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective CMD/ENTRYPOINT interaction state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "CMD/ENTRYPOINT interaction",
      "Dockerfile Semantics",
      "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart",
      "cmd",
      "entrypoint",
      "interaction",
      "dockerfile",
      "semantics",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected CMD/ENTRYPOINT interaction; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the CMD/ENTRYPOINT interaction inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart”",
      "Effective CMD/ENTRYPOINT interaction configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for CMD/ENTRYPOINT interaction\nThe effective runtime value differs from expectation.",
        "root": "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The CMD/ENTRYPOINT interaction configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nFor “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart”, the effective CMD/ENTRYPOINT interaction state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the CMD/ENTRYPOINT interaction path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "CMD/ENTRYPOINT interaction — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-256",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "WORKDIR — Dockerfile Semantics: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective WORKDIR state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "WORKDIR",
      "Dockerfile Semantics",
      "WORKDIR — Dockerfile Semantics: fails completely",
      "workdir",
      "dockerfile",
      "semantics",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “WORKDIR — Dockerfile Semantics: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected WORKDIR; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “WORKDIR — Dockerfile Semantics: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “WORKDIR — Dockerfile Semantics: fails completely”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for WORKDIR",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The WORKDIR configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “WORKDIR — Dockerfile Semantics: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the WORKDIR inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “WORKDIR — Dockerfile Semantics: fails completely”",
      "Effective WORKDIR configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: WORKDIR — Dockerfile Semantics: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “WORKDIR — Dockerfile Semantics: fails completely”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "WORKDIR — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for WORKDIR",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for WORKDIR\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for WORKDIR\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for WORKDIR",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “WORKDIR — Dockerfile Semantics: fails completely”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "WORKDIR — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The WORKDIR configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The WORKDIR configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The WORKDIR configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The WORKDIR configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "WORKDIR — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for WORKDIR",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for WORKDIR\nThe effective runtime value differs from expectation.",
        "root": "For “WORKDIR — Dockerfile Semantics: fails completely”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The WORKDIR configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The WORKDIR configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “WORKDIR — Dockerfile Semantics: fails completely”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "WORKDIR — Dockerfile Semantics: fails completely: evidence found\nFor “WORKDIR — Dockerfile Semantics: fails completely”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “WORKDIR — Dockerfile Semantics: fails completely”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "WORKDIR — Dockerfile Semantics: fails completely: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "WORKDIR — Dockerfile Semantics: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-257",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "WORKDIR — Dockerfile Semantics: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective WORKDIR state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "WORKDIR",
      "Dockerfile Semantics",
      "WORKDIR — Dockerfile Semantics: behaves differently than configured",
      "workdir",
      "dockerfile",
      "semantics",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “WORKDIR — Dockerfile Semantics: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected WORKDIR; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “WORKDIR — Dockerfile Semantics: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “WORKDIR — Dockerfile Semantics: behaves differently than configured”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for WORKDIR",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The WORKDIR configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “WORKDIR — Dockerfile Semantics: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the WORKDIR inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “WORKDIR — Dockerfile Semantics: behaves differently than configured”",
      "Effective WORKDIR configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: WORKDIR — Dockerfile Semantics: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “WORKDIR — Dockerfile Semantics: behaves differently than configured”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "WORKDIR — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for WORKDIR",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for WORKDIR\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for WORKDIR\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for WORKDIR",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “WORKDIR — Dockerfile Semantics: behaves differently than configured”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "WORKDIR — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The WORKDIR configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The WORKDIR configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The WORKDIR configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The WORKDIR configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "WORKDIR — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for WORKDIR",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for WORKDIR\nThe effective runtime value differs from expectation.",
        "root": "For “WORKDIR — Dockerfile Semantics: behaves differently than configured”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The WORKDIR configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The WORKDIR configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “WORKDIR — Dockerfile Semantics: behaves differently than configured”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "WORKDIR — Dockerfile Semantics: behaves differently than configured: evidence found\nFor “WORKDIR — Dockerfile Semantics: behaves differently than configured”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “WORKDIR — Dockerfile Semantics: behaves differently than configured”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "WORKDIR — Dockerfile Semantics: behaves differently than configured: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "WORKDIR — Dockerfile Semantics: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-258",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "WORKDIR — Dockerfile Semantics: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective WORKDIR state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "WORKDIR",
      "Dockerfile Semantics",
      "WORKDIR — Dockerfile Semantics: fails intermittently under load or restart",
      "workdir",
      "dockerfile",
      "semantics",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “WORKDIR — Dockerfile Semantics: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected WORKDIR; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “WORKDIR — Dockerfile Semantics: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “WORKDIR — Dockerfile Semantics: fails intermittently under load or restart”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for WORKDIR",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The WORKDIR configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “WORKDIR — Dockerfile Semantics: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the WORKDIR inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “WORKDIR — Dockerfile Semantics: fails intermittently under load or restart”",
      "Effective WORKDIR configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: WORKDIR — Dockerfile Semantics: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “WORKDIR — Dockerfile Semantics: fails intermittently under load or restart”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "WORKDIR — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for WORKDIR",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for WORKDIR\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for WORKDIR\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for WORKDIR",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “WORKDIR — Dockerfile Semantics: fails intermittently under load or restart”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "WORKDIR — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The WORKDIR configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The WORKDIR configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The WORKDIR configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The WORKDIR configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "WORKDIR — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for WORKDIR",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for WORKDIR\nThe effective runtime value differs from expectation.",
        "root": "For “WORKDIR — Dockerfile Semantics: fails intermittently under load or restart”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The WORKDIR configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The WORKDIR configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “WORKDIR — Dockerfile Semantics: fails intermittently under load or restart”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "WORKDIR — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nFor “WORKDIR — Dockerfile Semantics: fails intermittently under load or restart”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “WORKDIR — Dockerfile Semantics: fails intermittently under load or restart”, the effective WORKDIR state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "WORKDIR — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the WORKDIR path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "WORKDIR — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-259",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "USER — Dockerfile Semantics: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective USER state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "USER",
      "Dockerfile Semantics",
      "USER — Dockerfile Semantics: fails completely",
      "user",
      "dockerfile",
      "semantics",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “USER — Dockerfile Semantics: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected USER; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “USER — Dockerfile Semantics: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “USER — Dockerfile Semantics: fails completely”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for USER",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The USER configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “USER — Dockerfile Semantics: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the USER inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “USER — Dockerfile Semantics: fails completely”",
      "Effective USER configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: USER — Dockerfile Semantics: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “USER — Dockerfile Semantics: fails completely”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "USER — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for USER",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for USER\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for USER\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for USER",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “USER — Dockerfile Semantics: fails completely”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "USER — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The USER configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The USER configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The USER configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The USER configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "USER — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for USER",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for USER\nThe effective runtime value differs from expectation.",
        "root": "For “USER — Dockerfile Semantics: fails completely”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The USER configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The USER configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “USER — Dockerfile Semantics: fails completely”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "USER — Dockerfile Semantics: fails completely: evidence found\nFor “USER — Dockerfile Semantics: fails completely”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “USER — Dockerfile Semantics: fails completely”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "USER — Dockerfile Semantics: fails completely: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "USER — Dockerfile Semantics: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-260",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "USER — Dockerfile Semantics: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective USER state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "USER",
      "Dockerfile Semantics",
      "USER — Dockerfile Semantics: behaves differently than configured",
      "user",
      "dockerfile",
      "semantics",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “USER — Dockerfile Semantics: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected USER; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “USER — Dockerfile Semantics: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “USER — Dockerfile Semantics: behaves differently than configured”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for USER",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The USER configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “USER — Dockerfile Semantics: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the USER inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “USER — Dockerfile Semantics: behaves differently than configured”",
      "Effective USER configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: USER — Dockerfile Semantics: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “USER — Dockerfile Semantics: behaves differently than configured”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "USER — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for USER",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for USER\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for USER\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for USER",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “USER — Dockerfile Semantics: behaves differently than configured”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "USER — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The USER configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The USER configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The USER configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The USER configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "USER — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for USER",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for USER\nThe effective runtime value differs from expectation.",
        "root": "For “USER — Dockerfile Semantics: behaves differently than configured”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The USER configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The USER configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “USER — Dockerfile Semantics: behaves differently than configured”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "USER — Dockerfile Semantics: behaves differently than configured: evidence found\nFor “USER — Dockerfile Semantics: behaves differently than configured”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “USER — Dockerfile Semantics: behaves differently than configured”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "USER — Dockerfile Semantics: behaves differently than configured: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "USER — Dockerfile Semantics: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-261",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "USER — Dockerfile Semantics: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective USER state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "USER",
      "Dockerfile Semantics",
      "USER — Dockerfile Semantics: fails intermittently under load or restart",
      "user",
      "dockerfile",
      "semantics",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “USER — Dockerfile Semantics: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected USER; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “USER — Dockerfile Semantics: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “USER — Dockerfile Semantics: fails intermittently under load or restart”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for USER",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The USER configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “USER — Dockerfile Semantics: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the USER inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “USER — Dockerfile Semantics: fails intermittently under load or restart”",
      "Effective USER configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: USER — Dockerfile Semantics: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “USER — Dockerfile Semantics: fails intermittently under load or restart”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "USER — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for USER",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for USER\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for USER\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for USER",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “USER — Dockerfile Semantics: fails intermittently under load or restart”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "USER — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The USER configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The USER configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The USER configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The USER configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "USER — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for USER",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for USER\nThe effective runtime value differs from expectation.",
        "root": "For “USER — Dockerfile Semantics: fails intermittently under load or restart”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The USER configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The USER configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “USER — Dockerfile Semantics: fails intermittently under load or restart”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "USER — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nFor “USER — Dockerfile Semantics: fails intermittently under load or restart”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “USER — Dockerfile Semantics: fails intermittently under load or restart”, the effective USER state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "USER — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the USER path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "USER — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-262",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "COPY/ADD — Dockerfile Semantics: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective COPY/ADD state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "COPY/ADD",
      "Dockerfile Semantics",
      "COPY/ADD — Dockerfile Semantics: fails completely",
      "copy",
      "add",
      "dockerfile",
      "semantics",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “COPY/ADD — Dockerfile Semantics: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected COPY/ADD; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “COPY/ADD — Dockerfile Semantics: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “COPY/ADD — Dockerfile Semantics: fails completely”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for COPY/ADD",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The COPY/ADD configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “COPY/ADD — Dockerfile Semantics: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the COPY/ADD inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “COPY/ADD — Dockerfile Semantics: fails completely”",
      "Effective COPY/ADD configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: COPY/ADD — Dockerfile Semantics: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “COPY/ADD — Dockerfile Semantics: fails completely”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "COPY/ADD — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for COPY/ADD",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for COPY/ADD\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for COPY/ADD\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for COPY/ADD",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “COPY/ADD — Dockerfile Semantics: fails completely”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "COPY/ADD — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The COPY/ADD configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The COPY/ADD configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The COPY/ADD configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The COPY/ADD configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "COPY/ADD — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for COPY/ADD",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for COPY/ADD\nThe effective runtime value differs from expectation.",
        "root": "For “COPY/ADD — Dockerfile Semantics: fails completely”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The COPY/ADD configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The COPY/ADD configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “COPY/ADD — Dockerfile Semantics: fails completely”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "COPY/ADD — Dockerfile Semantics: fails completely: evidence found\nFor “COPY/ADD — Dockerfile Semantics: fails completely”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “COPY/ADD — Dockerfile Semantics: fails completely”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "COPY/ADD — Dockerfile Semantics: fails completely: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "COPY/ADD — Dockerfile Semantics: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-263",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "COPY/ADD — Dockerfile Semantics: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective COPY/ADD state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "COPY/ADD",
      "Dockerfile Semantics",
      "COPY/ADD — Dockerfile Semantics: behaves differently than configured",
      "copy",
      "add",
      "dockerfile",
      "semantics",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “COPY/ADD — Dockerfile Semantics: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected COPY/ADD; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “COPY/ADD — Dockerfile Semantics: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “COPY/ADD — Dockerfile Semantics: behaves differently than configured”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for COPY/ADD",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The COPY/ADD configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “COPY/ADD — Dockerfile Semantics: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the COPY/ADD inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “COPY/ADD — Dockerfile Semantics: behaves differently than configured”",
      "Effective COPY/ADD configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: COPY/ADD — Dockerfile Semantics: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “COPY/ADD — Dockerfile Semantics: behaves differently than configured”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "COPY/ADD — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for COPY/ADD",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for COPY/ADD\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for COPY/ADD\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for COPY/ADD",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “COPY/ADD — Dockerfile Semantics: behaves differently than configured”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "COPY/ADD — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The COPY/ADD configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The COPY/ADD configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The COPY/ADD configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The COPY/ADD configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "COPY/ADD — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for COPY/ADD",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for COPY/ADD\nThe effective runtime value differs from expectation.",
        "root": "For “COPY/ADD — Dockerfile Semantics: behaves differently than configured”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The COPY/ADD configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The COPY/ADD configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “COPY/ADD — Dockerfile Semantics: behaves differently than configured”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "COPY/ADD — Dockerfile Semantics: behaves differently than configured: evidence found\nFor “COPY/ADD — Dockerfile Semantics: behaves differently than configured”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “COPY/ADD — Dockerfile Semantics: behaves differently than configured”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "COPY/ADD — Dockerfile Semantics: behaves differently than configured: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "COPY/ADD — Dockerfile Semantics: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-264",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective COPY/ADD state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "COPY/ADD",
      "Dockerfile Semantics",
      "COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart",
      "copy",
      "add",
      "dockerfile",
      "semantics",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected COPY/ADD; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for COPY/ADD",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The COPY/ADD configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the COPY/ADD inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart”",
      "Effective COPY/ADD configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for COPY/ADD",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for COPY/ADD\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for COPY/ADD\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for COPY/ADD",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The COPY/ADD configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The COPY/ADD configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The COPY/ADD configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The COPY/ADD configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for COPY/ADD",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for COPY/ADD\nThe effective runtime value differs from expectation.",
        "root": "For “COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The COPY/ADD configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The COPY/ADD configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nFor “COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart”, the effective COPY/ADD state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the COPY/ADD path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "COPY/ADD — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-265",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "ARG/ENV — Dockerfile Semantics: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective ARG/ENV state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "ARG/ENV",
      "Dockerfile Semantics",
      "ARG/ENV — Dockerfile Semantics: fails completely",
      "arg",
      "env",
      "dockerfile",
      "semantics",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “ARG/ENV — Dockerfile Semantics: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected ARG/ENV; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “ARG/ENV — Dockerfile Semantics: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “ARG/ENV — Dockerfile Semantics: fails completely”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for ARG/ENV",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The ARG/ENV configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “ARG/ENV — Dockerfile Semantics: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the ARG/ENV inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “ARG/ENV — Dockerfile Semantics: fails completely”",
      "Effective ARG/ENV configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: ARG/ENV — Dockerfile Semantics: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “ARG/ENV — Dockerfile Semantics: fails completely”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "ARG/ENV — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for ARG/ENV",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ARG/ENV\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ARG/ENV\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for ARG/ENV",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “ARG/ENV — Dockerfile Semantics: fails completely”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "ARG/ENV — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The ARG/ENV configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ARG/ENV configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ARG/ENV configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The ARG/ENV configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "ARG/ENV — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for ARG/ENV",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ARG/ENV\nThe effective runtime value differs from expectation.",
        "root": "For “ARG/ENV — Dockerfile Semantics: fails completely”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The ARG/ENV configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ARG/ENV configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “ARG/ENV — Dockerfile Semantics: fails completely”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "ARG/ENV — Dockerfile Semantics: fails completely: evidence found\nFor “ARG/ENV — Dockerfile Semantics: fails completely”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “ARG/ENV — Dockerfile Semantics: fails completely”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "ARG/ENV — Dockerfile Semantics: fails completely: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "ARG/ENV — Dockerfile Semantics: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-266",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "ARG/ENV — Dockerfile Semantics: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective ARG/ENV state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "ARG/ENV",
      "Dockerfile Semantics",
      "ARG/ENV — Dockerfile Semantics: behaves differently than configured",
      "arg",
      "env",
      "dockerfile",
      "semantics",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “ARG/ENV — Dockerfile Semantics: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected ARG/ENV; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “ARG/ENV — Dockerfile Semantics: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “ARG/ENV — Dockerfile Semantics: behaves differently than configured”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for ARG/ENV",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The ARG/ENV configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “ARG/ENV — Dockerfile Semantics: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the ARG/ENV inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “ARG/ENV — Dockerfile Semantics: behaves differently than configured”",
      "Effective ARG/ENV configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: ARG/ENV — Dockerfile Semantics: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “ARG/ENV — Dockerfile Semantics: behaves differently than configured”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "ARG/ENV — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for ARG/ENV",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ARG/ENV\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ARG/ENV\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for ARG/ENV",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “ARG/ENV — Dockerfile Semantics: behaves differently than configured”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "ARG/ENV — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The ARG/ENV configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ARG/ENV configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ARG/ENV configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The ARG/ENV configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "ARG/ENV — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for ARG/ENV",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ARG/ENV\nThe effective runtime value differs from expectation.",
        "root": "For “ARG/ENV — Dockerfile Semantics: behaves differently than configured”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The ARG/ENV configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ARG/ENV configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “ARG/ENV — Dockerfile Semantics: behaves differently than configured”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "ARG/ENV — Dockerfile Semantics: behaves differently than configured: evidence found\nFor “ARG/ENV — Dockerfile Semantics: behaves differently than configured”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “ARG/ENV — Dockerfile Semantics: behaves differently than configured”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "ARG/ENV — Dockerfile Semantics: behaves differently than configured: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "ARG/ENV — Dockerfile Semantics: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-267",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective ARG/ENV state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "ARG/ENV",
      "Dockerfile Semantics",
      "ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart",
      "arg",
      "env",
      "dockerfile",
      "semantics",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected ARG/ENV; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for ARG/ENV",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The ARG/ENV configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the ARG/ENV inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart”",
      "Effective ARG/ENV configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for ARG/ENV",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ARG/ENV\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ARG/ENV\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for ARG/ENV",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The ARG/ENV configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ARG/ENV configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ARG/ENV configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The ARG/ENV configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for ARG/ENV",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for ARG/ENV\nThe effective runtime value differs from expectation.",
        "root": "For “ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The ARG/ENV configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The ARG/ENV configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nFor “ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart”, the effective ARG/ENV state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the ARG/ENV path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "ARG/ENV — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-268",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "VOLUME/EXPOSE — Dockerfile Semantics: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective VOLUME/EXPOSE state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "VOLUME/EXPOSE",
      "Dockerfile Semantics",
      "VOLUME/EXPOSE — Dockerfile Semantics: fails completely",
      "volume",
      "expose",
      "dockerfile",
      "semantics",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “VOLUME/EXPOSE — Dockerfile Semantics: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected VOLUME/EXPOSE; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “VOLUME/EXPOSE — Dockerfile Semantics: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “VOLUME/EXPOSE — Dockerfile Semantics: fails completely”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The VOLUME/EXPOSE configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “VOLUME/EXPOSE — Dockerfile Semantics: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the VOLUME/EXPOSE inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “VOLUME/EXPOSE — Dockerfile Semantics: fails completely”",
      "Effective VOLUME/EXPOSE configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: VOLUME/EXPOSE — Dockerfile Semantics: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “VOLUME/EXPOSE — Dockerfile Semantics: fails completely”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "VOLUME/EXPOSE — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “VOLUME/EXPOSE — Dockerfile Semantics: fails completely”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "VOLUME/EXPOSE — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The VOLUME/EXPOSE configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VOLUME/EXPOSE configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VOLUME/EXPOSE configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The VOLUME/EXPOSE configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "VOLUME/EXPOSE — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE\nThe effective runtime value differs from expectation.",
        "root": "For “VOLUME/EXPOSE — Dockerfile Semantics: fails completely”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The VOLUME/EXPOSE configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VOLUME/EXPOSE configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “VOLUME/EXPOSE — Dockerfile Semantics: fails completely”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "VOLUME/EXPOSE — Dockerfile Semantics: fails completely: evidence found\nFor “VOLUME/EXPOSE — Dockerfile Semantics: fails completely”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “VOLUME/EXPOSE — Dockerfile Semantics: fails completely”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "VOLUME/EXPOSE — Dockerfile Semantics: fails completely: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "VOLUME/EXPOSE — Dockerfile Semantics: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-269",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective VOLUME/EXPOSE state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "VOLUME/EXPOSE",
      "Dockerfile Semantics",
      "VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured",
      "volume",
      "expose",
      "dockerfile",
      "semantics",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected VOLUME/EXPOSE; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The VOLUME/EXPOSE configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the VOLUME/EXPOSE inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured”",
      "Effective VOLUME/EXPOSE configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The VOLUME/EXPOSE configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VOLUME/EXPOSE configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VOLUME/EXPOSE configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The VOLUME/EXPOSE configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE\nThe effective runtime value differs from expectation.",
        "root": "For “VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The VOLUME/EXPOSE configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VOLUME/EXPOSE configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured: evidence found\nFor “VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "VOLUME/EXPOSE — Dockerfile Semantics: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-270",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective VOLUME/EXPOSE state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "VOLUME/EXPOSE",
      "Dockerfile Semantics",
      "VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart",
      "volume",
      "expose",
      "dockerfile",
      "semantics",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected VOLUME/EXPOSE; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The VOLUME/EXPOSE configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the VOLUME/EXPOSE inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart”",
      "Effective VOLUME/EXPOSE configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The VOLUME/EXPOSE configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VOLUME/EXPOSE configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VOLUME/EXPOSE configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The VOLUME/EXPOSE configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for VOLUME/EXPOSE\nThe effective runtime value differs from expectation.",
        "root": "For “VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The VOLUME/EXPOSE configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The VOLUME/EXPOSE configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nFor “VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart”, the effective VOLUME/EXPOSE state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the VOLUME/EXPOSE path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "VOLUME/EXPOSE — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-271",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "Dockerfile syntax feature — Dockerfile Semantics: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Dockerfile syntax feature state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "Dockerfile syntax feature",
      "Dockerfile Semantics",
      "Dockerfile syntax feature — Dockerfile Semantics: fails completely",
      "dockerfile",
      "syntax",
      "feature",
      "semantics",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Dockerfile syntax feature — Dockerfile Semantics: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Dockerfile syntax feature; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Dockerfile syntax feature — Dockerfile Semantics: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Dockerfile syntax feature — Dockerfile Semantics: fails completely”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Dockerfile syntax feature configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Dockerfile syntax feature — Dockerfile Semantics: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Dockerfile syntax feature inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Dockerfile syntax feature — Dockerfile Semantics: fails completely”",
      "Effective Dockerfile syntax feature configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Dockerfile syntax feature — Dockerfile Semantics: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Dockerfile syntax feature — Dockerfile Semantics: fails completely”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Dockerfile syntax feature — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Dockerfile syntax feature — Dockerfile Semantics: fails completely”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Dockerfile syntax feature — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Dockerfile syntax feature configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile syntax feature configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile syntax feature configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The Dockerfile syntax feature configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Dockerfile syntax feature — Dockerfile Semantics: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature\nThe effective runtime value differs from expectation.",
        "root": "For “Dockerfile syntax feature — Dockerfile Semantics: fails completely”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Dockerfile syntax feature configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile syntax feature configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Dockerfile syntax feature — Dockerfile Semantics: fails completely”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Dockerfile syntax feature — Dockerfile Semantics: fails completely: evidence found\nFor “Dockerfile syntax feature — Dockerfile Semantics: fails completely”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Dockerfile syntax feature — Dockerfile Semantics: fails completely”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Dockerfile syntax feature — Dockerfile Semantics: fails completely: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Dockerfile syntax feature — Dockerfile Semantics: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-272",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Dockerfile syntax feature state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "Dockerfile syntax feature",
      "Dockerfile Semantics",
      "Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured",
      "dockerfile",
      "syntax",
      "feature",
      "semantics",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Dockerfile syntax feature; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Dockerfile syntax feature configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Dockerfile syntax feature inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured”",
      "Effective Dockerfile syntax feature configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Dockerfile syntax feature configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile syntax feature configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile syntax feature configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The Dockerfile syntax feature configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature\nThe effective runtime value differs from expectation.",
        "root": "For “Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Dockerfile syntax feature configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile syntax feature configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured: evidence found\nFor “Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Dockerfile syntax feature — Dockerfile Semantics: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-273",
    "technology": "Docker",
    "category": "Dockerfile Semantics",
    "title": "Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Dockerfile syntax feature state first, then narrow the failure through the dockerfile semantics dependency chain.",
    "aliases": [
      "Dockerfile syntax feature",
      "Dockerfile Semantics",
      "Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart",
      "dockerfile",
      "syntax",
      "feature",
      "semantics",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect built image defaults",
        "command": "docker image inspect <image> --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect layer history",
        "command": "docker history --no-trunc <image>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Dockerfile syntax feature; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Run Dockerfile checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect build outline",
        "command": "docker buildx build --call=outline -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
      "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Dockerfile syntax feature configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Dockerfile syntax feature inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart”",
      "Effective Dockerfile syntax feature configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Dockerfile Semantics Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Dockerfile Semantics path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature",
        "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Dockerfile syntax feature configuration is correct but the failure remains",
        "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile syntax feature configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile syntax feature configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The Dockerfile syntax feature configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --call=outline -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile syntax feature\nThe effective runtime value differs from expectation.",
        "root": "For “Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Dockerfile syntax feature configuration is correct but the failure remains",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile syntax feature configuration is correct but the failure remains",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker image inspect registry.example.com/payments-api:2026.09.12 --format 'Entrypoint={{json .Config.Entrypoint}} Cmd={{json .Config.Cmd}} User={{.Config.User}} WorkingDir={{.Config.WorkingDir}}'",
        "output": "Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nFor “Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart”, the effective Dockerfile syntax feature state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "check": "docker history --no-trunc registry.example.com/payments-api:2026.09.12",
        "output": "Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nA dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "root": "A dockerfile semantics dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile syntax feature path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Dockerfile syntax feature — Dockerfile Semantics: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --call=outline -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-274",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Build context size — Build Context & .dockerignore: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective build context size state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "build context size",
      "Build Context & .dockerignore",
      "Build context size — Build Context & .dockerignore: fails completely",
      "build",
      "context",
      "size",
      "dockerignore",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Build context size — Build Context & .dockerignore: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected build context size; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Build context size — Build Context & .dockerignore: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Build context size — Build Context & .dockerignore: fails completely”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for build context size",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The build context size configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Build context size — Build Context & .dockerignore: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the build context size inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Build context size — Build Context & .dockerignore: fails completely”",
      "Effective build context size configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Build context size — Build Context & .dockerignore: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Build context size — Build Context & .dockerignore: fails completely”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Build context size — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for build context size",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build context size\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build context size\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for build context size",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Build context size — Build Context & .dockerignore: fails completely”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Build context size — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The build context size configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build context size configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build context size configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The build context size configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Build context size — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for build context size",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build context size\nThe effective runtime value differs from expectation.",
        "root": "For “Build context size — Build Context & .dockerignore: fails completely”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The build context size configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build context size configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Build context size — Build Context & .dockerignore: fails completely”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Build context size — Build Context & .dockerignore: fails completely: evidence found\nFor “Build context size — Build Context & .dockerignore: fails completely”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Build context size — Build Context & .dockerignore: fails completely”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Build context size — Build Context & .dockerignore: fails completely: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Build context size — Build Context & .dockerignore: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-275",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Build context size — Build Context & .dockerignore: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective build context size state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "build context size",
      "Build Context & .dockerignore",
      "Build context size — Build Context & .dockerignore: behaves differently than configured",
      "build",
      "context",
      "size",
      "dockerignore",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Build context size — Build Context & .dockerignore: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected build context size; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Build context size — Build Context & .dockerignore: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Build context size — Build Context & .dockerignore: behaves differently than configured”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for build context size",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The build context size configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Build context size — Build Context & .dockerignore: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the build context size inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Build context size — Build Context & .dockerignore: behaves differently than configured”",
      "Effective build context size configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Build context size — Build Context & .dockerignore: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Build context size — Build Context & .dockerignore: behaves differently than configured”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Build context size — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for build context size",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build context size\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build context size\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for build context size",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Build context size — Build Context & .dockerignore: behaves differently than configured”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Build context size — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The build context size configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build context size configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build context size configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The build context size configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Build context size — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for build context size",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build context size\nThe effective runtime value differs from expectation.",
        "root": "For “Build context size — Build Context & .dockerignore: behaves differently than configured”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The build context size configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build context size configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Build context size — Build Context & .dockerignore: behaves differently than configured”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Build context size — Build Context & .dockerignore: behaves differently than configured: evidence found\nFor “Build context size — Build Context & .dockerignore: behaves differently than configured”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Build context size — Build Context & .dockerignore: behaves differently than configured”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Build context size — Build Context & .dockerignore: behaves differently than configured: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Build context size — Build Context & .dockerignore: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-276",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Build context size — Build Context & .dockerignore: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective build context size state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "build context size",
      "Build Context & .dockerignore",
      "Build context size — Build Context & .dockerignore: fails intermittently under load or restart",
      "build",
      "context",
      "size",
      "dockerignore",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Build context size — Build Context & .dockerignore: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected build context size; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Build context size — Build Context & .dockerignore: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Build context size — Build Context & .dockerignore: fails intermittently under load or restart”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for build context size",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The build context size configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Build context size — Build Context & .dockerignore: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the build context size inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Build context size — Build Context & .dockerignore: fails intermittently under load or restart”",
      "Effective build context size configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Build context size — Build Context & .dockerignore: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Build context size — Build Context & .dockerignore: fails intermittently under load or restart”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Build context size — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for build context size",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build context size\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build context size\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for build context size",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Build context size — Build Context & .dockerignore: fails intermittently under load or restart”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Build context size — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The build context size configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build context size configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build context size configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The build context size configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Build context size — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for build context size",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build context size\nThe effective runtime value differs from expectation.",
        "root": "For “Build context size — Build Context & .dockerignore: fails intermittently under load or restart”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The build context size configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build context size configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Build context size — Build Context & .dockerignore: fails intermittently under load or restart”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Build context size — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nFor “Build context size — Build Context & .dockerignore: fails intermittently under load or restart”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Build context size — Build Context & .dockerignore: fails intermittently under load or restart”, the effective build context size state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Build context size — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build context size path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Build context size — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-277",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Required context file — Build Context & .dockerignore: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective required context file state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "required context file",
      "Build Context & .dockerignore",
      "Required context file — Build Context & .dockerignore: fails completely",
      "required",
      "context",
      "file",
      "build",
      "dockerignore",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Required context file — Build Context & .dockerignore: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected required context file; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Required context file — Build Context & .dockerignore: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Required context file — Build Context & .dockerignore: fails completely”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for required context file",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The required context file configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Required context file — Build Context & .dockerignore: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the required context file inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Required context file — Build Context & .dockerignore: fails completely”",
      "Effective required context file configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Required context file — Build Context & .dockerignore: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Required context file — Build Context & .dockerignore: fails completely”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Required context file — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for required context file",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for required context file\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for required context file\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for required context file",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Required context file — Build Context & .dockerignore: fails completely”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Required context file — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The required context file configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The required context file configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The required context file configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The required context file configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Required context file — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for required context file",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for required context file\nThe effective runtime value differs from expectation.",
        "root": "For “Required context file — Build Context & .dockerignore: fails completely”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The required context file configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The required context file configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Required context file — Build Context & .dockerignore: fails completely”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Required context file — Build Context & .dockerignore: fails completely: evidence found\nFor “Required context file — Build Context & .dockerignore: fails completely”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Required context file — Build Context & .dockerignore: fails completely”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Required context file — Build Context & .dockerignore: fails completely: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Required context file — Build Context & .dockerignore: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-278",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Required context file — Build Context & .dockerignore: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective required context file state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "required context file",
      "Build Context & .dockerignore",
      "Required context file — Build Context & .dockerignore: behaves differently than configured",
      "required",
      "context",
      "file",
      "build",
      "dockerignore",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Required context file — Build Context & .dockerignore: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected required context file; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Required context file — Build Context & .dockerignore: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Required context file — Build Context & .dockerignore: behaves differently than configured”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for required context file",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The required context file configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Required context file — Build Context & .dockerignore: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the required context file inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Required context file — Build Context & .dockerignore: behaves differently than configured”",
      "Effective required context file configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Required context file — Build Context & .dockerignore: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Required context file — Build Context & .dockerignore: behaves differently than configured”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Required context file — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for required context file",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for required context file\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for required context file\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for required context file",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Required context file — Build Context & .dockerignore: behaves differently than configured”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Required context file — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The required context file configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The required context file configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The required context file configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The required context file configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Required context file — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for required context file",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for required context file\nThe effective runtime value differs from expectation.",
        "root": "For “Required context file — Build Context & .dockerignore: behaves differently than configured”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The required context file configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The required context file configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Required context file — Build Context & .dockerignore: behaves differently than configured”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Required context file — Build Context & .dockerignore: behaves differently than configured: evidence found\nFor “Required context file — Build Context & .dockerignore: behaves differently than configured”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Required context file — Build Context & .dockerignore: behaves differently than configured”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Required context file — Build Context & .dockerignore: behaves differently than configured: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Required context file — Build Context & .dockerignore: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-279",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Required context file — Build Context & .dockerignore: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective required context file state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "required context file",
      "Build Context & .dockerignore",
      "Required context file — Build Context & .dockerignore: fails intermittently under load or restart",
      "required",
      "context",
      "file",
      "build",
      "dockerignore",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Required context file — Build Context & .dockerignore: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected required context file; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Required context file — Build Context & .dockerignore: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Required context file — Build Context & .dockerignore: fails intermittently under load or restart”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for required context file",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The required context file configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Required context file — Build Context & .dockerignore: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the required context file inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Required context file — Build Context & .dockerignore: fails intermittently under load or restart”",
      "Effective required context file configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Required context file — Build Context & .dockerignore: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Required context file — Build Context & .dockerignore: fails intermittently under load or restart”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Required context file — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for required context file",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for required context file\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for required context file\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for required context file",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Required context file — Build Context & .dockerignore: fails intermittently under load or restart”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Required context file — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The required context file configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The required context file configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The required context file configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The required context file configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Required context file — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for required context file",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for required context file\nThe effective runtime value differs from expectation.",
        "root": "For “Required context file — Build Context & .dockerignore: fails intermittently under load or restart”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The required context file configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The required context file configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Required context file — Build Context & .dockerignore: fails intermittently under load or restart”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Required context file — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nFor “Required context file — Build Context & .dockerignore: fails intermittently under load or restart”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Required context file — Build Context & .dockerignore: fails intermittently under load or restart”, the effective required context file state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Required context file — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the required context file path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Required context file — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-280",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": ".dockerignore rule — Build Context & .dockerignore: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective .dockerignore rule state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      ".dockerignore rule",
      "Build Context & .dockerignore",
      ".dockerignore rule — Build Context & .dockerignore: fails completely",
      "dockerignore",
      "rule",
      "build",
      "context",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “.dockerignore rule — Build Context & .dockerignore: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected .dockerignore rule; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “.dockerignore rule — Build Context & .dockerignore: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “.dockerignore rule — Build Context & .dockerignore: fails completely”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for .dockerignore rule",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The .dockerignore rule configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “.dockerignore rule — Build Context & .dockerignore: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the .dockerignore rule inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “.dockerignore rule — Build Context & .dockerignore: fails completely”",
      "Effective .dockerignore rule configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: .dockerignore rule — Build Context & .dockerignore: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “.dockerignore rule — Build Context & .dockerignore: fails completely”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": ".dockerignore rule — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for .dockerignore rule",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .dockerignore rule\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .dockerignore rule\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for .dockerignore rule",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “.dockerignore rule — Build Context & .dockerignore: fails completely”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": ".dockerignore rule — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The .dockerignore rule configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .dockerignore rule configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .dockerignore rule configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The .dockerignore rule configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": ".dockerignore rule — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for .dockerignore rule",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .dockerignore rule\nThe effective runtime value differs from expectation.",
        "root": "For “.dockerignore rule — Build Context & .dockerignore: fails completely”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The .dockerignore rule configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .dockerignore rule configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “.dockerignore rule — Build Context & .dockerignore: fails completely”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": ".dockerignore rule — Build Context & .dockerignore: fails completely: evidence found\nFor “.dockerignore rule — Build Context & .dockerignore: fails completely”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “.dockerignore rule — Build Context & .dockerignore: fails completely”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": ".dockerignore rule — Build Context & .dockerignore: fails completely: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": ".dockerignore rule — Build Context & .dockerignore: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-281",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": ".dockerignore rule — Build Context & .dockerignore: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective .dockerignore rule state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      ".dockerignore rule",
      "Build Context & .dockerignore",
      ".dockerignore rule — Build Context & .dockerignore: behaves differently than configured",
      "dockerignore",
      "rule",
      "build",
      "context",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “.dockerignore rule — Build Context & .dockerignore: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected .dockerignore rule; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “.dockerignore rule — Build Context & .dockerignore: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “.dockerignore rule — Build Context & .dockerignore: behaves differently than configured”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for .dockerignore rule",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The .dockerignore rule configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “.dockerignore rule — Build Context & .dockerignore: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the .dockerignore rule inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “.dockerignore rule — Build Context & .dockerignore: behaves differently than configured”",
      "Effective .dockerignore rule configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: .dockerignore rule — Build Context & .dockerignore: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “.dockerignore rule — Build Context & .dockerignore: behaves differently than configured”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": ".dockerignore rule — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for .dockerignore rule",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .dockerignore rule\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .dockerignore rule\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for .dockerignore rule",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “.dockerignore rule — Build Context & .dockerignore: behaves differently than configured”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": ".dockerignore rule — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The .dockerignore rule configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .dockerignore rule configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .dockerignore rule configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The .dockerignore rule configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": ".dockerignore rule — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for .dockerignore rule",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .dockerignore rule\nThe effective runtime value differs from expectation.",
        "root": "For “.dockerignore rule — Build Context & .dockerignore: behaves differently than configured”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The .dockerignore rule configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .dockerignore rule configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “.dockerignore rule — Build Context & .dockerignore: behaves differently than configured”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": ".dockerignore rule — Build Context & .dockerignore: behaves differently than configured: evidence found\nFor “.dockerignore rule — Build Context & .dockerignore: behaves differently than configured”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “.dockerignore rule — Build Context & .dockerignore: behaves differently than configured”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": ".dockerignore rule — Build Context & .dockerignore: behaves differently than configured: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": ".dockerignore rule — Build Context & .dockerignore: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-282",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": ".dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective .dockerignore rule state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      ".dockerignore rule",
      "Build Context & .dockerignore",
      ".dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart",
      "dockerignore",
      "rule",
      "build",
      "context",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “.dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected .dockerignore rule; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “.dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “.dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for .dockerignore rule",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The .dockerignore rule configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “.dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the .dockerignore rule inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “.dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart”",
      "Effective .dockerignore rule configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: .dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “.dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": ".dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for .dockerignore rule",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .dockerignore rule\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .dockerignore rule\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for .dockerignore rule",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “.dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": ".dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The .dockerignore rule configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .dockerignore rule configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .dockerignore rule configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The .dockerignore rule configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": ".dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for .dockerignore rule",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .dockerignore rule\nThe effective runtime value differs from expectation.",
        "root": "For “.dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The .dockerignore rule configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .dockerignore rule configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “.dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": ".dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nFor “.dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “.dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart”, the effective .dockerignore rule state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": ".dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .dockerignore rule path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": ".dockerignore rule — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-283",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Git build context — Build Context & .dockerignore: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Git build context state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "Git build context",
      "Build Context & .dockerignore",
      "Git build context — Build Context & .dockerignore: fails completely",
      "git",
      "build",
      "context",
      "dockerignore",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Git build context — Build Context & .dockerignore: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Git build context; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Git build context — Build Context & .dockerignore: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Git build context — Build Context & .dockerignore: fails completely”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Git build context",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Git build context configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Git build context — Build Context & .dockerignore: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Git build context inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Git build context — Build Context & .dockerignore: fails completely”",
      "Effective Git build context configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Git build context — Build Context & .dockerignore: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Git build context — Build Context & .dockerignore: fails completely”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Git build context — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Git build context",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Git build context\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Git build context\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Git build context",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Git build context — Build Context & .dockerignore: fails completely”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Git build context — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Git build context configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Git build context configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Git build context configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The Git build context configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Git build context — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Git build context",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Git build context\nThe effective runtime value differs from expectation.",
        "root": "For “Git build context — Build Context & .dockerignore: fails completely”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Git build context configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Git build context configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Git build context — Build Context & .dockerignore: fails completely”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Git build context — Build Context & .dockerignore: fails completely: evidence found\nFor “Git build context — Build Context & .dockerignore: fails completely”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Git build context — Build Context & .dockerignore: fails completely”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Git build context — Build Context & .dockerignore: fails completely: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Git build context — Build Context & .dockerignore: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-284",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Git build context — Build Context & .dockerignore: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Git build context state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "Git build context",
      "Build Context & .dockerignore",
      "Git build context — Build Context & .dockerignore: behaves differently than configured",
      "git",
      "build",
      "context",
      "dockerignore",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Git build context — Build Context & .dockerignore: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Git build context; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Git build context — Build Context & .dockerignore: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Git build context — Build Context & .dockerignore: behaves differently than configured”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Git build context",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Git build context configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Git build context — Build Context & .dockerignore: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Git build context inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Git build context — Build Context & .dockerignore: behaves differently than configured”",
      "Effective Git build context configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Git build context — Build Context & .dockerignore: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Git build context — Build Context & .dockerignore: behaves differently than configured”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Git build context — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Git build context",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Git build context\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Git build context\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Git build context",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Git build context — Build Context & .dockerignore: behaves differently than configured”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Git build context — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Git build context configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Git build context configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Git build context configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The Git build context configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Git build context — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Git build context",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Git build context\nThe effective runtime value differs from expectation.",
        "root": "For “Git build context — Build Context & .dockerignore: behaves differently than configured”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Git build context configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Git build context configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Git build context — Build Context & .dockerignore: behaves differently than configured”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Git build context — Build Context & .dockerignore: behaves differently than configured: evidence found\nFor “Git build context — Build Context & .dockerignore: behaves differently than configured”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Git build context — Build Context & .dockerignore: behaves differently than configured”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Git build context — Build Context & .dockerignore: behaves differently than configured: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Git build context — Build Context & .dockerignore: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-285",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Git build context — Build Context & .dockerignore: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Git build context state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "Git build context",
      "Build Context & .dockerignore",
      "Git build context — Build Context & .dockerignore: fails intermittently under load or restart",
      "git",
      "build",
      "context",
      "dockerignore",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Git build context — Build Context & .dockerignore: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Git build context; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Git build context — Build Context & .dockerignore: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Git build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Git build context",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Git build context configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Git build context — Build Context & .dockerignore: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Git build context inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Git build context — Build Context & .dockerignore: fails intermittently under load or restart”",
      "Effective Git build context configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Git build context — Build Context & .dockerignore: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Git build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Git build context — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Git build context",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Git build context\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Git build context\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Git build context",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Git build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Git build context — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Git build context configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Git build context configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Git build context configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The Git build context configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Git build context — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Git build context",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Git build context\nThe effective runtime value differs from expectation.",
        "root": "For “Git build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Git build context configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Git build context configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Git build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Git build context — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nFor “Git build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Git build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Git build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Git build context — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Git build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Git build context — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-286",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Named build context — Build Context & .dockerignore: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective named build context state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "named build context",
      "Build Context & .dockerignore",
      "Named build context — Build Context & .dockerignore: fails completely",
      "named",
      "build",
      "context",
      "dockerignore",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Named build context — Build Context & .dockerignore: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected named build context; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Named build context — Build Context & .dockerignore: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Named build context — Build Context & .dockerignore: fails completely”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for named build context",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The named build context configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Named build context — Build Context & .dockerignore: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the named build context inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Named build context — Build Context & .dockerignore: fails completely”",
      "Effective named build context configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Named build context — Build Context & .dockerignore: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Named build context — Build Context & .dockerignore: fails completely”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Named build context — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for named build context",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named build context\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named build context\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for named build context",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Named build context — Build Context & .dockerignore: fails completely”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Named build context — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The named build context configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named build context configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named build context configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The named build context configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Named build context — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for named build context",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named build context\nThe effective runtime value differs from expectation.",
        "root": "For “Named build context — Build Context & .dockerignore: fails completely”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The named build context configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named build context configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Named build context — Build Context & .dockerignore: fails completely”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Named build context — Build Context & .dockerignore: fails completely: evidence found\nFor “Named build context — Build Context & .dockerignore: fails completely”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Named build context — Build Context & .dockerignore: fails completely”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Named build context — Build Context & .dockerignore: fails completely: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Named build context — Build Context & .dockerignore: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-287",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Named build context — Build Context & .dockerignore: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective named build context state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "named build context",
      "Build Context & .dockerignore",
      "Named build context — Build Context & .dockerignore: behaves differently than configured",
      "named",
      "build",
      "context",
      "dockerignore",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Named build context — Build Context & .dockerignore: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected named build context; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Named build context — Build Context & .dockerignore: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Named build context — Build Context & .dockerignore: behaves differently than configured”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for named build context",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The named build context configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Named build context — Build Context & .dockerignore: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the named build context inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Named build context — Build Context & .dockerignore: behaves differently than configured”",
      "Effective named build context configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Named build context — Build Context & .dockerignore: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Named build context — Build Context & .dockerignore: behaves differently than configured”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Named build context — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for named build context",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named build context\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named build context\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for named build context",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Named build context — Build Context & .dockerignore: behaves differently than configured”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Named build context — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The named build context configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named build context configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named build context configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The named build context configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Named build context — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for named build context",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named build context\nThe effective runtime value differs from expectation.",
        "root": "For “Named build context — Build Context & .dockerignore: behaves differently than configured”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The named build context configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named build context configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Named build context — Build Context & .dockerignore: behaves differently than configured”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Named build context — Build Context & .dockerignore: behaves differently than configured: evidence found\nFor “Named build context — Build Context & .dockerignore: behaves differently than configured”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Named build context — Build Context & .dockerignore: behaves differently than configured”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Named build context — Build Context & .dockerignore: behaves differently than configured: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Named build context — Build Context & .dockerignore: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-288",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Named build context — Build Context & .dockerignore: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective named build context state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "named build context",
      "Build Context & .dockerignore",
      "Named build context — Build Context & .dockerignore: fails intermittently under load or restart",
      "named",
      "build",
      "context",
      "dockerignore",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Named build context — Build Context & .dockerignore: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected named build context; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Named build context — Build Context & .dockerignore: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Named build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for named build context",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The named build context configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Named build context — Build Context & .dockerignore: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the named build context inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Named build context — Build Context & .dockerignore: fails intermittently under load or restart”",
      "Effective named build context configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Named build context — Build Context & .dockerignore: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Named build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Named build context — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for named build context",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named build context\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named build context\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for named build context",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Named build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Named build context — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The named build context configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named build context configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named build context configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The named build context configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Named build context — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for named build context",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for named build context\nThe effective runtime value differs from expectation.",
        "root": "For “Named build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The named build context configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The named build context configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Named build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Named build context — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nFor “Named build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Named build context — Build Context & .dockerignore: fails intermittently under load or restart”, the effective named build context state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Named build context — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the named build context path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Named build context — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-289",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Dockerfile/context path — Build Context & .dockerignore: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Dockerfile/context path state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "Dockerfile/context path",
      "Build Context & .dockerignore",
      "Dockerfile/context path — Build Context & .dockerignore: fails completely",
      "dockerfile",
      "context",
      "path",
      "build",
      "dockerignore",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Dockerfile/context path — Build Context & .dockerignore: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Dockerfile/context path; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Dockerfile/context path — Build Context & .dockerignore: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Dockerfile/context path — Build Context & .dockerignore: fails completely”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Dockerfile/context path",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Dockerfile/context path configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Dockerfile/context path — Build Context & .dockerignore: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Dockerfile/context path inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Dockerfile/context path — Build Context & .dockerignore: fails completely”",
      "Effective Dockerfile/context path configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Dockerfile/context path — Build Context & .dockerignore: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Dockerfile/context path — Build Context & .dockerignore: fails completely”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Dockerfile/context path — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Dockerfile/context path",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile/context path\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile/context path\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Dockerfile/context path",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Dockerfile/context path — Build Context & .dockerignore: fails completely”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Dockerfile/context path — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Dockerfile/context path configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile/context path configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile/context path configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The Dockerfile/context path configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Dockerfile/context path — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Dockerfile/context path",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile/context path\nThe effective runtime value differs from expectation.",
        "root": "For “Dockerfile/context path — Build Context & .dockerignore: fails completely”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Dockerfile/context path configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile/context path configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Dockerfile/context path — Build Context & .dockerignore: fails completely”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Dockerfile/context path — Build Context & .dockerignore: fails completely: evidence found\nFor “Dockerfile/context path — Build Context & .dockerignore: fails completely”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Dockerfile/context path — Build Context & .dockerignore: fails completely”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Dockerfile/context path — Build Context & .dockerignore: fails completely: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Dockerfile/context path — Build Context & .dockerignore: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-290",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Dockerfile/context path state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "Dockerfile/context path",
      "Build Context & .dockerignore",
      "Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured",
      "dockerfile",
      "context",
      "path",
      "build",
      "dockerignore",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Dockerfile/context path; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Dockerfile/context path",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Dockerfile/context path configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Dockerfile/context path inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured”",
      "Effective Dockerfile/context path configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Dockerfile/context path",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile/context path\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile/context path\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Dockerfile/context path",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Dockerfile/context path configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile/context path configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile/context path configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The Dockerfile/context path configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Dockerfile/context path",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile/context path\nThe effective runtime value differs from expectation.",
        "root": "For “Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Dockerfile/context path configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile/context path configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured: evidence found\nFor “Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Dockerfile/context path — Build Context & .dockerignore: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-291",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Dockerfile/context path state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "Dockerfile/context path",
      "Build Context & .dockerignore",
      "Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart",
      "dockerfile",
      "context",
      "path",
      "build",
      "dockerignore",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Dockerfile/context path; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Dockerfile/context path",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Dockerfile/context path configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Dockerfile/context path inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart”",
      "Effective Dockerfile/context path configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Dockerfile/context path",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile/context path\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile/context path\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Dockerfile/context path",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Dockerfile/context path configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile/context path configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile/context path configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The Dockerfile/context path configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Dockerfile/context path",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Dockerfile/context path\nThe effective runtime value differs from expectation.",
        "root": "For “Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Dockerfile/context path configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Dockerfile/context path configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nFor “Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart”, the effective Dockerfile/context path state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Dockerfile/context path path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Dockerfile/context path — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-292",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Context permissions — Build Context & .dockerignore: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective context permissions state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "context permissions",
      "Build Context & .dockerignore",
      "Context permissions — Build Context & .dockerignore: fails completely",
      "context",
      "permissions",
      "build",
      "dockerignore",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Context permissions — Build Context & .dockerignore: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected context permissions; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Context permissions — Build Context & .dockerignore: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Context permissions — Build Context & .dockerignore: fails completely”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for context permissions",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The context permissions configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Context permissions — Build Context & .dockerignore: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the context permissions inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Context permissions — Build Context & .dockerignore: fails completely”",
      "Effective context permissions configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Context permissions — Build Context & .dockerignore: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Context permissions — Build Context & .dockerignore: fails completely”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Context permissions — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for context permissions",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for context permissions\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for context permissions\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for context permissions",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Context permissions — Build Context & .dockerignore: fails completely”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Context permissions — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The context permissions configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The context permissions configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The context permissions configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The context permissions configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Context permissions — Build Context & .dockerignore: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for context permissions",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for context permissions\nThe effective runtime value differs from expectation.",
        "root": "For “Context permissions — Build Context & .dockerignore: fails completely”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The context permissions configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The context permissions configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Context permissions — Build Context & .dockerignore: fails completely”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Context permissions — Build Context & .dockerignore: fails completely: evidence found\nFor “Context permissions — Build Context & .dockerignore: fails completely”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Context permissions — Build Context & .dockerignore: fails completely”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Context permissions — Build Context & .dockerignore: fails completely: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Context permissions — Build Context & .dockerignore: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-293",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Context permissions — Build Context & .dockerignore: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective context permissions state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "context permissions",
      "Build Context & .dockerignore",
      "Context permissions — Build Context & .dockerignore: behaves differently than configured",
      "context",
      "permissions",
      "build",
      "dockerignore",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Context permissions — Build Context & .dockerignore: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected context permissions; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Context permissions — Build Context & .dockerignore: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Context permissions — Build Context & .dockerignore: behaves differently than configured”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for context permissions",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The context permissions configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Context permissions — Build Context & .dockerignore: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the context permissions inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Context permissions — Build Context & .dockerignore: behaves differently than configured”",
      "Effective context permissions configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Context permissions — Build Context & .dockerignore: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Context permissions — Build Context & .dockerignore: behaves differently than configured”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Context permissions — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for context permissions",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for context permissions\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for context permissions\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for context permissions",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Context permissions — Build Context & .dockerignore: behaves differently than configured”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Context permissions — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The context permissions configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The context permissions configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The context permissions configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The context permissions configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Context permissions — Build Context & .dockerignore: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for context permissions",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for context permissions\nThe effective runtime value differs from expectation.",
        "root": "For “Context permissions — Build Context & .dockerignore: behaves differently than configured”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The context permissions configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The context permissions configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Context permissions — Build Context & .dockerignore: behaves differently than configured”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Context permissions — Build Context & .dockerignore: behaves differently than configured: evidence found\nFor “Context permissions — Build Context & .dockerignore: behaves differently than configured”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Context permissions — Build Context & .dockerignore: behaves differently than configured”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Context permissions — Build Context & .dockerignore: behaves differently than configured: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Context permissions — Build Context & .dockerignore: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-294",
    "technology": "Docker",
    "category": "Build Context & .dockerignore",
    "title": "Context permissions — Build Context & .dockerignore: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective context permissions state first, then narrow the failure through the build context & .dockerignore dependency chain.",
    "aliases": [
      "context permissions",
      "Build Context & .dockerignore",
      "Context permissions — Build Context & .dockerignore: fails intermittently under load or restart",
      "context",
      "permissions",
      "build",
      "dockerignore",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Run build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Capture the effective state relevant to “Context permissions — Build Context & .dockerignore: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Run build checks",
        "command": "docker buildx build --check -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected context permissions; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List build targets",
        "command": "docker buildx build --call=targets -f <dockerfile> <context>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Context permissions — Build Context & .dockerignore: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Context permissions — Build Context & .dockerignore: fails intermittently under load or restart”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
      "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for context permissions",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The context permissions configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Context permissions — Build Context & .dockerignore: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the context permissions inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Context permissions — Build Context & .dockerignore: fails intermittently under load or restart”",
      "Effective context permissions configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Build Context & .dockerignore Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Context permissions — Build Context & .dockerignore: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx build --check -f Dockerfile .",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --call=targets -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Context permissions — Build Context & .dockerignore: fails intermittently under load or restart”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Build Context & .dockerignore path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Context permissions — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for context permissions",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for context permissions\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for context permissions\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --check -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for context permissions",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Context permissions — Build Context & .dockerignore: fails intermittently under load or restart”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Context permissions — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The context permissions configuration is correct but the failure remains",
        "command": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The context permissions configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --check -f Dockerfile .",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The context permissions configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --call=targets -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The context permissions configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Context permissions — Build Context & .dockerignore: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --call=targets -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx inspect --bootstrap",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for context permissions",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for context permissions\nThe effective runtime value differs from expectation.",
        "root": "For “Context permissions — Build Context & .dockerignore: fails intermittently under load or restart”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The context permissions configuration is correct but the failure remains",
        "check": "docker buildx build --check -f Dockerfile .",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The context permissions configuration is correct but the failure remains",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --call=targets -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Context permissions — Build Context & .dockerignore: fails intermittently under load or restart”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Context permissions — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nFor “Context permissions — Build Context & .dockerignore: fails intermittently under load or restart”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Context permissions — Build Context & .dockerignore: fails intermittently under load or restart”, the effective context permissions state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --check -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "check": "docker buildx build --check -f Dockerfile prod-gke",
        "output": "Context permissions — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nA build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "root": "A build context & .dockerignore dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the context permissions path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --call=targets -f Dockerfile prod-gke",
        "output": "Context permissions — Build Context & .dockerignore: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-295",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "BuildKit cache reuse — BuildKit & Cache: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective BuildKit cache reuse state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "BuildKit cache reuse",
      "BuildKit & Cache",
      "BuildKit cache reuse — BuildKit & Cache: fails completely",
      "buildkit",
      "cache",
      "reuse",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “BuildKit cache reuse — BuildKit & Cache: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected BuildKit cache reuse; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “BuildKit cache reuse — BuildKit & Cache: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “BuildKit cache reuse — BuildKit & Cache: fails completely”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for BuildKit cache reuse",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The BuildKit cache reuse configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “BuildKit cache reuse — BuildKit & Cache: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the BuildKit cache reuse inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “BuildKit cache reuse — BuildKit & Cache: fails completely”",
      "Effective BuildKit cache reuse configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: BuildKit cache reuse — BuildKit & Cache: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “BuildKit cache reuse — BuildKit & Cache: fails completely”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "BuildKit cache reuse — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “BuildKit cache reuse — BuildKit & Cache: fails completely”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "BuildKit cache reuse — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The BuildKit cache reuse configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BuildKit cache reuse configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BuildKit cache reuse configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The BuildKit cache reuse configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "BuildKit cache reuse — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for BuildKit cache reuse",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse\nThe effective runtime value differs from expectation.",
        "root": "For “BuildKit cache reuse — BuildKit & Cache: fails completely”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The BuildKit cache reuse configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BuildKit cache reuse configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “BuildKit cache reuse — BuildKit & Cache: fails completely”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "BuildKit cache reuse — BuildKit & Cache: fails completely: evidence found\nFor “BuildKit cache reuse — BuildKit & Cache: fails completely”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “BuildKit cache reuse — BuildKit & Cache: fails completely”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "BuildKit cache reuse — BuildKit & Cache: fails completely: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "BuildKit cache reuse — BuildKit & Cache: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-296",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "BuildKit cache reuse — BuildKit & Cache: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective BuildKit cache reuse state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "BuildKit cache reuse",
      "BuildKit & Cache",
      "BuildKit cache reuse — BuildKit & Cache: behaves differently than configured",
      "buildkit",
      "cache",
      "reuse",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “BuildKit cache reuse — BuildKit & Cache: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected BuildKit cache reuse; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “BuildKit cache reuse — BuildKit & Cache: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “BuildKit cache reuse — BuildKit & Cache: behaves differently than configured”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for BuildKit cache reuse",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The BuildKit cache reuse configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “BuildKit cache reuse — BuildKit & Cache: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the BuildKit cache reuse inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “BuildKit cache reuse — BuildKit & Cache: behaves differently than configured”",
      "Effective BuildKit cache reuse configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: BuildKit cache reuse — BuildKit & Cache: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “BuildKit cache reuse — BuildKit & Cache: behaves differently than configured”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "BuildKit cache reuse — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “BuildKit cache reuse — BuildKit & Cache: behaves differently than configured”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "BuildKit cache reuse — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The BuildKit cache reuse configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BuildKit cache reuse configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BuildKit cache reuse configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The BuildKit cache reuse configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "BuildKit cache reuse — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for BuildKit cache reuse",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse\nThe effective runtime value differs from expectation.",
        "root": "For “BuildKit cache reuse — BuildKit & Cache: behaves differently than configured”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The BuildKit cache reuse configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BuildKit cache reuse configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “BuildKit cache reuse — BuildKit & Cache: behaves differently than configured”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "BuildKit cache reuse — BuildKit & Cache: behaves differently than configured: evidence found\nFor “BuildKit cache reuse — BuildKit & Cache: behaves differently than configured”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “BuildKit cache reuse — BuildKit & Cache: behaves differently than configured”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "BuildKit cache reuse — BuildKit & Cache: behaves differently than configured: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "BuildKit cache reuse — BuildKit & Cache: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-297",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective BuildKit cache reuse state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "BuildKit cache reuse",
      "BuildKit & Cache",
      "BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart",
      "buildkit",
      "cache",
      "reuse",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected BuildKit cache reuse; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for BuildKit cache reuse",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The BuildKit cache reuse configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the BuildKit cache reuse inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart”",
      "Effective BuildKit cache reuse configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The BuildKit cache reuse configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BuildKit cache reuse configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BuildKit cache reuse configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The BuildKit cache reuse configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for BuildKit cache reuse",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BuildKit cache reuse\nThe effective runtime value differs from expectation.",
        "root": "For “BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The BuildKit cache reuse configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BuildKit cache reuse configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart: evidence found\nFor “BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart”, the effective BuildKit cache reuse state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BuildKit cache reuse path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "BuildKit cache reuse — BuildKit & Cache: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-298",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "Cache invalidation — BuildKit & Cache: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective cache invalidation state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "cache invalidation",
      "BuildKit & Cache",
      "Cache invalidation — BuildKit & Cache: fails completely",
      "cache",
      "invalidation",
      "buildkit",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Cache invalidation — BuildKit & Cache: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected cache invalidation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “Cache invalidation — BuildKit & Cache: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Cache invalidation — BuildKit & Cache: fails completely”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for cache invalidation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The cache invalidation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Cache invalidation — BuildKit & Cache: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the cache invalidation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Cache invalidation — BuildKit & Cache: fails completely”",
      "Effective cache invalidation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Cache invalidation — BuildKit & Cache: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Cache invalidation — BuildKit & Cache: fails completely”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Cache invalidation — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for cache invalidation",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache invalidation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache invalidation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for cache invalidation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Cache invalidation — BuildKit & Cache: fails completely”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Cache invalidation — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The cache invalidation configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache invalidation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache invalidation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The cache invalidation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Cache invalidation — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for cache invalidation",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache invalidation\nThe effective runtime value differs from expectation.",
        "root": "For “Cache invalidation — BuildKit & Cache: fails completely”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The cache invalidation configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache invalidation configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Cache invalidation — BuildKit & Cache: fails completely”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Cache invalidation — BuildKit & Cache: fails completely: evidence found\nFor “Cache invalidation — BuildKit & Cache: fails completely”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Cache invalidation — BuildKit & Cache: fails completely”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "Cache invalidation — BuildKit & Cache: fails completely: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Cache invalidation — BuildKit & Cache: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-299",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "Cache invalidation — BuildKit & Cache: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective cache invalidation state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "cache invalidation",
      "BuildKit & Cache",
      "Cache invalidation — BuildKit & Cache: behaves differently than configured",
      "cache",
      "invalidation",
      "buildkit",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Cache invalidation — BuildKit & Cache: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected cache invalidation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “Cache invalidation — BuildKit & Cache: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Cache invalidation — BuildKit & Cache: behaves differently than configured”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for cache invalidation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The cache invalidation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Cache invalidation — BuildKit & Cache: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the cache invalidation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Cache invalidation — BuildKit & Cache: behaves differently than configured”",
      "Effective cache invalidation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Cache invalidation — BuildKit & Cache: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Cache invalidation — BuildKit & Cache: behaves differently than configured”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Cache invalidation — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for cache invalidation",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache invalidation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache invalidation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for cache invalidation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Cache invalidation — BuildKit & Cache: behaves differently than configured”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Cache invalidation — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The cache invalidation configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache invalidation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache invalidation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The cache invalidation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Cache invalidation — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for cache invalidation",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache invalidation\nThe effective runtime value differs from expectation.",
        "root": "For “Cache invalidation — BuildKit & Cache: behaves differently than configured”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The cache invalidation configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache invalidation configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Cache invalidation — BuildKit & Cache: behaves differently than configured”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Cache invalidation — BuildKit & Cache: behaves differently than configured: evidence found\nFor “Cache invalidation — BuildKit & Cache: behaves differently than configured”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Cache invalidation — BuildKit & Cache: behaves differently than configured”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "Cache invalidation — BuildKit & Cache: behaves differently than configured: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Cache invalidation — BuildKit & Cache: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-300",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "Cache invalidation — BuildKit & Cache: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective cache invalidation state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "cache invalidation",
      "BuildKit & Cache",
      "Cache invalidation — BuildKit & Cache: fails intermittently under load or restart",
      "cache",
      "invalidation",
      "buildkit",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Cache invalidation — BuildKit & Cache: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected cache invalidation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “Cache invalidation — BuildKit & Cache: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Cache invalidation — BuildKit & Cache: fails intermittently under load or restart”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for cache invalidation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The cache invalidation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Cache invalidation — BuildKit & Cache: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the cache invalidation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Cache invalidation — BuildKit & Cache: fails intermittently under load or restart”",
      "Effective cache invalidation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Cache invalidation — BuildKit & Cache: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Cache invalidation — BuildKit & Cache: fails intermittently under load or restart”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Cache invalidation — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for cache invalidation",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache invalidation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache invalidation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for cache invalidation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Cache invalidation — BuildKit & Cache: fails intermittently under load or restart”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Cache invalidation — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The cache invalidation configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache invalidation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache invalidation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The cache invalidation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Cache invalidation — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for cache invalidation",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache invalidation\nThe effective runtime value differs from expectation.",
        "root": "For “Cache invalidation — BuildKit & Cache: fails intermittently under load or restart”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The cache invalidation configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache invalidation configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Cache invalidation — BuildKit & Cache: fails intermittently under load or restart”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Cache invalidation — BuildKit & Cache: fails intermittently under load or restart: evidence found\nFor “Cache invalidation — BuildKit & Cache: fails intermittently under load or restart”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Cache invalidation — BuildKit & Cache: fails intermittently under load or restart”, the effective cache invalidation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "Cache invalidation — BuildKit & Cache: fails intermittently under load or restart: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache invalidation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Cache invalidation — BuildKit & Cache: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-301",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "Cache mount — BuildKit & Cache: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective cache mount state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "cache mount",
      "BuildKit & Cache",
      "Cache mount — BuildKit & Cache: fails completely",
      "cache",
      "mount",
      "buildkit",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Cache mount — BuildKit & Cache: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected cache mount; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “Cache mount — BuildKit & Cache: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Cache mount — BuildKit & Cache: fails completely”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for cache mount",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The cache mount configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Cache mount — BuildKit & Cache: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the cache mount inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Cache mount — BuildKit & Cache: fails completely”",
      "Effective cache mount configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Cache mount — BuildKit & Cache: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Cache mount — BuildKit & Cache: fails completely”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Cache mount — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for cache mount",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache mount\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache mount\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for cache mount",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Cache mount — BuildKit & Cache: fails completely”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Cache mount — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The cache mount configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache mount configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache mount configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The cache mount configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Cache mount — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for cache mount",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache mount\nThe effective runtime value differs from expectation.",
        "root": "For “Cache mount — BuildKit & Cache: fails completely”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The cache mount configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache mount configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Cache mount — BuildKit & Cache: fails completely”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Cache mount — BuildKit & Cache: fails completely: evidence found\nFor “Cache mount — BuildKit & Cache: fails completely”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Cache mount — BuildKit & Cache: fails completely”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "Cache mount — BuildKit & Cache: fails completely: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Cache mount — BuildKit & Cache: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-302",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "Cache mount — BuildKit & Cache: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective cache mount state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "cache mount",
      "BuildKit & Cache",
      "Cache mount — BuildKit & Cache: behaves differently than configured",
      "cache",
      "mount",
      "buildkit",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Cache mount — BuildKit & Cache: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected cache mount; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “Cache mount — BuildKit & Cache: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Cache mount — BuildKit & Cache: behaves differently than configured”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for cache mount",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The cache mount configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Cache mount — BuildKit & Cache: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the cache mount inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Cache mount — BuildKit & Cache: behaves differently than configured”",
      "Effective cache mount configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Cache mount — BuildKit & Cache: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Cache mount — BuildKit & Cache: behaves differently than configured”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Cache mount — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for cache mount",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache mount\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache mount\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for cache mount",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Cache mount — BuildKit & Cache: behaves differently than configured”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Cache mount — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The cache mount configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache mount configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache mount configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The cache mount configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Cache mount — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for cache mount",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache mount\nThe effective runtime value differs from expectation.",
        "root": "For “Cache mount — BuildKit & Cache: behaves differently than configured”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The cache mount configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache mount configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Cache mount — BuildKit & Cache: behaves differently than configured”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Cache mount — BuildKit & Cache: behaves differently than configured: evidence found\nFor “Cache mount — BuildKit & Cache: behaves differently than configured”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Cache mount — BuildKit & Cache: behaves differently than configured”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "Cache mount — BuildKit & Cache: behaves differently than configured: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Cache mount — BuildKit & Cache: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-303",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "Cache mount — BuildKit & Cache: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective cache mount state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "cache mount",
      "BuildKit & Cache",
      "Cache mount — BuildKit & Cache: fails intermittently under load or restart",
      "cache",
      "mount",
      "buildkit",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Cache mount — BuildKit & Cache: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected cache mount; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “Cache mount — BuildKit & Cache: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Cache mount — BuildKit & Cache: fails intermittently under load or restart”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for cache mount",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The cache mount configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Cache mount — BuildKit & Cache: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the cache mount inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Cache mount — BuildKit & Cache: fails intermittently under load or restart”",
      "Effective cache mount configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Cache mount — BuildKit & Cache: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Cache mount — BuildKit & Cache: fails intermittently under load or restart”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Cache mount — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for cache mount",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache mount\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache mount\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for cache mount",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Cache mount — BuildKit & Cache: fails intermittently under load or restart”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Cache mount — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The cache mount configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache mount configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache mount configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The cache mount configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Cache mount — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for cache mount",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for cache mount\nThe effective runtime value differs from expectation.",
        "root": "For “Cache mount — BuildKit & Cache: fails intermittently under load or restart”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The cache mount configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The cache mount configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Cache mount — BuildKit & Cache: fails intermittently under load or restart”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Cache mount — BuildKit & Cache: fails intermittently under load or restart: evidence found\nFor “Cache mount — BuildKit & Cache: fails intermittently under load or restart”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Cache mount — BuildKit & Cache: fails intermittently under load or restart”, the effective cache mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "Cache mount — BuildKit & Cache: fails intermittently under load or restart: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the cache mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Cache mount — BuildKit & Cache: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-304",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "External cache import/export — BuildKit & Cache: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective external cache import/export state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "external cache import/export",
      "BuildKit & Cache",
      "External cache import/export — BuildKit & Cache: fails completely",
      "external",
      "cache",
      "import",
      "export",
      "buildkit",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “External cache import/export — BuildKit & Cache: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected external cache import/export; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “External cache import/export — BuildKit & Cache: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “External cache import/export — BuildKit & Cache: fails completely”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for external cache import/export",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The external cache import/export configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “External cache import/export — BuildKit & Cache: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the external cache import/export inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “External cache import/export — BuildKit & Cache: fails completely”",
      "Effective external cache import/export configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: External cache import/export — BuildKit & Cache: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “External cache import/export — BuildKit & Cache: fails completely”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "External cache import/export — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for external cache import/export",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external cache import/export\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external cache import/export\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for external cache import/export",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “External cache import/export — BuildKit & Cache: fails completely”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "External cache import/export — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The external cache import/export configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external cache import/export configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external cache import/export configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The external cache import/export configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "External cache import/export — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for external cache import/export",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external cache import/export\nThe effective runtime value differs from expectation.",
        "root": "For “External cache import/export — BuildKit & Cache: fails completely”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The external cache import/export configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external cache import/export configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “External cache import/export — BuildKit & Cache: fails completely”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "External cache import/export — BuildKit & Cache: fails completely: evidence found\nFor “External cache import/export — BuildKit & Cache: fails completely”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “External cache import/export — BuildKit & Cache: fails completely”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "External cache import/export — BuildKit & Cache: fails completely: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "External cache import/export — BuildKit & Cache: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-305",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "External cache import/export — BuildKit & Cache: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective external cache import/export state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "external cache import/export",
      "BuildKit & Cache",
      "External cache import/export — BuildKit & Cache: behaves differently than configured",
      "external",
      "cache",
      "import",
      "export",
      "buildkit",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “External cache import/export — BuildKit & Cache: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected external cache import/export; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “External cache import/export — BuildKit & Cache: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “External cache import/export — BuildKit & Cache: behaves differently than configured”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for external cache import/export",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The external cache import/export configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “External cache import/export — BuildKit & Cache: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the external cache import/export inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “External cache import/export — BuildKit & Cache: behaves differently than configured”",
      "Effective external cache import/export configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: External cache import/export — BuildKit & Cache: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “External cache import/export — BuildKit & Cache: behaves differently than configured”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "External cache import/export — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for external cache import/export",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external cache import/export\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external cache import/export\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for external cache import/export",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “External cache import/export — BuildKit & Cache: behaves differently than configured”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "External cache import/export — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The external cache import/export configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external cache import/export configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external cache import/export configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The external cache import/export configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "External cache import/export — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for external cache import/export",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external cache import/export\nThe effective runtime value differs from expectation.",
        "root": "For “External cache import/export — BuildKit & Cache: behaves differently than configured”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The external cache import/export configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external cache import/export configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “External cache import/export — BuildKit & Cache: behaves differently than configured”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "External cache import/export — BuildKit & Cache: behaves differently than configured: evidence found\nFor “External cache import/export — BuildKit & Cache: behaves differently than configured”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “External cache import/export — BuildKit & Cache: behaves differently than configured”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "External cache import/export — BuildKit & Cache: behaves differently than configured: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "External cache import/export — BuildKit & Cache: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-306",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "External cache import/export — BuildKit & Cache: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective external cache import/export state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "external cache import/export",
      "BuildKit & Cache",
      "External cache import/export — BuildKit & Cache: fails intermittently under load or restart",
      "external",
      "cache",
      "import",
      "export",
      "buildkit",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “External cache import/export — BuildKit & Cache: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected external cache import/export; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “External cache import/export — BuildKit & Cache: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “External cache import/export — BuildKit & Cache: fails intermittently under load or restart”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for external cache import/export",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The external cache import/export configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “External cache import/export — BuildKit & Cache: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the external cache import/export inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “External cache import/export — BuildKit & Cache: fails intermittently under load or restart”",
      "Effective external cache import/export configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: External cache import/export — BuildKit & Cache: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “External cache import/export — BuildKit & Cache: fails intermittently under load or restart”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "External cache import/export — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for external cache import/export",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external cache import/export\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external cache import/export\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for external cache import/export",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “External cache import/export — BuildKit & Cache: fails intermittently under load or restart”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "External cache import/export — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The external cache import/export configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external cache import/export configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external cache import/export configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The external cache import/export configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "External cache import/export — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for external cache import/export",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for external cache import/export\nThe effective runtime value differs from expectation.",
        "root": "For “External cache import/export — BuildKit & Cache: fails intermittently under load or restart”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The external cache import/export configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The external cache import/export configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “External cache import/export — BuildKit & Cache: fails intermittently under load or restart”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "External cache import/export — BuildKit & Cache: fails intermittently under load or restart: evidence found\nFor “External cache import/export — BuildKit & Cache: fails intermittently under load or restart”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “External cache import/export — BuildKit & Cache: fails intermittently under load or restart”, the effective external cache import/export state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "External cache import/export — BuildKit & Cache: fails intermittently under load or restart: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the external cache import/export path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "External cache import/export — BuildKit & Cache: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-307",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "Build secret — BuildKit & Cache: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective build secret state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "build secret",
      "BuildKit & Cache",
      "Build secret — BuildKit & Cache: fails completely",
      "build",
      "secret",
      "buildkit",
      "cache",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Build secret — BuildKit & Cache: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected build secret; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “Build secret — BuildKit & Cache: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Build secret — BuildKit & Cache: fails completely”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for build secret",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The build secret configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Build secret — BuildKit & Cache: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the build secret inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Build secret — BuildKit & Cache: fails completely”",
      "Effective build secret configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Build secret — BuildKit & Cache: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Build secret — BuildKit & Cache: fails completely”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Build secret — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for build secret",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build secret\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build secret\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for build secret",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Build secret — BuildKit & Cache: fails completely”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Build secret — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The build secret configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build secret configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build secret configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The build secret configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Build secret — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for build secret",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build secret\nThe effective runtime value differs from expectation.",
        "root": "For “Build secret — BuildKit & Cache: fails completely”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The build secret configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build secret configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Build secret — BuildKit & Cache: fails completely”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Build secret — BuildKit & Cache: fails completely: evidence found\nFor “Build secret — BuildKit & Cache: fails completely”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Build secret — BuildKit & Cache: fails completely”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "Build secret — BuildKit & Cache: fails completely: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Build secret — BuildKit & Cache: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-308",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "Build secret — BuildKit & Cache: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective build secret state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "build secret",
      "BuildKit & Cache",
      "Build secret — BuildKit & Cache: behaves differently than configured",
      "build",
      "secret",
      "buildkit",
      "cache",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Build secret — BuildKit & Cache: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected build secret; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “Build secret — BuildKit & Cache: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Build secret — BuildKit & Cache: behaves differently than configured”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for build secret",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The build secret configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Build secret — BuildKit & Cache: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the build secret inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Build secret — BuildKit & Cache: behaves differently than configured”",
      "Effective build secret configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Build secret — BuildKit & Cache: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Build secret — BuildKit & Cache: behaves differently than configured”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Build secret — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for build secret",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build secret\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build secret\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for build secret",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Build secret — BuildKit & Cache: behaves differently than configured”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Build secret — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The build secret configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build secret configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build secret configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The build secret configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Build secret — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for build secret",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build secret\nThe effective runtime value differs from expectation.",
        "root": "For “Build secret — BuildKit & Cache: behaves differently than configured”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The build secret configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build secret configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Build secret — BuildKit & Cache: behaves differently than configured”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Build secret — BuildKit & Cache: behaves differently than configured: evidence found\nFor “Build secret — BuildKit & Cache: behaves differently than configured”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Build secret — BuildKit & Cache: behaves differently than configured”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "Build secret — BuildKit & Cache: behaves differently than configured: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Build secret — BuildKit & Cache: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-309",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "Build secret — BuildKit & Cache: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective build secret state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "build secret",
      "BuildKit & Cache",
      "Build secret — BuildKit & Cache: fails intermittently under load or restart",
      "build",
      "secret",
      "buildkit",
      "cache",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Build secret — BuildKit & Cache: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected build secret; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “Build secret — BuildKit & Cache: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Build secret — BuildKit & Cache: fails intermittently under load or restart”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for build secret",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The build secret configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Build secret — BuildKit & Cache: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the build secret inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Build secret — BuildKit & Cache: fails intermittently under load or restart”",
      "Effective build secret configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Build secret — BuildKit & Cache: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Build secret — BuildKit & Cache: fails intermittently under load or restart”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Build secret — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for build secret",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build secret\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build secret\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for build secret",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Build secret — BuildKit & Cache: fails intermittently under load or restart”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Build secret — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The build secret configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build secret configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build secret configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The build secret configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Build secret — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for build secret",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build secret\nThe effective runtime value differs from expectation.",
        "root": "For “Build secret — BuildKit & Cache: fails intermittently under load or restart”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The build secret configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build secret configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Build secret — BuildKit & Cache: fails intermittently under load or restart”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Build secret — BuildKit & Cache: fails intermittently under load or restart: evidence found\nFor “Build secret — BuildKit & Cache: fails intermittently under load or restart”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Build secret — BuildKit & Cache: fails intermittently under load or restart”, the effective build secret state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "Build secret — BuildKit & Cache: fails intermittently under load or restart: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build secret path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Build secret — BuildKit & Cache: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-310",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "SSH mount — BuildKit & Cache: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective SSH mount state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "SSH mount",
      "BuildKit & Cache",
      "SSH mount — BuildKit & Cache: fails completely",
      "ssh",
      "mount",
      "buildkit",
      "cache",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “SSH mount — BuildKit & Cache: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected SSH mount; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “SSH mount — BuildKit & Cache: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “SSH mount — BuildKit & Cache: fails completely”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for SSH mount",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The SSH mount configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “SSH mount — BuildKit & Cache: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the SSH mount inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “SSH mount — BuildKit & Cache: fails completely”",
      "Effective SSH mount configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: SSH mount — BuildKit & Cache: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “SSH mount — BuildKit & Cache: fails completely”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "SSH mount — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for SSH mount",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for SSH mount\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for SSH mount\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for SSH mount",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “SSH mount — BuildKit & Cache: fails completely”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "SSH mount — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The SSH mount configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The SSH mount configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The SSH mount configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The SSH mount configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "SSH mount — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for SSH mount",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for SSH mount\nThe effective runtime value differs from expectation.",
        "root": "For “SSH mount — BuildKit & Cache: fails completely”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The SSH mount configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The SSH mount configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “SSH mount — BuildKit & Cache: fails completely”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "SSH mount — BuildKit & Cache: fails completely: evidence found\nFor “SSH mount — BuildKit & Cache: fails completely”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “SSH mount — BuildKit & Cache: fails completely”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "SSH mount — BuildKit & Cache: fails completely: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "SSH mount — BuildKit & Cache: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-311",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "SSH mount — BuildKit & Cache: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective SSH mount state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "SSH mount",
      "BuildKit & Cache",
      "SSH mount — BuildKit & Cache: behaves differently than configured",
      "ssh",
      "mount",
      "buildkit",
      "cache",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “SSH mount — BuildKit & Cache: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected SSH mount; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “SSH mount — BuildKit & Cache: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “SSH mount — BuildKit & Cache: behaves differently than configured”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for SSH mount",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The SSH mount configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “SSH mount — BuildKit & Cache: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the SSH mount inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “SSH mount — BuildKit & Cache: behaves differently than configured”",
      "Effective SSH mount configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: SSH mount — BuildKit & Cache: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “SSH mount — BuildKit & Cache: behaves differently than configured”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "SSH mount — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for SSH mount",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for SSH mount\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for SSH mount\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for SSH mount",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “SSH mount — BuildKit & Cache: behaves differently than configured”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "SSH mount — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The SSH mount configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The SSH mount configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The SSH mount configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The SSH mount configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "SSH mount — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for SSH mount",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for SSH mount\nThe effective runtime value differs from expectation.",
        "root": "For “SSH mount — BuildKit & Cache: behaves differently than configured”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The SSH mount configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The SSH mount configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “SSH mount — BuildKit & Cache: behaves differently than configured”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "SSH mount — BuildKit & Cache: behaves differently than configured: evidence found\nFor “SSH mount — BuildKit & Cache: behaves differently than configured”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “SSH mount — BuildKit & Cache: behaves differently than configured”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "SSH mount — BuildKit & Cache: behaves differently than configured: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "SSH mount — BuildKit & Cache: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-312",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "SSH mount — BuildKit & Cache: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective SSH mount state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "SSH mount",
      "BuildKit & Cache",
      "SSH mount — BuildKit & Cache: fails intermittently under load or restart",
      "ssh",
      "mount",
      "buildkit",
      "cache",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “SSH mount — BuildKit & Cache: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected SSH mount; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “SSH mount — BuildKit & Cache: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “SSH mount — BuildKit & Cache: fails intermittently under load or restart”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for SSH mount",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The SSH mount configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “SSH mount — BuildKit & Cache: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the SSH mount inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “SSH mount — BuildKit & Cache: fails intermittently under load or restart”",
      "Effective SSH mount configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: SSH mount — BuildKit & Cache: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “SSH mount — BuildKit & Cache: fails intermittently under load or restart”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "SSH mount — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for SSH mount",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for SSH mount\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for SSH mount\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for SSH mount",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “SSH mount — BuildKit & Cache: fails intermittently under load or restart”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "SSH mount — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The SSH mount configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The SSH mount configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The SSH mount configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The SSH mount configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "SSH mount — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for SSH mount",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for SSH mount\nThe effective runtime value differs from expectation.",
        "root": "For “SSH mount — BuildKit & Cache: fails intermittently under load or restart”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The SSH mount configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The SSH mount configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “SSH mount — BuildKit & Cache: fails intermittently under load or restart”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "SSH mount — BuildKit & Cache: fails intermittently under load or restart: evidence found\nFor “SSH mount — BuildKit & Cache: fails intermittently under load or restart”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “SSH mount — BuildKit & Cache: fails intermittently under load or restart”, the effective SSH mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "SSH mount — BuildKit & Cache: fails intermittently under load or restart: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the SSH mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "SSH mount — BuildKit & Cache: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-313",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "Build result output — BuildKit & Cache: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective build result output state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "build result output",
      "BuildKit & Cache",
      "Build result output — BuildKit & Cache: fails completely",
      "build",
      "result",
      "output",
      "buildkit",
      "cache",
      "fails",
      "completely"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Build result output — BuildKit & Cache: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected build result output; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “Build result output — BuildKit & Cache: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Build result output — BuildKit & Cache: fails completely”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for build result output",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The build result output configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Build result output — BuildKit & Cache: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the build result output inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Build result output — BuildKit & Cache: fails completely”",
      "Effective build result output configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Build result output — BuildKit & Cache: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Build result output — BuildKit & Cache: fails completely”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Build result output — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for build result output",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build result output\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build result output\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for build result output",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Build result output — BuildKit & Cache: fails completely”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Build result output — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The build result output configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build result output configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build result output configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The build result output configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Build result output — BuildKit & Cache: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for build result output",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build result output\nThe effective runtime value differs from expectation.",
        "root": "For “Build result output — BuildKit & Cache: fails completely”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The build result output configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build result output configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Build result output — BuildKit & Cache: fails completely”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Build result output — BuildKit & Cache: fails completely: evidence found\nFor “Build result output — BuildKit & Cache: fails completely”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Build result output — BuildKit & Cache: fails completely”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "Build result output — BuildKit & Cache: fails completely: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Build result output — BuildKit & Cache: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-314",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "Build result output — BuildKit & Cache: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective build result output state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "build result output",
      "BuildKit & Cache",
      "Build result output — BuildKit & Cache: behaves differently than configured",
      "build",
      "result",
      "output",
      "buildkit",
      "cache",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Build result output — BuildKit & Cache: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected build result output; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “Build result output — BuildKit & Cache: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Build result output — BuildKit & Cache: behaves differently than configured”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for build result output",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The build result output configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Build result output — BuildKit & Cache: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the build result output inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Build result output — BuildKit & Cache: behaves differently than configured”",
      "Effective build result output configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Build result output — BuildKit & Cache: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Build result output — BuildKit & Cache: behaves differently than configured”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Build result output — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for build result output",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build result output\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build result output\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for build result output",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Build result output — BuildKit & Cache: behaves differently than configured”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Build result output — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The build result output configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build result output configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build result output configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The build result output configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Build result output — BuildKit & Cache: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for build result output",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build result output\nThe effective runtime value differs from expectation.",
        "root": "For “Build result output — BuildKit & Cache: behaves differently than configured”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The build result output configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build result output configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Build result output — BuildKit & Cache: behaves differently than configured”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Build result output — BuildKit & Cache: behaves differently than configured: evidence found\nFor “Build result output — BuildKit & Cache: behaves differently than configured”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Build result output — BuildKit & Cache: behaves differently than configured”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "Build result output — BuildKit & Cache: behaves differently than configured: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Build result output — BuildKit & Cache: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-315",
    "technology": "Docker",
    "category": "BuildKit & Cache",
    "title": "Build result output — BuildKit & Cache: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective build result output state first, then narrow the failure through the buildkit & cache dependency chain.",
    "aliases": [
      "build result output",
      "BuildKit & Cache",
      "Build result output — BuildKit & Cache: fails intermittently under load or restart",
      "build",
      "result",
      "output",
      "buildkit",
      "cache",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "Inspect builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Build result output — BuildKit & Cache: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect cache usage",
        "command": "docker buildx du",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected build result output; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Build with plain progress",
        "command": "docker buildx build --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Use this output to decide whether “Build result output — BuildKit & Cache: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Compare without cache",
        "command": "docker buildx build --no-cache --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Build result output — BuildKit & Cache: fails intermittently under load or restart”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for build result output",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The build result output configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Build result output — BuildKit & Cache: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the build result output inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Build result output — BuildKit & Cache: fails intermittently under load or restart”",
      "Effective build result output configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant BuildKit & Cache Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Build result output — BuildKit & Cache: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx inspect --bootstrap",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx du",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx build --progress=plain -f Dockerfile .",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Build result output — BuildKit & Cache: fails intermittently under load or restart”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the BuildKit & Cache path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Build result output — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for build result output",
        "command": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build result output\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build result output\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx du",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for build result output",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Build result output — BuildKit & Cache: fails intermittently under load or restart”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Build result output — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The build result output configuration is correct but the failure remains",
        "command": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build result output configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx du",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build result output configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The build result output configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Build result output — BuildKit & Cache: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx build --progress=plain -f Dockerfile .",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for build result output",
        "check": "docker buildx inspect --bootstrap",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for build result output\nThe effective runtime value differs from expectation.",
        "root": "For “Build result output — BuildKit & Cache: fails intermittently under load or restart”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The build result output configuration is correct but the failure remains",
        "check": "docker buildx du",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The build result output configuration is correct but the failure remains",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx build --progress=plain -f Dockerfile .",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Build result output — BuildKit & Cache: fails intermittently under load or restart”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Build result output — BuildKit & Cache: fails intermittently under load or restart: evidence found\nFor “Build result output — BuildKit & Cache: fails intermittently under load or restart”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Build result output — BuildKit & Cache: fails intermittently under load or restart”, the effective build result output state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx du",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "check": "docker buildx du",
        "output": "Build result output — BuildKit & Cache: fails intermittently under load or restart: evidence found\nA buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "root": "A buildkit & cache dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the build result output path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx build --progress=plain -f Dockerfile prod-gke",
        "output": "Build result output — BuildKit & Cache: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --no-cache --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-316",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Selected builder — Buildx & Multi-platform: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective selected builder state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "selected builder",
      "Buildx & Multi-platform",
      "Selected builder — Buildx & Multi-platform: fails completely",
      "selected",
      "builder",
      "buildx",
      "multi",
      "platform",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Selected builder — Buildx & Multi-platform: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected selected builder; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Selected builder — Buildx & Multi-platform: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Selected builder — Buildx & Multi-platform: fails completely”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for selected builder",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The selected builder configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Selected builder — Buildx & Multi-platform: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the selected builder inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Selected builder — Buildx & Multi-platform: fails completely”",
      "Effective selected builder configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Selected builder — Buildx & Multi-platform: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Selected builder — Buildx & Multi-platform: fails completely”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Selected builder — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for selected builder",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for selected builder\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for selected builder\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for selected builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Selected builder — Buildx & Multi-platform: fails completely”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Selected builder — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The selected builder configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The selected builder configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The selected builder configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The selected builder configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Selected builder — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for selected builder",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for selected builder\nThe effective runtime value differs from expectation.",
        "root": "For “Selected builder — Buildx & Multi-platform: fails completely”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The selected builder configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The selected builder configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Selected builder — Buildx & Multi-platform: fails completely”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Selected builder — Buildx & Multi-platform: fails completely: evidence found\nFor “Selected builder — Buildx & Multi-platform: fails completely”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Selected builder — Buildx & Multi-platform: fails completely”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Selected builder — Buildx & Multi-platform: fails completely: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Selected builder — Buildx & Multi-platform: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-317",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Selected builder — Buildx & Multi-platform: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective selected builder state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "selected builder",
      "Buildx & Multi-platform",
      "Selected builder — Buildx & Multi-platform: behaves differently than configured",
      "selected",
      "builder",
      "buildx",
      "multi",
      "platform",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Selected builder — Buildx & Multi-platform: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected selected builder; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Selected builder — Buildx & Multi-platform: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Selected builder — Buildx & Multi-platform: behaves differently than configured”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for selected builder",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The selected builder configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Selected builder — Buildx & Multi-platform: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the selected builder inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Selected builder — Buildx & Multi-platform: behaves differently than configured”",
      "Effective selected builder configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Selected builder — Buildx & Multi-platform: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Selected builder — Buildx & Multi-platform: behaves differently than configured”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Selected builder — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for selected builder",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for selected builder\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for selected builder\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for selected builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Selected builder — Buildx & Multi-platform: behaves differently than configured”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Selected builder — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The selected builder configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The selected builder configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The selected builder configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The selected builder configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Selected builder — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for selected builder",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for selected builder\nThe effective runtime value differs from expectation.",
        "root": "For “Selected builder — Buildx & Multi-platform: behaves differently than configured”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The selected builder configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The selected builder configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Selected builder — Buildx & Multi-platform: behaves differently than configured”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Selected builder — Buildx & Multi-platform: behaves differently than configured: evidence found\nFor “Selected builder — Buildx & Multi-platform: behaves differently than configured”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Selected builder — Buildx & Multi-platform: behaves differently than configured”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Selected builder — Buildx & Multi-platform: behaves differently than configured: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Selected builder — Buildx & Multi-platform: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-318",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Selected builder — Buildx & Multi-platform: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective selected builder state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "selected builder",
      "Buildx & Multi-platform",
      "Selected builder — Buildx & Multi-platform: fails intermittently under load or restart",
      "selected",
      "builder",
      "buildx",
      "multi",
      "platform",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Selected builder — Buildx & Multi-platform: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected selected builder; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Selected builder — Buildx & Multi-platform: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Selected builder — Buildx & Multi-platform: fails intermittently under load or restart”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for selected builder",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The selected builder configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Selected builder — Buildx & Multi-platform: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the selected builder inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Selected builder — Buildx & Multi-platform: fails intermittently under load or restart”",
      "Effective selected builder configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Selected builder — Buildx & Multi-platform: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Selected builder — Buildx & Multi-platform: fails intermittently under load or restart”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Selected builder — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for selected builder",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for selected builder\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for selected builder\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for selected builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Selected builder — Buildx & Multi-platform: fails intermittently under load or restart”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Selected builder — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The selected builder configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The selected builder configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The selected builder configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The selected builder configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Selected builder — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for selected builder",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for selected builder\nThe effective runtime value differs from expectation.",
        "root": "For “Selected builder — Buildx & Multi-platform: fails intermittently under load or restart”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The selected builder configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The selected builder configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Selected builder — Buildx & Multi-platform: fails intermittently under load or restart”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Selected builder — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nFor “Selected builder — Buildx & Multi-platform: fails intermittently under load or restart”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Selected builder — Buildx & Multi-platform: fails intermittently under load or restart”, the effective selected builder state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Selected builder — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the selected builder path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Selected builder — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-319",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Builder node — Buildx & Multi-platform: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective builder node state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "builder node",
      "Buildx & Multi-platform",
      "Builder node — Buildx & Multi-platform: fails completely",
      "builder",
      "node",
      "buildx",
      "multi",
      "platform",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Builder node — Buildx & Multi-platform: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected builder node; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Builder node — Buildx & Multi-platform: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Builder node — Buildx & Multi-platform: fails completely”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for builder node",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The builder node configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Builder node — Buildx & Multi-platform: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the builder node inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Builder node — Buildx & Multi-platform: fails completely”",
      "Effective builder node configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Builder node — Buildx & Multi-platform: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Builder node — Buildx & Multi-platform: fails completely”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Builder node — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for builder node",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for builder node\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for builder node\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for builder node",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Builder node — Buildx & Multi-platform: fails completely”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Builder node — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The builder node configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The builder node configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The builder node configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The builder node configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Builder node — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for builder node",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for builder node\nThe effective runtime value differs from expectation.",
        "root": "For “Builder node — Buildx & Multi-platform: fails completely”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The builder node configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The builder node configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Builder node — Buildx & Multi-platform: fails completely”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Builder node — Buildx & Multi-platform: fails completely: evidence found\nFor “Builder node — Buildx & Multi-platform: fails completely”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Builder node — Buildx & Multi-platform: fails completely”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Builder node — Buildx & Multi-platform: fails completely: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Builder node — Buildx & Multi-platform: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-320",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Builder node — Buildx & Multi-platform: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective builder node state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "builder node",
      "Buildx & Multi-platform",
      "Builder node — Buildx & Multi-platform: behaves differently than configured",
      "builder",
      "node",
      "buildx",
      "multi",
      "platform",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Builder node — Buildx & Multi-platform: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected builder node; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Builder node — Buildx & Multi-platform: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Builder node — Buildx & Multi-platform: behaves differently than configured”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for builder node",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The builder node configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Builder node — Buildx & Multi-platform: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the builder node inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Builder node — Buildx & Multi-platform: behaves differently than configured”",
      "Effective builder node configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Builder node — Buildx & Multi-platform: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Builder node — Buildx & Multi-platform: behaves differently than configured”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Builder node — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for builder node",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for builder node\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for builder node\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for builder node",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Builder node — Buildx & Multi-platform: behaves differently than configured”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Builder node — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The builder node configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The builder node configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The builder node configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The builder node configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Builder node — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for builder node",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for builder node\nThe effective runtime value differs from expectation.",
        "root": "For “Builder node — Buildx & Multi-platform: behaves differently than configured”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The builder node configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The builder node configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Builder node — Buildx & Multi-platform: behaves differently than configured”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Builder node — Buildx & Multi-platform: behaves differently than configured: evidence found\nFor “Builder node — Buildx & Multi-platform: behaves differently than configured”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Builder node — Buildx & Multi-platform: behaves differently than configured”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Builder node — Buildx & Multi-platform: behaves differently than configured: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Builder node — Buildx & Multi-platform: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-321",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Builder node — Buildx & Multi-platform: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective builder node state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "builder node",
      "Buildx & Multi-platform",
      "Builder node — Buildx & Multi-platform: fails intermittently under load or restart",
      "builder",
      "node",
      "buildx",
      "multi",
      "platform",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Builder node — Buildx & Multi-platform: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected builder node; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Builder node — Buildx & Multi-platform: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Builder node — Buildx & Multi-platform: fails intermittently under load or restart”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for builder node",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The builder node configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Builder node — Buildx & Multi-platform: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the builder node inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Builder node — Buildx & Multi-platform: fails intermittently under load or restart”",
      "Effective builder node configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Builder node — Buildx & Multi-platform: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Builder node — Buildx & Multi-platform: fails intermittently under load or restart”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Builder node — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for builder node",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for builder node\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for builder node\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for builder node",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Builder node — Buildx & Multi-platform: fails intermittently under load or restart”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Builder node — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The builder node configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The builder node configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The builder node configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The builder node configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Builder node — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for builder node",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for builder node\nThe effective runtime value differs from expectation.",
        "root": "For “Builder node — Buildx & Multi-platform: fails intermittently under load or restart”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The builder node configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The builder node configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Builder node — Buildx & Multi-platform: fails intermittently under load or restart”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Builder node — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nFor “Builder node — Buildx & Multi-platform: fails intermittently under load or restart”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Builder node — Buildx & Multi-platform: fails intermittently under load or restart”, the effective builder node state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Builder node — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the builder node path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Builder node — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-322",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Target platform — Buildx & Multi-platform: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective target platform state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "target platform",
      "Buildx & Multi-platform",
      "Target platform — Buildx & Multi-platform: fails completely",
      "target",
      "platform",
      "buildx",
      "multi",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Target platform — Buildx & Multi-platform: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected target platform; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Target platform — Buildx & Multi-platform: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Target platform — Buildx & Multi-platform: fails completely”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for target platform",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The target platform configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Target platform — Buildx & Multi-platform: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the target platform inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Target platform — Buildx & Multi-platform: fails completely”",
      "Effective target platform configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Target platform — Buildx & Multi-platform: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Target platform — Buildx & Multi-platform: fails completely”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Target platform — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for target platform",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for target platform\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for target platform\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for target platform",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Target platform — Buildx & Multi-platform: fails completely”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Target platform — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The target platform configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The target platform configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The target platform configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The target platform configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Target platform — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for target platform",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for target platform\nThe effective runtime value differs from expectation.",
        "root": "For “Target platform — Buildx & Multi-platform: fails completely”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The target platform configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The target platform configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Target platform — Buildx & Multi-platform: fails completely”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Target platform — Buildx & Multi-platform: fails completely: evidence found\nFor “Target platform — Buildx & Multi-platform: fails completely”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Target platform — Buildx & Multi-platform: fails completely”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Target platform — Buildx & Multi-platform: fails completely: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Target platform — Buildx & Multi-platform: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-323",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Target platform — Buildx & Multi-platform: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective target platform state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "target platform",
      "Buildx & Multi-platform",
      "Target platform — Buildx & Multi-platform: behaves differently than configured",
      "target",
      "platform",
      "buildx",
      "multi",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Target platform — Buildx & Multi-platform: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected target platform; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Target platform — Buildx & Multi-platform: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Target platform — Buildx & Multi-platform: behaves differently than configured”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for target platform",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The target platform configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Target platform — Buildx & Multi-platform: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the target platform inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Target platform — Buildx & Multi-platform: behaves differently than configured”",
      "Effective target platform configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Target platform — Buildx & Multi-platform: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Target platform — Buildx & Multi-platform: behaves differently than configured”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Target platform — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for target platform",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for target platform\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for target platform\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for target platform",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Target platform — Buildx & Multi-platform: behaves differently than configured”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Target platform — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The target platform configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The target platform configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The target platform configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The target platform configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Target platform — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for target platform",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for target platform\nThe effective runtime value differs from expectation.",
        "root": "For “Target platform — Buildx & Multi-platform: behaves differently than configured”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The target platform configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The target platform configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Target platform — Buildx & Multi-platform: behaves differently than configured”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Target platform — Buildx & Multi-platform: behaves differently than configured: evidence found\nFor “Target platform — Buildx & Multi-platform: behaves differently than configured”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Target platform — Buildx & Multi-platform: behaves differently than configured”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Target platform — Buildx & Multi-platform: behaves differently than configured: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Target platform — Buildx & Multi-platform: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-324",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Target platform — Buildx & Multi-platform: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective target platform state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "target platform",
      "Buildx & Multi-platform",
      "Target platform — Buildx & Multi-platform: fails intermittently under load or restart",
      "target",
      "platform",
      "buildx",
      "multi",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Target platform — Buildx & Multi-platform: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected target platform; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Target platform — Buildx & Multi-platform: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Target platform — Buildx & Multi-platform: fails intermittently under load or restart”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for target platform",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The target platform configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Target platform — Buildx & Multi-platform: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the target platform inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Target platform — Buildx & Multi-platform: fails intermittently under load or restart”",
      "Effective target platform configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Target platform — Buildx & Multi-platform: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Target platform — Buildx & Multi-platform: fails intermittently under load or restart”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Target platform — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for target platform",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for target platform\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for target platform\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for target platform",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Target platform — Buildx & Multi-platform: fails intermittently under load or restart”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Target platform — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The target platform configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The target platform configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The target platform configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The target platform configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Target platform — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for target platform",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for target platform\nThe effective runtime value differs from expectation.",
        "root": "For “Target platform — Buildx & Multi-platform: fails intermittently under load or restart”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The target platform configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The target platform configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Target platform — Buildx & Multi-platform: fails intermittently under load or restart”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Target platform — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nFor “Target platform — Buildx & Multi-platform: fails intermittently under load or restart”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Target platform — Buildx & Multi-platform: fails intermittently under load or restart”, the effective target platform state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Target platform — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the target platform path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Target platform — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-325",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "QEMU emulation — Buildx & Multi-platform: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective QEMU emulation state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "QEMU emulation",
      "Buildx & Multi-platform",
      "QEMU emulation — Buildx & Multi-platform: fails completely",
      "qemu",
      "emulation",
      "buildx",
      "multi",
      "platform",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “QEMU emulation — Buildx & Multi-platform: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected QEMU emulation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “QEMU emulation — Buildx & Multi-platform: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “QEMU emulation — Buildx & Multi-platform: fails completely”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for QEMU emulation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The QEMU emulation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “QEMU emulation — Buildx & Multi-platform: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the QEMU emulation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “QEMU emulation — Buildx & Multi-platform: fails completely”",
      "Effective QEMU emulation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: QEMU emulation — Buildx & Multi-platform: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “QEMU emulation — Buildx & Multi-platform: fails completely”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "QEMU emulation — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for QEMU emulation",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for QEMU emulation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for QEMU emulation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for QEMU emulation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “QEMU emulation — Buildx & Multi-platform: fails completely”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "QEMU emulation — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The QEMU emulation configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The QEMU emulation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The QEMU emulation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The QEMU emulation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "QEMU emulation — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for QEMU emulation",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for QEMU emulation\nThe effective runtime value differs from expectation.",
        "root": "For “QEMU emulation — Buildx & Multi-platform: fails completely”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The QEMU emulation configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The QEMU emulation configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “QEMU emulation — Buildx & Multi-platform: fails completely”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "QEMU emulation — Buildx & Multi-platform: fails completely: evidence found\nFor “QEMU emulation — Buildx & Multi-platform: fails completely”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “QEMU emulation — Buildx & Multi-platform: fails completely”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "QEMU emulation — Buildx & Multi-platform: fails completely: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "QEMU emulation — Buildx & Multi-platform: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-326",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "QEMU emulation — Buildx & Multi-platform: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective QEMU emulation state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "QEMU emulation",
      "Buildx & Multi-platform",
      "QEMU emulation — Buildx & Multi-platform: behaves differently than configured",
      "qemu",
      "emulation",
      "buildx",
      "multi",
      "platform",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “QEMU emulation — Buildx & Multi-platform: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected QEMU emulation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “QEMU emulation — Buildx & Multi-platform: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “QEMU emulation — Buildx & Multi-platform: behaves differently than configured”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for QEMU emulation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The QEMU emulation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “QEMU emulation — Buildx & Multi-platform: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the QEMU emulation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “QEMU emulation — Buildx & Multi-platform: behaves differently than configured”",
      "Effective QEMU emulation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: QEMU emulation — Buildx & Multi-platform: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “QEMU emulation — Buildx & Multi-platform: behaves differently than configured”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "QEMU emulation — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for QEMU emulation",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for QEMU emulation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for QEMU emulation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for QEMU emulation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “QEMU emulation — Buildx & Multi-platform: behaves differently than configured”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "QEMU emulation — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The QEMU emulation configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The QEMU emulation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The QEMU emulation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The QEMU emulation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "QEMU emulation — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for QEMU emulation",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for QEMU emulation\nThe effective runtime value differs from expectation.",
        "root": "For “QEMU emulation — Buildx & Multi-platform: behaves differently than configured”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The QEMU emulation configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The QEMU emulation configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “QEMU emulation — Buildx & Multi-platform: behaves differently than configured”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "QEMU emulation — Buildx & Multi-platform: behaves differently than configured: evidence found\nFor “QEMU emulation — Buildx & Multi-platform: behaves differently than configured”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “QEMU emulation — Buildx & Multi-platform: behaves differently than configured”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "QEMU emulation — Buildx & Multi-platform: behaves differently than configured: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "QEMU emulation — Buildx & Multi-platform: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-327",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective QEMU emulation state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "QEMU emulation",
      "Buildx & Multi-platform",
      "QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart",
      "qemu",
      "emulation",
      "buildx",
      "multi",
      "platform",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected QEMU emulation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for QEMU emulation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The QEMU emulation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the QEMU emulation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart”",
      "Effective QEMU emulation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for QEMU emulation",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for QEMU emulation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for QEMU emulation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for QEMU emulation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The QEMU emulation configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The QEMU emulation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The QEMU emulation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The QEMU emulation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for QEMU emulation",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for QEMU emulation\nThe effective runtime value differs from expectation.",
        "root": "For “QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The QEMU emulation configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The QEMU emulation configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nFor “QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart”, the effective QEMU emulation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the QEMU emulation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "QEMU emulation — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-328",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Multi-platform manifest — Buildx & Multi-platform: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective multi-platform manifest state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "multi-platform manifest",
      "Buildx & Multi-platform",
      "Multi-platform manifest — Buildx & Multi-platform: fails completely",
      "multi",
      "platform",
      "manifest",
      "buildx",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Multi-platform manifest — Buildx & Multi-platform: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected multi-platform manifest; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Multi-platform manifest — Buildx & Multi-platform: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Multi-platform manifest — Buildx & Multi-platform: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The multi-platform manifest configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Multi-platform manifest — Buildx & Multi-platform: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the multi-platform manifest inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Multi-platform manifest — Buildx & Multi-platform: fails completely”",
      "Effective multi-platform manifest configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Multi-platform manifest — Buildx & Multi-platform: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Multi-platform manifest — Buildx & Multi-platform: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Multi-platform manifest — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for multi-platform manifest",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Multi-platform manifest — Buildx & Multi-platform: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Multi-platform manifest — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The multi-platform manifest configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The multi-platform manifest configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Multi-platform manifest — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
        "root": "For “Multi-platform manifest — Buildx & Multi-platform: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The multi-platform manifest configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Multi-platform manifest — Buildx & Multi-platform: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Multi-platform manifest — Buildx & Multi-platform: fails completely: evidence found\nFor “Multi-platform manifest — Buildx & Multi-platform: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Multi-platform manifest — Buildx & Multi-platform: fails completely”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Multi-platform manifest — Buildx & Multi-platform: fails completely: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Multi-platform manifest — Buildx & Multi-platform: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-329",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective multi-platform manifest state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "multi-platform manifest",
      "Buildx & Multi-platform",
      "Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured",
      "multi",
      "platform",
      "manifest",
      "buildx",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected multi-platform manifest; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The multi-platform manifest configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the multi-platform manifest inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured”",
      "Effective multi-platform manifest configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for multi-platform manifest",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The multi-platform manifest configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The multi-platform manifest configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
        "root": "For “Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The multi-platform manifest configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured: evidence found\nFor “Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Multi-platform manifest — Buildx & Multi-platform: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-330",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective multi-platform manifest state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "multi-platform manifest",
      "Buildx & Multi-platform",
      "Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart",
      "multi",
      "platform",
      "manifest",
      "buildx",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected multi-platform manifest; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The multi-platform manifest configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the multi-platform manifest inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart”",
      "Effective multi-platform manifest configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for multi-platform manifest",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The multi-platform manifest configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The multi-platform manifest configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for multi-platform manifest",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform manifest\nThe effective runtime value differs from expectation.",
        "root": "For “Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The multi-platform manifest configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform manifest configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nFor “Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform manifest state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform manifest path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Multi-platform manifest — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-331",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective BUILDPLATFORM/TARGETPLATFORM state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "BUILDPLATFORM/TARGETPLATFORM",
      "Buildx & Multi-platform",
      "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely",
      "buildplatform",
      "targetplatform",
      "buildx",
      "multi",
      "platform",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected BUILDPLATFORM/TARGETPLATFORM; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the BUILDPLATFORM/TARGETPLATFORM inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely”",
      "Effective BUILDPLATFORM/TARGETPLATFORM configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM\nThe effective runtime value differs from expectation.",
        "root": "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely: evidence found\nFor “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-332",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective BUILDPLATFORM/TARGETPLATFORM state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "BUILDPLATFORM/TARGETPLATFORM",
      "Buildx & Multi-platform",
      "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured",
      "buildplatform",
      "targetplatform",
      "buildx",
      "multi",
      "platform",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected BUILDPLATFORM/TARGETPLATFORM; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the BUILDPLATFORM/TARGETPLATFORM inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured”",
      "Effective BUILDPLATFORM/TARGETPLATFORM configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM\nThe effective runtime value differs from expectation.",
        "root": "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured: evidence found\nFor “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-333",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective BUILDPLATFORM/TARGETPLATFORM state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "BUILDPLATFORM/TARGETPLATFORM",
      "Buildx & Multi-platform",
      "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart",
      "buildplatform",
      "targetplatform",
      "buildx",
      "multi",
      "platform",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected BUILDPLATFORM/TARGETPLATFORM; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the BUILDPLATFORM/TARGETPLATFORM inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart”",
      "Effective BUILDPLATFORM/TARGETPLATFORM configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for BUILDPLATFORM/TARGETPLATFORM\nThe effective runtime value differs from expectation.",
        "root": "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The BUILDPLATFORM/TARGETPLATFORM configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nFor “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart”, the effective BUILDPLATFORM/TARGETPLATFORM state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the BUILDPLATFORM/TARGETPLATFORM path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "BUILDPLATFORM/TARGETPLATFORM — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-334",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Multi-platform cache — Buildx & Multi-platform: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective multi-platform cache state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "multi-platform cache",
      "Buildx & Multi-platform",
      "Multi-platform cache — Buildx & Multi-platform: fails completely",
      "multi",
      "platform",
      "cache",
      "buildx",
      "fails",
      "completely"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Multi-platform cache — Buildx & Multi-platform: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected multi-platform cache; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Multi-platform cache — Buildx & Multi-platform: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Multi-platform cache — Buildx & Multi-platform: fails completely”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for multi-platform cache",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The multi-platform cache configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Multi-platform cache — Buildx & Multi-platform: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the multi-platform cache inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Multi-platform cache — Buildx & Multi-platform: fails completely”",
      "Effective multi-platform cache configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Multi-platform cache — Buildx & Multi-platform: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Multi-platform cache — Buildx & Multi-platform: fails completely”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Multi-platform cache — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for multi-platform cache",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform cache\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform cache\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for multi-platform cache",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Multi-platform cache — Buildx & Multi-platform: fails completely”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Multi-platform cache — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The multi-platform cache configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform cache configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform cache configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The multi-platform cache configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Multi-platform cache — Buildx & Multi-platform: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for multi-platform cache",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform cache\nThe effective runtime value differs from expectation.",
        "root": "For “Multi-platform cache — Buildx & Multi-platform: fails completely”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The multi-platform cache configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform cache configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Multi-platform cache — Buildx & Multi-platform: fails completely”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Multi-platform cache — Buildx & Multi-platform: fails completely: evidence found\nFor “Multi-platform cache — Buildx & Multi-platform: fails completely”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Multi-platform cache — Buildx & Multi-platform: fails completely”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Multi-platform cache — Buildx & Multi-platform: fails completely: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Multi-platform cache — Buildx & Multi-platform: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-335",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Multi-platform cache — Buildx & Multi-platform: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective multi-platform cache state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "multi-platform cache",
      "Buildx & Multi-platform",
      "Multi-platform cache — Buildx & Multi-platform: behaves differently than configured",
      "multi",
      "platform",
      "cache",
      "buildx",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Multi-platform cache — Buildx & Multi-platform: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected multi-platform cache; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Multi-platform cache — Buildx & Multi-platform: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Multi-platform cache — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for multi-platform cache",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The multi-platform cache configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Multi-platform cache — Buildx & Multi-platform: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the multi-platform cache inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Multi-platform cache — Buildx & Multi-platform: behaves differently than configured”",
      "Effective multi-platform cache configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Multi-platform cache — Buildx & Multi-platform: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Multi-platform cache — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Multi-platform cache — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for multi-platform cache",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform cache\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform cache\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for multi-platform cache",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Multi-platform cache — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Multi-platform cache — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The multi-platform cache configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform cache configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform cache configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The multi-platform cache configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Multi-platform cache — Buildx & Multi-platform: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for multi-platform cache",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform cache\nThe effective runtime value differs from expectation.",
        "root": "For “Multi-platform cache — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The multi-platform cache configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform cache configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Multi-platform cache — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Multi-platform cache — Buildx & Multi-platform: behaves differently than configured: evidence found\nFor “Multi-platform cache — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Multi-platform cache — Buildx & Multi-platform: behaves differently than configured”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Multi-platform cache — Buildx & Multi-platform: behaves differently than configured: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Multi-platform cache — Buildx & Multi-platform: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-336",
    "technology": "Docker",
    "category": "Buildx & Multi-platform",
    "title": "Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective multi-platform cache state first, then narrow the failure through the buildx & multi-platform dependency chain.",
    "aliases": [
      "multi-platform cache",
      "Buildx & Multi-platform",
      "Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart",
      "multi",
      "platform",
      "cache",
      "buildx",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "image",
      "platforms",
      "dockerfile",
      "context"
    ],
    "steps": [
      {
        "title": "List builders/platforms",
        "command": "docker buildx ls",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect selected builder",
        "command": "docker buildx inspect --bootstrap",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected multi-platform cache; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect registry manifest",
        "command": "docker buildx imagetools inspect <image>",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Run explicit platform build",
        "command": "docker buildx build --platform <platforms> --progress=plain -f <dockerfile> <context>",
        "risk": "CHANGES LOCAL",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
      "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for multi-platform cache",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The multi-platform cache configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the multi-platform cache inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart”",
      "Effective multi-platform cache configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Buildx & Multi-platform Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker buildx ls",
          "output": "#1 [internal] load build definition\n#2 [internal] load .dockerignore\n#3 ERROR: failed to calculate checksum: /config/app.yaml: not found",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker buildx inspect --bootstrap",
          "output": "Check complete: build context/Dockerfile input issue detected",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
          "output": "Builder: default\nDriver: docker-container\nPlatforms: linux/amd64, linux/arm64",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The Dockerfile references a file that is not present in the effective build context, commonly because of context selection or .dockerignore.",
      "remediation": "Correct the build context or ignore rule and keep the required input inside the intended context; then rebuild with normal cache behavior.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Build failures involving COPY should be traced through the effective context before changing BuildKit/cache settings.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Buildx & Multi-platform path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for multi-platform cache",
        "command": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform cache\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx ls",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform cache\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker buildx inspect --bootstrap",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for multi-platform cache",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The multi-platform cache configuration is correct but the failure remains",
        "command": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform cache configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx inspect --bootstrap",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform cache configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Evidence matches this path: The multi-platform cache configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for multi-platform cache",
        "check": "docker buildx ls",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for multi-platform cache\nThe effective runtime value differs from expectation.",
        "root": "For “Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The multi-platform cache configuration is correct but the failure remains",
        "check": "docker buildx inspect --bootstrap",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The multi-platform cache configuration is correct but the failure remains",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker buildx ls",
        "output": "Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nFor “Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart”, the effective multi-platform cache state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker buildx inspect --bootstrap",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "check": "docker buildx inspect --bootstrap",
        "output": "Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nA buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "root": "A buildx & multi-platform dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the multi-platform cache path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker buildx imagetools inspect registry.example.com/payments-api:2026.09.12",
        "output": "Multi-platform cache — Buildx & Multi-platform: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker buildx build --platform linux/amd64,linux/arm64 --progress=plain -f Dockerfile prod-gke",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-337",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose interpolation — Compose Configuration: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Compose interpolation state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose interpolation",
      "Compose Configuration",
      "Compose interpolation — Compose Configuration: fails completely",
      "compose",
      "interpolation",
      "configuration",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose interpolation — Compose Configuration: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose interpolation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose interpolation — Compose Configuration: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose interpolation — Compose Configuration: fails completely”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose interpolation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose interpolation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose interpolation — Compose Configuration: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Compose interpolation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose interpolation — Compose Configuration: fails completely”",
      "Effective Compose interpolation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose interpolation — Compose Configuration: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose interpolation — Compose Configuration: fails completely”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose interpolation — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose interpolation",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose interpolation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose interpolation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose interpolation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose interpolation — Compose Configuration: fails completely”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose interpolation — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose interpolation configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose interpolation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose interpolation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose interpolation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose interpolation — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose interpolation",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose interpolation\nThe effective runtime value differs from expectation.",
        "root": "For “Compose interpolation — Compose Configuration: fails completely”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose interpolation configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose interpolation configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose interpolation — Compose Configuration: fails completely”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose interpolation — Compose Configuration: fails completely: evidence found\nFor “Compose interpolation — Compose Configuration: fails completely”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose interpolation — Compose Configuration: fails completely”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose interpolation — Compose Configuration: fails completely: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose interpolation — Compose Configuration: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-338",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose interpolation — Compose Configuration: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Compose interpolation state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose interpolation",
      "Compose Configuration",
      "Compose interpolation — Compose Configuration: behaves differently than configured",
      "compose",
      "interpolation",
      "configuration",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose interpolation — Compose Configuration: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose interpolation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose interpolation — Compose Configuration: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose interpolation — Compose Configuration: behaves differently than configured”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose interpolation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose interpolation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose interpolation — Compose Configuration: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Compose interpolation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose interpolation — Compose Configuration: behaves differently than configured”",
      "Effective Compose interpolation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose interpolation — Compose Configuration: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose interpolation — Compose Configuration: behaves differently than configured”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose interpolation — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose interpolation",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose interpolation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose interpolation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose interpolation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose interpolation — Compose Configuration: behaves differently than configured”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose interpolation — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose interpolation configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose interpolation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose interpolation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose interpolation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose interpolation — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose interpolation",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose interpolation\nThe effective runtime value differs from expectation.",
        "root": "For “Compose interpolation — Compose Configuration: behaves differently than configured”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose interpolation configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose interpolation configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose interpolation — Compose Configuration: behaves differently than configured”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose interpolation — Compose Configuration: behaves differently than configured: evidence found\nFor “Compose interpolation — Compose Configuration: behaves differently than configured”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose interpolation — Compose Configuration: behaves differently than configured”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose interpolation — Compose Configuration: behaves differently than configured: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose interpolation — Compose Configuration: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-339",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose interpolation — Compose Configuration: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Compose interpolation state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose interpolation",
      "Compose Configuration",
      "Compose interpolation — Compose Configuration: fails intermittently under load or restart",
      "compose",
      "interpolation",
      "configuration",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose interpolation — Compose Configuration: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose interpolation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose interpolation — Compose Configuration: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose interpolation — Compose Configuration: fails intermittently under load or restart”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose interpolation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose interpolation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose interpolation — Compose Configuration: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Compose interpolation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose interpolation — Compose Configuration: fails intermittently under load or restart”",
      "Effective Compose interpolation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose interpolation — Compose Configuration: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose interpolation — Compose Configuration: fails intermittently under load or restart”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose interpolation — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose interpolation",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose interpolation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose interpolation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose interpolation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose interpolation — Compose Configuration: fails intermittently under load or restart”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose interpolation — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose interpolation configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose interpolation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose interpolation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose interpolation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose interpolation — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose interpolation",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose interpolation\nThe effective runtime value differs from expectation.",
        "root": "For “Compose interpolation — Compose Configuration: fails intermittently under load or restart”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose interpolation configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose interpolation configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose interpolation — Compose Configuration: fails intermittently under load or restart”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose interpolation — Compose Configuration: fails intermittently under load or restart: evidence found\nFor “Compose interpolation — Compose Configuration: fails intermittently under load or restart”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose interpolation — Compose Configuration: fails intermittently under load or restart”, the effective Compose interpolation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose interpolation — Compose Configuration: fails intermittently under load or restart: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose interpolation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose interpolation — Compose Configuration: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-340",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": ".env loading — Compose Configuration: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective .env loading state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      ".env loading",
      "Compose Configuration",
      ".env loading — Compose Configuration: fails completely",
      "env",
      "loading",
      "compose",
      "configuration",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “.env loading — Compose Configuration: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected .env loading; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “.env loading — Compose Configuration: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “.env loading — Compose Configuration: fails completely”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for .env loading",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The .env loading configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “.env loading — Compose Configuration: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the .env loading inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “.env loading — Compose Configuration: fails completely”",
      "Effective .env loading configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: .env loading — Compose Configuration: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “.env loading — Compose Configuration: fails completely”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": ".env loading — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for .env loading",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .env loading\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .env loading\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for .env loading",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “.env loading — Compose Configuration: fails completely”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": ".env loading — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The .env loading configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .env loading configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .env loading configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The .env loading configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": ".env loading — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for .env loading",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .env loading\nThe effective runtime value differs from expectation.",
        "root": "For “.env loading — Compose Configuration: fails completely”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The .env loading configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .env loading configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “.env loading — Compose Configuration: fails completely”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": ".env loading — Compose Configuration: fails completely: evidence found\nFor “.env loading — Compose Configuration: fails completely”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “.env loading — Compose Configuration: fails completely”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": ".env loading — Compose Configuration: fails completely: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": ".env loading — Compose Configuration: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-341",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": ".env loading — Compose Configuration: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective .env loading state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      ".env loading",
      "Compose Configuration",
      ".env loading — Compose Configuration: behaves differently than configured",
      "env",
      "loading",
      "compose",
      "configuration",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “.env loading — Compose Configuration: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected .env loading; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “.env loading — Compose Configuration: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “.env loading — Compose Configuration: behaves differently than configured”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for .env loading",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The .env loading configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “.env loading — Compose Configuration: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the .env loading inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “.env loading — Compose Configuration: behaves differently than configured”",
      "Effective .env loading configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: .env loading — Compose Configuration: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “.env loading — Compose Configuration: behaves differently than configured”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": ".env loading — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for .env loading",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .env loading\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .env loading\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for .env loading",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “.env loading — Compose Configuration: behaves differently than configured”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": ".env loading — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The .env loading configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .env loading configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .env loading configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The .env loading configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": ".env loading — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for .env loading",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .env loading\nThe effective runtime value differs from expectation.",
        "root": "For “.env loading — Compose Configuration: behaves differently than configured”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The .env loading configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .env loading configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “.env loading — Compose Configuration: behaves differently than configured”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": ".env loading — Compose Configuration: behaves differently than configured: evidence found\nFor “.env loading — Compose Configuration: behaves differently than configured”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “.env loading — Compose Configuration: behaves differently than configured”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": ".env loading — Compose Configuration: behaves differently than configured: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": ".env loading — Compose Configuration: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-342",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": ".env loading — Compose Configuration: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective .env loading state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      ".env loading",
      "Compose Configuration",
      ".env loading — Compose Configuration: fails intermittently under load or restart",
      "env",
      "loading",
      "compose",
      "configuration",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “.env loading — Compose Configuration: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected .env loading; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “.env loading — Compose Configuration: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “.env loading — Compose Configuration: fails intermittently under load or restart”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for .env loading",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The .env loading configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “.env loading — Compose Configuration: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the .env loading inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “.env loading — Compose Configuration: fails intermittently under load or restart”",
      "Effective .env loading configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: .env loading — Compose Configuration: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “.env loading — Compose Configuration: fails intermittently under load or restart”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": ".env loading — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for .env loading",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .env loading\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .env loading\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for .env loading",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “.env loading — Compose Configuration: fails intermittently under load or restart”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": ".env loading — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The .env loading configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .env loading configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .env loading configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The .env loading configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": ".env loading — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for .env loading",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for .env loading\nThe effective runtime value differs from expectation.",
        "root": "For “.env loading — Compose Configuration: fails intermittently under load or restart”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The .env loading configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The .env loading configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “.env loading — Compose Configuration: fails intermittently under load or restart”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": ".env loading — Compose Configuration: fails intermittently under load or restart: evidence found\nFor “.env loading — Compose Configuration: fails intermittently under load or restart”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “.env loading — Compose Configuration: fails intermittently under load or restart”, the effective .env loading state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": ".env loading — Compose Configuration: fails intermittently under load or restart: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the .env loading path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": ".env loading — Compose Configuration: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-343",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose project name — Compose Configuration: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Compose project name state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose project name",
      "Compose Configuration",
      "Compose project name — Compose Configuration: fails completely",
      "compose",
      "project",
      "name",
      "configuration",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose project name — Compose Configuration: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose project name; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose project name — Compose Configuration: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose project name — Compose Configuration: fails completely”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose project name",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose project name configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose project name — Compose Configuration: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Compose project name inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose project name — Compose Configuration: fails completely”",
      "Effective Compose project name configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose project name — Compose Configuration: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose project name — Compose Configuration: fails completely”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose project name — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose project name",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose project name\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose project name\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose project name",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose project name — Compose Configuration: fails completely”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose project name — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose project name configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose project name configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose project name configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose project name configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose project name — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose project name",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose project name\nThe effective runtime value differs from expectation.",
        "root": "For “Compose project name — Compose Configuration: fails completely”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose project name configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose project name configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose project name — Compose Configuration: fails completely”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose project name — Compose Configuration: fails completely: evidence found\nFor “Compose project name — Compose Configuration: fails completely”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose project name — Compose Configuration: fails completely”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose project name — Compose Configuration: fails completely: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose project name — Compose Configuration: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-344",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose project name — Compose Configuration: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Compose project name state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose project name",
      "Compose Configuration",
      "Compose project name — Compose Configuration: behaves differently than configured",
      "compose",
      "project",
      "name",
      "configuration",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose project name — Compose Configuration: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose project name; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose project name — Compose Configuration: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose project name — Compose Configuration: behaves differently than configured”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose project name",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose project name configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose project name — Compose Configuration: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Compose project name inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose project name — Compose Configuration: behaves differently than configured”",
      "Effective Compose project name configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose project name — Compose Configuration: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose project name — Compose Configuration: behaves differently than configured”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose project name — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose project name",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose project name\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose project name\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose project name",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose project name — Compose Configuration: behaves differently than configured”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose project name — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose project name configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose project name configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose project name configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose project name configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose project name — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose project name",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose project name\nThe effective runtime value differs from expectation.",
        "root": "For “Compose project name — Compose Configuration: behaves differently than configured”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose project name configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose project name configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose project name — Compose Configuration: behaves differently than configured”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose project name — Compose Configuration: behaves differently than configured: evidence found\nFor “Compose project name — Compose Configuration: behaves differently than configured”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose project name — Compose Configuration: behaves differently than configured”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose project name — Compose Configuration: behaves differently than configured: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose project name — Compose Configuration: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-345",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose project name — Compose Configuration: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Compose project name state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose project name",
      "Compose Configuration",
      "Compose project name — Compose Configuration: fails intermittently under load or restart",
      "compose",
      "project",
      "name",
      "configuration",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose project name — Compose Configuration: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose project name; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose project name — Compose Configuration: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose project name — Compose Configuration: fails intermittently under load or restart”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose project name",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose project name configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose project name — Compose Configuration: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Compose project name inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose project name — Compose Configuration: fails intermittently under load or restart”",
      "Effective Compose project name configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose project name — Compose Configuration: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose project name — Compose Configuration: fails intermittently under load or restart”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose project name — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose project name",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose project name\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose project name\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose project name",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose project name — Compose Configuration: fails intermittently under load or restart”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose project name — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose project name configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose project name configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose project name configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose project name configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose project name — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose project name",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose project name\nThe effective runtime value differs from expectation.",
        "root": "For “Compose project name — Compose Configuration: fails intermittently under load or restart”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose project name configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose project name configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose project name — Compose Configuration: fails intermittently under load or restart”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose project name — Compose Configuration: fails intermittently under load or restart: evidence found\nFor “Compose project name — Compose Configuration: fails intermittently under load or restart”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose project name — Compose Configuration: fails intermittently under load or restart”, the effective Compose project name state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose project name — Compose Configuration: fails intermittently under load or restart: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose project name path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose project name — Compose Configuration: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-346",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose merge/include/extends — Compose Configuration: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Compose merge/include/extends state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose merge/include/extends",
      "Compose Configuration",
      "Compose merge/include/extends — Compose Configuration: fails completely",
      "compose",
      "merge",
      "include",
      "extends",
      "configuration",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose merge/include/extends — Compose Configuration: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose merge/include/extends; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose merge/include/extends — Compose Configuration: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose merge/include/extends — Compose Configuration: fails completely”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose merge/include/extends",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose merge/include/extends configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose merge/include/extends — Compose Configuration: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Compose merge/include/extends inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose merge/include/extends — Compose Configuration: fails completely”",
      "Effective Compose merge/include/extends configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose merge/include/extends — Compose Configuration: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose merge/include/extends — Compose Configuration: fails completely”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose merge/include/extends — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose merge/include/extends — Compose Configuration: fails completely”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose merge/include/extends — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose merge/include/extends configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose merge/include/extends configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose merge/include/extends configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose merge/include/extends configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose merge/include/extends — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose merge/include/extends",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends\nThe effective runtime value differs from expectation.",
        "root": "For “Compose merge/include/extends — Compose Configuration: fails completely”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose merge/include/extends configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose merge/include/extends configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose merge/include/extends — Compose Configuration: fails completely”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose merge/include/extends — Compose Configuration: fails completely: evidence found\nFor “Compose merge/include/extends — Compose Configuration: fails completely”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose merge/include/extends — Compose Configuration: fails completely”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose merge/include/extends — Compose Configuration: fails completely: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose merge/include/extends — Compose Configuration: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-347",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose merge/include/extends — Compose Configuration: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Compose merge/include/extends state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose merge/include/extends",
      "Compose Configuration",
      "Compose merge/include/extends — Compose Configuration: behaves differently than configured",
      "compose",
      "merge",
      "include",
      "extends",
      "configuration",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose merge/include/extends — Compose Configuration: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose merge/include/extends; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose merge/include/extends — Compose Configuration: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose merge/include/extends — Compose Configuration: behaves differently than configured”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose merge/include/extends",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose merge/include/extends configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose merge/include/extends — Compose Configuration: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Compose merge/include/extends inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose merge/include/extends — Compose Configuration: behaves differently than configured”",
      "Effective Compose merge/include/extends configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose merge/include/extends — Compose Configuration: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose merge/include/extends — Compose Configuration: behaves differently than configured”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose merge/include/extends — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose merge/include/extends — Compose Configuration: behaves differently than configured”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose merge/include/extends — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose merge/include/extends configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose merge/include/extends configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose merge/include/extends configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose merge/include/extends configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose merge/include/extends — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose merge/include/extends",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends\nThe effective runtime value differs from expectation.",
        "root": "For “Compose merge/include/extends — Compose Configuration: behaves differently than configured”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose merge/include/extends configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose merge/include/extends configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose merge/include/extends — Compose Configuration: behaves differently than configured”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose merge/include/extends — Compose Configuration: behaves differently than configured: evidence found\nFor “Compose merge/include/extends — Compose Configuration: behaves differently than configured”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose merge/include/extends — Compose Configuration: behaves differently than configured”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose merge/include/extends — Compose Configuration: behaves differently than configured: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose merge/include/extends — Compose Configuration: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-348",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Compose merge/include/extends state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose merge/include/extends",
      "Compose Configuration",
      "Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart",
      "compose",
      "merge",
      "include",
      "extends",
      "configuration",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose merge/include/extends; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose merge/include/extends",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose merge/include/extends configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Compose merge/include/extends inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart”",
      "Effective Compose merge/include/extends configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose merge/include/extends configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose merge/include/extends configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose merge/include/extends configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose merge/include/extends configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose merge/include/extends",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose merge/include/extends\nThe effective runtime value differs from expectation.",
        "root": "For “Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose merge/include/extends configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose merge/include/extends configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart: evidence found\nFor “Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart”, the effective Compose merge/include/extends state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose merge/include/extends path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose merge/include/extends — Compose Configuration: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-349",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose profile — Compose Configuration: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Compose profile state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose profile",
      "Compose Configuration",
      "Compose profile — Compose Configuration: fails completely",
      "compose",
      "profile",
      "configuration",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose profile — Compose Configuration: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose profile; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose profile — Compose Configuration: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose profile — Compose Configuration: fails completely”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose profile",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose profile configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose profile — Compose Configuration: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Compose profile inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose profile — Compose Configuration: fails completely”",
      "Effective Compose profile configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose profile — Compose Configuration: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose profile — Compose Configuration: fails completely”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose profile — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose profile",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose profile\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose profile\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose profile",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose profile — Compose Configuration: fails completely”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose profile — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose profile configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose profile configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose profile configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose profile configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose profile — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose profile",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose profile\nThe effective runtime value differs from expectation.",
        "root": "For “Compose profile — Compose Configuration: fails completely”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose profile configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose profile configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose profile — Compose Configuration: fails completely”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose profile — Compose Configuration: fails completely: evidence found\nFor “Compose profile — Compose Configuration: fails completely”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose profile — Compose Configuration: fails completely”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose profile — Compose Configuration: fails completely: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose profile — Compose Configuration: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-350",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose profile — Compose Configuration: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Compose profile state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose profile",
      "Compose Configuration",
      "Compose profile — Compose Configuration: behaves differently than configured",
      "compose",
      "profile",
      "configuration",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose profile — Compose Configuration: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose profile; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose profile — Compose Configuration: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose profile — Compose Configuration: behaves differently than configured”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose profile",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose profile configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose profile — Compose Configuration: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Compose profile inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose profile — Compose Configuration: behaves differently than configured”",
      "Effective Compose profile configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose profile — Compose Configuration: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose profile — Compose Configuration: behaves differently than configured”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose profile — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose profile",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose profile\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose profile\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose profile",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose profile — Compose Configuration: behaves differently than configured”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose profile — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose profile configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose profile configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose profile configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose profile configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose profile — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose profile",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose profile\nThe effective runtime value differs from expectation.",
        "root": "For “Compose profile — Compose Configuration: behaves differently than configured”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose profile configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose profile configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose profile — Compose Configuration: behaves differently than configured”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose profile — Compose Configuration: behaves differently than configured: evidence found\nFor “Compose profile — Compose Configuration: behaves differently than configured”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose profile — Compose Configuration: behaves differently than configured”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose profile — Compose Configuration: behaves differently than configured: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose profile — Compose Configuration: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-351",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose profile — Compose Configuration: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Compose profile state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose profile",
      "Compose Configuration",
      "Compose profile — Compose Configuration: fails intermittently under load or restart",
      "compose",
      "profile",
      "configuration",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose profile — Compose Configuration: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose profile; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose profile — Compose Configuration: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose profile — Compose Configuration: fails intermittently under load or restart”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose profile",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose profile configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose profile — Compose Configuration: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Compose profile inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose profile — Compose Configuration: fails intermittently under load or restart”",
      "Effective Compose profile configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose profile — Compose Configuration: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose profile — Compose Configuration: fails intermittently under load or restart”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose profile — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose profile",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose profile\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose profile\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose profile",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose profile — Compose Configuration: fails intermittently under load or restart”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose profile — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose profile configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose profile configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose profile configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose profile configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose profile — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose profile",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose profile\nThe effective runtime value differs from expectation.",
        "root": "For “Compose profile — Compose Configuration: fails intermittently under load or restart”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose profile configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose profile configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose profile — Compose Configuration: fails intermittently under load or restart”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose profile — Compose Configuration: fails intermittently under load or restart: evidence found\nFor “Compose profile — Compose Configuration: fails intermittently under load or restart”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose profile — Compose Configuration: fails intermittently under load or restart”, the effective Compose profile state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose profile — Compose Configuration: fails intermittently under load or restart: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose profile path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose profile — Compose Configuration: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-352",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose secret/config — Compose Configuration: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Compose secret/config state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose secret/config",
      "Compose Configuration",
      "Compose secret/config — Compose Configuration: fails completely",
      "compose",
      "secret",
      "config",
      "configuration",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose secret/config — Compose Configuration: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose secret/config; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose secret/config — Compose Configuration: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose secret/config — Compose Configuration: fails completely”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose secret/config",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose secret/config configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose secret/config — Compose Configuration: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Compose secret/config inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose secret/config — Compose Configuration: fails completely”",
      "Effective Compose secret/config configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose secret/config — Compose Configuration: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose secret/config — Compose Configuration: fails completely”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose secret/config — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose secret/config",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose secret/config\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose secret/config\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose secret/config",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose secret/config — Compose Configuration: fails completely”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose secret/config — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose secret/config configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose secret/config configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose secret/config configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose secret/config configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose secret/config — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose secret/config",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose secret/config\nThe effective runtime value differs from expectation.",
        "root": "For “Compose secret/config — Compose Configuration: fails completely”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose secret/config configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose secret/config configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose secret/config — Compose Configuration: fails completely”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose secret/config — Compose Configuration: fails completely: evidence found\nFor “Compose secret/config — Compose Configuration: fails completely”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose secret/config — Compose Configuration: fails completely”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose secret/config — Compose Configuration: fails completely: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose secret/config — Compose Configuration: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-353",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose secret/config — Compose Configuration: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Compose secret/config state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose secret/config",
      "Compose Configuration",
      "Compose secret/config — Compose Configuration: behaves differently than configured",
      "compose",
      "secret",
      "config",
      "configuration",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose secret/config — Compose Configuration: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose secret/config; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose secret/config — Compose Configuration: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose secret/config — Compose Configuration: behaves differently than configured”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose secret/config",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose secret/config configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose secret/config — Compose Configuration: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Compose secret/config inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose secret/config — Compose Configuration: behaves differently than configured”",
      "Effective Compose secret/config configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose secret/config — Compose Configuration: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose secret/config — Compose Configuration: behaves differently than configured”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose secret/config — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose secret/config",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose secret/config\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose secret/config\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose secret/config",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose secret/config — Compose Configuration: behaves differently than configured”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose secret/config — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose secret/config configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose secret/config configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose secret/config configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose secret/config configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose secret/config — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose secret/config",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose secret/config\nThe effective runtime value differs from expectation.",
        "root": "For “Compose secret/config — Compose Configuration: behaves differently than configured”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose secret/config configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose secret/config configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose secret/config — Compose Configuration: behaves differently than configured”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose secret/config — Compose Configuration: behaves differently than configured: evidence found\nFor “Compose secret/config — Compose Configuration: behaves differently than configured”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose secret/config — Compose Configuration: behaves differently than configured”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose secret/config — Compose Configuration: behaves differently than configured: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose secret/config — Compose Configuration: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-354",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Compose secret/config — Compose Configuration: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Compose secret/config state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "Compose secret/config",
      "Compose Configuration",
      "Compose secret/config — Compose Configuration: fails intermittently under load or restart",
      "compose",
      "secret",
      "config",
      "configuration",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose secret/config — Compose Configuration: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose secret/config; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose secret/config — Compose Configuration: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose secret/config — Compose Configuration: fails intermittently under load or restart”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose secret/config",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose secret/config configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose secret/config — Compose Configuration: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Compose secret/config inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose secret/config — Compose Configuration: fails intermittently under load or restart”",
      "Effective Compose secret/config configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose secret/config — Compose Configuration: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose secret/config — Compose Configuration: fails intermittently under load or restart”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose secret/config — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose secret/config",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose secret/config\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose secret/config\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose secret/config",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose secret/config — Compose Configuration: fails intermittently under load or restart”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose secret/config — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose secret/config configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose secret/config configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose secret/config configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The Compose secret/config configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose secret/config — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose secret/config",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose secret/config\nThe effective runtime value differs from expectation.",
        "root": "For “Compose secret/config — Compose Configuration: fails intermittently under load or restart”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose secret/config configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose secret/config configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose secret/config — Compose Configuration: fails intermittently under load or restart”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Compose secret/config — Compose Configuration: fails intermittently under load or restart: evidence found\nFor “Compose secret/config — Compose Configuration: fails intermittently under load or restart”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose secret/config — Compose Configuration: fails intermittently under load or restart”, the effective Compose secret/config state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Compose secret/config — Compose Configuration: fails intermittently under load or restart: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose secret/config path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Compose secret/config — Compose Configuration: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-355",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Rendered Compose model — Compose Configuration: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective rendered Compose model state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "rendered Compose model",
      "Compose Configuration",
      "Rendered Compose model — Compose Configuration: fails completely",
      "rendered",
      "compose",
      "model",
      "configuration",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rendered Compose model — Compose Configuration: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rendered Compose model; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rendered Compose model — Compose Configuration: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rendered Compose model — Compose Configuration: fails completely”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rendered Compose model",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rendered Compose model configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rendered Compose model — Compose Configuration: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the rendered Compose model inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rendered Compose model — Compose Configuration: fails completely”",
      "Effective rendered Compose model configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rendered Compose model — Compose Configuration: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rendered Compose model — Compose Configuration: fails completely”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rendered Compose model — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rendered Compose model",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rendered Compose model\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rendered Compose model\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rendered Compose model",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rendered Compose model — Compose Configuration: fails completely”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rendered Compose model — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rendered Compose model configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rendered Compose model configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rendered Compose model configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The rendered Compose model configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rendered Compose model — Compose Configuration: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rendered Compose model",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rendered Compose model\nThe effective runtime value differs from expectation.",
        "root": "For “Rendered Compose model — Compose Configuration: fails completely”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rendered Compose model configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rendered Compose model configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rendered Compose model — Compose Configuration: fails completely”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Rendered Compose model — Compose Configuration: fails completely: evidence found\nFor “Rendered Compose model — Compose Configuration: fails completely”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rendered Compose model — Compose Configuration: fails completely”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Rendered Compose model — Compose Configuration: fails completely: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Rendered Compose model — Compose Configuration: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-356",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Rendered Compose model — Compose Configuration: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective rendered Compose model state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "rendered Compose model",
      "Compose Configuration",
      "Rendered Compose model — Compose Configuration: behaves differently than configured",
      "rendered",
      "compose",
      "model",
      "configuration",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rendered Compose model — Compose Configuration: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rendered Compose model; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rendered Compose model — Compose Configuration: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rendered Compose model — Compose Configuration: behaves differently than configured”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rendered Compose model",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rendered Compose model configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rendered Compose model — Compose Configuration: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the rendered Compose model inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rendered Compose model — Compose Configuration: behaves differently than configured”",
      "Effective rendered Compose model configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rendered Compose model — Compose Configuration: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rendered Compose model — Compose Configuration: behaves differently than configured”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rendered Compose model — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rendered Compose model",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rendered Compose model\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rendered Compose model\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rendered Compose model",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rendered Compose model — Compose Configuration: behaves differently than configured”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rendered Compose model — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rendered Compose model configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rendered Compose model configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rendered Compose model configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The rendered Compose model configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rendered Compose model — Compose Configuration: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rendered Compose model",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rendered Compose model\nThe effective runtime value differs from expectation.",
        "root": "For “Rendered Compose model — Compose Configuration: behaves differently than configured”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rendered Compose model configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rendered Compose model configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rendered Compose model — Compose Configuration: behaves differently than configured”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Rendered Compose model — Compose Configuration: behaves differently than configured: evidence found\nFor “Rendered Compose model — Compose Configuration: behaves differently than configured”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rendered Compose model — Compose Configuration: behaves differently than configured”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Rendered Compose model — Compose Configuration: behaves differently than configured: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Rendered Compose model — Compose Configuration: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-357",
    "technology": "Docker",
    "category": "Compose Configuration",
    "title": "Rendered Compose model — Compose Configuration: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective rendered Compose model state first, then narrow the failure through the compose configuration dependency chain.",
    "aliases": [
      "rendered Compose model",
      "Compose Configuration",
      "Rendered Compose model — Compose Configuration: fails intermittently under load or restart",
      "rendered",
      "compose",
      "model",
      "configuration",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Render Compose model",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rendered Compose model — Compose Configuration: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "List rendered services",
        "command": "docker compose config --services",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rendered Compose model; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List rendered images",
        "command": "docker compose config --images",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rendered Compose model — Compose Configuration: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "List Compose projects",
        "command": "docker compose ls",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rendered Compose model — Compose Configuration: fails intermittently under load or restart”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rendered Compose model",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rendered Compose model configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rendered Compose model — Compose Configuration: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the rendered Compose model inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rendered Compose model — Compose Configuration: fails intermittently under load or restart”",
      "Effective rendered Compose model configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Configuration Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rendered Compose model — Compose Configuration: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose config",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose config --services",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config --images",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rendered Compose model — Compose Configuration: fails intermittently under load or restart”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Configuration path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rendered Compose model — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rendered Compose model",
        "command": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rendered Compose model\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rendered Compose model\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose config --services",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rendered Compose model",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rendered Compose model — Compose Configuration: fails intermittently under load or restart”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rendered Compose model — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rendered Compose model configuration is correct but the failure remains",
        "command": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rendered Compose model configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --services",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rendered Compose model configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config --images",
            "output": "Evidence matches this path: The rendered Compose model configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rendered Compose model — Compose Configuration: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config --images",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ls",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ls",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rendered Compose model",
        "check": "docker compose config",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rendered Compose model\nThe effective runtime value differs from expectation.",
        "root": "For “Rendered Compose model — Compose Configuration: fails intermittently under load or restart”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rendered Compose model configuration is correct but the failure remains",
        "check": "docker compose config --services",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rendered Compose model configuration is correct but the failure remains",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config --images",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rendered Compose model — Compose Configuration: fails intermittently under load or restart”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose config",
        "output": "Rendered Compose model — Compose Configuration: fails intermittently under load or restart: evidence found\nFor “Rendered Compose model — Compose Configuration: fails intermittently under load or restart”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rendered Compose model — Compose Configuration: fails intermittently under load or restart”, the effective rendered Compose model state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose config --services",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "check": "docker compose config --services",
        "output": "Rendered Compose model — Compose Configuration: fails intermittently under load or restart: evidence found\nA compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "root": "A compose configuration dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rendered Compose model path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config --images",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config --images",
        "output": "Rendered Compose model — Compose Configuration: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-358",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose service startup — Compose Runtime & Dependencies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Compose service startup state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose service startup",
      "Compose Runtime & Dependencies",
      "Compose service startup — Compose Runtime & Dependencies: fails completely",
      "compose",
      "service",
      "startup",
      "runtime",
      "dependencies",
      "fails",
      "completely"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose service startup — Compose Runtime & Dependencies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose service startup; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose service startup — Compose Runtime & Dependencies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose service startup — Compose Runtime & Dependencies: fails completely”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose service startup",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose service startup configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose service startup — Compose Runtime & Dependencies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Compose service startup inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose service startup — Compose Runtime & Dependencies: fails completely”",
      "Effective Compose service startup configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose service startup — Compose Runtime & Dependencies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose service startup — Compose Runtime & Dependencies: fails completely”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose service startup — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose service startup",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service startup\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service startup\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose service startup",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose service startup — Compose Runtime & Dependencies: fails completely”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose service startup — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose service startup configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service startup configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service startup configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose service startup configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose service startup — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose service startup",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service startup\nThe effective runtime value differs from expectation.",
        "root": "For “Compose service startup — Compose Runtime & Dependencies: fails completely”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose service startup configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service startup configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose service startup — Compose Runtime & Dependencies: fails completely”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose service startup — Compose Runtime & Dependencies: fails completely: evidence found\nFor “Compose service startup — Compose Runtime & Dependencies: fails completely”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose service startup — Compose Runtime & Dependencies: fails completely”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose service startup — Compose Runtime & Dependencies: fails completely: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose service startup — Compose Runtime & Dependencies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-359",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose service startup — Compose Runtime & Dependencies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Compose service startup state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose service startup",
      "Compose Runtime & Dependencies",
      "Compose service startup — Compose Runtime & Dependencies: behaves differently than configured",
      "compose",
      "service",
      "startup",
      "runtime",
      "dependencies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose service startup — Compose Runtime & Dependencies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose service startup; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose service startup — Compose Runtime & Dependencies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose service startup — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose service startup",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose service startup configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose service startup — Compose Runtime & Dependencies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Compose service startup inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose service startup — Compose Runtime & Dependencies: behaves differently than configured”",
      "Effective Compose service startup configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose service startup — Compose Runtime & Dependencies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose service startup — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose service startup — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose service startup",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service startup\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service startup\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose service startup",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose service startup — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose service startup — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose service startup configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service startup configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service startup configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose service startup configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose service startup — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose service startup",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service startup\nThe effective runtime value differs from expectation.",
        "root": "For “Compose service startup — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose service startup configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service startup configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose service startup — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose service startup — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nFor “Compose service startup — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose service startup — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose service startup — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose service startup — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-360",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Compose service startup state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose service startup",
      "Compose Runtime & Dependencies",
      "Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart",
      "compose",
      "service",
      "startup",
      "runtime",
      "dependencies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose service startup; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose service startup",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose service startup configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Compose service startup inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart”",
      "Effective Compose service startup configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose service startup",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service startup\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service startup\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose service startup",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose service startup configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service startup configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service startup configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose service startup configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose service startup",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service startup\nThe effective runtime value differs from expectation.",
        "root": "For “Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose service startup configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service startup configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nFor “Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service startup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service startup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose service startup — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-361",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Service health dependency — Compose Runtime & Dependencies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective service health dependency state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "service health dependency",
      "Compose Runtime & Dependencies",
      "Service health dependency — Compose Runtime & Dependencies: fails completely",
      "service",
      "health",
      "dependency",
      "compose",
      "runtime",
      "dependencies",
      "fails",
      "completely"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Service health dependency — Compose Runtime & Dependencies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected service health dependency; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Service health dependency — Compose Runtime & Dependencies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Service health dependency — Compose Runtime & Dependencies: fails completely”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for service health dependency",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The service health dependency configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Service health dependency — Compose Runtime & Dependencies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the service health dependency inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Service health dependency — Compose Runtime & Dependencies: fails completely”",
      "Effective service health dependency configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Service health dependency — Compose Runtime & Dependencies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Service health dependency — Compose Runtime & Dependencies: fails completely”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Service health dependency — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for service health dependency",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for service health dependency\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for service health dependency\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for service health dependency",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Service health dependency — Compose Runtime & Dependencies: fails completely”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Service health dependency — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The service health dependency configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The service health dependency configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The service health dependency configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The service health dependency configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Service health dependency — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for service health dependency",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for service health dependency\nThe effective runtime value differs from expectation.",
        "root": "For “Service health dependency — Compose Runtime & Dependencies: fails completely”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The service health dependency configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The service health dependency configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Service health dependency — Compose Runtime & Dependencies: fails completely”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Service health dependency — Compose Runtime & Dependencies: fails completely: evidence found\nFor “Service health dependency — Compose Runtime & Dependencies: fails completely”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Service health dependency — Compose Runtime & Dependencies: fails completely”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Service health dependency — Compose Runtime & Dependencies: fails completely: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Service health dependency — Compose Runtime & Dependencies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-362",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Service health dependency — Compose Runtime & Dependencies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective service health dependency state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "service health dependency",
      "Compose Runtime & Dependencies",
      "Service health dependency — Compose Runtime & Dependencies: behaves differently than configured",
      "service",
      "health",
      "dependency",
      "compose",
      "runtime",
      "dependencies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Service health dependency — Compose Runtime & Dependencies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected service health dependency; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Service health dependency — Compose Runtime & Dependencies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Service health dependency — Compose Runtime & Dependencies: behaves differently than configured”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for service health dependency",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The service health dependency configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Service health dependency — Compose Runtime & Dependencies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the service health dependency inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Service health dependency — Compose Runtime & Dependencies: behaves differently than configured”",
      "Effective service health dependency configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Service health dependency — Compose Runtime & Dependencies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Service health dependency — Compose Runtime & Dependencies: behaves differently than configured”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Service health dependency — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for service health dependency",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for service health dependency\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for service health dependency\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for service health dependency",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Service health dependency — Compose Runtime & Dependencies: behaves differently than configured”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Service health dependency — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The service health dependency configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The service health dependency configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The service health dependency configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The service health dependency configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Service health dependency — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for service health dependency",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for service health dependency\nThe effective runtime value differs from expectation.",
        "root": "For “Service health dependency — Compose Runtime & Dependencies: behaves differently than configured”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The service health dependency configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The service health dependency configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Service health dependency — Compose Runtime & Dependencies: behaves differently than configured”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Service health dependency — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nFor “Service health dependency — Compose Runtime & Dependencies: behaves differently than configured”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Service health dependency — Compose Runtime & Dependencies: behaves differently than configured”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Service health dependency — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Service health dependency — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-363",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective service health dependency state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "service health dependency",
      "Compose Runtime & Dependencies",
      "Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart",
      "service",
      "health",
      "dependency",
      "compose",
      "runtime",
      "dependencies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected service health dependency; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for service health dependency",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The service health dependency configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the service health dependency inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart”",
      "Effective service health dependency configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "{\"Status\":\"unhealthy\",\"FailingStreak\":4,\"Log\":[{\"ExitCode\":1,\"Output\":\"curl: (7) connection refused\"}]}",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "{\"Test\":[\"CMD-SHELL\",\"curl -f http://localhost:8080/health || exit 1\"],\"Interval\":30000000000,\"Timeout\":3000000000}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "payments-api  Up 2 minutes (unhealthy)",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The healthcheck runs before the application listener is available or targets a listener that is not actually serving on the configured endpoint.",
      "remediation": "Correct the endpoint and health timing based on measured startup behavior; do not hide a genuine application failure with excessive retries.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Docker health status is the result of the configured test, not an independent guarantee that the application is usable.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for service health dependency",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for service health dependency\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for service health dependency\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for service health dependency",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The service health dependency configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The service health dependency configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The service health dependency configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The service health dependency configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for service health dependency",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for service health dependency\nThe effective runtime value differs from expectation.",
        "root": "For “Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The service health dependency configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The service health dependency configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nFor “Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective service health dependency state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the service health dependency path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Service health dependency — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-364",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose service DNS — Compose Runtime & Dependencies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Compose service DNS state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose service DNS",
      "Compose Runtime & Dependencies",
      "Compose service DNS — Compose Runtime & Dependencies: fails completely",
      "compose",
      "service",
      "dns",
      "runtime",
      "dependencies",
      "fails",
      "completely"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose service DNS — Compose Runtime & Dependencies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose service DNS; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose service DNS — Compose Runtime & Dependencies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose service DNS — Compose Runtime & Dependencies: fails completely”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose service DNS",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose service DNS configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose service DNS — Compose Runtime & Dependencies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Compose service DNS inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose service DNS — Compose Runtime & Dependencies: fails completely”",
      "Effective Compose service DNS configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose service DNS — Compose Runtime & Dependencies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose service DNS — Compose Runtime & Dependencies: fails completely”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose service DNS — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose service DNS",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service DNS\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service DNS\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose service DNS",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose service DNS — Compose Runtime & Dependencies: fails completely”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose service DNS — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose service DNS configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service DNS configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service DNS configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose service DNS configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose service DNS — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose service DNS",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service DNS\nThe effective runtime value differs from expectation.",
        "root": "For “Compose service DNS — Compose Runtime & Dependencies: fails completely”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose service DNS configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service DNS configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose service DNS — Compose Runtime & Dependencies: fails completely”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose service DNS — Compose Runtime & Dependencies: fails completely: evidence found\nFor “Compose service DNS — Compose Runtime & Dependencies: fails completely”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose service DNS — Compose Runtime & Dependencies: fails completely”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose service DNS — Compose Runtime & Dependencies: fails completely: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose service DNS — Compose Runtime & Dependencies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-365",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Compose service DNS state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose service DNS",
      "Compose Runtime & Dependencies",
      "Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured",
      "compose",
      "service",
      "dns",
      "runtime",
      "dependencies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose service DNS; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose service DNS",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose service DNS configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Compose service DNS inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured”",
      "Effective Compose service DNS configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose service DNS",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service DNS\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service DNS\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose service DNS",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose service DNS configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service DNS configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service DNS configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose service DNS configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose service DNS",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service DNS\nThe effective runtime value differs from expectation.",
        "root": "For “Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose service DNS configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service DNS configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nFor “Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose service DNS — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-366",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Compose service DNS state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose service DNS",
      "Compose Runtime & Dependencies",
      "Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart",
      "compose",
      "service",
      "dns",
      "runtime",
      "dependencies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose service DNS; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose service DNS",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose service DNS configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Compose service DNS inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart”",
      "Effective Compose service DNS configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose service DNS",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service DNS\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service DNS\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose service DNS",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose service DNS configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service DNS configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service DNS configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose service DNS configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose service DNS",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose service DNS\nThe effective runtime value differs from expectation.",
        "root": "For “Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose service DNS configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose service DNS configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nFor “Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose service DNS state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose service DNS path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose service DNS — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-367",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose network — Compose Runtime & Dependencies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Compose network state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose network",
      "Compose Runtime & Dependencies",
      "Compose network — Compose Runtime & Dependencies: fails completely",
      "compose",
      "network",
      "runtime",
      "dependencies",
      "fails",
      "completely"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose network — Compose Runtime & Dependencies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose network; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose network — Compose Runtime & Dependencies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose network — Compose Runtime & Dependencies: fails completely”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose network",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose network configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose network — Compose Runtime & Dependencies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Compose network inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose network — Compose Runtime & Dependencies: fails completely”",
      "Effective Compose network configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose network — Compose Runtime & Dependencies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose network — Compose Runtime & Dependencies: fails completely”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose network — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose network",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose network\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose network\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose network",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose network — Compose Runtime & Dependencies: fails completely”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose network — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose network configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose network configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose network configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose network configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose network — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose network",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose network\nThe effective runtime value differs from expectation.",
        "root": "For “Compose network — Compose Runtime & Dependencies: fails completely”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose network configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose network configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose network — Compose Runtime & Dependencies: fails completely”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose network — Compose Runtime & Dependencies: fails completely: evidence found\nFor “Compose network — Compose Runtime & Dependencies: fails completely”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose network — Compose Runtime & Dependencies: fails completely”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose network — Compose Runtime & Dependencies: fails completely: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose network — Compose Runtime & Dependencies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-368",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose network — Compose Runtime & Dependencies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Compose network state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose network",
      "Compose Runtime & Dependencies",
      "Compose network — Compose Runtime & Dependencies: behaves differently than configured",
      "compose",
      "network",
      "runtime",
      "dependencies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose network — Compose Runtime & Dependencies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose network; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose network — Compose Runtime & Dependencies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose network — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose network",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose network configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose network — Compose Runtime & Dependencies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Compose network inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose network — Compose Runtime & Dependencies: behaves differently than configured”",
      "Effective Compose network configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose network — Compose Runtime & Dependencies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose network — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose network — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose network",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose network\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose network\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose network",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose network — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose network — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose network configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose network configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose network configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose network configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose network — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose network",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose network\nThe effective runtime value differs from expectation.",
        "root": "For “Compose network — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose network configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose network configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose network — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose network — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nFor “Compose network — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose network — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose network — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose network — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-369",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Compose network state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose network",
      "Compose Runtime & Dependencies",
      "Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart",
      "compose",
      "network",
      "runtime",
      "dependencies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose network; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose network",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose network configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Compose network inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart”",
      "Effective Compose network configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose network",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose network\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose network\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose network",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose network configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose network configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose network configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose network configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose network",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose network\nThe effective runtime value differs from expectation.",
        "root": "For “Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose network configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose network configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nFor “Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose network state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose network path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose network — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-370",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose volume — Compose Runtime & Dependencies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Compose volume state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose volume",
      "Compose Runtime & Dependencies",
      "Compose volume — Compose Runtime & Dependencies: fails completely",
      "compose",
      "volume",
      "runtime",
      "dependencies",
      "fails",
      "completely"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose volume — Compose Runtime & Dependencies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose volume; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose volume — Compose Runtime & Dependencies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose volume — Compose Runtime & Dependencies: fails completely”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose volume",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose volume configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose volume — Compose Runtime & Dependencies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Compose volume inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose volume — Compose Runtime & Dependencies: fails completely”",
      "Effective Compose volume configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose volume — Compose Runtime & Dependencies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose volume — Compose Runtime & Dependencies: fails completely”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose volume — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose volume",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose volume\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose volume\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose volume",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose volume — Compose Runtime & Dependencies: fails completely”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose volume — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose volume configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose volume configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose volume configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose volume configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose volume — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose volume",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose volume\nThe effective runtime value differs from expectation.",
        "root": "For “Compose volume — Compose Runtime & Dependencies: fails completely”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose volume configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose volume configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose volume — Compose Runtime & Dependencies: fails completely”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose volume — Compose Runtime & Dependencies: fails completely: evidence found\nFor “Compose volume — Compose Runtime & Dependencies: fails completely”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose volume — Compose Runtime & Dependencies: fails completely”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose volume — Compose Runtime & Dependencies: fails completely: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose volume — Compose Runtime & Dependencies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-371",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose volume — Compose Runtime & Dependencies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Compose volume state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose volume",
      "Compose Runtime & Dependencies",
      "Compose volume — Compose Runtime & Dependencies: behaves differently than configured",
      "compose",
      "volume",
      "runtime",
      "dependencies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose volume — Compose Runtime & Dependencies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose volume; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose volume — Compose Runtime & Dependencies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose volume — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose volume",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose volume configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose volume — Compose Runtime & Dependencies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Compose volume inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose volume — Compose Runtime & Dependencies: behaves differently than configured”",
      "Effective Compose volume configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose volume — Compose Runtime & Dependencies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose volume — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose volume — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose volume",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose volume\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose volume\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose volume",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose volume — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose volume — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose volume configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose volume configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose volume configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose volume configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose volume — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose volume",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose volume\nThe effective runtime value differs from expectation.",
        "root": "For “Compose volume — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose volume configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose volume configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose volume — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose volume — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nFor “Compose volume — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose volume — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose volume — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose volume — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-372",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Compose volume state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose volume",
      "Compose Runtime & Dependencies",
      "Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart",
      "compose",
      "volume",
      "runtime",
      "dependencies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose volume; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose volume",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose volume configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Compose volume inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart”",
      "Effective Compose volume configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose volume",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose volume\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose volume\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose volume",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose volume configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose volume configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose volume configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose volume configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose volume",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose volume\nThe effective runtime value differs from expectation.",
        "root": "For “Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose volume configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose volume configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nFor “Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose volume state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose volume path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose volume — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-373",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose recreation — Compose Runtime & Dependencies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Compose recreation state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose recreation",
      "Compose Runtime & Dependencies",
      "Compose recreation — Compose Runtime & Dependencies: fails completely",
      "compose",
      "recreation",
      "runtime",
      "dependencies",
      "fails",
      "completely"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose recreation — Compose Runtime & Dependencies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose recreation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose recreation — Compose Runtime & Dependencies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose recreation — Compose Runtime & Dependencies: fails completely”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose recreation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose recreation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose recreation — Compose Runtime & Dependencies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Compose recreation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose recreation — Compose Runtime & Dependencies: fails completely”",
      "Effective Compose recreation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose recreation — Compose Runtime & Dependencies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose recreation — Compose Runtime & Dependencies: fails completely”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose recreation — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose recreation",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose recreation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose recreation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose recreation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose recreation — Compose Runtime & Dependencies: fails completely”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose recreation — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose recreation configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose recreation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose recreation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose recreation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose recreation — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose recreation",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose recreation\nThe effective runtime value differs from expectation.",
        "root": "For “Compose recreation — Compose Runtime & Dependencies: fails completely”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose recreation configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose recreation configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose recreation — Compose Runtime & Dependencies: fails completely”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose recreation — Compose Runtime & Dependencies: fails completely: evidence found\nFor “Compose recreation — Compose Runtime & Dependencies: fails completely”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose recreation — Compose Runtime & Dependencies: fails completely”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose recreation — Compose Runtime & Dependencies: fails completely: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose recreation — Compose Runtime & Dependencies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-374",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose recreation — Compose Runtime & Dependencies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Compose recreation state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose recreation",
      "Compose Runtime & Dependencies",
      "Compose recreation — Compose Runtime & Dependencies: behaves differently than configured",
      "compose",
      "recreation",
      "runtime",
      "dependencies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose recreation — Compose Runtime & Dependencies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose recreation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose recreation — Compose Runtime & Dependencies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose recreation — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose recreation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose recreation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose recreation — Compose Runtime & Dependencies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Compose recreation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose recreation — Compose Runtime & Dependencies: behaves differently than configured”",
      "Effective Compose recreation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose recreation — Compose Runtime & Dependencies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose recreation — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose recreation — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose recreation",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose recreation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose recreation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose recreation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose recreation — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose recreation — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose recreation configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose recreation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose recreation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose recreation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose recreation — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose recreation",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose recreation\nThe effective runtime value differs from expectation.",
        "root": "For “Compose recreation — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose recreation configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose recreation configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose recreation — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose recreation — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nFor “Compose recreation — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose recreation — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose recreation — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose recreation — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-375",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Compose recreation state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose recreation",
      "Compose Runtime & Dependencies",
      "Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart",
      "compose",
      "recreation",
      "runtime",
      "dependencies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose recreation; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose recreation",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose recreation configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Compose recreation inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart”",
      "Effective Compose recreation configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose recreation",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose recreation\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose recreation\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose recreation",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose recreation configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose recreation configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose recreation configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose recreation configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose recreation",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose recreation\nThe effective runtime value differs from expectation.",
        "root": "For “Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose recreation configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose recreation configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nFor “Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose recreation state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose recreation path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose recreation — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-376",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose shutdown — Compose Runtime & Dependencies: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Compose shutdown state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose shutdown",
      "Compose Runtime & Dependencies",
      "Compose shutdown — Compose Runtime & Dependencies: fails completely",
      "compose",
      "shutdown",
      "runtime",
      "dependencies",
      "fails",
      "completely"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose shutdown — Compose Runtime & Dependencies: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose shutdown; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose shutdown — Compose Runtime & Dependencies: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose shutdown — Compose Runtime & Dependencies: fails completely”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose shutdown",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose shutdown configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose shutdown — Compose Runtime & Dependencies: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Compose shutdown inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose shutdown — Compose Runtime & Dependencies: fails completely”",
      "Effective Compose shutdown configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose shutdown — Compose Runtime & Dependencies: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose shutdown — Compose Runtime & Dependencies: fails completely”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose shutdown — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose shutdown",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose shutdown\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose shutdown\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose shutdown",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose shutdown — Compose Runtime & Dependencies: fails completely”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose shutdown — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose shutdown configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose shutdown configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose shutdown configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose shutdown configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose shutdown — Compose Runtime & Dependencies: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose shutdown",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose shutdown\nThe effective runtime value differs from expectation.",
        "root": "For “Compose shutdown — Compose Runtime & Dependencies: fails completely”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose shutdown configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose shutdown configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose shutdown — Compose Runtime & Dependencies: fails completely”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose shutdown — Compose Runtime & Dependencies: fails completely: evidence found\nFor “Compose shutdown — Compose Runtime & Dependencies: fails completely”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose shutdown — Compose Runtime & Dependencies: fails completely”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose shutdown — Compose Runtime & Dependencies: fails completely: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose shutdown — Compose Runtime & Dependencies: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-377",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Compose shutdown state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose shutdown",
      "Compose Runtime & Dependencies",
      "Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured",
      "compose",
      "shutdown",
      "runtime",
      "dependencies",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose shutdown; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose shutdown",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose shutdown configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Compose shutdown inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured”",
      "Effective Compose shutdown configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose shutdown",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose shutdown\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose shutdown\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose shutdown",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose shutdown configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose shutdown configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose shutdown configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose shutdown configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose shutdown",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose shutdown\nThe effective runtime value differs from expectation.",
        "root": "For “Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose shutdown configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose shutdown configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nFor “Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose shutdown — Compose Runtime & Dependencies: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-378",
    "technology": "Docker",
    "category": "Compose Runtime & Dependencies",
    "title": "Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Compose shutdown state first, then narrow the failure through the compose runtime & dependencies dependency chain.",
    "aliases": [
      "Compose shutdown",
      "Compose Runtime & Dependencies",
      "Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart",
      "compose",
      "shutdown",
      "runtime",
      "dependencies",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "service"
    ],
    "steps": [
      {
        "title": "Inspect service state",
        "command": "docker compose ps -a",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Read service logs",
        "command": "docker compose logs --tail 200 --timestamps <service>",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Compose shutdown; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Render effective config",
        "command": "docker compose config",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Resolve service container ID",
        "command": "docker compose ps -q <service>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
      "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Compose shutdown",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Compose shutdown configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Compose shutdown inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart”",
      "Effective Compose shutdown configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Compose Runtime & Dependencies Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker compose ps -a",
          "output": "payments-api  exited (1)\npostgres  running (healthy)",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker compose logs --tail 200 --timestamps payments-api",
          "output": "payments-api | ERROR: DB_HOST resolves but connection refused",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker compose config",
          "output": "services:\n  payments-api:\n    depends_on:\n      postgres:\n        condition: service_started",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Compose starts the dependency container but the application assumes that started means ready to accept connections.",
      "remediation": "Add an appropriate dependency healthcheck/readiness strategy and application retry behavior rather than relying only on start order.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Compose dependency ordering and application readiness are different concepts.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Compose Runtime & Dependencies path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Compose shutdown",
        "command": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose shutdown\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose ps -a",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose shutdown\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Compose shutdown",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Compose shutdown configuration is correct but the failure remains",
        "command": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose shutdown configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose logs --tail 200 --timestamps payments-api",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose shutdown configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker compose config",
            "output": "Evidence matches this path: The Compose shutdown configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker compose config",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker compose ps -q payments-api",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker compose ps -q payments-api",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Compose shutdown",
        "check": "docker compose ps -a",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Compose shutdown\nThe effective runtime value differs from expectation.",
        "root": "For “Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Compose shutdown configuration is correct but the failure remains",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Compose shutdown configuration is correct but the failure remains",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker compose config",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker compose ps -a",
        "output": "Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nFor “Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart”, the effective Compose shutdown state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker compose logs --tail 200 --timestamps payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "check": "docker compose logs --tail 200 --timestamps payments-api",
        "output": "Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nA compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "root": "A compose runtime & dependencies dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Compose shutdown path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker compose config",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker compose config",
        "output": "Compose shutdown — Compose Runtime & Dependencies: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker compose ps -q payments-api",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-379",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Container user/UID — Security, Users & Capabilities: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective container user/UID state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "container user/UID",
      "Security, Users & Capabilities",
      "Container user/UID — Security, Users & Capabilities: fails completely",
      "container",
      "user",
      "uid",
      "security",
      "users",
      "capabilities",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container user/UID — Security, Users & Capabilities: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container user/UID; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Container user/UID — Security, Users & Capabilities: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container user/UID — Security, Users & Capabilities: fails completely”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container user/UID",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container user/UID configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container user/UID — Security, Users & Capabilities: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the container user/UID inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container user/UID — Security, Users & Capabilities: fails completely”",
      "Effective container user/UID configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container user/UID — Security, Users & Capabilities: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Container user/UID — Security, Users & Capabilities: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container user/UID — Security, Users & Capabilities: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container user/UID — Security, Users & Capabilities: fails completely”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container user/UID — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container user/UID",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container user/UID\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container user/UID\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container user/UID",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container user/UID — Security, Users & Capabilities: fails completely”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container user/UID — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container user/UID configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container user/UID configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container user/UID configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The container user/UID configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container user/UID — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container user/UID",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container user/UID\nThe effective runtime value differs from expectation.",
        "root": "For “Container user/UID — Security, Users & Capabilities: fails completely”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container user/UID configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container user/UID configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container user/UID — Security, Users & Capabilities: fails completely”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Container user/UID — Security, Users & Capabilities: fails completely: evidence found\nFor “Container user/UID — Security, Users & Capabilities: fails completely”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container user/UID — Security, Users & Capabilities: fails completely”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Container user/UID — Security, Users & Capabilities: fails completely: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Container user/UID — Security, Users & Capabilities: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-380",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Container user/UID — Security, Users & Capabilities: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective container user/UID state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "container user/UID",
      "Security, Users & Capabilities",
      "Container user/UID — Security, Users & Capabilities: behaves differently than configured",
      "container",
      "user",
      "uid",
      "security",
      "users",
      "capabilities",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container user/UID — Security, Users & Capabilities: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container user/UID; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Container user/UID — Security, Users & Capabilities: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container user/UID — Security, Users & Capabilities: behaves differently than configured”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container user/UID",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container user/UID configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container user/UID — Security, Users & Capabilities: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the container user/UID inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container user/UID — Security, Users & Capabilities: behaves differently than configured”",
      "Effective container user/UID configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container user/UID — Security, Users & Capabilities: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Container user/UID — Security, Users & Capabilities: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container user/UID — Security, Users & Capabilities: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container user/UID — Security, Users & Capabilities: behaves differently than configured”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container user/UID — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container user/UID",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container user/UID\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container user/UID\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container user/UID",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container user/UID — Security, Users & Capabilities: behaves differently than configured”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container user/UID — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container user/UID configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container user/UID configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container user/UID configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The container user/UID configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container user/UID — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container user/UID",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container user/UID\nThe effective runtime value differs from expectation.",
        "root": "For “Container user/UID — Security, Users & Capabilities: behaves differently than configured”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container user/UID configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container user/UID configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container user/UID — Security, Users & Capabilities: behaves differently than configured”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Container user/UID — Security, Users & Capabilities: behaves differently than configured: evidence found\nFor “Container user/UID — Security, Users & Capabilities: behaves differently than configured”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container user/UID — Security, Users & Capabilities: behaves differently than configured”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Container user/UID — Security, Users & Capabilities: behaves differently than configured: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Container user/UID — Security, Users & Capabilities: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-381",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective container user/UID state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "container user/UID",
      "Security, Users & Capabilities",
      "Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart",
      "container",
      "user",
      "uid",
      "security",
      "users",
      "capabilities",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container user/UID; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container user/UID",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container user/UID configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the container user/UID inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart”",
      "Effective container user/UID configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container user/UID",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container user/UID\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container user/UID\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container user/UID",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container user/UID configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container user/UID configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container user/UID configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The container user/UID configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container user/UID",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container user/UID\nThe effective runtime value differs from expectation.",
        "root": "For “Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container user/UID configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container user/UID configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nFor “Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart”, the effective container user/UID state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container user/UID path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Container user/UID — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-382",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Linux capability — Security, Users & Capabilities: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Linux capability state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "Linux capability",
      "Security, Users & Capabilities",
      "Linux capability — Security, Users & Capabilities: fails completely",
      "linux",
      "capability",
      "security",
      "users",
      "capabilities",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Linux capability — Security, Users & Capabilities: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Linux capability; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Linux capability — Security, Users & Capabilities: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Linux capability — Security, Users & Capabilities: fails completely”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Linux capability",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Linux capability configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Linux capability — Security, Users & Capabilities: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Linux capability inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Linux capability — Security, Users & Capabilities: fails completely”",
      "Effective Linux capability configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Linux capability — Security, Users & Capabilities: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Linux capability — Security, Users & Capabilities: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Linux capability — Security, Users & Capabilities: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Linux capability — Security, Users & Capabilities: fails completely”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Linux capability — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Linux capability",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Linux capability\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Linux capability\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Linux capability",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Linux capability — Security, Users & Capabilities: fails completely”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Linux capability — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Linux capability configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Linux capability configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Linux capability configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The Linux capability configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Linux capability — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Linux capability",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Linux capability\nThe effective runtime value differs from expectation.",
        "root": "For “Linux capability — Security, Users & Capabilities: fails completely”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Linux capability configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Linux capability configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Linux capability — Security, Users & Capabilities: fails completely”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Linux capability — Security, Users & Capabilities: fails completely: evidence found\nFor “Linux capability — Security, Users & Capabilities: fails completely”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Linux capability — Security, Users & Capabilities: fails completely”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Linux capability — Security, Users & Capabilities: fails completely: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Linux capability — Security, Users & Capabilities: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-383",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Linux capability — Security, Users & Capabilities: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Linux capability state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "Linux capability",
      "Security, Users & Capabilities",
      "Linux capability — Security, Users & Capabilities: behaves differently than configured",
      "linux",
      "capability",
      "security",
      "users",
      "capabilities",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Linux capability — Security, Users & Capabilities: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Linux capability; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Linux capability — Security, Users & Capabilities: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Linux capability — Security, Users & Capabilities: behaves differently than configured”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Linux capability",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Linux capability configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Linux capability — Security, Users & Capabilities: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Linux capability inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Linux capability — Security, Users & Capabilities: behaves differently than configured”",
      "Effective Linux capability configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Linux capability — Security, Users & Capabilities: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Linux capability — Security, Users & Capabilities: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Linux capability — Security, Users & Capabilities: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Linux capability — Security, Users & Capabilities: behaves differently than configured”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Linux capability — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Linux capability",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Linux capability\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Linux capability\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Linux capability",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Linux capability — Security, Users & Capabilities: behaves differently than configured”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Linux capability — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Linux capability configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Linux capability configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Linux capability configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The Linux capability configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Linux capability — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Linux capability",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Linux capability\nThe effective runtime value differs from expectation.",
        "root": "For “Linux capability — Security, Users & Capabilities: behaves differently than configured”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Linux capability configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Linux capability configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Linux capability — Security, Users & Capabilities: behaves differently than configured”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Linux capability — Security, Users & Capabilities: behaves differently than configured: evidence found\nFor “Linux capability — Security, Users & Capabilities: behaves differently than configured”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Linux capability — Security, Users & Capabilities: behaves differently than configured”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Linux capability — Security, Users & Capabilities: behaves differently than configured: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Linux capability — Security, Users & Capabilities: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-384",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Linux capability — Security, Users & Capabilities: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Linux capability state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "Linux capability",
      "Security, Users & Capabilities",
      "Linux capability — Security, Users & Capabilities: fails intermittently under load or restart",
      "linux",
      "capability",
      "security",
      "users",
      "capabilities",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Linux capability — Security, Users & Capabilities: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Linux capability; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Linux capability — Security, Users & Capabilities: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Linux capability — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Linux capability",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Linux capability configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Linux capability — Security, Users & Capabilities: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Linux capability inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Linux capability — Security, Users & Capabilities: fails intermittently under load or restart”",
      "Effective Linux capability configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Linux capability — Security, Users & Capabilities: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Linux capability — Security, Users & Capabilities: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Linux capability — Security, Users & Capabilities: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Linux capability — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Linux capability — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Linux capability",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Linux capability\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Linux capability\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Linux capability",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Linux capability — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Linux capability — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Linux capability configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Linux capability configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Linux capability configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The Linux capability configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Linux capability — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Linux capability",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Linux capability\nThe effective runtime value differs from expectation.",
        "root": "For “Linux capability — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Linux capability configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Linux capability configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Linux capability — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Linux capability — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nFor “Linux capability — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Linux capability — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Linux capability state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Linux capability — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Linux capability path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Linux capability — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-385",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Read-only root filesystem — Security, Users & Capabilities: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective read-only root filesystem state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "read-only root filesystem",
      "Security, Users & Capabilities",
      "Read-only root filesystem — Security, Users & Capabilities: fails completely",
      "read",
      "only",
      "root",
      "filesystem",
      "security",
      "users",
      "capabilities",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Read-only root filesystem — Security, Users & Capabilities: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected read-only root filesystem; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Read-only root filesystem — Security, Users & Capabilities: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Read-only root filesystem — Security, Users & Capabilities: fails completely”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for read-only root filesystem",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The read-only root filesystem configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Read-only root filesystem — Security, Users & Capabilities: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the read-only root filesystem inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Read-only root filesystem — Security, Users & Capabilities: fails completely”",
      "Effective read-only root filesystem configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Read-only root filesystem — Security, Users & Capabilities: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Read-only root filesystem — Security, Users & Capabilities: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Read-only root filesystem — Security, Users & Capabilities: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Read-only root filesystem — Security, Users & Capabilities: fails completely”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Read-only root filesystem — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for read-only root filesystem",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for read-only root filesystem\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for read-only root filesystem\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for read-only root filesystem",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Read-only root filesystem — Security, Users & Capabilities: fails completely”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Read-only root filesystem — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The read-only root filesystem configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The read-only root filesystem configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The read-only root filesystem configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The read-only root filesystem configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Read-only root filesystem — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for read-only root filesystem",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for read-only root filesystem\nThe effective runtime value differs from expectation.",
        "root": "For “Read-only root filesystem — Security, Users & Capabilities: fails completely”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The read-only root filesystem configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The read-only root filesystem configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Read-only root filesystem — Security, Users & Capabilities: fails completely”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Read-only root filesystem — Security, Users & Capabilities: fails completely: evidence found\nFor “Read-only root filesystem — Security, Users & Capabilities: fails completely”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Read-only root filesystem — Security, Users & Capabilities: fails completely”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Read-only root filesystem — Security, Users & Capabilities: fails completely: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Read-only root filesystem — Security, Users & Capabilities: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-386",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective read-only root filesystem state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "read-only root filesystem",
      "Security, Users & Capabilities",
      "Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured",
      "read",
      "only",
      "root",
      "filesystem",
      "security",
      "users",
      "capabilities",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected read-only root filesystem; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for read-only root filesystem",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The read-only root filesystem configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the read-only root filesystem inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured”",
      "Effective read-only root filesystem configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for read-only root filesystem",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for read-only root filesystem\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for read-only root filesystem\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for read-only root filesystem",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The read-only root filesystem configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The read-only root filesystem configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The read-only root filesystem configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The read-only root filesystem configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for read-only root filesystem",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for read-only root filesystem\nThe effective runtime value differs from expectation.",
        "root": "For “Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The read-only root filesystem configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The read-only root filesystem configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured: evidence found\nFor “Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Read-only root filesystem — Security, Users & Capabilities: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-387",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective read-only root filesystem state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "read-only root filesystem",
      "Security, Users & Capabilities",
      "Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart",
      "read",
      "only",
      "root",
      "filesystem",
      "security",
      "users",
      "capabilities",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected read-only root filesystem; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for read-only root filesystem",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The read-only root filesystem configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the read-only root filesystem inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart”",
      "Effective read-only root filesystem configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for read-only root filesystem",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for read-only root filesystem\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for read-only root filesystem\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for read-only root filesystem",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The read-only root filesystem configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The read-only root filesystem configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The read-only root filesystem configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The read-only root filesystem configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for read-only root filesystem",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for read-only root filesystem\nThe effective runtime value differs from expectation.",
        "root": "For “Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The read-only root filesystem configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The read-only root filesystem configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nFor “Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart”, the effective read-only root filesystem state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the read-only root filesystem path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Read-only root filesystem — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-388",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective seccomp/AppArmor/SELinux state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "seccomp/AppArmor/SELinux",
      "Security, Users & Capabilities",
      "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely",
      "seccomp",
      "apparmor",
      "selinux",
      "security",
      "users",
      "capabilities",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected seccomp/AppArmor/SELinux; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the seccomp/AppArmor/SELinux inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely”",
      "Effective seccomp/AppArmor/SELinux configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux\nThe effective runtime value differs from expectation.",
        "root": "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely: evidence found\nFor “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-389",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective seccomp/AppArmor/SELinux state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "seccomp/AppArmor/SELinux",
      "Security, Users & Capabilities",
      "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured",
      "seccomp",
      "apparmor",
      "selinux",
      "security",
      "users",
      "capabilities",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected seccomp/AppArmor/SELinux; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the seccomp/AppArmor/SELinux inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured”",
      "Effective seccomp/AppArmor/SELinux configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux\nThe effective runtime value differs from expectation.",
        "root": "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured: evidence found\nFor “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-390",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective seccomp/AppArmor/SELinux state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "seccomp/AppArmor/SELinux",
      "Security, Users & Capabilities",
      "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart",
      "seccomp",
      "apparmor",
      "selinux",
      "security",
      "users",
      "capabilities",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected seccomp/AppArmor/SELinux; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the seccomp/AppArmor/SELinux inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart”",
      "Effective seccomp/AppArmor/SELinux configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for seccomp/AppArmor/SELinux\nThe effective runtime value differs from expectation.",
        "root": "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The seccomp/AppArmor/SELinux configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nFor “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart”, the effective seccomp/AppArmor/SELinux state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the seccomp/AppArmor/SELinux path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Seccomp/AppArmor/SELinux — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-391",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Device access — Security, Users & Capabilities: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective device access state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "device access",
      "Security, Users & Capabilities",
      "Device access — Security, Users & Capabilities: fails completely",
      "device",
      "access",
      "security",
      "users",
      "capabilities",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Device access — Security, Users & Capabilities: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected device access; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Device access — Security, Users & Capabilities: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Device access — Security, Users & Capabilities: fails completely”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for device access",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The device access configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Device access — Security, Users & Capabilities: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the device access inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Device access — Security, Users & Capabilities: fails completely”",
      "Effective device access configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Device access — Security, Users & Capabilities: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Device access — Security, Users & Capabilities: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Device access — Security, Users & Capabilities: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Device access — Security, Users & Capabilities: fails completely”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Device access — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for device access",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for device access\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for device access\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for device access",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Device access — Security, Users & Capabilities: fails completely”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Device access — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The device access configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The device access configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The device access configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The device access configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Device access — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for device access",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for device access\nThe effective runtime value differs from expectation.",
        "root": "For “Device access — Security, Users & Capabilities: fails completely”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The device access configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The device access configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Device access — Security, Users & Capabilities: fails completely”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Device access — Security, Users & Capabilities: fails completely: evidence found\nFor “Device access — Security, Users & Capabilities: fails completely”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Device access — Security, Users & Capabilities: fails completely”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Device access — Security, Users & Capabilities: fails completely: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Device access — Security, Users & Capabilities: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-392",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Device access — Security, Users & Capabilities: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective device access state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "device access",
      "Security, Users & Capabilities",
      "Device access — Security, Users & Capabilities: behaves differently than configured",
      "device",
      "access",
      "security",
      "users",
      "capabilities",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Device access — Security, Users & Capabilities: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected device access; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Device access — Security, Users & Capabilities: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Device access — Security, Users & Capabilities: behaves differently than configured”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for device access",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The device access configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Device access — Security, Users & Capabilities: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the device access inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Device access — Security, Users & Capabilities: behaves differently than configured”",
      "Effective device access configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Device access — Security, Users & Capabilities: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Device access — Security, Users & Capabilities: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Device access — Security, Users & Capabilities: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Device access — Security, Users & Capabilities: behaves differently than configured”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Device access — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for device access",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for device access\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for device access\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for device access",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Device access — Security, Users & Capabilities: behaves differently than configured”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Device access — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The device access configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The device access configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The device access configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The device access configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Device access — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for device access",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for device access\nThe effective runtime value differs from expectation.",
        "root": "For “Device access — Security, Users & Capabilities: behaves differently than configured”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The device access configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The device access configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Device access — Security, Users & Capabilities: behaves differently than configured”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Device access — Security, Users & Capabilities: behaves differently than configured: evidence found\nFor “Device access — Security, Users & Capabilities: behaves differently than configured”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Device access — Security, Users & Capabilities: behaves differently than configured”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Device access — Security, Users & Capabilities: behaves differently than configured: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Device access — Security, Users & Capabilities: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-393",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Device access — Security, Users & Capabilities: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective device access state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "device access",
      "Security, Users & Capabilities",
      "Device access — Security, Users & Capabilities: fails intermittently under load or restart",
      "device",
      "access",
      "security",
      "users",
      "capabilities",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Device access — Security, Users & Capabilities: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected device access; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Device access — Security, Users & Capabilities: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Device access — Security, Users & Capabilities: fails intermittently under load or restart”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for device access",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The device access configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Device access — Security, Users & Capabilities: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the device access inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Device access — Security, Users & Capabilities: fails intermittently under load or restart”",
      "Effective device access configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Device access — Security, Users & Capabilities: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Device access — Security, Users & Capabilities: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Device access — Security, Users & Capabilities: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Device access — Security, Users & Capabilities: fails intermittently under load or restart”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Device access — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for device access",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for device access\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for device access\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for device access",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Device access — Security, Users & Capabilities: fails intermittently under load or restart”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Device access — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The device access configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The device access configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The device access configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The device access configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Device access — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for device access",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for device access\nThe effective runtime value differs from expectation.",
        "root": "For “Device access — Security, Users & Capabilities: fails intermittently under load or restart”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The device access configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The device access configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Device access — Security, Users & Capabilities: fails intermittently under load or restart”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Device access — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nFor “Device access — Security, Users & Capabilities: fails intermittently under load or restart”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Device access — Security, Users & Capabilities: fails intermittently under load or restart”, the effective device access state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Device access — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the device access path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Device access — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-394",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Docker socket mount — Security, Users & Capabilities: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Docker socket mount state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "Docker socket mount",
      "Security, Users & Capabilities",
      "Docker socket mount — Security, Users & Capabilities: fails completely",
      "docker",
      "socket",
      "mount",
      "security",
      "users",
      "capabilities",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker socket mount — Security, Users & Capabilities: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker socket mount; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Docker socket mount — Security, Users & Capabilities: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker socket mount — Security, Users & Capabilities: fails completely”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker socket mount",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker socket mount configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker socket mount — Security, Users & Capabilities: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Docker socket mount inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker socket mount — Security, Users & Capabilities: fails completely”",
      "Effective Docker socket mount configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker socket mount — Security, Users & Capabilities: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker socket mount — Security, Users & Capabilities: fails completely”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker socket mount — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker socket mount",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket mount\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket mount\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker socket mount",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker socket mount — Security, Users & Capabilities: fails completely”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker socket mount — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker socket mount configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket mount configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket mount configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The Docker socket mount configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker socket mount — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker socket mount",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket mount\nThe effective runtime value differs from expectation.",
        "root": "For “Docker socket mount — Security, Users & Capabilities: fails completely”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker socket mount configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket mount configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker socket mount — Security, Users & Capabilities: fails completely”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker socket mount — Security, Users & Capabilities: fails completely: evidence found\nFor “Docker socket mount — Security, Users & Capabilities: fails completely”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker socket mount — Security, Users & Capabilities: fails completely”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Docker socket mount — Security, Users & Capabilities: fails completely: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Docker socket mount — Security, Users & Capabilities: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-395",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Docker socket mount — Security, Users & Capabilities: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Docker socket mount state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "Docker socket mount",
      "Security, Users & Capabilities",
      "Docker socket mount — Security, Users & Capabilities: behaves differently than configured",
      "docker",
      "socket",
      "mount",
      "security",
      "users",
      "capabilities",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker socket mount — Security, Users & Capabilities: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker socket mount; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Docker socket mount — Security, Users & Capabilities: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker socket mount — Security, Users & Capabilities: behaves differently than configured”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker socket mount",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker socket mount configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker socket mount — Security, Users & Capabilities: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Docker socket mount inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker socket mount — Security, Users & Capabilities: behaves differently than configured”",
      "Effective Docker socket mount configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker socket mount — Security, Users & Capabilities: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker socket mount — Security, Users & Capabilities: behaves differently than configured”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker socket mount — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker socket mount",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket mount\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket mount\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker socket mount",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker socket mount — Security, Users & Capabilities: behaves differently than configured”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker socket mount — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker socket mount configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket mount configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket mount configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The Docker socket mount configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker socket mount — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker socket mount",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket mount\nThe effective runtime value differs from expectation.",
        "root": "For “Docker socket mount — Security, Users & Capabilities: behaves differently than configured”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker socket mount configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket mount configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker socket mount — Security, Users & Capabilities: behaves differently than configured”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker socket mount — Security, Users & Capabilities: behaves differently than configured: evidence found\nFor “Docker socket mount — Security, Users & Capabilities: behaves differently than configured”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker socket mount — Security, Users & Capabilities: behaves differently than configured”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Docker socket mount — Security, Users & Capabilities: behaves differently than configured: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Docker socket mount — Security, Users & Capabilities: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-396",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Docker socket mount state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "Docker socket mount",
      "Security, Users & Capabilities",
      "Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart",
      "docker",
      "socket",
      "mount",
      "security",
      "users",
      "capabilities",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker socket mount; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker socket mount",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker socket mount configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Docker socket mount inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart”",
      "Effective Docker socket mount configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker socket mount",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket mount\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket mount\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker socket mount",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker socket mount configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket mount configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket mount configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The Docker socket mount configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker socket mount",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker socket mount\nThe effective runtime value differs from expectation.",
        "root": "For “Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker socket mount configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker socket mount configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nFor “Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart”, the effective Docker socket mount state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker socket mount path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Docker socket mount — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-397",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Secret exposure — Security, Users & Capabilities: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective secret exposure state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "secret exposure",
      "Security, Users & Capabilities",
      "Secret exposure — Security, Users & Capabilities: fails completely",
      "secret",
      "exposure",
      "security",
      "users",
      "capabilities",
      "fails",
      "completely"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Secret exposure — Security, Users & Capabilities: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected secret exposure; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Secret exposure — Security, Users & Capabilities: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Secret exposure — Security, Users & Capabilities: fails completely”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for secret exposure",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The secret exposure configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Secret exposure — Security, Users & Capabilities: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the secret exposure inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Secret exposure — Security, Users & Capabilities: fails completely”",
      "Effective secret exposure configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Secret exposure — Security, Users & Capabilities: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Secret exposure — Security, Users & Capabilities: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Secret exposure — Security, Users & Capabilities: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Secret exposure — Security, Users & Capabilities: fails completely”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Secret exposure — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for secret exposure",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for secret exposure\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for secret exposure\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for secret exposure",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Secret exposure — Security, Users & Capabilities: fails completely”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Secret exposure — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The secret exposure configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The secret exposure configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The secret exposure configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The secret exposure configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Secret exposure — Security, Users & Capabilities: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for secret exposure",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for secret exposure\nThe effective runtime value differs from expectation.",
        "root": "For “Secret exposure — Security, Users & Capabilities: fails completely”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The secret exposure configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The secret exposure configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Secret exposure — Security, Users & Capabilities: fails completely”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Secret exposure — Security, Users & Capabilities: fails completely: evidence found\nFor “Secret exposure — Security, Users & Capabilities: fails completely”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Secret exposure — Security, Users & Capabilities: fails completely”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Secret exposure — Security, Users & Capabilities: fails completely: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Secret exposure — Security, Users & Capabilities: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-398",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Secret exposure — Security, Users & Capabilities: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective secret exposure state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "secret exposure",
      "Security, Users & Capabilities",
      "Secret exposure — Security, Users & Capabilities: behaves differently than configured",
      "secret",
      "exposure",
      "security",
      "users",
      "capabilities",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Secret exposure — Security, Users & Capabilities: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected secret exposure; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Secret exposure — Security, Users & Capabilities: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Secret exposure — Security, Users & Capabilities: behaves differently than configured”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for secret exposure",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The secret exposure configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Secret exposure — Security, Users & Capabilities: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the secret exposure inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Secret exposure — Security, Users & Capabilities: behaves differently than configured”",
      "Effective secret exposure configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Secret exposure — Security, Users & Capabilities: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Secret exposure — Security, Users & Capabilities: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Secret exposure — Security, Users & Capabilities: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Secret exposure — Security, Users & Capabilities: behaves differently than configured”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Secret exposure — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for secret exposure",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for secret exposure\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for secret exposure\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for secret exposure",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Secret exposure — Security, Users & Capabilities: behaves differently than configured”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Secret exposure — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The secret exposure configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The secret exposure configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The secret exposure configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The secret exposure configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Secret exposure — Security, Users & Capabilities: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for secret exposure",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for secret exposure\nThe effective runtime value differs from expectation.",
        "root": "For “Secret exposure — Security, Users & Capabilities: behaves differently than configured”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The secret exposure configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The secret exposure configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Secret exposure — Security, Users & Capabilities: behaves differently than configured”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Secret exposure — Security, Users & Capabilities: behaves differently than configured: evidence found\nFor “Secret exposure — Security, Users & Capabilities: behaves differently than configured”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Secret exposure — Security, Users & Capabilities: behaves differently than configured”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Secret exposure — Security, Users & Capabilities: behaves differently than configured: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Secret exposure — Security, Users & Capabilities: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-399",
    "technology": "Docker",
    "category": "Security, Users & Capabilities",
    "title": "Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective secret exposure state first, then narrow the failure through the security, users & capabilities dependency chain.",
    "aliases": [
      "secret exposure",
      "Security, Users & Capabilities",
      "Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart",
      "secret",
      "exposure",
      "security",
      "users",
      "capabilities",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "container"
    ],
    "steps": [
      {
        "title": "Inspect security settings",
        "command": "docker inspect <container> --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect mounts",
        "command": "docker inspect <container> --format '{{json .Mounts}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected secret exposure; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect environment carefully",
        "command": "docker inspect <container> --format '{{json .Config.Env}}'",
        "risk": "SENSITIVE READ",
        "look": "Use this output to decide whether “Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect process users",
        "command": "docker top <container> -eo pid,ppid,user,group,args",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
      "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for secret exposure",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The secret exposure configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the secret exposure inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart”",
      "Effective secret exposure configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Security, Users & Capabilities Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
          "output": "Observed Docker state for Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker inspect payments-api --format '{{json .Mounts}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Security, Users & Capabilities path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for secret exposure",
        "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for secret exposure\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for secret exposure\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Mounts}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for secret exposure",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The secret exposure configuration is correct but the failure remains",
        "command": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The secret exposure configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Mounts}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The secret exposure configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker inspect checkout-api --format '{{json .Config.Env}}'",
            "output": "Evidence matches this path: The secret exposure configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker inspect payments-api --format '{{json .Config.Env}}'",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker top checkout-api -eo pid,ppid,user,group,args",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for secret exposure",
        "check": "docker inspect payments-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for secret exposure\nThe effective runtime value differs from expectation.",
        "root": "For “Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The secret exposure configuration is correct but the failure remains",
        "check": "docker inspect payments-api --format '{{json .Mounts}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The secret exposure configuration is correct but the failure remains",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker inspect payments-api --format '{{json .Config.Env}}'",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker inspect checkout-api --format 'User={{.Config.User}} Privileged={{.HostConfig.Privileged}} ReadonlyRootfs={{.HostConfig.ReadonlyRootfs}} CapAdd={{json .HostConfig.CapAdd}} CapDrop={{json .HostConfig.CapDrop}} SecurityOpt={{json .HostConfig.SecurityOpt}}'",
        "output": "Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nFor “Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart”, the effective secret exposure state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "check": "docker inspect checkout-api --format '{{json .Mounts}}'",
        "output": "Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nA security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "root": "A security, users & capabilities dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the secret exposure path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker inspect checkout-api --format '{{json .Config.Env}}'",
        "output": "Secret exposure — Security, Users & Capabilities: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker top checkout-api -eo pid,ppid,user,group,args",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-400",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless daemon — Rootless Docker: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective rootless daemon state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless daemon",
      "Rootless Docker",
      "Rootless daemon — Rootless Docker: fails completely",
      "rootless",
      "daemon",
      "docker",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless daemon — Rootless Docker: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless daemon; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless daemon — Rootless Docker: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless daemon — Rootless Docker: fails completely”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless daemon",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless daemon configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless daemon — Rootless Docker: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the rootless daemon inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless daemon — Rootless Docker: fails completely”",
      "Effective rootless daemon configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless daemon — Rootless Docker: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless daemon — Rootless Docker: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless daemon — Rootless Docker: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless daemon — Rootless Docker: fails completely”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless daemon — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless daemon",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless daemon\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless daemon\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless daemon",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless daemon — Rootless Docker: fails completely”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless daemon — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless daemon configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless daemon configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless daemon configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless daemon configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless daemon — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless daemon",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless daemon\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless daemon — Rootless Docker: fails completely”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless daemon configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless daemon configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless daemon — Rootless Docker: fails completely”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless daemon — Rootless Docker: fails completely: evidence found\nFor “Rootless daemon — Rootless Docker: fails completely”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless daemon — Rootless Docker: fails completely”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless daemon — Rootless Docker: fails completely: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless daemon — Rootless Docker: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-401",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless daemon — Rootless Docker: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective rootless daemon state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless daemon",
      "Rootless Docker",
      "Rootless daemon — Rootless Docker: behaves differently than configured",
      "rootless",
      "daemon",
      "docker",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless daemon — Rootless Docker: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless daemon; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless daemon — Rootless Docker: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless daemon — Rootless Docker: behaves differently than configured”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless daemon",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless daemon configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless daemon — Rootless Docker: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the rootless daemon inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless daemon — Rootless Docker: behaves differently than configured”",
      "Effective rootless daemon configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless daemon — Rootless Docker: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless daemon — Rootless Docker: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless daemon — Rootless Docker: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless daemon — Rootless Docker: behaves differently than configured”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless daemon — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless daemon",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless daemon\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless daemon\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless daemon",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless daemon — Rootless Docker: behaves differently than configured”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless daemon — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless daemon configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless daemon configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless daemon configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless daemon configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless daemon — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless daemon",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless daemon\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless daemon — Rootless Docker: behaves differently than configured”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless daemon configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless daemon configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless daemon — Rootless Docker: behaves differently than configured”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless daemon — Rootless Docker: behaves differently than configured: evidence found\nFor “Rootless daemon — Rootless Docker: behaves differently than configured”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless daemon — Rootless Docker: behaves differently than configured”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless daemon — Rootless Docker: behaves differently than configured: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless daemon — Rootless Docker: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-402",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless daemon — Rootless Docker: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective rootless daemon state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless daemon",
      "Rootless Docker",
      "Rootless daemon — Rootless Docker: fails intermittently under load or restart",
      "rootless",
      "daemon",
      "docker",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless daemon — Rootless Docker: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless daemon; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless daemon — Rootless Docker: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless daemon — Rootless Docker: fails intermittently under load or restart”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless daemon",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless daemon configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless daemon — Rootless Docker: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the rootless daemon inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless daemon — Rootless Docker: fails intermittently under load or restart”",
      "Effective rootless daemon configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless daemon — Rootless Docker: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless daemon — Rootless Docker: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless daemon — Rootless Docker: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless daemon — Rootless Docker: fails intermittently under load or restart”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless daemon — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless daemon",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless daemon\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless daemon\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless daemon",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless daemon — Rootless Docker: fails intermittently under load or restart”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless daemon — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless daemon configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless daemon configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless daemon configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless daemon configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless daemon — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless daemon",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless daemon\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless daemon — Rootless Docker: fails intermittently under load or restart”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless daemon configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless daemon configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless daemon — Rootless Docker: fails intermittently under load or restart”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless daemon — Rootless Docker: fails intermittently under load or restart: evidence found\nFor “Rootless daemon — Rootless Docker: fails intermittently under load or restart”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless daemon — Rootless Docker: fails intermittently under load or restart”, the effective rootless daemon state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless daemon — Rootless Docker: fails intermittently under load or restart: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless daemon path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless daemon — Rootless Docker: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-403",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless context/socket — Rootless Docker: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective rootless context/socket state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless context/socket",
      "Rootless Docker",
      "Rootless context/socket — Rootless Docker: fails completely",
      "rootless",
      "context",
      "socket",
      "docker",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless context/socket — Rootless Docker: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless context/socket; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless context/socket — Rootless Docker: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless context/socket — Rootless Docker: fails completely”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless context/socket",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless context/socket configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless context/socket — Rootless Docker: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the rootless context/socket inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless context/socket — Rootless Docker: fails completely”",
      "Effective rootless context/socket configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless context/socket — Rootless Docker: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless context/socket — Rootless Docker: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless context/socket — Rootless Docker: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless context/socket — Rootless Docker: fails completely”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless context/socket — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless context/socket",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless context/socket\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless context/socket\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless context/socket",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless context/socket — Rootless Docker: fails completely”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless context/socket — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless context/socket configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless context/socket configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless context/socket configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless context/socket configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless context/socket — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless context/socket",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless context/socket\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless context/socket — Rootless Docker: fails completely”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless context/socket configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless context/socket configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless context/socket — Rootless Docker: fails completely”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless context/socket — Rootless Docker: fails completely: evidence found\nFor “Rootless context/socket — Rootless Docker: fails completely”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless context/socket — Rootless Docker: fails completely”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless context/socket — Rootless Docker: fails completely: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless context/socket — Rootless Docker: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-404",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless context/socket — Rootless Docker: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective rootless context/socket state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless context/socket",
      "Rootless Docker",
      "Rootless context/socket — Rootless Docker: behaves differently than configured",
      "rootless",
      "context",
      "socket",
      "docker",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless context/socket — Rootless Docker: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless context/socket; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless context/socket — Rootless Docker: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless context/socket — Rootless Docker: behaves differently than configured”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless context/socket",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless context/socket configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless context/socket — Rootless Docker: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the rootless context/socket inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless context/socket — Rootless Docker: behaves differently than configured”",
      "Effective rootless context/socket configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless context/socket — Rootless Docker: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless context/socket — Rootless Docker: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless context/socket — Rootless Docker: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless context/socket — Rootless Docker: behaves differently than configured”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless context/socket — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless context/socket",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless context/socket\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless context/socket\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless context/socket",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless context/socket — Rootless Docker: behaves differently than configured”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless context/socket — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless context/socket configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless context/socket configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless context/socket configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless context/socket configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless context/socket — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless context/socket",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless context/socket\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless context/socket — Rootless Docker: behaves differently than configured”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless context/socket configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless context/socket configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless context/socket — Rootless Docker: behaves differently than configured”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless context/socket — Rootless Docker: behaves differently than configured: evidence found\nFor “Rootless context/socket — Rootless Docker: behaves differently than configured”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless context/socket — Rootless Docker: behaves differently than configured”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless context/socket — Rootless Docker: behaves differently than configured: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless context/socket — Rootless Docker: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-405",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless context/socket — Rootless Docker: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective rootless context/socket state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless context/socket",
      "Rootless Docker",
      "Rootless context/socket — Rootless Docker: fails intermittently under load or restart",
      "rootless",
      "context",
      "socket",
      "docker",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless context/socket — Rootless Docker: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless context/socket; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless context/socket — Rootless Docker: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless context/socket — Rootless Docker: fails intermittently under load or restart”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless context/socket",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless context/socket configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless context/socket — Rootless Docker: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the rootless context/socket inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless context/socket — Rootless Docker: fails intermittently under load or restart”",
      "Effective rootless context/socket configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless context/socket — Rootless Docker: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless context/socket — Rootless Docker: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless context/socket — Rootless Docker: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless context/socket — Rootless Docker: fails intermittently under load or restart”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless context/socket — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless context/socket",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless context/socket\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless context/socket\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless context/socket",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless context/socket — Rootless Docker: fails intermittently under load or restart”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless context/socket — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless context/socket configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless context/socket configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless context/socket configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless context/socket configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless context/socket — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless context/socket",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless context/socket\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless context/socket — Rootless Docker: fails intermittently under load or restart”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless context/socket configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless context/socket configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless context/socket — Rootless Docker: fails intermittently under load or restart”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless context/socket — Rootless Docker: fails intermittently under load or restart: evidence found\nFor “Rootless context/socket — Rootless Docker: fails intermittently under load or restart”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless context/socket — Rootless Docker: fails intermittently under load or restart”, the effective rootless context/socket state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless context/socket — Rootless Docker: fails intermittently under load or restart: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless context/socket path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless context/socket — Rootless Docker: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-406",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Privileged port publishing — Rootless Docker: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective privileged port publishing state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "privileged port publishing",
      "Rootless Docker",
      "Privileged port publishing — Rootless Docker: fails completely",
      "privileged",
      "port",
      "publishing",
      "rootless",
      "docker",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Privileged port publishing — Rootless Docker: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected privileged port publishing; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Privileged port publishing — Rootless Docker: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Privileged port publishing — Rootless Docker: fails completely”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for privileged port publishing",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The privileged port publishing configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Privileged port publishing — Rootless Docker: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the privileged port publishing inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Privileged port publishing — Rootless Docker: fails completely”",
      "Effective privileged port publishing configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Privileged port publishing — Rootless Docker: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Privileged port publishing — Rootless Docker: fails completely”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Privileged port publishing — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for privileged port publishing",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for privileged port publishing\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for privileged port publishing\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for privileged port publishing",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Privileged port publishing — Rootless Docker: fails completely”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Privileged port publishing — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The privileged port publishing configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The privileged port publishing configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The privileged port publishing configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The privileged port publishing configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Privileged port publishing — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for privileged port publishing",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for privileged port publishing\nThe effective runtime value differs from expectation.",
        "root": "For “Privileged port publishing — Rootless Docker: fails completely”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The privileged port publishing configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The privileged port publishing configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Privileged port publishing — Rootless Docker: fails completely”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Privileged port publishing — Rootless Docker: fails completely: evidence found\nFor “Privileged port publishing — Rootless Docker: fails completely”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Privileged port publishing — Rootless Docker: fails completely”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Privileged port publishing — Rootless Docker: fails completely: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Privileged port publishing — Rootless Docker: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-407",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Privileged port publishing — Rootless Docker: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective privileged port publishing state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "privileged port publishing",
      "Rootless Docker",
      "Privileged port publishing — Rootless Docker: behaves differently than configured",
      "privileged",
      "port",
      "publishing",
      "rootless",
      "docker",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Privileged port publishing — Rootless Docker: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected privileged port publishing; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Privileged port publishing — Rootless Docker: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Privileged port publishing — Rootless Docker: behaves differently than configured”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for privileged port publishing",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The privileged port publishing configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Privileged port publishing — Rootless Docker: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the privileged port publishing inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Privileged port publishing — Rootless Docker: behaves differently than configured”",
      "Effective privileged port publishing configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Privileged port publishing — Rootless Docker: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Privileged port publishing — Rootless Docker: behaves differently than configured”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Privileged port publishing — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for privileged port publishing",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for privileged port publishing\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for privileged port publishing\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for privileged port publishing",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Privileged port publishing — Rootless Docker: behaves differently than configured”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Privileged port publishing — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The privileged port publishing configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The privileged port publishing configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The privileged port publishing configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The privileged port publishing configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Privileged port publishing — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for privileged port publishing",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for privileged port publishing\nThe effective runtime value differs from expectation.",
        "root": "For “Privileged port publishing — Rootless Docker: behaves differently than configured”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The privileged port publishing configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The privileged port publishing configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Privileged port publishing — Rootless Docker: behaves differently than configured”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Privileged port publishing — Rootless Docker: behaves differently than configured: evidence found\nFor “Privileged port publishing — Rootless Docker: behaves differently than configured”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Privileged port publishing — Rootless Docker: behaves differently than configured”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Privileged port publishing — Rootless Docker: behaves differently than configured: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Privileged port publishing — Rootless Docker: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-408",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Privileged port publishing — Rootless Docker: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective privileged port publishing state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "privileged port publishing",
      "Rootless Docker",
      "Privileged port publishing — Rootless Docker: fails intermittently under load or restart",
      "privileged",
      "port",
      "publishing",
      "rootless",
      "docker",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Privileged port publishing — Rootless Docker: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected privileged port publishing; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Privileged port publishing — Rootless Docker: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Privileged port publishing — Rootless Docker: fails intermittently under load or restart”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for privileged port publishing",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The privileged port publishing configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Privileged port publishing — Rootless Docker: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the privileged port publishing inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Privileged port publishing — Rootless Docker: fails intermittently under load or restart”",
      "Effective privileged port publishing configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Privileged port publishing — Rootless Docker: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "8080/tcp -> 0.0.0.0:8080",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Gateway\":\"172.22.0.1\"}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Port bindings: 8080/tcp -> HostPort 8080\nApplication log: listening on 127.0.0.1:8080",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "Docker publishes the port correctly, but the application listens only on container loopback instead of an externally reachable interface.",
      "remediation": "Configure the application to listen on the intended container interface (commonly 0.0.0.0) and recreate it from the source configuration.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Port publishing cannot make a process reachable if that process only listens on 127.0.0.1 inside the container.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Privileged port publishing — Rootless Docker: fails intermittently under load or restart”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Privileged port publishing — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for privileged port publishing",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for privileged port publishing\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for privileged port publishing\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for privileged port publishing",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Privileged port publishing — Rootless Docker: fails intermittently under load or restart”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Privileged port publishing — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The privileged port publishing configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The privileged port publishing configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The privileged port publishing configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The privileged port publishing configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Privileged port publishing — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for privileged port publishing",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for privileged port publishing\nThe effective runtime value differs from expectation.",
        "root": "For “Privileged port publishing — Rootless Docker: fails intermittently under load or restart”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The privileged port publishing configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The privileged port publishing configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Privileged port publishing — Rootless Docker: fails intermittently under load or restart”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Privileged port publishing — Rootless Docker: fails intermittently under load or restart: evidence found\nFor “Privileged port publishing — Rootless Docker: fails intermittently under load or restart”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Privileged port publishing — Rootless Docker: fails intermittently under load or restart”, the effective privileged port publishing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Privileged port publishing — Rootless Docker: fails intermittently under load or restart: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the privileged port publishing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Privileged port publishing — Rootless Docker: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-409",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless networking — Rootless Docker: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective rootless networking state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless networking",
      "Rootless Docker",
      "Rootless networking — Rootless Docker: fails completely",
      "rootless",
      "networking",
      "docker",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless networking — Rootless Docker: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless networking; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless networking — Rootless Docker: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless networking — Rootless Docker: fails completely”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless networking",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless networking configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless networking — Rootless Docker: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the rootless networking inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless networking — Rootless Docker: fails completely”",
      "Effective rootless networking configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless networking — Rootless Docker: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless networking — Rootless Docker: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless networking — Rootless Docker: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless networking — Rootless Docker: fails completely”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless networking — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless networking",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless networking\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless networking\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless networking",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless networking — Rootless Docker: fails completely”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless networking — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless networking configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless networking configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless networking configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless networking configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless networking — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless networking",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless networking\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless networking — Rootless Docker: fails completely”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless networking configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless networking configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless networking — Rootless Docker: fails completely”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless networking — Rootless Docker: fails completely: evidence found\nFor “Rootless networking — Rootless Docker: fails completely”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless networking — Rootless Docker: fails completely”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless networking — Rootless Docker: fails completely: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless networking — Rootless Docker: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-410",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless networking — Rootless Docker: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective rootless networking state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless networking",
      "Rootless Docker",
      "Rootless networking — Rootless Docker: behaves differently than configured",
      "rootless",
      "networking",
      "docker",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless networking — Rootless Docker: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless networking; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless networking — Rootless Docker: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless networking — Rootless Docker: behaves differently than configured”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless networking",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless networking configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless networking — Rootless Docker: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the rootless networking inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless networking — Rootless Docker: behaves differently than configured”",
      "Effective rootless networking configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless networking — Rootless Docker: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless networking — Rootless Docker: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless networking — Rootless Docker: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless networking — Rootless Docker: behaves differently than configured”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless networking — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless networking",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless networking\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless networking\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless networking",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless networking — Rootless Docker: behaves differently than configured”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless networking — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless networking configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless networking configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless networking configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless networking configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless networking — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless networking",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless networking\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless networking — Rootless Docker: behaves differently than configured”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless networking configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless networking configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless networking — Rootless Docker: behaves differently than configured”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless networking — Rootless Docker: behaves differently than configured: evidence found\nFor “Rootless networking — Rootless Docker: behaves differently than configured”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless networking — Rootless Docker: behaves differently than configured”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless networking — Rootless Docker: behaves differently than configured: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless networking — Rootless Docker: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-411",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless networking — Rootless Docker: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective rootless networking state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless networking",
      "Rootless Docker",
      "Rootless networking — Rootless Docker: fails intermittently under load or restart",
      "rootless",
      "networking",
      "docker",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless networking — Rootless Docker: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless networking; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless networking — Rootless Docker: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless networking — Rootless Docker: fails intermittently under load or restart”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless networking",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless networking configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless networking — Rootless Docker: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the rootless networking inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless networking — Rootless Docker: fails intermittently under load or restart”",
      "Effective rootless networking configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless networking — Rootless Docker: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless networking — Rootless Docker: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless networking — Rootless Docker: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless networking — Rootless Docker: fails intermittently under load or restart”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless networking — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless networking",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless networking\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless networking\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless networking",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless networking — Rootless Docker: fails intermittently under load or restart”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless networking — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless networking configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless networking configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless networking configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless networking configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless networking — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless networking",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless networking\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless networking — Rootless Docker: fails intermittently under load or restart”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless networking configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless networking configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless networking — Rootless Docker: fails intermittently under load or restart”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless networking — Rootless Docker: fails intermittently under load or restart: evidence found\nFor “Rootless networking — Rootless Docker: fails intermittently under load or restart”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless networking — Rootless Docker: fails intermittently under load or restart”, the effective rootless networking state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless networking — Rootless Docker: fails intermittently under load or restart: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless networking path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless networking — Rootless Docker: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-412",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless cgroup limits — Rootless Docker: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective rootless cgroup limits state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless cgroup limits",
      "Rootless Docker",
      "Rootless cgroup limits — Rootless Docker: fails completely",
      "rootless",
      "cgroup",
      "limits",
      "docker",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless cgroup limits — Rootless Docker: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless cgroup limits; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless cgroup limits — Rootless Docker: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless cgroup limits — Rootless Docker: fails completely”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless cgroup limits",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless cgroup limits configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless cgroup limits — Rootless Docker: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the rootless cgroup limits inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless cgroup limits — Rootless Docker: fails completely”",
      "Effective rootless cgroup limits configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless cgroup limits — Rootless Docker: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless cgroup limits — Rootless Docker: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless cgroup limits — Rootless Docker: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless cgroup limits — Rootless Docker: fails completely”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless cgroup limits — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless cgroup limits",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless cgroup limits\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless cgroup limits\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless cgroup limits",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless cgroup limits — Rootless Docker: fails completely”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless cgroup limits — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless cgroup limits configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless cgroup limits configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless cgroup limits configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless cgroup limits configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless cgroup limits — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless cgroup limits",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless cgroup limits\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless cgroup limits — Rootless Docker: fails completely”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless cgroup limits configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless cgroup limits configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless cgroup limits — Rootless Docker: fails completely”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless cgroup limits — Rootless Docker: fails completely: evidence found\nFor “Rootless cgroup limits — Rootless Docker: fails completely”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless cgroup limits — Rootless Docker: fails completely”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless cgroup limits — Rootless Docker: fails completely: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless cgroup limits — Rootless Docker: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-413",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless cgroup limits — Rootless Docker: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective rootless cgroup limits state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless cgroup limits",
      "Rootless Docker",
      "Rootless cgroup limits — Rootless Docker: behaves differently than configured",
      "rootless",
      "cgroup",
      "limits",
      "docker",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless cgroup limits — Rootless Docker: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless cgroup limits; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless cgroup limits — Rootless Docker: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless cgroup limits — Rootless Docker: behaves differently than configured”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless cgroup limits",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless cgroup limits configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless cgroup limits — Rootless Docker: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the rootless cgroup limits inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless cgroup limits — Rootless Docker: behaves differently than configured”",
      "Effective rootless cgroup limits configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless cgroup limits — Rootless Docker: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless cgroup limits — Rootless Docker: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless cgroup limits — Rootless Docker: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless cgroup limits — Rootless Docker: behaves differently than configured”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless cgroup limits — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless cgroup limits",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless cgroup limits\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless cgroup limits\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless cgroup limits",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless cgroup limits — Rootless Docker: behaves differently than configured”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless cgroup limits — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless cgroup limits configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless cgroup limits configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless cgroup limits configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless cgroup limits configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless cgroup limits — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless cgroup limits",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless cgroup limits\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless cgroup limits — Rootless Docker: behaves differently than configured”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless cgroup limits configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless cgroup limits configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless cgroup limits — Rootless Docker: behaves differently than configured”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless cgroup limits — Rootless Docker: behaves differently than configured: evidence found\nFor “Rootless cgroup limits — Rootless Docker: behaves differently than configured”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless cgroup limits — Rootless Docker: behaves differently than configured”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless cgroup limits — Rootless Docker: behaves differently than configured: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless cgroup limits — Rootless Docker: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-414",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective rootless cgroup limits state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless cgroup limits",
      "Rootless Docker",
      "Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart",
      "rootless",
      "cgroup",
      "limits",
      "docker",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless cgroup limits; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless cgroup limits",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless cgroup limits configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the rootless cgroup limits inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart”",
      "Effective rootless cgroup limits configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless cgroup limits",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless cgroup limits\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless cgroup limits\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless cgroup limits",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless cgroup limits configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless cgroup limits configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless cgroup limits configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless cgroup limits configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless cgroup limits",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless cgroup limits\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless cgroup limits configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless cgroup limits configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart: evidence found\nFor “Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart”, the effective rootless cgroup limits state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless cgroup limits path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless cgroup limits — Rootless Docker: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-415",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless storage driver — Rootless Docker: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective rootless storage driver state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless storage driver",
      "Rootless Docker",
      "Rootless storage driver — Rootless Docker: fails completely",
      "rootless",
      "storage",
      "driver",
      "docker",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless storage driver — Rootless Docker: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless storage driver; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless storage driver — Rootless Docker: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless storage driver — Rootless Docker: fails completely”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless storage driver",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless storage driver configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless storage driver — Rootless Docker: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the rootless storage driver inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless storage driver — Rootless Docker: fails completely”",
      "Effective rootless storage driver configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless storage driver — Rootless Docker: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless storage driver — Rootless Docker: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless storage driver — Rootless Docker: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless storage driver — Rootless Docker: fails completely”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless storage driver — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless storage driver",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless storage driver\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless storage driver\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless storage driver",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless storage driver — Rootless Docker: fails completely”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless storage driver — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless storage driver configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless storage driver configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless storage driver configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless storage driver configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless storage driver — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless storage driver",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless storage driver\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless storage driver — Rootless Docker: fails completely”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless storage driver configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless storage driver configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless storage driver — Rootless Docker: fails completely”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless storage driver — Rootless Docker: fails completely: evidence found\nFor “Rootless storage driver — Rootless Docker: fails completely”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless storage driver — Rootless Docker: fails completely”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless storage driver — Rootless Docker: fails completely: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless storage driver — Rootless Docker: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-416",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless storage driver — Rootless Docker: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective rootless storage driver state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless storage driver",
      "Rootless Docker",
      "Rootless storage driver — Rootless Docker: behaves differently than configured",
      "rootless",
      "storage",
      "driver",
      "docker",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless storage driver — Rootless Docker: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless storage driver; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless storage driver — Rootless Docker: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless storage driver — Rootless Docker: behaves differently than configured”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless storage driver",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless storage driver configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless storage driver — Rootless Docker: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the rootless storage driver inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless storage driver — Rootless Docker: behaves differently than configured”",
      "Effective rootless storage driver configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless storage driver — Rootless Docker: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless storage driver — Rootless Docker: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless storage driver — Rootless Docker: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless storage driver — Rootless Docker: behaves differently than configured”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless storage driver — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless storage driver",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless storage driver\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless storage driver\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless storage driver",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless storage driver — Rootless Docker: behaves differently than configured”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless storage driver — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless storage driver configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless storage driver configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless storage driver configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless storage driver configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless storage driver — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless storage driver",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless storage driver\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless storage driver — Rootless Docker: behaves differently than configured”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless storage driver configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless storage driver configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless storage driver — Rootless Docker: behaves differently than configured”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless storage driver — Rootless Docker: behaves differently than configured: evidence found\nFor “Rootless storage driver — Rootless Docker: behaves differently than configured”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless storage driver — Rootless Docker: behaves differently than configured”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless storage driver — Rootless Docker: behaves differently than configured: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless storage driver — Rootless Docker: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-417",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless storage driver — Rootless Docker: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective rootless storage driver state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless storage driver",
      "Rootless Docker",
      "Rootless storage driver — Rootless Docker: fails intermittently under load or restart",
      "rootless",
      "storage",
      "driver",
      "docker",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless storage driver — Rootless Docker: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless storage driver; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless storage driver — Rootless Docker: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless storage driver — Rootless Docker: fails intermittently under load or restart”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless storage driver",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless storage driver configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless storage driver — Rootless Docker: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the rootless storage driver inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless storage driver — Rootless Docker: fails intermittently under load or restart”",
      "Effective rootless storage driver configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless storage driver — Rootless Docker: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "Observed Docker state for Rootless storage driver — Rootless Docker: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Rootless storage driver — Rootless Docker: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless storage driver — Rootless Docker: fails intermittently under load or restart”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless storage driver — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless storage driver",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless storage driver\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless storage driver\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless storage driver",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless storage driver — Rootless Docker: fails intermittently under load or restart”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless storage driver — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless storage driver configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless storage driver configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless storage driver configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless storage driver configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless storage driver — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless storage driver",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless storage driver\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless storage driver — Rootless Docker: fails intermittently under load or restart”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless storage driver configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless storage driver configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless storage driver — Rootless Docker: fails intermittently under load or restart”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless storage driver — Rootless Docker: fails intermittently under load or restart: evidence found\nFor “Rootless storage driver — Rootless Docker: fails intermittently under load or restart”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless storage driver — Rootless Docker: fails intermittently under load or restart”, the effective rootless storage driver state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless storage driver — Rootless Docker: fails intermittently under load or restart: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless storage driver path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless storage driver — Rootless Docker: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-418",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless volume ownership — Rootless Docker: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective rootless volume ownership state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless volume ownership",
      "Rootless Docker",
      "Rootless volume ownership — Rootless Docker: fails completely",
      "rootless",
      "volume",
      "ownership",
      "docker",
      "fails",
      "completely"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless volume ownership — Rootless Docker: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless volume ownership; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless volume ownership — Rootless Docker: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless volume ownership — Rootless Docker: fails completely”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless volume ownership",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless volume ownership configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless volume ownership — Rootless Docker: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the rootless volume ownership inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless volume ownership — Rootless Docker: fails completely”",
      "Effective rootless volume ownership configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless volume ownership — Rootless Docker: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless volume ownership — Rootless Docker: fails completely”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless volume ownership — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless volume ownership",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless volume ownership\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless volume ownership\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless volume ownership",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless volume ownership — Rootless Docker: fails completely”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless volume ownership — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless volume ownership configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless volume ownership configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless volume ownership configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless volume ownership configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless volume ownership — Rootless Docker: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless volume ownership",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless volume ownership\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless volume ownership — Rootless Docker: fails completely”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless volume ownership configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless volume ownership configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless volume ownership — Rootless Docker: fails completely”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless volume ownership — Rootless Docker: fails completely: evidence found\nFor “Rootless volume ownership — Rootless Docker: fails completely”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless volume ownership — Rootless Docker: fails completely”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless volume ownership — Rootless Docker: fails completely: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless volume ownership — Rootless Docker: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-419",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless volume ownership — Rootless Docker: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective rootless volume ownership state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless volume ownership",
      "Rootless Docker",
      "Rootless volume ownership — Rootless Docker: behaves differently than configured",
      "rootless",
      "volume",
      "ownership",
      "docker",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless volume ownership — Rootless Docker: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless volume ownership; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless volume ownership — Rootless Docker: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless volume ownership — Rootless Docker: behaves differently than configured”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless volume ownership",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless volume ownership configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless volume ownership — Rootless Docker: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the rootless volume ownership inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless volume ownership — Rootless Docker: behaves differently than configured”",
      "Effective rootless volume ownership configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless volume ownership — Rootless Docker: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless volume ownership — Rootless Docker: behaves differently than configured”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless volume ownership — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless volume ownership",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless volume ownership\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless volume ownership\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless volume ownership",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless volume ownership — Rootless Docker: behaves differently than configured”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless volume ownership — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless volume ownership configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless volume ownership configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless volume ownership configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless volume ownership configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless volume ownership — Rootless Docker: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless volume ownership",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless volume ownership\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless volume ownership — Rootless Docker: behaves differently than configured”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless volume ownership configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless volume ownership configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless volume ownership — Rootless Docker: behaves differently than configured”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless volume ownership — Rootless Docker: behaves differently than configured: evidence found\nFor “Rootless volume ownership — Rootless Docker: behaves differently than configured”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless volume ownership — Rootless Docker: behaves differently than configured”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless volume ownership — Rootless Docker: behaves differently than configured: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless volume ownership — Rootless Docker: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-420",
    "technology": "Docker",
    "category": "Rootless Docker",
    "title": "Rootless volume ownership — Rootless Docker: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective rootless volume ownership state first, then narrow the failure through the rootless docker dependency chain.",
    "aliases": [
      "rootless volume ownership",
      "Rootless Docker",
      "Rootless volume ownership — Rootless Docker: fails intermittently under load or restart",
      "rootless",
      "volume",
      "ownership",
      "docker",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [],
    "steps": [
      {
        "title": "Confirm context",
        "command": "docker context show",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Rootless volume ownership — Rootless Docker: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Inspect rootless/security state",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected rootless volume ownership; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "List contexts",
        "command": "docker context ls",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Rootless volume ownership — Rootless Docker: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect full engine state",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Rootless volume ownership — Rootless Docker: fails intermittently under load or restart”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
      "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for rootless volume ownership",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The rootless volume ownership configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Rootless volume ownership — Rootless Docker: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the rootless volume ownership inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Rootless volume ownership — Rootless Docker: fails intermittently under load or restart”",
      "Effective rootless volume ownership configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Rootless Docker Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Rootless volume ownership — Rootless Docker: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker context show",
          "output": "[{\"Type\":\"volume\",\"Name\":\"payments-data\",\"Destination\":\"/var/lib/payments\",\"RW\":true}]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
          "output": "[{\"Name\":\"payments-data\",\"Mountpoint\":\"/var/lib/docker/volumes/payments-data/_data\"}]",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker context ls",
          "output": "C /var/lib/payments/cache.tmp\nA /app/local-data.db",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The persistent volume is mounted, but the application writes important data to a different path in the container writable layer.",
      "remediation": "Align the application data path and volume destination in source configuration, migrate required data carefully, then recreate and verify persistence.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Seeing a volume attached is not enough; verify that the application actually writes persistent data under that mount.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Rootless volume ownership — Rootless Docker: fails intermittently under load or restart”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Rootless Docker path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Rootless volume ownership — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for rootless volume ownership",
        "command": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless volume ownership\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context show",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless volume ownership\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for rootless volume ownership",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Rootless volume ownership — Rootless Docker: fails intermittently under load or restart”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Rootless volume ownership — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The rootless volume ownership configuration is correct but the failure remains",
        "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless volume ownership configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless volume ownership configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker context ls",
            "output": "Evidence matches this path: The rootless volume ownership configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Rootless volume ownership — Rootless Docker: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker context ls",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker info",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker info",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for rootless volume ownership",
        "check": "docker context show",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for rootless volume ownership\nThe effective runtime value differs from expectation.",
        "root": "For “Rootless volume ownership — Rootless Docker: fails intermittently under load or restart”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The rootless volume ownership configuration is correct but the failure remains",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The rootless volume ownership configuration is correct but the failure remains",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker context ls",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Rootless volume ownership — Rootless Docker: fails intermittently under load or restart”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker context show",
        "output": "Rootless volume ownership — Rootless Docker: fails intermittently under load or restart: evidence found\nFor “Rootless volume ownership — Rootless Docker: fails intermittently under load or restart”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Rootless volume ownership — Rootless Docker: fails intermittently under load or restart”, the effective rootless volume ownership state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "check": "docker info --format 'RootDir={{.DockerRootDir}} CgroupDriver={{.CgroupDriver}} Security={{json .SecurityOptions}}'",
        "output": "Rootless volume ownership — Rootless Docker: fails intermittently under load or restart: evidence found\nA rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "root": "A rootless docker dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the rootless volume ownership path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker context ls",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker context ls",
        "output": "Rootless volume ownership — Rootless Docker: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker info",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-421",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Host firewall/VPN — Host Integration & Performance: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective host firewall/VPN state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "host firewall/VPN",
      "Host Integration & Performance",
      "Host firewall/VPN — Host Integration & Performance: fails completely",
      "host",
      "firewall",
      "vpn",
      "integration",
      "performance",
      "fails",
      "completely"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host firewall/VPN — Host Integration & Performance: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host firewall/VPN; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host firewall/VPN — Host Integration & Performance: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host firewall/VPN — Host Integration & Performance: fails completely”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host firewall/VPN",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host firewall/VPN configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host firewall/VPN — Host Integration & Performance: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the host firewall/VPN inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host firewall/VPN — Host Integration & Performance: fails completely”",
      "Effective host firewall/VPN configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host firewall/VPN — Host Integration & Performance: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Host firewall/VPN — Host Integration & Performance: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Host firewall/VPN — Host Integration & Performance: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host firewall/VPN — Host Integration & Performance: fails completely”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host firewall/VPN — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host firewall/VPN",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host firewall/VPN\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host firewall/VPN\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host firewall/VPN",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host firewall/VPN — Host Integration & Performance: fails completely”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host firewall/VPN — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host firewall/VPN configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host firewall/VPN configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host firewall/VPN configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The host firewall/VPN configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host firewall/VPN — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host firewall/VPN",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host firewall/VPN\nThe effective runtime value differs from expectation.",
        "root": "For “Host firewall/VPN — Host Integration & Performance: fails completely”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host firewall/VPN configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host firewall/VPN configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host firewall/VPN — Host Integration & Performance: fails completely”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Host firewall/VPN — Host Integration & Performance: fails completely: evidence found\nFor “Host firewall/VPN — Host Integration & Performance: fails completely”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host firewall/VPN — Host Integration & Performance: fails completely”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Host firewall/VPN — Host Integration & Performance: fails completely: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Host firewall/VPN — Host Integration & Performance: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-422",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Host firewall/VPN — Host Integration & Performance: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective host firewall/VPN state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "host firewall/VPN",
      "Host Integration & Performance",
      "Host firewall/VPN — Host Integration & Performance: behaves differently than configured",
      "host",
      "firewall",
      "vpn",
      "integration",
      "performance",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host firewall/VPN — Host Integration & Performance: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host firewall/VPN; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host firewall/VPN — Host Integration & Performance: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host firewall/VPN — Host Integration & Performance: behaves differently than configured”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host firewall/VPN",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host firewall/VPN configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host firewall/VPN — Host Integration & Performance: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the host firewall/VPN inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host firewall/VPN — Host Integration & Performance: behaves differently than configured”",
      "Effective host firewall/VPN configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host firewall/VPN — Host Integration & Performance: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Host firewall/VPN — Host Integration & Performance: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Host firewall/VPN — Host Integration & Performance: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host firewall/VPN — Host Integration & Performance: behaves differently than configured”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host firewall/VPN — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host firewall/VPN",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host firewall/VPN\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host firewall/VPN\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host firewall/VPN",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host firewall/VPN — Host Integration & Performance: behaves differently than configured”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host firewall/VPN — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host firewall/VPN configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host firewall/VPN configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host firewall/VPN configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The host firewall/VPN configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host firewall/VPN — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host firewall/VPN",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host firewall/VPN\nThe effective runtime value differs from expectation.",
        "root": "For “Host firewall/VPN — Host Integration & Performance: behaves differently than configured”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host firewall/VPN configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host firewall/VPN configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host firewall/VPN — Host Integration & Performance: behaves differently than configured”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Host firewall/VPN — Host Integration & Performance: behaves differently than configured: evidence found\nFor “Host firewall/VPN — Host Integration & Performance: behaves differently than configured”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host firewall/VPN — Host Integration & Performance: behaves differently than configured”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Host firewall/VPN — Host Integration & Performance: behaves differently than configured: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Host firewall/VPN — Host Integration & Performance: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-423",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective host firewall/VPN state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "host firewall/VPN",
      "Host Integration & Performance",
      "Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart",
      "host",
      "firewall",
      "vpn",
      "integration",
      "performance",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host firewall/VPN; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host firewall/VPN",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host firewall/VPN configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the host firewall/VPN inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart”",
      "Effective host firewall/VPN configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host firewall/VPN",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host firewall/VPN\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host firewall/VPN\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host firewall/VPN",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host firewall/VPN configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host firewall/VPN configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host firewall/VPN configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The host firewall/VPN configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host firewall/VPN",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host firewall/VPN\nThe effective runtime value differs from expectation.",
        "root": "For “Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host firewall/VPN configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host firewall/VPN configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart: evidence found\nFor “Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart”, the effective host firewall/VPN state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host firewall/VPN path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Host firewall/VPN — Host Integration & Performance: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-424",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Host DNS/proxy — Host Integration & Performance: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective host DNS/proxy state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "host DNS/proxy",
      "Host Integration & Performance",
      "Host DNS/proxy — Host Integration & Performance: fails completely",
      "host",
      "dns",
      "proxy",
      "integration",
      "performance",
      "fails",
      "completely"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host DNS/proxy — Host Integration & Performance: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host DNS/proxy; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host DNS/proxy — Host Integration & Performance: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host DNS/proxy — Host Integration & Performance: fails completely”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host DNS/proxy",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host DNS/proxy configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host DNS/proxy — Host Integration & Performance: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the host DNS/proxy inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host DNS/proxy — Host Integration & Performance: fails completely”",
      "Effective host DNS/proxy configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host DNS/proxy — Host Integration & Performance: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host DNS/proxy — Host Integration & Performance: fails completely”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host DNS/proxy — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host DNS/proxy",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host DNS/proxy\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host DNS/proxy\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host DNS/proxy",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host DNS/proxy — Host Integration & Performance: fails completely”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host DNS/proxy — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host DNS/proxy configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host DNS/proxy configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host DNS/proxy configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The host DNS/proxy configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host DNS/proxy — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host DNS/proxy",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host DNS/proxy\nThe effective runtime value differs from expectation.",
        "root": "For “Host DNS/proxy — Host Integration & Performance: fails completely”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host DNS/proxy configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host DNS/proxy configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host DNS/proxy — Host Integration & Performance: fails completely”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Host DNS/proxy — Host Integration & Performance: fails completely: evidence found\nFor “Host DNS/proxy — Host Integration & Performance: fails completely”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host DNS/proxy — Host Integration & Performance: fails completely”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Host DNS/proxy — Host Integration & Performance: fails completely: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Host DNS/proxy — Host Integration & Performance: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-425",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Host DNS/proxy — Host Integration & Performance: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective host DNS/proxy state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "host DNS/proxy",
      "Host Integration & Performance",
      "Host DNS/proxy — Host Integration & Performance: behaves differently than configured",
      "host",
      "dns",
      "proxy",
      "integration",
      "performance",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host DNS/proxy — Host Integration & Performance: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host DNS/proxy; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host DNS/proxy — Host Integration & Performance: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host DNS/proxy — Host Integration & Performance: behaves differently than configured”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host DNS/proxy",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host DNS/proxy configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host DNS/proxy — Host Integration & Performance: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the host DNS/proxy inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host DNS/proxy — Host Integration & Performance: behaves differently than configured”",
      "Effective host DNS/proxy configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host DNS/proxy — Host Integration & Performance: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host DNS/proxy — Host Integration & Performance: behaves differently than configured”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host DNS/proxy — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host DNS/proxy",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host DNS/proxy\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host DNS/proxy\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host DNS/proxy",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host DNS/proxy — Host Integration & Performance: behaves differently than configured”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host DNS/proxy — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host DNS/proxy configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host DNS/proxy configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host DNS/proxy configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The host DNS/proxy configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host DNS/proxy — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host DNS/proxy",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host DNS/proxy\nThe effective runtime value differs from expectation.",
        "root": "For “Host DNS/proxy — Host Integration & Performance: behaves differently than configured”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host DNS/proxy configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host DNS/proxy configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host DNS/proxy — Host Integration & Performance: behaves differently than configured”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Host DNS/proxy — Host Integration & Performance: behaves differently than configured: evidence found\nFor “Host DNS/proxy — Host Integration & Performance: behaves differently than configured”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host DNS/proxy — Host Integration & Performance: behaves differently than configured”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Host DNS/proxy — Host Integration & Performance: behaves differently than configured: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Host DNS/proxy — Host Integration & Performance: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-426",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective host DNS/proxy state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "host DNS/proxy",
      "Host Integration & Performance",
      "Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart",
      "host",
      "dns",
      "proxy",
      "integration",
      "performance",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host DNS/proxy; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host DNS/proxy",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host DNS/proxy configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the host DNS/proxy inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart”",
      "Effective host DNS/proxy configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "DNS=[] Search=[] ExtraHosts=[]",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "{\"payments-net\":{\"IPAddress\":\"172.22.0.5\",\"Aliases\":[\"payments-api\"]}}",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "[\"HTTPS_PROXY=http://proxy.corp:8080\",\"NO_PROXY=localhost,127.0.0.1\"]",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The runtime environment's DNS/proxy path does not include the internal destination in the expected bypass/resolution scope.",
      "remediation": "Correct the runtime DNS/proxy/NO_PROXY source configuration and recreate the container; avoid one-off changes inside the running container.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Daemon proxy, build proxy and runtime-container proxy are separate paths and should be diagnosed separately.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host DNS/proxy",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host DNS/proxy\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host DNS/proxy\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host DNS/proxy",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host DNS/proxy configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host DNS/proxy configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host DNS/proxy configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The host DNS/proxy configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host DNS/proxy",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host DNS/proxy\nThe effective runtime value differs from expectation.",
        "root": "For “Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host DNS/proxy configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host DNS/proxy configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart: evidence found\nFor “Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart”, the effective host DNS/proxy state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host DNS/proxy path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Host DNS/proxy — Host Integration & Performance: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-427",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Host kernel/cgroup — Host Integration & Performance: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective host kernel/cgroup state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "host kernel/cgroup",
      "Host Integration & Performance",
      "Host kernel/cgroup — Host Integration & Performance: fails completely",
      "host",
      "kernel",
      "cgroup",
      "integration",
      "performance",
      "fails",
      "completely"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host kernel/cgroup — Host Integration & Performance: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host kernel/cgroup; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host kernel/cgroup — Host Integration & Performance: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host kernel/cgroup — Host Integration & Performance: fails completely”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host kernel/cgroup",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host kernel/cgroup configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host kernel/cgroup — Host Integration & Performance: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the host kernel/cgroup inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host kernel/cgroup — Host Integration & Performance: fails completely”",
      "Effective host kernel/cgroup configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host kernel/cgroup — Host Integration & Performance: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Host kernel/cgroup — Host Integration & Performance: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Host kernel/cgroup — Host Integration & Performance: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host kernel/cgroup — Host Integration & Performance: fails completely”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host kernel/cgroup — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host kernel/cgroup",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host kernel/cgroup\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host kernel/cgroup\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host kernel/cgroup",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host kernel/cgroup — Host Integration & Performance: fails completely”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host kernel/cgroup — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host kernel/cgroup configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host kernel/cgroup configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host kernel/cgroup configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The host kernel/cgroup configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host kernel/cgroup — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host kernel/cgroup",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host kernel/cgroup\nThe effective runtime value differs from expectation.",
        "root": "For “Host kernel/cgroup — Host Integration & Performance: fails completely”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host kernel/cgroup configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host kernel/cgroup configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host kernel/cgroup — Host Integration & Performance: fails completely”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Host kernel/cgroup — Host Integration & Performance: fails completely: evidence found\nFor “Host kernel/cgroup — Host Integration & Performance: fails completely”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host kernel/cgroup — Host Integration & Performance: fails completely”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Host kernel/cgroup — Host Integration & Performance: fails completely: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Host kernel/cgroup — Host Integration & Performance: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-428",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Host kernel/cgroup — Host Integration & Performance: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective host kernel/cgroup state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "host kernel/cgroup",
      "Host Integration & Performance",
      "Host kernel/cgroup — Host Integration & Performance: behaves differently than configured",
      "host",
      "kernel",
      "cgroup",
      "integration",
      "performance",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host kernel/cgroup — Host Integration & Performance: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host kernel/cgroup; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host kernel/cgroup — Host Integration & Performance: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host kernel/cgroup — Host Integration & Performance: behaves differently than configured”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host kernel/cgroup",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host kernel/cgroup configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host kernel/cgroup — Host Integration & Performance: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the host kernel/cgroup inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host kernel/cgroup — Host Integration & Performance: behaves differently than configured”",
      "Effective host kernel/cgroup configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host kernel/cgroup — Host Integration & Performance: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Host kernel/cgroup — Host Integration & Performance: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Host kernel/cgroup — Host Integration & Performance: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host kernel/cgroup — Host Integration & Performance: behaves differently than configured”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host kernel/cgroup — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host kernel/cgroup",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host kernel/cgroup\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host kernel/cgroup\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host kernel/cgroup",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host kernel/cgroup — Host Integration & Performance: behaves differently than configured”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host kernel/cgroup — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host kernel/cgroup configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host kernel/cgroup configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host kernel/cgroup configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The host kernel/cgroup configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host kernel/cgroup — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host kernel/cgroup",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host kernel/cgroup\nThe effective runtime value differs from expectation.",
        "root": "For “Host kernel/cgroup — Host Integration & Performance: behaves differently than configured”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host kernel/cgroup configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host kernel/cgroup configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host kernel/cgroup — Host Integration & Performance: behaves differently than configured”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Host kernel/cgroup — Host Integration & Performance: behaves differently than configured: evidence found\nFor “Host kernel/cgroup — Host Integration & Performance: behaves differently than configured”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host kernel/cgroup — Host Integration & Performance: behaves differently than configured”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Host kernel/cgroup — Host Integration & Performance: behaves differently than configured: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Host kernel/cgroup — Host Integration & Performance: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-429",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective host kernel/cgroup state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "host kernel/cgroup",
      "Host Integration & Performance",
      "Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart",
      "host",
      "kernel",
      "cgroup",
      "integration",
      "performance",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host kernel/cgroup; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host kernel/cgroup",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host kernel/cgroup configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the host kernel/cgroup inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart”",
      "Effective host kernel/cgroup configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host kernel/cgroup",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host kernel/cgroup\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host kernel/cgroup\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host kernel/cgroup",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host kernel/cgroup configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host kernel/cgroup configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host kernel/cgroup configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The host kernel/cgroup configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host kernel/cgroup",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host kernel/cgroup\nThe effective runtime value differs from expectation.",
        "root": "For “Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host kernel/cgroup configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host kernel/cgroup configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart: evidence found\nFor “Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart”, the effective host kernel/cgroup state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host kernel/cgroup path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Host kernel/cgroup — Host Integration & Performance: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-430",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Docker Desktop file sharing — Host Integration & Performance: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective Docker Desktop file sharing state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "Docker Desktop file sharing",
      "Host Integration & Performance",
      "Docker Desktop file sharing — Host Integration & Performance: fails completely",
      "docker",
      "desktop",
      "file",
      "sharing",
      "host",
      "integration",
      "performance",
      "fails",
      "completely"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker Desktop file sharing — Host Integration & Performance: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker Desktop file sharing; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker Desktop file sharing — Host Integration & Performance: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker Desktop file sharing — Host Integration & Performance: fails completely”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker Desktop file sharing configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker Desktop file sharing — Host Integration & Performance: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the Docker Desktop file sharing inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker Desktop file sharing — Host Integration & Performance: fails completely”",
      "Effective Docker Desktop file sharing configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker Desktop file sharing — Host Integration & Performance: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Docker Desktop file sharing — Host Integration & Performance: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker Desktop file sharing — Host Integration & Performance: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker Desktop file sharing — Host Integration & Performance: fails completely”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker Desktop file sharing — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker Desktop file sharing — Host Integration & Performance: fails completely”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker Desktop file sharing — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker Desktop file sharing configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker Desktop file sharing configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker Desktop file sharing configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The Docker Desktop file sharing configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker Desktop file sharing — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing\nThe effective runtime value differs from expectation.",
        "root": "For “Docker Desktop file sharing — Host Integration & Performance: fails completely”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker Desktop file sharing configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker Desktop file sharing configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker Desktop file sharing — Host Integration & Performance: fails completely”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Docker Desktop file sharing — Host Integration & Performance: fails completely: evidence found\nFor “Docker Desktop file sharing — Host Integration & Performance: fails completely”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker Desktop file sharing — Host Integration & Performance: fails completely”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Docker Desktop file sharing — Host Integration & Performance: fails completely: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Docker Desktop file sharing — Host Integration & Performance: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-431",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective Docker Desktop file sharing state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "Docker Desktop file sharing",
      "Host Integration & Performance",
      "Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured",
      "docker",
      "desktop",
      "file",
      "sharing",
      "host",
      "integration",
      "performance",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker Desktop file sharing; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker Desktop file sharing configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the Docker Desktop file sharing inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured”",
      "Effective Docker Desktop file sharing configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker Desktop file sharing configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker Desktop file sharing configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker Desktop file sharing configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The Docker Desktop file sharing configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing\nThe effective runtime value differs from expectation.",
        "root": "For “Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker Desktop file sharing configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker Desktop file sharing configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured: evidence found\nFor “Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Docker Desktop file sharing — Host Integration & Performance: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-432",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective Docker Desktop file sharing state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "Docker Desktop file sharing",
      "Host Integration & Performance",
      "Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart",
      "docker",
      "desktop",
      "file",
      "sharing",
      "host",
      "integration",
      "performance",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected Docker Desktop file sharing; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The Docker Desktop file sharing configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the Docker Desktop file sharing inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart”",
      "Effective Docker Desktop file sharing configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The Docker Desktop file sharing configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker Desktop file sharing configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker Desktop file sharing configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The Docker Desktop file sharing configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for Docker Desktop file sharing\nThe effective runtime value differs from expectation.",
        "root": "For “Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The Docker Desktop file sharing configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The Docker Desktop file sharing configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart: evidence found\nFor “Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart”, the effective Docker Desktop file sharing state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the Docker Desktop file sharing path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Docker Desktop file sharing — Host Integration & Performance: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-433",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Container I/O performance — Host Integration & Performance: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective container I/O performance state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "container I/O performance",
      "Host Integration & Performance",
      "Container I/O performance — Host Integration & Performance: fails completely",
      "container",
      "i",
      "o",
      "performance",
      "host",
      "integration",
      "fails",
      "completely"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container I/O performance — Host Integration & Performance: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container I/O performance; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container I/O performance — Host Integration & Performance: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container I/O performance — Host Integration & Performance: fails completely”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container I/O performance",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container I/O performance configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container I/O performance — Host Integration & Performance: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the container I/O performance inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container I/O performance — Host Integration & Performance: fails completely”",
      "Effective container I/O performance configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container I/O performance — Host Integration & Performance: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Container I/O performance — Host Integration & Performance: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container I/O performance — Host Integration & Performance: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container I/O performance — Host Integration & Performance: fails completely”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container I/O performance — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container I/O performance",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container I/O performance\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container I/O performance\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container I/O performance",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container I/O performance — Host Integration & Performance: fails completely”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container I/O performance — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container I/O performance configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container I/O performance configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container I/O performance configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The container I/O performance configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container I/O performance — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container I/O performance",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container I/O performance\nThe effective runtime value differs from expectation.",
        "root": "For “Container I/O performance — Host Integration & Performance: fails completely”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container I/O performance configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container I/O performance configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container I/O performance — Host Integration & Performance: fails completely”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Container I/O performance — Host Integration & Performance: fails completely: evidence found\nFor “Container I/O performance — Host Integration & Performance: fails completely”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container I/O performance — Host Integration & Performance: fails completely”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Container I/O performance — Host Integration & Performance: fails completely: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Container I/O performance — Host Integration & Performance: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-434",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Container I/O performance — Host Integration & Performance: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective container I/O performance state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "container I/O performance",
      "Host Integration & Performance",
      "Container I/O performance — Host Integration & Performance: behaves differently than configured",
      "container",
      "i",
      "o",
      "performance",
      "host",
      "integration",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container I/O performance — Host Integration & Performance: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container I/O performance; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container I/O performance — Host Integration & Performance: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container I/O performance — Host Integration & Performance: behaves differently than configured”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container I/O performance",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container I/O performance configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container I/O performance — Host Integration & Performance: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the container I/O performance inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container I/O performance — Host Integration & Performance: behaves differently than configured”",
      "Effective container I/O performance configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container I/O performance — Host Integration & Performance: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Container I/O performance — Host Integration & Performance: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container I/O performance — Host Integration & Performance: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container I/O performance — Host Integration & Performance: behaves differently than configured”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container I/O performance — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container I/O performance",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container I/O performance\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container I/O performance\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container I/O performance",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container I/O performance — Host Integration & Performance: behaves differently than configured”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container I/O performance — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container I/O performance configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container I/O performance configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container I/O performance configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The container I/O performance configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container I/O performance — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container I/O performance",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container I/O performance\nThe effective runtime value differs from expectation.",
        "root": "For “Container I/O performance — Host Integration & Performance: behaves differently than configured”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container I/O performance configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container I/O performance configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container I/O performance — Host Integration & Performance: behaves differently than configured”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Container I/O performance — Host Integration & Performance: behaves differently than configured: evidence found\nFor “Container I/O performance — Host Integration & Performance: behaves differently than configured”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container I/O performance — Host Integration & Performance: behaves differently than configured”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Container I/O performance — Host Integration & Performance: behaves differently than configured: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Container I/O performance — Host Integration & Performance: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-435",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Container I/O performance — Host Integration & Performance: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective container I/O performance state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "container I/O performance",
      "Host Integration & Performance",
      "Container I/O performance — Host Integration & Performance: fails intermittently under load or restart",
      "container",
      "i",
      "o",
      "performance",
      "host",
      "integration",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Container I/O performance — Host Integration & Performance: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected container I/O performance; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Container I/O performance — Host Integration & Performance: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Container I/O performance — Host Integration & Performance: fails intermittently under load or restart”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for container I/O performance",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The container I/O performance configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Container I/O performance — Host Integration & Performance: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the container I/O performance inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Container I/O performance — Host Integration & Performance: fails intermittently under load or restart”",
      "Effective container I/O performance configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Container I/O performance — Host Integration & Performance: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Container I/O performance — Host Integration & Performance: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Container I/O performance — Host Integration & Performance: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Container I/O performance — Host Integration & Performance: fails intermittently under load or restart”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Container I/O performance — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for container I/O performance",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container I/O performance\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container I/O performance\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for container I/O performance",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Container I/O performance — Host Integration & Performance: fails intermittently under load or restart”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Container I/O performance — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The container I/O performance configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container I/O performance configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container I/O performance configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The container I/O performance configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Container I/O performance — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for container I/O performance",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for container I/O performance\nThe effective runtime value differs from expectation.",
        "root": "For “Container I/O performance — Host Integration & Performance: fails intermittently under load or restart”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The container I/O performance configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The container I/O performance configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Container I/O performance — Host Integration & Performance: fails intermittently under load or restart”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Container I/O performance — Host Integration & Performance: fails intermittently under load or restart: evidence found\nFor “Container I/O performance — Host Integration & Performance: fails intermittently under load or restart”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Container I/O performance — Host Integration & Performance: fails intermittently under load or restart”, the effective container I/O performance state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Container I/O performance — Host Integration & Performance: fails intermittently under load or restart: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the container I/O performance path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Container I/O performance — Host Integration & Performance: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-436",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Daemon overload — Host Integration & Performance: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective daemon overload state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "daemon overload",
      "Host Integration & Performance",
      "Daemon overload — Host Integration & Performance: fails completely",
      "daemon",
      "overload",
      "host",
      "integration",
      "performance",
      "fails",
      "completely"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon overload — Host Integration & Performance: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon overload; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon overload — Host Integration & Performance: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon overload — Host Integration & Performance: fails completely”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon overload",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon overload configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon overload — Host Integration & Performance: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the daemon overload inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon overload — Host Integration & Performance: fails completely”",
      "Effective daemon overload configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon overload — Host Integration & Performance: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Daemon overload — Host Integration & Performance: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Daemon overload — Host Integration & Performance: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon overload — Host Integration & Performance: fails completely”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon overload — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon overload",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon overload\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon overload\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon overload",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon overload — Host Integration & Performance: fails completely”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon overload — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon overload configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon overload configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon overload configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The daemon overload configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon overload — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon overload",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon overload\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon overload — Host Integration & Performance: fails completely”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon overload configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon overload configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon overload — Host Integration & Performance: fails completely”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Daemon overload — Host Integration & Performance: fails completely: evidence found\nFor “Daemon overload — Host Integration & Performance: fails completely”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon overload — Host Integration & Performance: fails completely”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Daemon overload — Host Integration & Performance: fails completely: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Daemon overload — Host Integration & Performance: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-437",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Daemon overload — Host Integration & Performance: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective daemon overload state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "daemon overload",
      "Host Integration & Performance",
      "Daemon overload — Host Integration & Performance: behaves differently than configured",
      "daemon",
      "overload",
      "host",
      "integration",
      "performance",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon overload — Host Integration & Performance: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon overload; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon overload — Host Integration & Performance: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon overload — Host Integration & Performance: behaves differently than configured”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon overload",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon overload configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon overload — Host Integration & Performance: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the daemon overload inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon overload — Host Integration & Performance: behaves differently than configured”",
      "Effective daemon overload configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon overload — Host Integration & Performance: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Daemon overload — Host Integration & Performance: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Daemon overload — Host Integration & Performance: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon overload — Host Integration & Performance: behaves differently than configured”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon overload — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon overload",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon overload\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon overload\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon overload",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon overload — Host Integration & Performance: behaves differently than configured”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon overload — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon overload configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon overload configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon overload configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The daemon overload configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon overload — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon overload",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon overload\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon overload — Host Integration & Performance: behaves differently than configured”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon overload configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon overload configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon overload — Host Integration & Performance: behaves differently than configured”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Daemon overload — Host Integration & Performance: behaves differently than configured: evidence found\nFor “Daemon overload — Host Integration & Performance: behaves differently than configured”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon overload — Host Integration & Performance: behaves differently than configured”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Daemon overload — Host Integration & Performance: behaves differently than configured: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Daemon overload — Host Integration & Performance: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-438",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Daemon overload — Host Integration & Performance: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective daemon overload state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "daemon overload",
      "Host Integration & Performance",
      "Daemon overload — Host Integration & Performance: fails intermittently under load or restart",
      "daemon",
      "overload",
      "host",
      "integration",
      "performance",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Daemon overload — Host Integration & Performance: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected daemon overload; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Daemon overload — Host Integration & Performance: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Daemon overload — Host Integration & Performance: fails intermittently under load or restart”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for daemon overload",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The daemon overload configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Daemon overload — Host Integration & Performance: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the daemon overload inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Daemon overload — Host Integration & Performance: fails intermittently under load or restart”",
      "Effective daemon overload configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Daemon overload — Host Integration & Performance: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Daemon overload — Host Integration & Performance: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Daemon overload — Host Integration & Performance: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Daemon overload — Host Integration & Performance: fails intermittently under load or restart”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Daemon overload — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for daemon overload",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon overload\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon overload\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for daemon overload",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Daemon overload — Host Integration & Performance: fails intermittently under load or restart”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Daemon overload — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The daemon overload configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon overload configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon overload configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The daemon overload configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Daemon overload — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for daemon overload",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for daemon overload\nThe effective runtime value differs from expectation.",
        "root": "For “Daemon overload — Host Integration & Performance: fails intermittently under load or restart”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The daemon overload configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The daemon overload configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Daemon overload — Host Integration & Performance: fails intermittently under load or restart”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Daemon overload — Host Integration & Performance: fails intermittently under load or restart: evidence found\nFor “Daemon overload — Host Integration & Performance: fails intermittently under load or restart”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Daemon overload — Host Integration & Performance: fails intermittently under load or restart”, the effective daemon overload state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Daemon overload — Host Integration & Performance: fails intermittently under load or restart: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the daemon overload path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Daemon overload — Host Integration & Performance: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-439",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Host reboot behavior — Host Integration & Performance: fails completely",
    "summary": "The operation fails consistently and does not reach its expected state. Diagnose the effective host reboot behavior state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "host reboot behavior",
      "Host Integration & Performance",
      "Host reboot behavior — Host Integration & Performance: fails completely",
      "host",
      "reboot",
      "behavior",
      "integration",
      "performance",
      "fails",
      "completely"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host reboot behavior — Host Integration & Performance: fails completely”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host reboot behavior; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host reboot behavior — Host Integration & Performance: fails completely” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host reboot behavior — Host Integration & Performance: fails completely”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host reboot behavior",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host reboot behavior configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host reboot behavior — Host Integration & Performance: fails completely” reproduction and confirm it no longer occurs.",
      "Re-run the host reboot behavior inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host reboot behavior — Host Integration & Performance: fails completely”",
      "Effective host reboot behavior configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host reboot behavior — Host Integration & Performance: fails completely. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Host reboot behavior — Host Integration & Performance: fails completely:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Host reboot behavior — Host Integration & Performance: fails completely” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host reboot behavior — Host Integration & Performance: fails completely”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host reboot behavior — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host reboot behavior",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host reboot behavior\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host reboot behavior\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host reboot behavior",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host reboot behavior — Host Integration & Performance: fails completely”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host reboot behavior — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host reboot behavior configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host reboot behavior configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host reboot behavior configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The host reboot behavior configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host reboot behavior — Host Integration & Performance: fails completely is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host reboot behavior",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host reboot behavior\nThe effective runtime value differs from expectation.",
        "root": "For “Host reboot behavior — Host Integration & Performance: fails completely”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host reboot behavior configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host reboot behavior configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host reboot behavior — Host Integration & Performance: fails completely”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Host reboot behavior — Host Integration & Performance: fails completely: evidence found\nFor “Host reboot behavior — Host Integration & Performance: fails completely”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host reboot behavior — Host Integration & Performance: fails completely”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Host reboot behavior — Host Integration & Performance: fails completely: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Host reboot behavior — Host Integration & Performance: fails completely: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-440",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Host reboot behavior — Host Integration & Performance: behaves differently than configured",
    "summary": "The live behavior differs from the effective Docker configuration or operator expectation. Diagnose the effective host reboot behavior state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "host reboot behavior",
      "Host Integration & Performance",
      "Host reboot behavior — Host Integration & Performance: behaves differently than configured",
      "host",
      "reboot",
      "behavior",
      "integration",
      "performance",
      "behaves",
      "differently",
      "than",
      "configured"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host reboot behavior — Host Integration & Performance: behaves differently than configured”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host reboot behavior; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host reboot behavior — Host Integration & Performance: behaves differently than configured” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host reboot behavior — Host Integration & Performance: behaves differently than configured”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host reboot behavior",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host reboot behavior configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host reboot behavior — Host Integration & Performance: behaves differently than configured” reproduction and confirm it no longer occurs.",
      "Re-run the host reboot behavior inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host reboot behavior — Host Integration & Performance: behaves differently than configured”",
      "Effective host reboot behavior configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host reboot behavior — Host Integration & Performance: behaves differently than configured. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Host reboot behavior — Host Integration & Performance: behaves differently than configured:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Host reboot behavior — Host Integration & Performance: behaves differently than configured” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host reboot behavior — Host Integration & Performance: behaves differently than configured”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host reboot behavior — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host reboot behavior",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host reboot behavior\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host reboot behavior\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host reboot behavior",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host reboot behavior — Host Integration & Performance: behaves differently than configured”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host reboot behavior — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host reboot behavior configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host reboot behavior configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host reboot behavior configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The host reboot behavior configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host reboot behavior — Host Integration & Performance: behaves differently than configured is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host reboot behavior",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host reboot behavior\nThe effective runtime value differs from expectation.",
        "root": "For “Host reboot behavior — Host Integration & Performance: behaves differently than configured”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host reboot behavior configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host reboot behavior configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host reboot behavior — Host Integration & Performance: behaves differently than configured”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Host reboot behavior — Host Integration & Performance: behaves differently than configured: evidence found\nFor “Host reboot behavior — Host Integration & Performance: behaves differently than configured”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host reboot behavior — Host Integration & Performance: behaves differently than configured”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Host reboot behavior — Host Integration & Performance: behaves differently than configured: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Host reboot behavior — Host Integration & Performance: behaves differently than configured: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  },
  {
    "id": "docker-441",
    "technology": "Docker",
    "category": "Host Integration & Performance",
    "title": "Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart",
    "summary": "The symptom is intermittent, timing-sensitive, or appears after load/restart. Diagnose the effective host reboot behavior state first, then narrow the failure through the host integration & performance dependency chain.",
    "aliases": [
      "host reboot behavior",
      "Host Integration & Performance",
      "Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart",
      "host",
      "reboot",
      "behavior",
      "integration",
      "performance",
      "fails",
      "intermittently",
      "under",
      "load",
      "or",
      "restart"
    ],
    "inputs": [
      "since",
      "until"
    ],
    "steps": [
      {
        "title": "Capture engine summary",
        "command": "docker info",
        "risk": "READ ONLY",
        "look": "Capture the effective state relevant to “Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart”; preserve the exact error/value and timestamp."
      },
      {
        "title": "Capture resource snapshot",
        "command": "docker stats --no-stream",
        "risk": "READ ONLY",
        "look": "Compare this layer with the expected host reboot behavior; a mismatch here narrows the failure before deeper host changes."
      },
      {
        "title": "Inspect Docker disk usage",
        "command": "docker system df -v",
        "risk": "READ ONLY",
        "look": "Use this output to decide whether “Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart” is object/configuration-specific or environment-wide."
      },
      {
        "title": "Inspect recent events",
        "command": "docker events --since <since> --until <until>",
        "risk": "READ ONLY",
        "look": "Correlate the result with the same failure window; avoid changing multiple Docker layers at once."
      }
    ],
    "likely_causes": [
      "For “Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
      "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
      "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes."
    ],
    "branches": [
      {
        "when": "The inspection output shows an explicit error or unexpected value for host reboot behavior",
        "then": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom."
      },
      {
        "when": "The host reboot behavior configuration is correct but the failure remains",
        "then": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps."
      },
      {
        "when": "The same configuration works on another host/context/builder",
        "then": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location."
      }
    ],
    "verification": [
      "Repeat the exact “Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart” reproduction and confirm it no longer occurs.",
      "Re-run the host reboot behavior inspection and confirm the effective value/state remains correct through the same load or restart window.",
      "Check fresh Docker/application/BuildKit events or logs and confirm the original error pattern does not recur."
    ],
    "escalation": [
      "Exact error/output and timestamp for “Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart”",
      "Effective host reboot behavior configuration/inspect output with credentials and sensitive environment values redacted",
      "Relevant Host Integration & Performance Docker Engine, BuildKit, Compose, application or host evidence from the same incident window"
    ],
    "example": {
      "incident": "Production-style incident: Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart. The engineer must identify the Docker failure before restarting, pruning or rebuilding blindly.",
      "environment": [
        "Host: prod-app-03",
        "Container: payments-api",
        "Image: registry.example.com/payments-api:2026.09.12"
      ],
      "timeline": [
        {
          "stage": "1. Capture effective state",
          "command": "docker info",
          "output": "Observed Docker state for Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart:\nactual value differs from expected configuration.",
          "reasoning": "Preserve the first concrete Docker state/error so later actions do not erase evidence."
        },
        {
          "stage": "2. Narrow the Docker layer",
          "command": "docker stats --no-stream",
          "output": "Docker inspect/info reveals the first differing runtime/configuration value.",
          "reasoning": "Compare effective runtime/build configuration with the intended behavior and identify the first mismatch."
        },
        {
          "stage": "3. Confirm before changing",
          "command": "docker system df -v",
          "output": "Related image/container/engine dependency confirms the same mismatch in the incident window.",
          "reasoning": "Validate the directly related dependency or configuration before choosing remediation."
        }
      ],
      "root_cause": "The example narrows “Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart” to the first effective Docker configuration/runtime dependency that differs from expectation.",
      "remediation": "Correct the source configuration or responsible runtime dependency, recreate/rebuild only what is necessary, then repeat the same checks.",
      "verification": "Repeat the original reproduction, confirm the effective Docker state is corrected, and observe logs/events through the same load or restart window.",
      "lesson": "Inspect effective Docker state first; avoid pruning, recreating or escalating privileges before identifying the failing layer.",
      "note": "Simulated production-style example. Names, IDs and outputs are illustrative; never paste real credentials or secrets into shared evidence.",
      "severity": "Production-impacting example; simulated for training and troubleshooting guidance.",
      "initial_hypotheses": [
        "For “Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "The visible symptom may be secondary, so preserve state and verify the failing layer before changing anything."
      ],
      "ruled_out": [
        "Do not assume the top-level status is the root cause.",
        "Do not restart, delete, prune, force, broaden permissions, or change multiple layers merely to test a guess.",
        "If the first diagnostic layer is healthy, explicitly move to the next dependency instead of forcing a fix at the healthy layer."
      ],
      "fix_decision": "Choose the smallest source-of-truth correction that addresses the confirmed root cause. Prefer fixing the owning manifest, image/build definition, Compose configuration, Docker daemon configuration, or responsible dependency over one-off live mutations.",
      "safety_and_rollback": "Before a state-changing action, preserve the before-state, confirm the exact target/environment, identify the rollback path, and follow the project's change process. Commands shown in the walkthrough are illustrative; destructive remediation is intentionally not auto-executed.",
      "success_criteria": [
        "The original symptom no longer reproduces.",
        "The corrected state remains healthy through the same restart/load/failure window.",
        "Fresh logs/events/status no longer contain the original failure signature."
      ],
      "transferable_pattern": "Docker pattern: start from the observable symptom, identify the first concrete mismatch in the Host Integration & Performance path, prove the responsible layer with a second piece of evidence, correct the source of truth, then verify using the original reproduction."
    },
    "branch_examples": [
      {
        "title": "Real scenario 1",
        "incident": "Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The inspection output shows an explicit error or unexpected value for host reboot behavior",
        "command": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host reboot behavior\nThe effective runtime value differs from expectation.",
        "reasoning": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning.",
        "action": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker info",
            "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host reboot behavior\nThe effective runtime value differs from expectation.",
            "point": "The engineer keeps the container evidence and follows branch 1 instead of immediately recreating or pruning."
          },
          {
            "label": "Prove",
            "command": "docker stats --no-stream",
            "output": "Evidence matches this path: The inspection output shows an explicit error or unexpected value for host reboot behavior",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "For “Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "lesson": "Branch 1: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 2",
        "incident": "Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The host reboot behavior configuration is correct but the failure remains",
        "command": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host reboot behavior configuration is correct but the failure remains",
        "reasoning": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path.",
        "action": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker stats --no-stream",
            "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host reboot behavior configuration is correct but the failure remains",
            "point": "The engineer rules out the first Docker layer and follows branch 2 into the dependent runtime/host/build path."
          },
          {
            "label": "Prove",
            "command": "docker system df -v",
            "output": "Evidence matches this path: The host reboot behavior configuration is correct but the failure remains",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "lesson": "Branch 2: prove this condition first, fix its source, then verify the original symptom."
      },
      {
        "title": "Real scenario 3",
        "incident": "Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart is reported in prod-app-03 / payments-api. During triage, this branch condition is observed: The same configuration works on another host/context/builder",
        "command": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "reasoning": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application.",
        "action": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "result": "After the branch-specific cause is corrected at its source, the engineer repeats the original reproduction and confirms the failure signature is gone.",
        "walkthrough": [
          {
            "label": "Observe",
            "command": "docker system df -v",
            "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
            "point": "The engineer treats this as environment-specific and diffs Docker Engine/context/host characteristics before changing the application."
          },
          {
            "label": "Prove",
            "command": "docker events --since 30m --until now",
            "output": "Evidence matches this path: The same configuration works on another host/context/builder",
            "point": "This confirms the branch before any fix."
          },
          {
            "label": "Verify",
            "command": "docker events --since 30m --until now",
            "output": "Healthy state confirmed; original failure signature is absent.",
            "point": "Re-run the original check after the source fix."
          }
        ],
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "lesson": "Branch 3: prove this condition first, fix its source, then verify the original symptom."
      }
    ],
    "all_walkthroughs": [
      {
        "kind": "Decision",
        "name": "Path 1",
        "case": "The inspection output shows an explicit error or unexpected value for host reboot behavior",
        "check": "docker info",
        "output": "Docker inspection reproduces: The inspection output shows an explicit error or unexpected value for host reboot behavior\nThe effective runtime value differs from expectation.",
        "root": "For “Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 2",
        "case": "The host reboot behavior configuration is correct but the failure remains",
        "check": "docker stats --no-stream",
        "output": "Container/image configuration appears correct.\nNext dependency check reproduces: The host reboot behavior configuration is correct but the failure remains",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Decision",
        "name": "Path 3",
        "case": "The same configuration works on another host/context/builder",
        "check": "docker system df -v",
        "output": "Affected host/context: prod-app-03\nKnown-good host/context: prod-app-02\nDifference observed: The same configuration works on another host/context/builder",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Healthy. Original failure is gone."
      },
      {
        "kind": "Cause",
        "name": "Path 4",
        "case": "For “Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "check": "docker info",
        "output": "Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart: evidence found\nFor “Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "root": "For “Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart”, the effective host reboot behavior state differs from the value, dependency or behavior Docker is expected to use.",
        "fix": "Stay at that layer, identify who owns the value, and correct the source configuration rather than masking the symptom.",
        "verify": "docker stats --no-stream",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 5",
        "case": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "check": "docker stats --no-stream",
        "output": "Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart: evidence found\nA host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "root": "A host integration & performance dependency—such as the engine, host, image, process, network, storage, builder or generated Compose model—changes the host reboot behavior path before it reaches the expected state.",
        "fix": "Move one layer down to its runtime dependency—process, engine, network, storage, host, registry or builder—and correlate timestamps.",
        "verify": "docker system df -v",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      },
      {
        "kind": "Cause",
        "name": "Path 6",
        "case": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "check": "docker system df -v",
        "output": "Host reboot behavior — Host Integration & Performance: fails intermittently under load or restart: evidence found\nThe symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "root": "The symptom is tied to a different Docker context, image/container revision, host capability, timing window or generated configuration than the operator assumes.",
        "fix": "Diff Docker version/info, security, storage, network, cgroup and environment settings against the known-good location.",
        "verify": "docker events --since 30m --until now",
        "verified": "Expected Docker state confirmed; original error no longer reproduces."
      }
    ]
  }
]